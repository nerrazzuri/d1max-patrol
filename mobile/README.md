# mobile —— D1 Max 巡检手机 app

Flutter，只做 Android。**说明看 `../docs/手机app.md`**：为什么名册在手机
这边、里面存什么、怎么在这台机器上跑、踩过哪些坑。

```powershell
$env:PATH = "D:\toolchain\flutter\bin;$env:PATH"
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio1\jbr"
flutter test
```
