# START HERE —— 给笔记本上那个 Claude 的开场指令

> **人类：** clone 完这个仓库、`cd` 进来之后，把下面这段**整段**贴给 Claude
> 就行。不用解释背景，它自己会读。

---

## 复制这一段贴给 Claude 👇

```
我带着一台智元 D1 Max 四足机器人在客户现场，窗口只有 1 天最多 2 天。
这个仓库里有完整的联调手册。请你：

1. 先完整读 docs/真机联调手册.md，从头读到尾，不要跳。
2. 再读 docs/真机待验证清单.md，那是要从真机采回来的问题清单。
3. 然后按手册的阶段顺序，一步一步带着我做。

规矩（手册里也写了，这里再强调一遍）：
- 一次只给我一步。先说"现在做什么、为什么"，我做完把看到的贴回来，
  你再给下一步。不要一次抛十条命令。
- 默认全程只读。任何会让机器移动的命令，先问我"周围清空了吗、可以动吗"，
  我明确回答了你再发。我没回答就不要发。
- 每条链路的原始报文都在自动录制，落在 runs/frames/。绝对不要加 --no-record。
- 不要在现场改产品代码去绕过报错。把报错原样留下来，回家再改。

现在开始：先做 A 阶段的环境自检，然后问我 C 阶段开头那三个问题。
```

---

## 人类自己要先确认的三件事

在贴上面那段之前，这三件事最好先有答案（Claude 也会问你）：

1. **网口的事：2026-09-01 那趟全程走 WiFi，一样干完了活，不用纠结。**
   机器热点 `XG2WIFI_xxxxxx`（密码 `12345678`），连上再加一条到 168 段的
   路由，导航和 SDK 就都够得着。有线是"更稳"，不是"必须"。

   真要走有线：《SDK 开发指南》1.5.3 / 1.5.4 给了**完整线序**，板端型号
   `M12-P12A-GPFM12`，**12 针 A 型航插**，同一个头上既走网又送电：

   | 针 | 用途 | 针 | 用途 |
   |---|---|---|---|
   | 1(棕) | GND | 7(黑) | 网口 B- |
   | 2(蓝) | 24V | 8(灰) | 网口 C+ |
   | 3(白) | 24V | 9(红) | 网口 C- |
   | 4(绿) | 网口 A+ | 10(紫) | GND |
   | 5(粉) | 网口 A- | 11(橙) | 网口 D+ |
   | 6(黄) | 网口 B+ | 12(浅绿) | 网口 D- |

   A/B/C/D 四对全在，所以是**千兆**；面板上共两个这样的口，一个配 24V(3A)、
   一个配 12V(5A)。《产品规格书》说的"工业防水 RJ45×2"多半指的就是这两个口，
   只是叫法糙。**头本身不是 RJ45，通用网线插不上，得做转接线**，
   到现场仍要看一眼面板确认。

   **不要买 PoE 设备。** 这个头是 A 型编码，不是以太网标准编码（4 针 D 型 /
   8 针 X 型），标准 PoE 供电设备根本插不上；而且也用不着 —— 电在
   2/3 针上直接给，比 PoE 干净。详见 `docs/superpowers/specs/` 里的交付架构设计。

2. 如果走有线：笔记本有网口吗？没有的话 **USB 转以太网**转接头带了吗？

3. 机器两台主机的**软件平台版本号**（`cat /opt/release/version.yaml`）。
   这决定用哪个版本的 SDK。Claude 可以自己 ssh 上去查。

## 出发前在家必须做完的

`docs/真机联调手册.md` 的 **A 阶段和 B 阶段**都是"在家做"的：装包（含
`ffmpeg`；要用 app 建图的还要装 `R阶段-ROS2与建图手册.md` R0 那批 ROS 包）、
建虚拟环境、跑一遍测试（全绿即可，现在是 `1460 passed, 11 skipped`）、
在仿真器上把现场要敲的命令全部彩排一遍、**在浏览器里把 app 从建图一路点到
报告走一遍**（B5）、把 SDK 0.1.1 的例子编好。

**现场没网、没时间装东西。A/B 两阶段没做完就别出门。**

> 现场干活主要是在 app 里点，不是敲 CLI。所以 B5 那一步不能省 —— 那是
> 唯一一次能在没有机器的情况下，把整条流水线连着走完的机会。

> **手机 app 的骨架已经在 `mobile/` 里了**（Flutter，只做 Android）。
> 现在只有名册这一屏：登记哪几只狗、各自的 SN / 热点名 / PIN。
> 装 Flutter 见 `docs/手机app.md`；跑它的测试是 `cd mobile && flutter test`
> （现在 56 条，跟 Python 那套是两码事，各跑各的）。

> **想在手机上开这个页面，得给 app 一个 PIN。** 默认它只听 `127.0.0.1`，
> 手机看不见；加 `--host 0.0.0.0` 露到局域网上，就必须同时给 PIN，
> 不给它**拒绝启动**（`D1MAX_PIN=428913 d1max-app --host 0.0.0.0 ...`）。
> 狗热点密码是 `12345678` 还印在我们自己的手册里，而这个页面能让狗走起来、
> 能把现场照片整包拉走。彩排时顺手在手机上试一次，别到现场才发现输不进去。

## ★ 摸到机器的头两件事（2026-09-05 起，优先级高于一切）

产品形态变了：**最终不带笔记本**。脑子搬到狗自己的 Orin NX 上（厂商《SDK
开发指南》第 25 页明确写了这台可以部署），手机 app 当遥控器。所以摸到机器
的第一时间要办这两件事 —— 它们决定后面所有安排，别的都往后排。

**1. 手机够不够得到 Orin（两分钟，但它决定架构成不成立）**

手机连上狗热点 `XG2WIFI_C40221`，浏览器开 `http://192.168.168.100:10010`。
有任何反应（哪怕是报错页）就算通。

- 通 → 架构成立，往下走。
- 不通 → 再在手机上 ping `192.168.234.1`（RK3588 本身）。ping 得通说明热点
  没问题，是**转发**不过去 —— 笔记本要手工加 `ip route` 才够得到 Orin，而
  手机加不了。那就得给 Orin 配自己的无线网卡让手机直连。

> **顺手把第二个问题也答了：那个页面在手机浏览器里能不能用。** 能用的话，
> `mobile/` 那个 Flutter app 的活就小得多 —— 名册 + 连热点 + 存 PIN +
> 把这个页面嵌进来，二十几个接口一个都不用重写。不能用（点不动、画面不出、
> 布局挤成一团）才需要原生重做操作界面。**这一条决定 app 后面几周的工作量，
> 现场花三十秒滑一遍就有答案。**

**2. 跑一遍勘察脚本，把 Orin 的底细一次量完**

```bash
bash scripts/orin-survey.sh robot@192.168.168.100     # 密码 1
```

只读，不装不改不停任何东西。约半分钟出一份报告，存在
`runs/survey/<时间戳>-orin.txt`，**这个文件要带回来**。它会直接给出结论：
磁盘够不够搬建图、Python 版本对不对得上、有没有 Docker、Orin 自己有没有
无线网卡、我们要的端口被没被占、能不能出外网装包。

> **报告里有一段专门找机身序列号。** 多机管理按 SN 走（每只狗的内网地址都
> 是同一套，靠地址分不出谁是谁），但厂商接口里根本没有这个字段，只能现场把
> 所有候选来源摊开看。那一段读到了什么，决定注册表怎么建——**带回来的时候
> 记得看一眼**。一个都读不到也是结论：那就靠人填 `D1MAX_SN`，机身上抄。

现场手敲去问这些，半天就没了，而漏问一条就得等下一次。

---

## 回来要带什么

看手册的 **F 阶段**。一句话版本：`runs/` 整个目录 + `~/sdk-data.log` +
所有报错原文 + 两台主机的版本号 + **导航到底在哪个 IP 的答案**。
