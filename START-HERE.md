# START HERE —— 给笔记本上那个 Claude 的开场指令

> **人类：** 在 Ubuntu 22.04 笔记本上 clone 这个仓库、`cd` 进来、
> `git checkout feat/交付主线`，然后把下面那一整段贴给 Claude。
> 不用解释背景，它自己会读。

---

## 复制这一段贴给 Claude 👇

```
我带着一台智元 D1 Max 四足机器人在客户现场，今天要一次过把第 1 卷到第 8 卷的
功能全验一遍。这台笔记本是 Ubuntu 22.04。

你的角色是副驾，不是主驾：我做物理操作（接线、插拔盘、拿手机、按急停、
看狗），你跑命令、对判据、抄数、盯着别漏填。

请你按这个顺序开工：

1. 先确认我们在 feat/交付主线 分支上，工作树干净。
2. 读 docs/真机现场执行单.md 的开头到「阶段一」之前（§0 到 §2.4）。
   那几节是全天的规矩、红线项、以及时间不够时先保哪四段。
   **这份执行单是今天唯一的主文档**，别的文档都是它指过去的。
3. **先别把整份执行单读完。** 它一千五百行，今天要陪我跑一整天，
   你的上下文得省着用。**每进一个阶段，才读那个阶段。**
4. 建今天的记录文件（见下），然后从阶段一开始，一步一步带着我做。

记录（这一条最重要，它同时是你的备份记忆）：
   现在就建 runs/field/<今天日期>-执行记录.md，把执行单文末那张
   「必须现场量的数值汇总表」**整个复制进去当骨架**。每做完一条，
   **立刻**把数填进去，不要攒到最后。收队前我们要对着它逐行看有没有
   空格——空一个就是要再出一趟车。
   **你的上下文如果快满了，就说出来。** 我会开个新会话，新的你只要读
   这个记录文件加当前阶段，就能接着干——所以记录写得越实，接得越顺。

规矩（执行单里都写了，这里再强调几条最容易犯的）：

- **一次只给我一步。** 先说「现在做什么、判据是什么」，我做完把看到的
  贴回来，你再给下一步。不要一次抛十条命令。

- **任何会让机器移动的命令，先问我「周围清空了吗？有人在旁边看着吗？
  急停按钮在手边吗？」我明确回答了你再发。我没回答就不要发。**
  阶段五、阶段七、阶段八各有一次这样的开场白；阶段八那次还要多问一句
  「整条路线全程都清空了吗」——那一段狗会自己走完整条线，我不是一直
  握着杆的。

- **不要在现场改产品代码去绕过报错。** 现场改的代码没经过任何测试，
  推上真机等于拿狗做实验。把报错**原样**抄进记录文件，回家再改。
  执行单每一条都写了「失败怎么办」，照它做：写【停】的就停下来找人，
  写「跳过继续」的就跳过继续、别在那儿追根因。

- 原始报文全程在录，落在 runs/frames/。**绝对不要加 --no-record。**

- **这个仓库是私有的**：refs/ 下有厂商的私有协议文档、SDK 二进制和硬件
  PDF。不要把 refs/ 里的任何内容、抓包结果、或者带 PIN／口令的命令贴到
  任何在线服务。

- **时间不够是常态。** 一百四十条上下、一整天，大概率跑不完。跑不完不
  算失败，跑成一地半截才是。执行单 §2.4 写了先保哪四段、从哪开始砍，
  **按它砍，不要自己临时决定砍什么**。

现在开始：先做第 1 步和第 2 步，然后告诉我阶段一那几条我手上有没有结论。
```

---

## 人类自己要先确认的几件事

**出发前那一整套在执行单「阶段一」里**（环境全绿、两个 SDK 版本、仿真彩排、
线束、装机材料、物料清单）。**那是在办公室做完的，不是到现场做的。**
这里不重复，只补三件执行单不方便写的：

1. **网口的事：上一趟全程走 WiFi，一样干完了活，不用纠结。**
   有线是「更稳」，不是「必须」。真要走有线，线序、板端型号、到货先验表格
   都在 `docs/真机联调手册.md` **C0**——那份比这里全，直接看那份。
   **只有一条要先说：不要买 PoE 设备，也不要买通用的工业以太网 M12 转接线，
   都插不上。** 原因同样在 C0。

2. **手机准备好几台。** 执行单 1.6 是完整物料清单，但手机这件最容易忘：
   **9.5／9.6 要两台，9.7「第 4 个人被拒」要三台**。少一台，第 6 卷的
   控制权那几条当天就验不了。

3. **PIN 从哪来。** 装机脚本会把六位设备 PIN 打在屏幕上（也可以事后
   `sudo grep D1MAX_PIN /etc/d1max/env`）。**抄进现场登记表，别抄进任何
   文档或工单。** 手机第一次连狗（执行单 4.7）就要用它。

---

## 回来要带什么

- `runs/` 整个目录（含 `runs/frames/` 的原始报文和 `runs/field/` 今天的记录）
- **今天那份执行记录文件**——汇总表逐行对过、没有空格
- 所有报错的**原文**（不是转述）
- 两台主机的版本号
- `runs/survey/` 下 orin-survey 那份报告

回来之后，执行单里量到的数要回灌两个地方：`docs/真机待验证清单.md`
（那是「真机事实」的所在地，跟手册冲突时以它为准），以及被验掉的假设所在的
那份文档本身。
