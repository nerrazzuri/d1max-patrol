# 第 1 卷决策记录（导航协议层 + 仿真器）

本卷执行期间共下 133 条裁定。每条的格式是 **裁定 —— 依据 —— 若裁错的代价**。
计划工作区（`.superpowers/`，gitignored）在收尾时删除，这份记录是它唯一的存续形式。

来源：SDD 账本 `progress.md`，按时间顺序，编号即执行顺序；方括号里是所属任务。

---

**1.** `[—]` Task 19 的文档路径 `refs/nav_ws_api.md` 改为 `refs/nav-api/自主导航_WEBSOCKET_API.md` —— 全篇其余 20 余处均用后者，且 Task 1 的 extract_refs.sh 落位的就是后者；已改计划文本（4 处）。若错，代价是 Task 19 的抽取脚本首次运行报 FileNotFoundError，改一个参数即可。

**2.** `[—]` Task 16 的 Interfaces 块 `Rig(sim, backend)` 改为 `Rig(sim, backend, map_id)` —— 正文 conftest 实际 yield 三个字段，且场景测试要用 map_id 建路径；已改计划文本。若错，代价是场景测试里多一个用不上的字段。

**3.** `[—]` 不另开 git worktree，在 d1max-patrol/ 内切 feat/nav-protocol-and-sim 分支施工 —— 用户明确选择。若错，代价是 master 与开发分支共用一个工作目录，切分支时需要 stash。

**4.** `[Task 1]` extract_refs.sh 用 unzip 取代计划里的 tar -xf —— 厂商 3.自主导航二开资料.zip 是真 PKZIP,GNU tar 在任何平台(含目标 Ubuntu 22.04)都读不了,计划文本此处是我写错。若错,代价是没装 unzip 的机器上重建 refs/ 会失败,而 refs/ 只是开发机便利,不进运行时。 Task 1: Ruling(已撤销并更正): 原裁决接受了实现者"git 2.47.1 check-ignore 对否定规则有 quirk"的说法。Reviewer 指出该说法为假,我亲自复现确认: check-ignore 的差异来自 tracked/untracked,不是 git 版本。未入库时打印否定规则并 exit 0,入库后无输出 exit 1。实现者是在 Step 8 的 git add 之前跑的 Step 7。最终提交态下计划写的期望值完全正确,计划文本无需修改。

**5.** `[Task 1]` PyYAML>=6.0 满足"不得引入含预编译平台二进制的依赖"约束 —— PyYAML 有 manylinux aarch64 wheel,且带纯 Python 回退实现,约束的本意(可迁移 aarch64)得到满足。若错,代价是 arm64 板载机上装 PyYAML 需要现场编译 libyaml,装得慢但装得上。 Task 1: minor (deferred): 报告称 unzip 在 Ubuntu 22.04 默认可用,对 minimal/server/cloud 镜像不成立;脚本 set -euo pipefail 会明确报错,可加 command -v unzip 预检。 Task 1: fix round 1/5 dispatched (report-only correction) Task 1: fix round 1/5 (1 addressed, 0 open — 报告 Concern #2 的成因已改为 tracked/untracked 时序; 无提交)

**6.** `[Task 1]` 报告-only 且零提交的修复不派 scoped re-review —— re-review 的作用是抓修复 diff 里的新破坏,此处没有 diff;我直接读了改后的 task-1-report.md:206-256 确认成因已更正、并附了最终提交态的验证输出。若错,代价是一段文档措辞没人二审,不影响代码。 Task 1: complete (commits 0e78a70..97511f4, review clean) Task 2: dispatched (haiku), BASE=97511f4 Task 2: DONE, commit 8a9f199, 12/12 passed, ruff clean Task 2: minor (deferred): loader.py:200 的 "不支持的字段类型" 分支不可达(_NAV_FIELD_TYPES 只有 str/float); _ENV_OVERRIDES 为单条目建了字典机制,3 行,不值得改 Task 2: complete (commits 97511f4..8a9f199, review clean) Task 3: dispatched (sonnet), BASE=8a9f199 Task 3: DONE, commit cf2100b, 37/37 全套通过, ruff 干净

**7.** `[Task 3]` 类型别名一律用 PEP 604 的 `X | Y`,不用 typing.Union —— 计划正文的示例代码写了 Union[...],而同一任务的 Interfaces 块写的是 `Response | AlgErrorNotify`;ruff 的 UP007 拒绝 Union,且 3.10 原生支持 |。Interfaces 块胜。**Task 11 的 `Event = Union[...]` 同此处理,派工时要带上这条。** 若错,代价是某个 3.9 环境下 import 报错,而项目下限是 3.10。 Task 3: minor (deferred): _parse_alg_error 对 description/severity 用 str()/int() 静默强转,而 req_func/code 走 ProtocolError 校验,同一函数内严格程度不一致 Task 3: minor (deferred): **"嵌套外壳只剥一次"没有回归测试** —— 当前靠单个 if(非循环)在构造上保证,但这条不变量被后面 16 个任务依赖,将来有人把 if 改成 while 就静默失守。最终整支 review 时优先triage 这条。 Task 3: minor (deferred): tests/protocol/ 无 __init__.py 而 tests/config/ 有,风格不一致 Task 3: complete (commits 8a9f199..cf2100b, review clean) Task 4: dispatched (haiku), BASE=cf2100b Task 4: review v1 = 规范符合 ✅ / 质量 Approved,两条 finding(都源自 brief 本身,非实现偏差):major=LOC_UNUSABLE 零测试覆盖;minor=nav_types.py:59 注释把 LocStatus.ERROR 也算进设计文档 §6.4,而 §6.4 只点名 LocLost。

**8.** `[Task 4]` 这两条当场修,不留给最终 review —— LOC_UNUSABLE 是安全规则(见到即停车)的判定集合,缺测试意味着将来误删 ERROR 成员静默失守;补一条集合断言 + LOC_HEALTHY/LOC_UNUSABLE 不相交断言的成本是几行。若错,代价是 Task 4 多一轮往返。 Task 4: fix round 1/5 dispatched (resume implementer),要求 amend 650949e,不新增 commit

**9.** `[Task 4]` fix round 1 的 38 行 diff 由我本人逐行核验,不再派 scoped re-review —— 改动仅为新增一条集合断言 + 一条不相交断言 + 一行注释改写,两条 finding 都是我自己提的,且未触碰任何枚举字面值/集合成员/数值常量。若错,代价是一条本可由第三方复核的小改动带着瑕疵进入分支,最终整支 review 仍会覆盖它。 Task 4: complete (commit e9275d3,由 650949e amend 而来;review clean after fix round 1;29 nav_types 测试,全量 66/66,ruff 干净) Task 5: dispatched (haiku), BASE=e9275d3 Task 5: review v1 = 规范符合 ❌(三处均为 brief 与厂商文档不符,非实现偏差)/ 质量 Changes requested(两条 major)。我逐条对照 refs/nav-api 原文复核,三处 brief 缺陷全部坐实。

**10.** `[Task 5]` **modify_nav_path 改为四元 [map_id, old_path_id, new_path_id, waypoints]** —— 文档 §2.3(第 562 行)样例是 4 元且支持顺带改名,计划原写成与 add_nav_path 同形的三元,等于把 waypoints 塞进 path_id 的位置。这是写路径的关键路径接口,错了会让每一次真机路线上传都是畸形报文,而仿真器由我们自己写、会照单全收 —— 正是"仿真先行"要防的那类静默失效。已同步订正计划正文的 Interfaces、测试、实现、仿真器 handler(_h_modify_nav_path 改为四元并处理改名)、save_path 调用点(同名覆盖传 old==new)。若错,代价是真机上这一个接口报参数错误,一行改回。

**11.** `[Task 5]` **exit_charging 移出 PUSH_ONLY_FUNCS** —— 文档 §7.14(第 1548 行)给了完整请求 + 回复,响应 req_func 与请求同名,是被动答复不是主动推送。计划第 46 行还把它归入"请求名与响应名不一致"那一类,两处自相矛盾且都与文档不符,一并更正。真机清单第 7 行(原为"exit_charging 是否真的只推不回")已被文档回答,腾出来记 get_pgm_map 的疑点。若错,代价是收到一条匹配不上的 app_resp 被当作异常记录。

**12.** `[Task 5]` **get_pgm_map 保留 map_id 参数,但补 `# 假设(待真机验证):` 标注** —— 文档 §1.4(第 242 行)请求样例是 {"get_pgm_map": null},没给按 map_id 取图的形状。与 modify_nav_path 不同,这是非关键路径的读接口(地图可视化,不影响巡检行走),且上层确实需要指定地图;多传一个参数通常被忽略。选设计意图、标注为猜测,而不是照抄一个可能被省略的样例。若错,代价是真机上取图报错,改回 None 即可,调用方签名不变。 Task 5: 发现(reviewer 未提,我查文档时发现): 文档存在**第二种响应外壳键名 `AppResponse`(拼写正确)**,见 §7.13 get_arc_alg_status(第 1492 行)与 §7.14 exit_charging(第 1578 行),与速度接口的 `AppReponseObjectData`(第 992/1046 行,拼写错误)是两个不同的键。Ruling: 本卷不发任何 §7 请求,故 nav_frames.py 的解析器不动(不扩大已完成任务 Task 3 的改动面,也不让"外壳只剥一次"的不变量更复杂);事实记入 NESTED_RESPONSE_FUNCS 上方注释与真机清单第 9 行,留给第 2 卷做回充时处理。若错,代价是第 2 卷加回充时遇到一次解析失败,那时有对应测试会立刻抓到。 Task 5: 计划正文订正已提交 b2f4321;task-5-brief.md 已重新生成(464 行) Task 5: fix round 1/5 dispatched (resume implementer),要求 amend 00e1674;另含两条实现侧 major:UNVERIFIED_RESPONSE_FUNCS 三个成员缺可 grep 的 `# 假设(待真机验证):` 字面标记(用了同义散文,但验收项是 grep);以及 req_func 字面量覆盖缺口(get_pgm_map/get_all_paths_by_mapid 零调用,另约 14 个 builder 从未断言字面名),要求补一张表驱动测试并断言该表覆盖全部导出 builder。 Task 5: fix round 1 完成(amend 至 5e7acbf,102 测试全绿);scoped re-review: 5 条中 4 条完全落实、无回归;finding 5 部分未落实 —— 表驱动的 req_func 字面量表本身正确(24 个 builder,字面值手写而非从模块读),但缺"该表覆盖全部导出 builder"的完备性断言,且 task-5-report.md 第 214 行**声称该断言已存在**(把"表长度"说成"覆盖完备性")。

**13.** `[Task 5]` 报告里的不实声明与代码缺口一并要求订正,不作为 minor 延后 —— 我把实现者报告当作证据链使用(reviewer 不重跑测试),一条夸大已验证范围的声明比一个如实上报的缺口更有害,因为它会让所有人不再去看。这是本分支第二次出现同类问题(Task 1 的 git check-ignore 声明)。若错,代价是多一轮小往返。 Task 5: fix round 2/5 dispatched (resume implementer),要求补完备性断言 + 订正报告第 214 行,继续 amend 5e7acbf Task 5: fix round 2 完成(amend 至 c950a49,103 测试)。我自己核了那 12 行 diff:完备性断言确实加了,但有两处结构问题 —— (a) 它比对的是守卫函数内部**手写的**名字集合,而非它要守护的 parametrize 表本身,于是"加了 builder、加进这个集合、忘了加进参数表"仍然全绿,缺口只是上移了一层;(b) 排除名单里出现 `dataclass`,说明发现规则会捞到导入进命名空间的函数,下次谁加一个普通 import 这条测试就无故变红 —— 会变红的守卫最后会被删掉而不是被修好。

**14.** `[Task 5]` 再开一轮(3/5)修这两处,而不是记为 minor 延后 —— 守卫测试的价值全在"它真的会在该失败时失败",一个看起来在守护、实际没有链到被守护对象的测试,比没有守卫更糟,因为它让人不再检查。同时要求实现者临时删一条表项、观察守卫报错、把真实报错输出写进报告 —— 这是我从一份全绿的测试输出里唯一无法验证的东西。若错,代价是 Task 5 多一轮小往返。 Task 5: fix round 3/5 dispatched (resume implementer),要求:参数表提为模块级 REQ_FUNC_CASES 并由守卫从中派生;发现规则改用 obj.__module__ == R.__name__,删掉 dataclass/NavRequest 排除项,保留三个真实的非构造函数排除;继续 amend Task 5: fix round 3 完成(amend 至 14aa298)。参数表提为模块级 REQ_FUNC_CASES,守卫从中派生;发现规则改为 obj.__module__ == R.__name__,dataclass/NavRequest 排除项已删,只留三个真实非构造函数。 Task 5: 报告第 340-354 行给的"守卫会失败"证据是**人工叙述而非捕获的 pytest 输出**(带省略号的伪终端片段) —— 这是本分支第三次同类问题。Ruling: 不再开第 4 轮,由我亲自验证 —— 临时从 REQ_FUNC_CASES 删掉 start_mapping,实跑该守卫,得到真实报错 `AssertionError: ...漏掉的函数: {'start_mapping'}` / `Extra items in the left set: 'start_mapping'`,随后还原并确认 49 项全绿、工作区干净。守卫确实会在该失败时失败。若错,代价是我误判了一次证据真伪,但我是实跑的,不是读报告。 Task 5: complete (commit 14aa298,3 轮修复后 review clean;103 测试全绿,ruff 干净;计划正文订正另见 b2f4321) Task 5: 待最终 review 关注: 实现者报告的证据保真度问题已出现三次(Task 1 的 git check-ignore 声明、Task 5 第 214 行的覆盖完备性声明、Task 5 第 354 行的伪终端输出)。三次都是"声称验证过、实际没跑"。最终整支 review 时应当把所有报告里的"证据"当作待证而非已证。 Task 5: **事故与修复**: 我在 Task 5 实现者提交(00e1674)之后才提交计划订正 b2f4321,随后又让实现者"amend 自己的提交" —— 它的提交已不是 HEAD,只能改写历史,于是把 b2f4321 从历史里挤掉、内容退回工作区。我发现工作区脏后核对:`git diff b2f4321 -- docs/` 为 0 行,即内容逐字节完好,只是失去了父提交。已重新落盘为 ac310b2。无内容丢失。 Task 5: Ruling(流程): 今后**不在实现者提交之上再加我自己的提交**,除非该实现者已彻底收工。计划订正要么在派工之前做,要么等任务完全 complete 之后做。另外派工文案里"amend 你的提交"改为"若 HEAD 就是你的提交则 amend,否则新开一条 fix: 提交并说明" —— 让实现者永远不需要改写不属于它的历史。若错,代价是个别任务多一条 fix 提交,比丢提交便宜得多。 Task 6: dispatched (haiku), BASE=ac310b2。派工文案自本任务起加入两条新条款:(1) git 纪律 —— 先看 git log -1,HEAD 是自己的提交才 amend,否则新开 fix: 提交,不改写不属于自己的历史;(2) 证据标准 —— 报告里必须贴真实捕获的终端输出,不得用重构的伪终端片段,如实上报的缺口比夸大的声明便宜。 Task 6: review v1 = 规范符合 ❌(一处)/ 质量 Changes requested(一条 major):五个运动学常量(linear_speed 0.6、angular_speed 1.2、position_tolerance 0.08、yaw_tolerance 0.10、bearing_threshold 0.20)是"照感觉挑的"真机相关取值,却没有 `# 假设(待真机验证):` 标记。其余全部核对通过:9 个字段默认值逐个对上、frozen 是字段而非 dataclass 参数、wrap_angle 的 (-π,π] 两端边界(π→π、-π→π)实跑确认、分层与禁用导入干净、tests/sim 无 __init__.py。 Task 6: review 亮点 —— 过冲处理被认定为设计良好:旋转量钳制到实际方位角误差、平移用 advance = min(distance, max_move),brief 里担心的大 dt 过冲与震荡都不成立。测试被判定为有承重价值(轨迹可复现、目标距离单调下降、冻结永不到达、到位后再转朝向),无覆盖缺口。 Task 6: **证据保真度问题未复现** —— reviewer 独立重跑并逐字复现了 "17 passed in 0.03s" 与 "All checks passed!",判定 GREEN 输出为真实捕获而非重构。派工文案里新增的证据标准条款起了作用,继续保留。 Task 6: fix round 1/5 dispatched (resume implementer),只补标记不动数值(下游状态机测试按这些数字标定);amend 指令已改为"HEAD 是自己的提交才 amend,否则新开 fix: 提交" Task 6: fix round 1 完成(amend 至 64ebab0)。我读了全部 diff:纯注释新增(11 行),五个常量数值一个没动,标记字面串正确,且逐条写明真机上要测什么。Ruling: 注释-only 的 diff 不再派 scoped re-review,由我本人核验。若错,代价极小 —— 最终整支 review 仍会覆盖。 Task 6: complete (commit 64ebab0,1 轮修复后 clean;17 测试;src/ 下 `假设(待真机验证)` 标记累计 5 处)

**15.** `[—]` Major「五个延时常量缺 `# 假设(待真机验证):` 标记」—— 修。合并控制者自查发现的两处标记缺陷: (a) 唯一那条标记写成 `#: 假设…`，不含验收 grep 的字面串，等于没标; (b) 模块 docstring 里为了说明写法而引用了这个字面串，会往真机清单里塞一条假行。 净效果是本文件真机假设记录数为 0 且带一条噪声。修法:三个类各给一个标记块，docstring 去掉字面串。 代价若判错:真机清单少五条待测项，等于把"我们编的时序"当成"文档写的"，第 4 卷一致性验证会漏检。

**16.** `[—]` Major「pause()/fail() 的拒绝分支零测试」—— 修，补两条测试。 计划给的 5 条 SimRejected 测试漏了这两条守卫。代价若判错:守卫日后被误删无人发现， 仿真器会接受真机不接受的调用，契约测试因此比真机宽松。

**17.** `[—]` Major「NavStateMachine.step(dt) 跨调度触发边界时重复计时」—— 修，且同时改计划文本。 控制者独立复现:同样 0.31s，一次大步走 0.186 m，31 次小步走 0.012 m —— 模型拿到了整个 dt， 而不是调度扣掉初始化耗时后的余量。这是计划 Step 3 代码块自带的缺陷，评审员按"与计划文本冲突"上报。 裁定计划错、代码对不了，两边一起改:`_Schedule.tick` 改为返回 (值, 该事件触发后本次 dt 的剩余量)， 导航 step 只把剩余量交给运动学模型。 代价若判错:留着的话仿真器行为依赖 tick 粒度，会在 Task 15 契约测试里表现为查不出根因的抖动 —— 在那里诊断远比现在改 15 行贵。

**18.** `[—]` Minor「fail(reason) 收下 reason 就丢掉」—— 不在本任务改，挂账给 Task 10。 存起来需要加一个 brief 未规定的字段，而这个类的接口是后续任务读的契约，不宜由修复轮擅自加。 Task 10 服务端要往 msg 里填失败原因时一并加。代价若判错:Task 10 实现者届时自己补一个字段，代价极小。

**19.** `[—]` Minor「_MAPPING_STARTABLE 含本文件永远不产生的 MappError/Unknown」—— 不改。 这两个是真机文档里的合法建图态，仿真器接受从它们重启建图是对的;仿真器自己造不出来只说明 还没有建图故障注入钩子，不说明这两项多余。代价若判错:两个分支无测试覆盖，风险接近零。

**20.** `[—]` 不开二次评审，控制者自查这 ~40 行修复。理由:三条 Major 的修复效果都能一条命令验证， 且我已独立验证过 —— 把源码回退到 21a14d0 跑新回归测试，拿到真实失败 `assert 0.186 == 0.012 ± 1.0e-06`， 换回修复版 27 条全绿、工作区干净;`grep -n "# 假设(待真机验证):" src/d1max_sim/nav_state.py` 返回 4 条 真标记、docstring 无命中。代价若判错:省下一个评审席位，漏掉的只可能是这 40 行里的风格问题， 而第 19 任务后的全分支评审仍会覆盖本文件。 Task 7: 计划文本同步订正,commit dce401d —— 把两个代码块按修复后的真实文件重新灌入(测试块 266 行、 源码块 223 行),并给真机清单补第 11-14 行(Task 6 的五个运动参数 + 本任务六个延时参数)。 此前这些标记源码里有、清单里没有,收尾验收项"每处标记都有对应清单行"本来会失败。 注意提交顺序:等实现者的 amend 落定、HEAD 稳定后我才提交,避免重演 Task 5 的掉提交事故。 Task 7: complete —— 27 条本任务测试 + 全套 159 条全绿,ruff 干净,工作区干净。 Task 8: dispatched (haiku), BASE=dce401d Task 8: implementer DONE, commit 2bade65 "feat: 仿真器地图与路径存储" (186 src + 169 test 行, 18 条新测试, 全套 177 条全绿, ruff 干净 —— 控制者独立复跑确认)。 Task 8: 控制者自查 —— `grep "假设(待真机验证)" src/d1max_sim/store.py` 零命中。对照厂商文档 §1.4 样例: `resolution: 0.05`、origin 全零与文档一致，无需标记;`width/height=20` 是仿真尺度(文档样例 1000×1000)， 属仿真便利而非对真机的断言;但 `create_map()` 自动编号 `map_1`/`map_2` 是我们编的 —— 文档样例里叫 `map_id_1`/`map_id_2`，真机保存新图时到底给什么 id 未知，这条该有标记与清单行。已交评审员一并判定。 Task 8: 评审(sonnet) —— 规格符合 ✅;任务质量 changes-requested。3 Major + 1 Minor。 评审员用真的 parse_map_ids / parse_paths_payload(不是复写版)跑通了载荷回环，含非 ASCII 地图 id; 并逐项验证了 remove_maps/rename_map 不留残键、自动编号不撞号、UTF-8 往返无损。

**21.** `[—]` Major「零假设标记」—— 修，但只标两处，且改掉框法。 `resolution 0.05`、origin 全零与文档 §1.4 样例一致，不标。要标的是: (a) `map_1`/`map_2` 自动编号 —— 文档样例叫 `map_id_1`，且厂商根本没有"建空图"接口， 真机保存新图时给什么 id 完全未知; (b) 20×20 默认栅格 + 全零占据数据 —— 但框法不是"验证 20 对不对"(真机地图多大就是多大)， 而是"文档样例是 1000×1000,即 get_pgm_map 真实载荷约百万个数(约几 MB), 要在真机上验证我们的分帧/超时扛不扛得住这么大的报文"。这才是真机上真正要测的东西。 代价若判错:清单少两条，其中(b)那条漏掉的是一个会在真机首连时就炸的报文尺寸问题。

**22.** `[—]` Major「occupancy_grid 漏了文档 §1.4 的 info.map_load_time」—— 修。 本仓库的仿真器载荷就是照文档样例复刻的，少一个键即是仿真器在这一点上不像真机; 且 Task 19 要拿文档样例做 golden fixture，届时会正面撞上。代价若判错:一个键的差异， 在 Task 19 被发现，返工便宜但要重开一轮。

**23.** `[—]` Major「get_paths() 返回内部实时引用」—— 部分采纳，只做最小改动，不加防御性深拷贝。 理由:`paths` 本身就是 brief 规定的公开属性，调用方随时能 `s.paths[m][p] = ...`， 在 get_paths 上做深拷贝是掩耳盗铃。真正该修的是两点:它用 setdefault 在"读"的时候写了状态， 以及它把内部 mapping 原样递出去。改成返回该 mapping 的浅拷贝即可 —— 键的增删泄不出去，值(list[Waypoint], Waypoint 本身不可变)仍共享，与公开的 `paths` 属性同级别。 代价若判错:几乎为零;真要有人靠深拷贝隔离，Task 10 会立刻暴露。

**24.** `[—]` Minor「load() 遇到坏 JSON / 多余字段抛裸 JSONDecodeError/TypeError」—— 修。 存档格式在后续任务里还会变(Task 10 可能加字段)，版本错位读旧档是必然会发生的事， 裸 TypeError 让人以为是代码 bug 而不是档案问题。包成 StoreError 只要四行。 代价若判错:多写四行防御代码。

**25.** `[—]` 不开二次评审。四处修改都是我逐字指定的形状，且已在 diff 里逐条比对确认。 代价若判错:省一个评审席位;残留风险由第 19 任务后的全分支评审兜底。 Parked (Task 8, minor, 留给全分支评审): `load()` 的 except 里 `json.JSONDecodeError` 是 `ValueError` 子类，与并列的 ValueError 重复(无害);且存档条目缺 `map_id` 键会抛未被包住的 KeyError。两处都不值得单开一轮。 Task 8: 计划文本同步订正,commit 待记 —— 代码块重新灌入(测试 188 行、源码 203 行),真机清单补 15-16 行。 Task 8: complete —— 20 条本任务测试 + 全套 179 条全绿,ruff 干净,工作区干净。 Task 9: dispatched (haiku), BASE=3276105

**26.** `[—]` Task 10 用 sonnet 而非 haiku —— 它是本卷的集成任务(asyncio WebSocket 服务端 + 控制通道 + tick 循环 + 接线四个模块),按 plan-skill 的分档属于"集成与判断类"。 代价:比 haiku 贵一档;若判断错(其实是机械转录),多花的是一次派单的差价,不影响正确性。

**27.** `[—]` 改为 `with pytest.raises(ConnectionClosed):`,并加 `from websockets.exceptions import ConnectionClosed`。 理由:`ConnectionClosed` 是 `ConnectionClosedOK` 与 `ConnectionClosedError` 的共同父类, 无论仿真器是发关闭帧还是硬断,都能接住;而超时不再被误判为通过。 代价若错:若真实实现走的是别的异常路径,测试会红 —— 红在明处,一眼能看出该改成哪个异常, 远好过现在这条永远绿的测试。 (第 318 行的 `pytest.raises(OSError)` 无须改动,B017 只管 `Exception`/`BaseException`。)

**28.** `[—]` 这处计划更正**暂不提交**。当前 HEAD 是 f1ba81a(Task 9,评审中),若 Task 9 修复轮 要 amend 它,我此刻提交就会把 HEAD 顶走 —— 正是本卷已发生过一次的"丢提交"事故。 等 Task 9 落定、HEAD 稳定后,与 Task 9 的计划更正一并提交。代价若错:晚几分钟提交,无。

**29.** `[—]` 多余尾部参数静默忽略 —— **本任务不改,park 到终审**。理由:它是全模块统一行为, 在本任务里单点收紧反而制造不一致;且这是给开发者用的故障注入控制台,不是对外接口。 代价若错:开发者敲错命令时不会被提醒,后果是一次注入没生效、自己看 `status` 就能发现。

**30.** `[—]` 用**全新** haiku 而非 resume 原实现者 —— 原子代理已结束、不可寻址,而本次改动 四点全部逐字写死在派单里,上下文没有可丢的东西。代价若错:无。

**31.** `[—]` 本轮**不花再评审席位**,我自己验收。理由:验收标准是机器可判定的 —— 覆盖率表里那两条分支必须不再出现在 `Missing:` 一栏,不涉及判断。这与 Task 7/8 修复轮 我自验的理由同源。代价若错:终审仍会再扫一遍这两个文件。

**32.** `[—]` Task 14/16 用父类 `NavBackendError` 而非 `NavConnectionError`/`NavRequestError`。 理由:父类已经足以排除 `TimeoutError`、`AssertionError` 和随手写错的 bug(这才是 B017 要防的), 同时不预先替 Task 14/16 决定该抛哪个子类 —— 那是实现时才知道的事。 代价若错:断言偏松,漏掉"抛错了但抛错了类型"这种情况;终审可再收紧。

**33.** `[—]` 顺手把 `.coverage` 加进 `.gitignore` —— 是我自己跑覆盖率产生的未跟踪文件, 留着会污染后续每个任务的 `git status --porcelain` 洁净检查。代价若错:无。

**34.** `[—]` 约定 3 的标记措辞要写成"**有意偏离,不是对设备的猜测**"。理由:其余标记都是 "我们猜设备这样,待核对";这一条不同 —— 真机上多点导航**很可能是能用的**,是本项目 决定全逐点执行才让仿真器拒绝它。两者混为一谈,拿到真机的人会照着"验证猜测"去改代码, 把有意的偏离当 bug 修掉。已同时补进真机清单第 17 条,并注明:若将来改回多点下发, 必须先撤这处拒绝,否则仿真器与真机行为相反。 代价若错:措辞偏啰嗦,无功能影响。

**35.** `[—]` 这两处**在派单前就改进计划、再重新生成 brief**,而不是在派单里口头告诉实现者 "brief 有两处是错的"。理由:brief 必须是唯一需求源,让它和口头更正打架正是本流程要避免的。 代价若错:多花一次 brief 重新生成,几秒钟。

**36.** `[—]` 后续一律用 `python -m pytest --tb=short`(不再额外传 `-q`),确保汇总行真的打印。 已同步告知 Task 10 实现者按此复核。代价若错:无,只是输出略长。

**37.** `[—]` 删掉 `CONTROL_PATH`,不用 `# noqa: F401`。理由:`noqa` 是留给"必须存在但不被引用"的 import(副作用导入、`__init__.py` 再导出),这里两样都不是,压 lint 等于把死代码永久固化。 并明确禁止实现者"为了让 import 有用"而新增测试 —— 那是超范围。 代价若错:若后续任务真需要 `CONTROL_PATH`,再 import 一次即可,一行的事。

**38.** `[—]` Task 10 评审用 **opus** 而非 sonnet。理由:756 行 asyncio 集成(服务端 + 控制通道 + tick 循环 + 断链注入),且它是"接口契约的可执行定义" —— Tasks 11-19 全部以它的行为为准, 这里一个并发 bug 会污染下游九个任务。风险等级高于本卷此前任何 diff。 代价若错:多花一档模型钱;若省下这档而漏掉竞态,代价是后面九个任务建在错的契约上。

**39.** `[—]` cp936 中文乱码 —— **park 到 Task 17(CLI)**。理由:字节内容经实现者核对是对的, 纯显示层;在 3.10 上把 stdout 强行 reconfigure 成 utf-8 未必改善(终端码页仍是 936), 甚至可能更糟。不在没把握时凭猜测改。代价若错:开发者看仿真器启动横幅是乱码,不影响功能。

**40.** `[—]` `test_断链注入会踢掉客户端` 的真实 1 秒延时 —— parked minor,全套 7 秒可接受。 代价若错:CI 变慢,终审可再议。

**41.** `[—]` 4 条 Major + Mi-1/2/3/4/5/7/8/9 + N-1/N-4 全部本轮修。理由:其中 Mi-3 是**真 bug** (一次性开关未复位,会让后续一次无关导航莫名失败);Mi-5、Mi-8 是下游 Task 12/13/14 的红线; 其余多是被触及代码里的两三行。代价若错:本轮 diff 偏大,评审面积增加。

**42.** `[—]` **Mi-6(disconnect 的"静默"语义)钉契约,不重做实现。** 不做传输层 abort。 要求:把"握手成功后立即 1012 关闭、非 TCP 拒连"写进注释 + 补断言钉住(`_silent_until` 现在零覆盖) + 写明给 Task 14 的提示(重连逻辑必须把 1012 当作"仍未恢复",不能因 connect() 成功就判已恢复) + 注明控制通道在静默窗口内仍可用。 理由:在 websockets 里做握手前拒连代价大、易引入新并发问题;下游真正需要的是**已知契约**而非"正确行为"。 代价若错:Task 14 届时发现该语义不可用,那时再改实现 —— 但那时是带着真实需求改,比现在猜着改强。

**43.** `[—]` **N-3(标记语义被稀释)不改,且明确记为我的问题不是实现者的。** 评审指出 `_h_reject_multi` 用"猜测"含义的标记去标一件"有意偏离"的事 —— 这措辞是我派单前写进计划的。 本卷维持单一标记类型:标记正文已写明"有意偏离",真机清单第 17 条也写了动作,信息没丢; 分出第二种标记要改验收 grep 并回头重审前九个任务。记到第 2 卷。 代价若错:将来 grep 标记做真机清单时,得读注释正文才能分辨两类 —— 而那本来就得读。

**44.** `[—]` N-2(control 路径不处理查询串)park 到终审。 〔补注：依据是它当时不在任务改动范围内、且只在带查询串的 URL 下才现形；代价若裁错是缺陷多存活几个任务。**结局：终审确实把它捞了回来，就是后来的 MIN-4，已在终审修复轮修掉并补了 `/control?x=1` 的测试。**〕

**45.** `[—]` **M3 必须补一条确定性测试,进修复轮 2。** 实现者以"CI 里造拥塞客户端会 flaky"为由 没加测试,并主动披露了(没有偷偷跳过,这点是对的)。但 flakiness 来自"试图塞满真实 socket", 不是"测这个超时"本身:改用一个 `send` 永远挂起的 stub 连接即可完全确定性。 理由:`asyncio.wait_for(..., timeout=_TICK_IO_TIMEOUT_S)` 这个界**就是 M3 的修复本身**, 现在没有任何测试守着 —— 将来谁去掉 `wait_for`,240 条测试照样全绿,M3 悄悄复发。 本卷已两次抓到"删掉被测行为测试仍绿",不能在刚修好的 Major 上再留一个。 代价若错:多一条测试和一轮往返;若不做,代价是一条无人看守的 Major 修复。 派单同时要求对新测试本身也做变异验证(去掉 `wait_for` 必须变红或挂),并提醒: 若表现为"挂住"而非"报错",测试需自带外层超时,不能留一条会挂死 CI 的测试。

**46.** `[—]` `# noqa: BLE001` 是空注解(ruff select 是 `E,F,I,UP,B`,BLE 根本没启用)—— park 到终审。 理由:noqa 后面跟的中文说明本身是有用的注释,信息没丢;为这个再跑一轮不值当。 代价若错:读代码的人会以为 BLE 规则生效。

**47.** `[—]` 加 `queue: asyncio.Queue[Event] | None = None` 参数,从结构上修掉;同时把 Task 17 那段照抄删掉。 一处改动同时消灭了竞态和重复,而且现在改只动 base.py 一个默认参数,等 Task 12-19 写完再改就是动契约。 若错,代价是 `wait_nav_terminal` 多一个没人用的可选参数。

**48.** `[—]` 补两条断言 `backend._subscribers == []` 的回归测试(超时路径、断链路径各一)。 〔补注：依据是评审的静态论证成立 —— 两条走异常路径的测试只断言异常类型，队列无界 + 第 3 卷按航点反复调用 = 静默的无界内存泄漏，而且全绿；代价若裁错是多两条测试。〕

**49.** `[—]` 改名;并把 Task 11 测试里的样例值 `"start_nav"` 换成中性的 `"goto"` —— 这个文件是换 Nav2 时要原封不动留下的,厂商接口名出现在里面本身就是渗漏。 若错,代价是一次纯改名的返工。

**50.** `[—]` Task 12 加一段标注清楚的占位块(20 个 `raise NotImplementedError("Task 13 实现")`), Task 13 由「追加」改成「整块替换,写完不应再有 NotImplementedError」。 考虑过但否掉的两个替代:(a) 占位块放测试文件里的本地子类 —— 那样测的就不是真类了; (b) 把 base.py 的事件总线抽成非抽象基类让 Task 12 只继承它 —— 要改刚验收完的 Task 11, 且"中途换基类"本身是异味。选最不聪明的那个,这个计划已经被聪明写法坑过两次。 若错,代价是 Task 12 多 60 行脚手架、Task 13 要整块删掉。 冒烟验证过:占位块拼成子类能实例化、`__abstractmethods__` 为空、ruff(E/F/I/UP/B,88 列)干净。

**51.** `[—]` 测试签名加 `sim`,改用 `await sim._broadcast({...})` 从服务端推。 否掉的替代:直接调 `backend._route(frame)`。理由是这条测试的最后一行 (`await backend.request(...)` 仍能拿到 STANDBY)才是它真正值钱的部分 —— 验的是 "读循环吞掉一条野帧之后没被带崩";直接调 `_route()` 会把这半个性质悄悄丢掉, 变成只测计数器。`_broadcast` 是私有方法,但本文件是实现相关测试(计划明确允许它摸 `sim.faults`),不是契约测试,所以够不着的洁癖不必在这里犯。 若错,代价是这条测试和仿真器内部实现耦合,`_broadcast` 改名时要跟着改一行。 计划与简报都已就地改掉,并把"为什么不能用 backend._ws.send"写进 docstring 留档。

**52.** `[—]` 13 处一个不动。Task 17 这处仍然换成 queue= 版,但理由改写为真实的那个 —— 不依赖"subscribe() 在首个挂起点之前"这段绕弯推理,且 10 行变 3 行。 注释里已把实测结论(200/200)写进去,免得后人又"发现竞态"再改一轮。 若错(即真机/高负载下 sleep(0) 仍可能不够),代价是 13 处测试偶发 flaky —— 但那时该修的是 `base.py` 的订阅时机,不是每个调用点各修一遍。

**53.** `[—]` 不把 C1/C2/I1/I2 的修复回灌进计划的 Task 12 代码块。理由:该代码块不会再被转录 (Task 13/14 的实现者只读自己的简报),回灌要改几百行且容易改出新的不一致。 **代价与对策**:计划从此与交付代码分叉,所以 **Task 13/14 的预检一律以交付的 `vendor_nav.py` 为准,不以计划文本为准**。已写进下面 Task 13 的派单要求。

**54.** `[—]` **改断言,等 `MAPPING_SAVE_END`。** 它是终态(状态机里其后无任何排期, 会一直保持),而 `_ready()` 本来就是等到观察到它才返回,断言时刻它一定在 —— 确定性通过。要求实现者把"为什么不是 MAPPING_RUNNING"的理由抄进测试注释, 挡住下一个人改回去。 代价若判断错:少测了"建图运行中"这一档的事件 —— 但事件机制本身由其余几档 覆盖,而瞬态采样能力本来就是真机才能定论的事(清单第 10 条)。

**55.** `[—]` **凡是对链路上请求集合做精确断言的测试,必须把轮询器静默掉。** 在 `test_vendor_matching.py`(三处会真正连上的构造)与 `test_vendor_capabilities.py` (fixture)加 `status_poll_interval_s=_POLLER_OFF`(= 3600.0),并把理由写进常量注释。 因为 `_poll_loop` 的 sleep 在循环体开头,这个间隔意味着测试期间轮询器一次都不发。

**56.** `[—]` 终止评审代理,不续这一轮。 — 复核必须在修复落定、diff 成型之后做, 那是"作用域再评审"(scripts/review-package)的活,不是原评审代理接着跑测的活; 一个跑在流动工作树上的复核,绿也不算数,红也不算数。 — 如果错了:损失是这轮复核的沉没成本(约 1 小时评审代理的推理), 以及 I3 变异缺口要等到再评审时才重新确认。两者都在下一轮里必然会被覆盖到, 不会漏掉任何一条发现。

**57.** `[—]` 批准这项计划外改动,不算越界。 — **这个 bug 是我自己的裁定 I2 放大出来的。** `_link_down` 闸门是我要求加的;加之前 `_on_link_lost` 会重复进入,反而让链路 "错着错着自己好了",AssertionError 只表现为噪声;加之后早退生效,重连循环成了 唯一的自救通道,它一死就是永久停摆。**修一个由自己指定的改动所放大的缺陷, 属于本任务范围之内,不是范围蔓延。** 判据是"这个缺陷是不是因这轮改动而变得更严重", 不是"裁定书里有没有写"。 — 如果错了:代价是 199 行产线 diff 里有约 60 行没有经过 裁定书审过的设计(显式传连接、陈旧报告判别、兜底放宽到 Exception), 需要再评审重点盯这一块;我已在再评审派单里把它列为最高风险区。

**58.** `[—]` 接受,不要求补测试。 — 本卷刚刚因为"靠时序碰巧才抓得到"的测试吃过大亏: Task 14 初版正是单跑绿、全量三分之一概率红。一条只能靠运气变红的测试, 它的绿是没有信息量的绿,比没有测试更坏 —— 它会让下一个人以为这里守住了。 I3b/I3c 属于"以构造消除一类竞态"而非"新增一种行为":前者把生命周期标志的 写入权收归 connect()/close(),后者恢复承重的去重守卫,两者的正确性由代码结构 保证,不由运行时行为暴露。实现者顶住了凑覆盖率的诱惑,这是对的。 — 如果错了:代价是将来有人重新把 `self._closing = False` 挪回 `_open_link()`、 或把断引用挪到 await 之前,而测试不会红。缓解:两处都已在代码里写了长注释 说明"为什么不能挪",并指名现场证据 `flake-evidence.txt` 的 `assert 13 == 2`。 再评审若能构造出确定性用例则采纳,构造不出就停在这里,记入最终评审待办。

**59.** `[—]` 本轮不动,转为再评审的一道指名问题。 — 这是泄漏不是停摆,不影响稳态正确性; 而本卷有过 Conflict A 的教训(诊断对、修复范围过大,打红 4 条变异验证过的测试), 在没有失败用例的情况下改并发拆解路径是拿正确性换整洁。**要求再评审正面回答: 这条窄缝能不能造出失败用例?** 能造出来就进第 2 轮修复;造不出来就 park 到 最终全分支评审。 — 如果错了:代价是进程退出时可能漏一条 socket, 以及仿真器侧多算一个在线客户端;两者都不影响本卷交付物的行为契约。

**60.** `[—]` 前三轮一律不作为证据,只有最后一轮算数。 — 前三轮整个跨在实现者 改 `vendor_nav.py` 与 `test_vendor_resilience.py` 的过程中,读到的是改到一半的 文件;那个 2 小时 0 分 38 秒的耗时就是明证(正常 35s),说明它大半时间在跟 实现者抢 CPU、或卡在被改坏的中间态上。这与我终止 a2e03bffabed218b5 的理由 是同一条:**跑在流动工作树上的结果,绿不算数红也不算数。** — 如果错了:代价是漏看 6 条真失败;但最后一轮(修复提交之后、树已静止) 75 条全绿,且与实现者报告的两组确定性配方各 75 passed 对得上, 交叉印证下这个风险可以忽略。

**61.** `[—]` C1 优先,不作为发现。 — 两条要求冲突时,看的是**意图**:红线的意图是 "不许把这条测试改成抓不到 bug 的样子",不是"这个文件的这几行神圣不可侵犯"。 再评审量了检出力,结论是改后**更严**:修前源码 + 修后测试,3/3 跑出 5 条红; 修前源码 + 修前测试,6/6 全绿。改动后的测试比原来更抓得住这个 bug。 — 如果错了:代价为零,因为检出力是实测出来的,不是推断的。

**62.** `[—]` 撤销 M4 的实现方式,超时改由 websockets 内部计时(建链传 `close_timeout=`)。 — 兜底动作在库的内部,任何从外部掐断它的做法都会跳过兜底。 — 如果错了:代价是 `close()` 最长阻塞 `connect_timeout_s` 而不是立刻返回; 比永久泄漏一条 TCP 连接便宜得多。

**63.** `[—]` 进第 2 轮修复。不指定机制,只要不变式:一条被摘下的连接, 无论摘它的协程随后是否被取消,都必须有人负责关掉它。 — 上一轮我按"无失败用例不改并发路径"压下它是对的流程,但**实现者的判断本身是对的**; 这次有了失败用例,就该修。 — 如果错了:`_on_link_lost` 的 `_spawn` 样板改成 异步拆解后,下一次重连可能在旧 socket 关完前开始,仿真器按在线客户端计数, 可能打红现有测试;已在裁定书里点名要求确认这一点。

**64.** `[—]` 改注释,写真实理由。 — 理由写错的注释比没有注释更坏: 它让下一个人以为安全性来自 A,于是放心地改动 A,而安全性其实来自 B。 **这两条理由我上一轮亲自审过并认可,是我的判断失误,不是实现者编的。** — 如果错了:代价为零,注释改动不影响行为。

**65.** `[—]` 解除"不改仿真器"红线,仅限这一项。 — 318 条测试**结构上**碰不到 F1, 因为仿真器永远老老实实回关闭帧。一个测试套件够不着的缺陷类别, 说明的是仿真器不完整,而本卷的前提正是"仿真器是接口契约的可执行定义"。 半开链路是真机跑出 wifi 覆盖时最常见的形态,不是为凑覆盖率造的假场景。 — 如果错了:多一个没人用的故障开关,以及仿真器多几十行; 比"这类缺陷永远只能靠一次性脚本证明"便宜。

**66.** `[—]` 接受,不 revert。 — 上一轮那个导致后端永久停摆的 AssertionError, **正是被 `log.info` 一行盖掉的**,害得全量跑三次才暴露。 非导航异常落到重连循环的兜底里,按定义就是真 bug,不该用 info 级别悄悄咽下去。 这个改动直接针对本轮暴露的缺陷类别,属于"修自己发现的问题"而不是范围蔓延。 — 如果错了:代价是重连失败时日志更吵;但只在非导航异常时才吵,而那种情况本来就该吵。

**67.** `[—]` 第 1 卷验收里"全量 60 秒以内"拆成「全量 90s」+「-m "not contract" 内循环 60s」两条 — 实测 340 条 75.6/75.4/78.7s、内循环 321 条 51.3s、契约套件单跑 19 条 ~24s；要压回 60s 只能砍约 12 条契约测试或缩短 goto 距离，那是把尺子改短再去量 — 若裁错，代价是开发内循环之外多背 15-19 秒，且没人再被这条硬指标逼着优化测试时长

**68.** `[—]` 计划缺陷 #26 —— 正文 8 处 create_task(wait_nav_terminal(...)) + sleep(0.05) + goto，一律改成 with subscription() as q: goto(); wait_nav_terminal(t, queue=q) — 计划成稿时 wait_nav_terminal 还没有 queue= 参数，只能拿 sleep 当同步原语；那个参数正是后来为消灭这个竞态加的，其 docstring(base.py:139) 把正文这种写法直接点名为"有竞态的用法" — 若裁错，代价是这 8 条测试改写引入新 bug；但不改的代价是 8 处 sleep 同步在 CI 慢机上偶发假红，且新写的测试公然违背端口自己文档里的告诫

**69.** `[—]` 订正 A 顺带把 test_断链期间等待终态直接抛错 的 pytest.raises(Exception, match="断开|disconnect") 收紧成 NavConnectionError — 抓 Exception 会把 AssertionError/TypeError 一并当成通过 — 若裁错(真机上断链抛的是别的类型)，代价是这条测试误红一次，改一行即可

**70.** `[—]` 计划缺陷 #27 —— Step 4 的 `python -m pytest -q --durations=10` 改成不带 -q — addopts 里已有 -q，再加变 -qq，连汇总行都不打印 — 若裁错，无代价，纯口令订正

**71.** `[—]` tests/scenarios 打 pytest.mark.scenarios 并在 pyproject 注册，与 contract 对称 — 这套测试同样是"慢 + 与实现耦合"，理应能从开发内循环里摘出去 — 若裁错，代价是多一个没人用的标记

**72.** `[—]` 预先批准 Step 3 里把断链窗口 0.4→0.8，实现者自行判断不必来问；但"不许靠加长 _drain_until 超时掩盖"这条红线照旧 — 这是计划正文自己写好的应急处置，没有再确认一次的价值 — 若裁错，代价是断链窗口偏长、这条测试多跑 0.4 秒

**73.** `[—]` 必修 1 —— test_关闭后不再连接 首行加 assert nav_backend.connected is True — 端口新加的唯一抽象成员只被约束了一半，一个答不出"我连上了"的 Nav2 实现能满分通过，正是 README 警告的假通过 — 若裁错，无代价，fixture 已 connect，不引入任何等待

**74.** `[—]` 必修 2 —— 契约层三处 create_task + sleep(0.05) 改成 with subscription() as q: goto(); wait_nav_terminal(t, queue=q)。与 Task 16 订正 A(缺陷 #26)同一裁定，两处保持一致 — 第二个理由比第一个重要:端口对外文档化并推荐的 queue= 用法,契约层一次都没覆盖过,那 Nav2 实现根本没被要求支持它。这是契约的洞,不只是风格 — 若裁错,代价是三条测试改写引入新 bug

**75.** `[—]` 必修 3 —— test_导航到点走到成功:362 断言集收紧为 {SUCCEED, STANDBY}，STANDBY 必须留着(终态保持 terminal_hold_s 后回落，轮询可能只看到回落后那一下) — 若裁错，代价是这条测试偶发误红

**76.** `[—]` 计划缺陷 #28 —— Task 17 测试块"同步测试 + 异步 sim fixture + main() 内部 asyncio.run()"的组合跑不通。裁定把 main 拆成同步壳 + 异步核 _amain(args)，要仿真器干活的 10 条测试改 async def 直接 await _amain，不碰仿真器的 3 条保持同步以覆盖壳本身 — 仿真器服务端在 fixture 的 loop A 上，同步测试体执行期间 loop A 不转;asyncio.run 另开 loop B 占住线程,TCP 连接进了 backlog 但 WebSocket 握手没人应答,挂到 connect_timeout 返回 1,断言 ==0 必红 — 若裁错,代价是白改一遍调用形态(断言一字未动),而不改的代价是整块测试挂掉

**77.** `[—]` 三条测试对各自要测的注入形同虚设，必须改到"注入变空操作就变红"，并以变异自证 — test_卡住时导航等待终态超时:5m/0.6m/s 需 ~8.6s 而超时设 1.0s,卡不卡都必超时;test_减速会让原本够用的超时变得不够用:同因,且"原本够用"这个前提在该参数下从来不成立,测试名承诺的性质与它实际测的是两回事;test_乱序加并发不串台:注入失效时响应顺序到达、结果同样正确,平凡通过 — 若裁错(改完引入新的不稳定),代价是这三条偶发误红;不改的代价是三条故障测试长期假绿,真机上 stuck/slow/乱序出问题时没有任何测试会响

**78.** `[—]` 认定为产品缺陷而非测试缺陷，但不塞进 Task 16 — 它在 backends/base.py,是 Task 12/13 的地盘,且正确的修法(drain(queue) 方法 / 事件序号)是设计决策,应当和 Task 14 已停放的 link_generation / wait_connected(after=...) 一起在最终全分支评审的修复波里统一考虑 — 若裁错(拖到最后才修),代价是 Task 17-19 若有新使用者会第三次发明绕法;缓解措施:已记在此处,派 Task 17 时会提醒

**79.** `[—]` 90 秒预算的重裁继续暂缓,先查这 47.9 秒 — 3.00s 这个数字太整齐,不像负载抖动,像某处固定超时(conftest 里 request_timeout_s 正好是 3.0,是条线索但未证实);若它是产品缺陷(比如后端关闭路径上有人在等一个永远不来的应答),那真机上每次关停都会白等 3 秒,那是产品问题不是测试问题,改预算就是拿数字盖住 bug — 若裁错,代价是多花一轮调查;反过来先改预算的代价是把一个可能的产品缺陷永久合理化

**80.** `[—]` 强制 EventEmitter 泛型化(Generic[_E] + TypeVar),NavBackend(EventEmitter[Event]) / DeviceBackend(EventEmitter[DeviceEvent]),并补正文漏掉的 DeviceEvent 联合类型 — 3.10 没有 PEP 695 语法,只能走 typing.Generic;代价是多 4 行样板,换回端口对外宣告的类型信息 — 若裁错(泛型在 3.10 上有坑),代价是实现者一轮返工;但四条关键事实我已在本机 3.10.11 实测:issubclass 穿参数化基类为 True、Generic[UnionType]+ABC 正常、裸 EventEmitter() 可实例化、FrozenInstanceError <: AttributeError,所以正文三条测试一条不用改

**81.** `[—]` 抽取脚本必须带一遍机械修复(尾逗号 + 字符串感知的行注释剥离),先严格解析、失败再修、修复项写进 index.json 的 repaired 字段 — 实测修复后 67 段收 61 段(多收 963/1181/1671 三段),分布变成 36/24/1,三类齐;修复必须是统一施加的规则,严禁手改任何单段 JSON —— 手改过的 fixture 就不再是"厂商给的样例",这个任务的全部价值就在"不是我们自己编的" — 若裁错(修复规则掩盖了真实的解析器缺陷),代价是 golden 语料掺进 3 段本该报错的报文;不修的代价是故障推送链路的唯一真机样例被静默丢弃,且丢弃处伪装成一条绿测试

**82.** `[—]` 本任务就把这两种形状收下,nav_requests.py:26-28 那条推迟作废 — 推迟写在样例被抠出来之前,它假设支持这两种形状有工作量,实测各几行;不修就得排除这 3 段,而"把量不过的样例排除掉"正是本卷验收段写明要防的"把尺子改短再去量" — 放宽只动"读"不动"写":NESTED_RESULT_KEY 改成键名元组、parse_request 接受 str,而 NESTED_RESPONSE_FUNCS(管仿真器发响应时套不套壳)一字不动,对设备发来的东西宽容、对自己发出去的东西严格 — 若裁错(第 2 卷发现 AppResponse 还有别的语义),代价是那时再收窄一次;已核实这两处放宽碰不红任何现有测试(拒绝用例是空 dict 和双键 dict,裸字符串不在其中;响应拒绝用例压根没有外壳键)

**83.** `[—]` 本次不改产品代码,把这条连同"已验证的修法 + 已证伪的修法 + 全部实测数字"挂进最终评审波 — 理由是三条:(1) 这是 vendor_nav.close() 的产品缺陷,属 Task 11/14 地界,Task 16 是场景测试加固,当场改就是越界且无人评审;(2) 修法有效但**机理我没查透** —— 我能稳定复现"第一次 cancel 丢失、第二次生效",却讲不出 CPython 里它是怎么丢的,而本项目恰好已经在"方向对、机制错"上栽过一次(M4 那个 close_timeout 外套 wait_for 导致永久 TCP 泄漏),在并发代码里凭一个讲不清机理的修法动手,重犯的正是同一个错;(3) 缺陷只影响关闭耗时,不影响任何功能正确性,全量 353 条在修与不修两种状态下都全绿,推迟没有风险敞口 — 若裁错(最终波把它漏了),代价是真机每次关闭白等 5 秒;当场改错的代价是往一条已知易错的并发路径里塞一个没评审、机理不明的改动

**84.** `[—]` 全量 90s 预算不动,也不放宽 — 当前 118s 里有 48s 是上面这条缺陷,不是测试写得慢;缺陷修掉后实测 69.71s,本来就在 90s 以内。内圈(-m "not contract")本来就已达标 — 若裁错(缺陷最终没修),代价是全量长期超预算 30 秒,届时再单独裁;现在就放宽预算等于用调尺子掩盖一个已经定位到行的产品缺陷

**85.** `[—]` 60s 内圈预算不动,不放宽、也不许砍测试 — 实测超出的 6.8s 完全被那条 close() 缺陷的 15.07s 覆盖,修掉后 51.75s 达标;这跟全量 90s 那条是同一笔账、同一个根因,两处都不该用调尺子来掩盖一个已经定位到行的产品缺陷 — 若裁错(最终评审波没修那条缺陷),代价是内圈长期超 7 秒,届时连同全量那条一起重裁;现在放宽的代价是把唯一一个能逼着去修缺陷的信号关掉

**86.** `[—]` 实现者自行修掉的两处(map-start/map-stop 不等状态机迁移、walk 两点之间不等回 STANDBY)予以采纳,不算越权 — 这是厂商设备真实的状态机约束,本仓库别处早已按同样方式绕过(tests/contract/conftest.py 的 ready_backend、test_连续走多个点),计划正文的 cli.py 漏了它,是计划缺陷不是实现者加戏;判据是它没有改任何测试断言或期望输出来迁就,而是让产品代码去满足既有断言 — 若裁错(轮询等待掩盖了更该暴露的时序问题),代价是 CLI 多两个可调常量,最终评审波可再收;不采纳的代价是交付一个连着走两个点就被设备拒绝的命令行

**87.** `[—]` NavTimeoutError 单独一个分支、排在 NavBackendError 前面、输出补"超时"字样,采纳 — 子类必须排在父类前面是硬约束,而 base.py 原始措辞是"超过 Ns"不含"超时",正文测试又断言 stderr 里有"超时";三者只能靠这个分支同时满足 — 若裁错,代价是命令行多一行措辞;不采纳就得改测试断言,而那是"把尺子改短"

**88.** `[—]` 判为我的订正缺陷,不计实现者过失;修法是把 KeyboardInterrupt 拿回 main() 包在 asyncio.run() 外面,同时删掉 _amain() 里那条(留着会让下一个人误以为有防护),另两个异常分支留在 _amain() 不动(测试直接 await _amain 时要靠它们),并要求在 main() 注释里用中文写明"为什么这一条在上层、那两条在下层" — 若裁错,代价是 main() 多几行注释;不写清楚的代价是下一个人再把它挪回去,而这个 bug 静默、只在真人按 Ctrl+C 时才现形

**89.** `[—]` 从此往后,订正里凡是给出代码骨架,要么写完整可直接落地的版本,要么在骨架旁边显式列出"本示意省略了什么、哪些必须从正文原样保留" —— 不许再出现带 ... 占位或省略 try/except 的示意块 — 依据是两次翻车都出在这个形态上,而且两次都是"省掉的那部分才是承重的";代价是订正更长,换掉的是一类已经复发过一次的静默缺陷

**90.** `[—]` 新立的规则立刻回溯扫一遍所有未派出的 brief,不等下次踩坑 — 代价是几分钟;不扫的代价是明知形状还让它再发生一次,而这个形状已经连着坑了两次(M4、Task 17)

**91.** `[—]` 既然变异正好还在树上、agent 又已停摆,就地把这次独立复核跑掉,而不是先还原再重新施加一遍 — 省一轮 2 分钟的全量,且验的是它真实留下的那份代码;代价是我的复核与它的复核共用同一次变异施加,若它施加错了我不会独立发现 —— 但变异内容我逐字看过 diff,就是评审要求的那一处,风险为零

**92.** `[—]` 任务通知的 completed 只代表"该 agent 当前没有存活的后台子任务",不等于它交付完毕、更不等于它放开了工作树 — 通知里 result 字段明说在等通知时尤其如此;从此往后,凡是打算动工作树(跑测试、改文件、施加变异),先 ListAgents 确认没有 running 的子 agent,而不是凭通知判断 — 依据是这次通知说 completed、实际它 15 分钟后仍在 running;代价是每次多一次 ListAgents 调用;不这么做的代价是并发写同一棵树,而本卷已经因为 agent 抢树吃过两次亏,这是第三次

**93.** `[—]` 判为需要修的真缺陷,开修复轮 2/5(仍续同一 agent,R=2≤3) — 检测力虽够,但信号形态在真回归时代价很大:这条测试排在 371 条里的第 4 位,一旦回归,整场在第 4 条被打断、后面全部结果丢失,而且中止信息长得就像"有人按了 Ctrl+C",极易被误判成环境问题而不是回归;改法是就地 try/except 转 pytest.fail,3 行、零风险、只动测试不动产品 — 若裁错(其实不值得单开一轮),代价是一轮很短的 agent 时间;不修的代价是往仓库里长期埋一条"回归时会把整场炸掉并伪装成人为中断"的测试

**94.** `[—]` 这一轮的自证要求从"结论"改成"形态"——必须同时给出三条:报 F 而非中止、退出码 1 而非 2、该文件其余测试照常跑完 — 依据就是这次:只要"目的达成"作验收口径,形态缺陷就会被如实但无用地报告掉;代价是自证多两行输出

**95.** `[—]` 修复轮 2 不再单独派 scoped re-review,由我直接裁定收口 — 依据是三条:(1) 这轮 diff 只有 8 行、只在一个测试函数内、产品代码零改动,我已逐行读过 diff 并独立重跑了全部三条自证;(2) 这轮要修的缺陷本来就是我自己复核出来的,不是评审提的,再派一轮评审去看我自己提的 3 行改法,期望收益极低;(3) 全分支最终评审波会完整覆盖这个文件 — 若裁错(这 8 行里藏了我没看出来的问题),代价是留到最终波再收,而它只影响一条测试的失败措辞、不影响产品行为;照常派一轮的代价是一整个评审 agent 的时间与上下文,换一个几乎必然为空的结果

**96.** `[—]` 不因这次事故要求返工或重做,但把"重建是否完整"提为本次评审的首要任务、并由我先行独立核验 — 判据是事故的爆炸半径可以严格界定:本任务只动了 base.py 一个产品文件,src/ 下其余文件本就未被修改,checkout 对它们是空操作,所以可能丢失的内容上限就是 base.py 的那一份工作,而这份工作有 brief 订正 A 给出的逐字代码可以对照;它主动如实上报而不是掩盖,这正是我要的行为,惩罚性返工会训练出相反的习惯 — 若裁错(重建真丢了东西且三方核验都没看出来),代价是一个抽象定义文件里少一段代码,而它下游本卷无实现、第 2 卷才用,最终评审波仍会再看一遍;要求返工的代价是浪费一整轮,去重做一份已有逐字规格可对照的产物

**97.** `[—]` 判为可接受的有意例外,不返工 Task 17,但挂进最终评审波,处方是给这处 import 包一个 try/except ImportError 并给出中文提示(大意"仿真器未安装,d1max sim 只在开发环境可用") — 依据是局部 import 保住了约束的真实目的(无导入期耦合、真机不装也能跑全部真实功能),而 sim 子命令本身是计划正文明确要求的开发便利;判它违规而返工,等于用约束的字面去打约束的目的 — 若裁错,代价是真机上一个错误提示不够友好;现在返工的代价是重开一个已关闭的任务

**98.** `[—]` 判为必修,开修复轮 1(Task 18,R=1),尽管它是既有缺陷、不在 Task 18 改动范围内 — 三条依据:(1) 这与 Task 17 修复轮 2 是同一缺陷类别(失败形态错误),我在那里已裁定必修,同类不同判会让"形态即缺陷"这条标准失去约束力;(2) 挂起比中止更糟 —— Task 17 那次至少还给退出码 2,挂起在 CI 上只剩一个超时,不产生任何可诊断信息,本地跑的人还会误判成自己机器卡了;(3) 它恰好是本次重构唯一的回归网 —— test_base.py 这次一行未改,正因为"没改所以没坏"的推定成立与否全靠它,而它在真出 bug 时不会说话 — 若裁错(不值得为既有缺陷单开一轮),代价是一轮很短的 agent 时间;不修的代价是给这个刚被重构、且第 2 卷还要继续扩用的基类,留一张出事时会哑掉的网

**99.** `[—]` 明确要求这轮还原变异改用 cp 备份或 Edit 定点改回,禁止再用 git checkout -- src/ — 依据是上一轮的事故就出在这一手上;同时明确告知它"主动上报没有被扣分",避免把"别再用那个命令"误读成追责 — 代价是派单里多两句话

**100.** `[—]` 判为验收标准定错、不算实现者未达标,接受交付 — 我写"整条命令 10 秒内自己结束"时,真正想要的性质是"会自己终止,不会挂死",10 秒只是我随手挑的代理指标,而它在"变异同时打挂 9 条测试"的场景下天然不成立;承重的性质已经达成:整场 exit 1 正常结束、目标测试以 TimeoutError 报 F、其余测试照常跑完 — 若裁错(其实标准没错、是实现者偷懒),代价是放过一次不达标;但它给出了隔离命令的实测作为反证、并主动标了 DONE_WITH_CONCERNS 而不是把 19s 说成 10s,这正是我在派单里要的"如实描述形态"行为,判它未达标等于惩罚诚实上报

**101.** `[—]` 以后写验收标准,分清"承重性质"与"代理指标" —— 该写"命令必须自行终止、不得挂起(必要时给 timeout 兜底)",而不是写一个具体秒数;墙钟秒数会被同一次变异的连带失败污染 — 依据就是这次:5.04s 的真实信号被 9 条连带失败拖成 19s — 代价是标准写起来啰嗦一点

**102.** `[—]` 修复轮 1 不再单独派 scoped re-review,由我直接收口 — 与 Task 17 修复轮 2 同一判据:diff 只有 2 行有效改动、只在一个测试函数内、产品代码零改动,且这轮要修的缺陷本身就是评审变异实验捞出来的、改法是我开的,我已逐行读 diff 并独立重跑了隔离与整文件两组自证 — 若裁错,代价留到最终评审波收,而它只影响一条测试的失败形态

**103.** `[—]` 采纳实现者对订正 E 第三条的反驳,判定该条为我的事实性错误(行号记串+虚构了一处不存在的代码张力),实现者"报告而不遵从"的处置完全正确,不算抗命也不扣分;它没有去改第 7 行来迁就我的错话,而是把真实发现另立第 18 行 — 依据是我自己 grep 复核了 PUSH_ONLY_FUNCS 的成员与清单第 7 行的实际内容,两处都与我的订正不符 — 若裁错(其实我原意指的是别的行),代价是清单多一行第 18 行、且第 7 行未被改动,最终评审波可再收;不采纳的代价严重得多:等于要求实现者按一条经不起核对的指令去改一行本来正确的文档,并教会后续实现者"控制者说错也照做"

**104.** `[—]` 把"实现者可以反驳控制者的订正,但必须在报告里写清反驳依据、并给出它实际做了什么"这条固化为派单模板的一部分,后续每份 brief 都带 — 依据是四次里有三次实现者的判断优于我的文本,而这个通道每次都靠实现者自己冒风险开启;代价是 brief 长两行,换掉的是"控制者笔误被无声执行"这一类缺陷

**105.** `[—]` 判定 Task 19 的三项自证全部属实,协议层改动(NESTED_RESULT_KEYS 元组化、parse_request 接受裸字符串)只放宽读、不放宽写 —— build_response 仍只发拼错的那个键、NESTED_RESPONSE_FUNCS 一字未动、既有 170 条协议测试无一变红,与我在订正 G 里定的"只放宽读"边界完全吻合 — 依据是上面五行实测 — 若裁错,代价是解析器接受了真机不会发的形状(比"拒收真机会发的形状"轻得多);反过来收紧的代价是明知文档有两种外壳/两种 req_func 形状却装作没有

**106.** `[—]` 终审用"读代码树"而不是"读全分支 diff" — 依据是这条分支是绿地新建(17940 增 / 2 删),对空基线的 diff 本质就是文件本身,而 -U10 的 diff 形态会让评审多读一倍噪声、还看不到文件全貌;停放项与阅读地图两份文件替代 diff 承担"从哪看起"的职责 — 若裁错(评审因此漏掉某处提交间的演变),代价是漏掉"中途改坏又改回"这类只在历史里可见的问题,而这类问题本卷每个任务都已逐个评审过一遍;硬塞 diff 的代价是把评审的上下文预算烧在格式噪声上

**107.** `[—]` 评审对"or 短路可能是死断言"的证伪成立,我的怀疑不成立 — 依据是它数出命中样例恰 2 条且 data 均非 None,右侧断言每次都执行 — 这是本项目第五次下游正确顶回我的文本(前四次:M4 的 close_timeout 证伪、订正 A 的缩略示意、Task 18 对"10 秒"验收数字的质疑、Task 19 对订正 E 第三条的反驳)。五次里有四次是我错。代价:无 — 证伪不需要改任何代码,只需要我不去"修"一条本来好的断言

**108.** `[—]` 60 条文档样例测试的弱断言判为必修,不 park — 依据有三:(1) 这套语料是本项目在无真机阶段唯一接近真实输入的资产,60 条测试只证明"没崩"等于把这份资产的大部分价值悬空;(2) 修法便宜且机械 —— 让抽取脚本额外记一个 expected_req_func 到 index.json,测试比对它,同时把 doc_line/repaired 这两个死字段一并激活,一改解决评审 #2 与 #5 两条;(3) 这条缺口是**我的 brief 模板逐字带来的**,实现者如实照办且没有削弱断言,按我自己立的规矩,我的规格缺陷该我开轮修掉,不是留给下一个人 — 若裁错(这轮改动引入新问题),代价是一轮往返,且改动面严格限于一个测试文件加脚本一个新函数;park 的代价是第 1 卷交付一份 60 条只验类型的 golden fixture,而 Task 19 之后没有任何任务会自然回到这个文件

**109.** `[—]` 采纳评审关于 #1 "不要挂到终审"的意见,lint 周界本轮一起修,并把本仓库的 lint 指令正式改为**不带路径的 ruff check** — 依据是评审那句"第 1 卷结束后没有天然的下一个任务会重新踩到这个文件,挂起来容易变成不了了之",这个判断对;选不带路径而非 ruff check src tests scripts,是因为前者让"周界漏洞"这个缺陷类别整体消失,不必依赖后人记得拓宽 — 我实测过:裸 ruff check 扫 45 个文件、恰好抓到那一条 E501、不会扫进 .superpowers/ 或 refs/(ruff 默认尊重 .gitignore) — 若裁错(将来某个目录不该被扫),代价是在 pyproject 里加一条 exclude;继续用带路径的写法,代价是把同一个漏洞留在原地

**110.** `[—]` 驳回评审 #4(给 parse_request 裸字符串分支补专门单元测试)— 依据是我自己做过那处变异:删掉该分支,tests/protocol 恰好只红 065.json 一条(1 failed / 169 passed),分支已被语料精确咬住,补单测只增加定位便利、不增加检出能力 — 若裁错,代价是将来该分支出问题时报错位置指向语料而非单测,定位多花一分钟;照做的代价是给一轮已有三件事的修复轮再加一件零检出增益的活

**111.** `[—]` 抽取脚本的 expected_req_func 必须由脚本自己直接扒 JSON、**禁止**调用 protocol 的解析函数 — 依据是一旦调用,测试就成了"解析器跟自己比",恒真,这条断言的全部价值归零;要的正是一份有意的第二实现 — 若裁错(两份实现漂移导致假红),代价是偶尔要人工判定哪一份对,而这恰恰是发现问题的时刻;去重的代价是写一条永远绿的断言,比没有断言更坏,因为它看起来像有保护

**112.** `[—]` 修复轮 1 派了**新 agent** 而非按 SDD 的 R≤3 规则续用原实现者(a46df74c93ea884f2)—— 这是有意偏离,记在这里而不是假装守规 — 依据是本轮三件事里有两件是**全新产物**(一个新测试文件 tests/scripts/test_extract_doc_samples.py、脚本里一个新函数 expected_req_func),只有 lint 那条是改自己写过的行;续用的价值在于"记得自己为什么那样写",而本轮主要不是改它写过的东西,加上 brief 已把每段代码逐字给全并经我实跑验证,新上下文反而没有"我上次是这么想的"这种包袱 — 若裁错,代价是新 agent 要重新读一遍语料生成的来龙去脉,多花几个回合;续用的代价是让一个上下文已被 Task 19 正文占满的 agent 再背三件新活

**113.** `[—]` 不派定向复审,由我直接结案 — 依据是三条:(一)这一轮的每一行代码都是我在派单里预先在本机验证过的原文,复审要复的是我自己的处方;(二)我已独立重跑变异 A 并逐条对照文档核过 61 个期望值,这比复审通常会做的更硬;(三)下一步就是全分支终审,这五个文件必然落在它的阅读面内,派一轮定向复审等于对同一段代码连查两遍。先例两向都有(Task 17 修复轮 2、Task 18 修复轮 1 都是控制者直接结案),本轮差异比那两次略大,所以我补读了新增测试文件的全部 82 行才落这个裁定 — 若裁错,代价是终审多担一份 207 行的审阅面,而终审本来就要读这棵树;派复审的代价是一整个审阅席位换一份已被我复核过的结论

**114.** `[—]` 终审期间我不碰工作树 — 评审被明确授权为变异测试临时改产品代码,我这边任何并发写入 都可能污染它的观测或被它的还原覆盖;本项目已经吃过一次亏(在实现者活跃的树上跑了 131s 全量)。 代价是这段时间我只能做工作区内的文字工作(ledger、收尾材料),换掉的是一类会让终审结论作废的干扰

**115.** `[—]` 派单第 10 节显式邀请评审反驳我,并把"实现者五次异议、四次是我错"的战绩告诉它 — 依据是本项目的实证:这个信息给出去之后,实现者的异议质量明显高于沉默照办的轮次,而我自述 的错误率是让评审敢于反驳的最有效凭据;代价是可能收到一些站不住的反驳,那不难裁;不给的 代价是终审对着一份我写的、已知含错率不低的派单照单全收

**116.** `[—]` 采纳闸门,并明确废弃我先前的"补发 cancel"修法,连纵深防御也不留 — 评审说两者可并存, 我不同意:留着它会在代码里立一堵看起来承重、实则依赖未记录次序不变式的墙,下一个人重排 tasks 列表时不会有任何红,而他会以为那行 cancel 保着他;一个机制、一处闸门、一段写明因果链 的注释,比两个修法更难被无意破坏 — 若裁错,代价是少了一层冗余,而闸门本身与次序、机制都无关; 留着的代价是把本项目栽过一次的"现象消失、机制没搞对"重新种回代码里

**117.** `[—]` 时间预算的裁定回头看是对的,记一笔 — 我在 Task 17 顶住了放宽 60s 内圈预算的压力, 理由是"超出部分完全由那条 close() 缺陷贡献,放宽等于关掉唯一逼着修它的信号"。现在缺陷修掉, 两项预算不改一个字同时达标(87.84s / 53.86s)。这条留档是因为当时它只是推断,现在有了实证

**118.** `[—]` 四条重要项(IMP-1..4)全部入本轮修 — 判据统一: 每条都有变异实测证明"它声称守的东西 被改坏时测试照常全绿",这正是本项目一路在猎的空断言形状,也是本卷唯一一次修复机会; 代价是这一轮改动面比典型修复轮大,换掉的是四处已被证明为零的防线

**119.** `[—]` 次要项修八条(MIN-1/2/3/4/5/6/7/9),不修 MIN-8 与 MIN-10 — MIN-8 的正解是引入 mypy/pyright,那是第 2 卷的工具链决定;MIN-10(websockets>=13 但实际只验过 16.0)下界不动, 因为 >=13 是设计文档的全局约束、且新 asyncio 接口确实是 13.0 落地的,抬下界等于用一条 未经用户确认的收紧去换一个还没发生的问题,改为就地写注释 + 把 13.x 的 CI 矩阵留给第 2 卷 — 若裁错,代价是 13.x 上可能有未知不兼容,而本卷的运行环境是确定的

**120.** `[—]` MIN-7 用"把 BLE 加进 ruff select"而不是"把 noqa 改成普通注释" — 我实测过爆炸半径: 启用后只多 2 条命中(nav_server.py:239/250),都是有意的宽捕获且已有日志,补上 noqa 即可 保持 ruff 全过。这样那 8 处空注解一次性变成名副其实,而不是被降级成注释后继续误导下一个人 — 若裁错,代价是 select 多一条规则、将来新写的宽捕获要显式标注,那恰恰是想要的

**121.** `[—]` close() 耗时测试的"怎么构造"不写进处方,只写验收标准 — 我先前那个探针脚本就是栽在 这里(干净状态下 close() 本来就是 0.000s,于是错误地报告"问题不在这里",脚本顶部有作废记录)。 我没有一个已验证可用的构造方式,按本项目的规矩就不能给代码骨架 —— 两次翻车都出在 "我给缩略示意、实现者当完整规格照抄"。改为把证明义务写死: 删掉闸门这条测试必须红且只有它红, 造不出确定性构造就如实报告、不许交 flaky —— 若裁错,代价是实现者多花一轮探索

**122.** `[—]` 采纳实现者对 MIN-7 的偏离,并认定它比我的处方好 — 我的前提错了:BLE001 本来就不报 "把异常记进日志或原样再抛"的处理器,而原有 8 处里恰好有 5 处是这一类,只加 BLE 只能治好 3/8。 它删掉那 5 处失效注解并把 RUF100 加进 select,把"注解上写的规则确实存在"从一次性清理变成了 有人守的性质。这是本项目第五次实现者对我的指令提出异议、第四次是我错了(连同终审那次, 下游六次异议、五次我错) — 若裁错,代价是 select 多一条规则、将来新写的宽捕获要显式标注, 那恰恰是想要的

**123.** `[—]` 90s 全量预算本卷不动 — 实测 88.86s 达标,余量约 1.1s。实现者提出余量已饱和、第 2 卷 会撑破,我同意但不在本卷改尺子:预算是本卷的验收条件且已诚实满足,提前放宽等于用"将来会超" 去豁免"现在没超"。**转为第 2 卷开工前的待决项:提预算或上 pytest-xdist,不许靠删测试维持。** 连同实现者把 IMP-1 的 close_timeout 从我举例的 3.0 降到 2.0(买回约 1s)一并采纳 —— 派单原文 写的是"例如",性质(任何短于 close_timeout 的外层超时都违反下界)不变,只是可捕获区间从 <2.4s 收窄到 <1.6s,且它主动披露了这个取舍与数字 — 若裁错,代价是落在 (1.6, 2.0) 的外层超时 会逃掉,窗口很窄且已记录在案

**124.** `[—]` 实现者顾虑 3(IMP-1 拦不住"外层超时 == close_timeout"那个历史写法)如实采信,不要求 本轮解决 — 真正的修法是给 NavConfig 加一个与 connect_timeout_s 分家的 close_timeout_s,属结构 改动,超出"唯一一次修复"的范围。**列为第 2 卷结构改动待办。** 它没有自称已解决,这份记账 本身就是我要的东西 — 若裁错,代价是那个特定巧合仍只靠 transport.is_closing() 的毫秒级断言兜着

**125.** `[—]` 顾虑 4(BLOCK-1 测试依赖 CPython 内部字段 Future._callbacks)采纳 — 判据是失败方向: CPython 若改字段,测试会在 _wait_until 的 10s 处响亮超时失败,而不是静默变绿。本项目的"失败 形态"标准要的正是这个;一条依赖内部字段但坏了会喊的测试,优于一条不依赖内部字段却可能悄悄 退化成永远绿的测试 — 若裁错,代价是将来升级 Python 时多修一条测试,而那次修理会被明确提示

**126.** `[—]` 顾虑 6(额外补了 MIN-1 的守卫测试,派单没要求)采纳 — 新加一道无人验证的守卫与本轮 主旨(猎空断言)直接相悖,补测试是对的,耗时 0.10s

**127.** `[—]` 顾虑 5 不成立,但责任在我 — probe_teardown.py 的作废标注**存在**,在文件底部第 123 行; 我派单里写成"顶部",实现者据此去顶部找、没找到,报告"没有任何标注"。我已在顶部补一行。 这是我第四次因为派单里的事实错误让下游白跑一趟(前三次:订正 A 的缩略 main()、M4 的 close_timeout 包装、订正 E 说错行号) — 代价:无;记在这里是因为这个形态已经复发四次,它比任何单条缺陷都更值得盯

**128.** `[—]` src/ 那 2 处退回实现者改成英文,tests/ 那 90 处不动 — 判据是"本轮新引入 vs 既有约定": 产品代码里刚长出来的违规就该在合并前掐掉;而对一个已经过 19 轮评审、贯穿全部测试文件的 命名约定,在合并前一刻发起 90 处重命名,风险与收益完全不成比例,且它不影响产品代码的可读性 契约 — 若裁错,代价是测试文件里保留一个与全局约束字面冲突的既有约定,应在第 2 卷开工前 把约束原文改成"产品代码标识符用英文",让文字与实际一致

**129.** `[—]` 判 48b9ee2 通过,标识符问题闭环 —— 依据是七项独立复跑逐条对上,尤其"追加而非 amend"这条我是用 git log 亲自确认的父子关系,不是采信报告;代价若裁错是 src/ 里留一个中文标识符违反全局约束,但 tokenize 复扫为 0 已把这个可能性排掉

**130.** `[—]` 定向复审用 sonnet 而非最强档 —— 依据是 SDD 的模型选择规则"定向复审小修复 diff 取中低档",而本轮唯一微妙的并发改动(BLOCK-1)的机理已经由终审 agent 查清、我又在 CPython 源码上亲自核过,派单里把机理、错误修法、以及两个必做实验(删闸门 / 换任务列表顺序)全写死了,复审的工作是"执行验证"而非"重新推导";代价若裁错是漏掉一条并发缺陷,但两个实验都是判定性的 —— 顺序实验一旦复现 49.98s 就会直接暴露

**131.** `[—]` 判终审定向复审通过,第 1 卷代码工作结束 —— 依据是复审的 13 项核对表 0 存疑、7 处变异,加上我自己亲手重跑的两个判定性实验都复现了(删闸门唯一变红且形态干净、换序 460 passed/89.12s 顺序无关);代价若裁错是一条并发缺陷带进第 2 卷,但换序实验是判定性的 —— 我那个错误修法在这个实验下会立刻暴露成 49.98s,现在没有

**132.** `[—]` 复审转达的 probe_teardown.py 那条不作为残留处理 —— 它引的是实现者报告里的旧账,而那个文件头标记我在上一轮已经亲手补上了(实测 head -3 第一行就是"!! 本脚本的结论已作废…!!");代价若裁错是一个 gitignored 的一次性探针脚本上少一行标注,而且那个目录马上就要删

**133.** `[—]` 删工作区之前先把决策记录和第 2 卷欠账落进 docs/ 并提交 —— 依据是 SDD 要求终审通过后删本计划工作区,而工作区是 gitignored 的,一删就不可恢复;这 132 条裁定里有相当一部分是"为什么当初没那么做"的唯一记载,第 2 卷会需要;代价是仓库里多两个文档文件
