# W07 文档订正 · 完工报告（供审核）

日期：2026-09-23 · 分支：`master` · 规模 S · 工单：`docs/庄园安防-差距核查与工单.md` W07

纯文档，无代码改动。审核只需核对下面每一处改法是否与仓库现状一致。

## 改了什么

| 文件 | 改法 | 依据 |
|---|---|---|
| `docs/分卷与进度.md` | 顶部加「口径更新（2026-09-23）」段，明说四件事：① 2026-09-22 起所有开发在 `master`，「每卷留在分支等真机验证才合」的规矩作废，真机状态改记在工单表与各 W0x 报告；② 产品方向改为庄园安防平台，之后按 W00–W30 走；③ 卷 9 不是未开工——狗侧上传链早已存在并接线，服务器侧只有 `ConsoleStore`；④ 测试命令必须剥掉 ROS 的三个环境变量，`master` 上 5 条已知环境性失败逐条列出 | `git log`（`ed580f7` 之前的整合）、`src/d1max_patrol/engine/{upload_queue,uploader,http_sink}.py`、`app/upload_pump.py`、`src/d1max_console/store.py`；W01–W06 全量测试记录 |
| 同上，卷 9 那一行 | `☐ 未开工` → `🔄 狗侧上传链已写并接线；服务器侧只有 store.py，归 W15`（原文划掉保留） | 同上 |
| `README.md` | 测试命令改为剥环境变量的形式，指向口径更新段；保留旧命令作对照 | — |
| `docs/真机现场执行单.md` 1.1 | `git pull`「在 `feat/交付主线` 上」→「在 `master` 上」；测试命令同步 | — |
| `docs/真机联调手册.md` | 测试命令同步 | — |
| `START-HERE.md` | 开头的 `git checkout feat/交付主线` → `master && pull`；第 1 步改为先读工单表与口径更新 | — |
| `docs/庄园安防-差距核查与工单.md` | W07 打勾 | — |

## 外部审核补改（第二稿）

审核指出：只换了命令没换判据——`真机现场执行单.md` 1.1 仍写「全绿/看没有红的就行/有红就停」，`真机联调手册.md` A4 仍写旧基线 `1460 passed, 11 skipped`，`START-HERE.md` 仍写「环境全绿」；按新命令跑必然见到那 5 条红，现场人会停下或误判环境坏了。已改为：判据 = **失败集合严格等于顶部列出的 5 条**，多一条才停、少一条不停；联调手册去掉旧数字，改为「收集 3570 条上下 + 精确失败名单」；三处措辞同步。

## 没改的

- 各卷收卷记录里的 `⏸ 不合回 master` 字样**原样保留**——它们是当时的事实与推导过程，顶部口径更新已声明作废；逐条改写会抹掉历史。
- `docs/第2卷待办.md`、`docs/真机待验证清单.md` 未动：里面的账仍有效，与分支口径无关。

## 审核者核对

```bash
git show --stat HEAD
grep -rn 'feat/交付主线' README.md START-HERE.md docs/真机*.md   # 只应剩「已整合/不再有」那类说明
head -20 docs/分卷与进度.md
```
