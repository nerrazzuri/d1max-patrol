# W00c6 系列 · 外审交接（W00c6a – W00c6i）

> 用户 2026-09-26：「全部采纳，A，B，C和几项能力回复的全做了，做完了就写报告，我让外审再查一下」。九张单的设计决定都按推荐项取
> （每份设计稿的状态行标了「按推荐」），每张都做了测试先行、突变检查和只读子代理内部评审，内审提出的阻断、应修都已修，再一起交外审。
> 中途用户另加一条：「手机app需要改成横屏模式」（`dddf09f`），一并交审。

日期：2026-09-26 · 分支：`master` · 范围：`e5816e3`（W00c5 系列外审第二轮 APPROVE 的记录）之后到本文档所在提交
（165 个文件，+12 872 / −511 行）

**这一批的来由**（都记在 `docs/庄园安防-差距核查与工单.md` 的决策表里）：

- **决策 10**（用户「全部采纳」外审第二轮建议）：核查 W01–W07 外审分析时新发现的 A、B、C 全开单 —— B 写盘失败卡死、叫停停不住（P0）、
  A 真狗返航绕过 W04（P0）、C 排程没派出去不报警（P1）→ W00c6a–c。
- **决策 9**（同上）：W00c5e 退役掉的能力按优先级补 —— P0 升级前检查、重定位；P1 当前位置标原点、建图日志；P2 建图时看地图 →
  W00c6d–h。**暂缓**：巡检暂停/继续、接管后续跑原任务（「结束旧任务并重新派单」更容易保证状态与安全语义），这一批没做。
- **决策 11 / W08 决定 11**（用户「开，排在 W00c6 里一起做」）：过渡期自主移动技术闸（P0）→ W00c6i。
- **W08 决定 4**：地图位姿只来自定位器；定位器（W09）上线之前的过渡实现是代理侧里程锚定 → W00c6e。

**当前状态（2026-09-26）**：九张单**代码完成、内审修复完成，代码外审通过**（Codex 批准，意见已修，见 §7）；**真机验收一条都没做** —— `D1MAX_HAL` 照旧是 `sim`，
这一批的真机项都排在 W00d 真机验收（`docs/庄园场景待真机测试.md` §3b）之后。**W09 等这一批外审通过再开始**（用户定，不并行）。

## 1. 这一批有什么

| 单 | 做了什么 | 设计稿（`docs/superpowers/specs/2026-09-26-…`） | 报告 | 内审 | 提交 |
|---|---|---|---|---|---|
| W00c6a | 写盘失败不卡死、叫停停得住（核查 B，P0）：归档写不进去（盘满、只读重挂）引擎不再带着异常死掉，这一趟按失败收场；叫停先中止再停车、立叫停栅栏（排队里已收下的也不起跑）；HAL 停车失败不卡住；代理落盘失败不拖垮 | `W00c6a-archive-failure-halt-design.md` | `docs/W00c6a-完工报告.md` | 先修再批准：阻断 2、应修 4 | `856feb3`、`8c8acc8` |
| W00c6b | 真狗返航沿来路（核查 A，P0）：直线桥不认 `return_home`，引擎走沿来路回（来路是真走过的路，环路消除）；半路返航报失败；站点在直线狗跑完巡检后派回程巡检（航点倒序 + 待命点，继承原任务 policy，电量到线接着往前走） | `W00c6b-real-dog-return-design.md` | `docs/W00c6b-完工报告.md` | 先修再批准：阻断 3、应修 6 | `6432a9d`、`5687960` |
| W00c6c | 排程没派出去要报警（核查 C，P1）：执行器知道这一轮不会按时跑时说一次（`schedule_missed` P1、`schedule_skipped`/`schedule_blocked` P2）；狗在忙不算没跑；回执超时两分钟后没见跑才报 `lost` | `W00c6c-schedule-miss-alerts-design.md` | `docs/W00c6c-完工报告.md` | 先修再批准：阻断 1、应修 4 | `2d76000`、`40b690d` |
| W00c6d | 升级前检查（退役能力恢复，P0）：狗上的清单（空闲、电量、盘、装没装、槽里的包对不对、起不起得来、定位器/感知/外参这些来源）+ 站点合上任务包 schema 与备份；切之前站点先核；契约加 `Ack.data`（回执带查询结果） | `W00c6d-release-precheck-design.md` | `docs/W00c6d-完工报告.md` | 先修再批准：无阻断 | `a5f84ae`、`edeb4e7` |
| W00c6e | 重定位 / 初始位姿（退役能力恢复，P0；W08 决定 4 的过渡实现）：代理侧里程锚定 `T_map_odom`，σ 随走过的距离涨、过线不可信；里程断过、跳了就作废；人「设位置」（坐标或「在原点」），任务跑着不收；中途重设时引擎把来路按修正量挪过去；遥测加 `loc` | `W00c6e-odom-anchor-design.md` | `docs/W00c6e-完工报告.md` | 先修再批准：阻断 2、应修 6 | `94c435b`、`16af7d7` |
| W00c6f | 当前位置标原点（退役能力恢复，P1）：狗用此刻锚定后的位姿当原点（要停着、不忙、定位好），**先落盘**（按图号版本的 `homes.json`）再生效；站点登记成默认待命点（站点是权威，下发地图时一律带它）；回执超时按狗的事件补登记；同名点跨图要说替换 | `W00c6f-mark-home-here-design.md` | `docs/W00c6f-完工报告.md` | 先修再批准：阻断 1、应修 8 | `a8df14b`、`620425c` |
| W00c6g | 建图进程日志（退役能力恢复，P1）：狗回日志列表与尾巴（只读查询，按句柄核、尾巴按编码后大小裁到 broker 单包以内），站点给管理员，手机看 | `W00c6g-proc-logs-design.md` | `docs/W00c6g-完工报告.md` | **批准**：应修 2 | `a6db213`、`2baffba` |
| W00c6h | 建图时看得见（退役能力恢复，P2）：**不恢复在线实时栅格**（见 §2），换成录包轨迹（狗按里程记、手机每 2 秒画）+ 建好的图的预览（站点纯 Python 渲 PNG，画待命点） | `W00c6h-mapping-view-design.md` | `docs/W00c6h-完工报告.md` | 先修再批准：应修 3 | `0e8ab67`、`6f9b1ed` |
| W00c6i | 过渡期自主移动技术闸（W08 决定 11，P0）：代理的自主级别 `supervised`/`autonomous`（真狗默认要人监护）；`supervise` 监护租约（手机「我在现场监护」，按会话记、防旧序号、过期先中止再停车）；站点只把排程、事件派遣派给可自主的狗 | `W00c6i-autonomy-gate-design.md` | `docs/W00c6i-完工报告.md` | 先修再批准：阻断 2、应修 5 | `eaf3360`、`f89891b` |
| 横屏 | 用户中途要求：手机只许横屏（安卓清单 + Flutter 各锁一次）；遥控页左杆、画面、「停」+ 右杆三栏；带输入框的对话框整个能滚；画面限高；整个 app 包安全区（横屏侧边导航栏） | — | `docs/手机app.md` 开头 | 批准（同 W00c6g 一轮审）：小问题 3 | `dddf09f`、`2baffba` |

每份报告里有：问题、交付、与设计稿不同的地方、已知限制、测试与突变、内部评审的每一条及修法。

## 2. 跟老能力不一样、要外审和用户看一眼的地方

- **W00c6h 决定 1：不恢复在线实时栅格。** 老服务录包时在线跑 2D slam_toolbox、推「正在长的那张图」。这一批换成「录包轨迹 + 建好的图的
  预览」：W08 决定 4 已定 2D 那一路不再往下走；在 Orin 上跟录包抢算力；过渡期里程本来就漂。代价：轨迹只说明「狗走过哪儿」，不说明
  「激光扫到了哪儿」（玻璃、超出量程的地方要等重建完看预览才知道）。**用户、外审可以否**。
- **W00c6f：原点就是这台狗在这张图上的默认待命点**（W00c5d 起就是同一个点）。W08 说换电位和待命位是两个点，要分开另开单。
- **W00c6e 的偏差模型是估的**：σ = 0.2 m + 走过的距离 × 2/9（W08 粗测 9 m 漂 2 m），过 2.0 m（约 8 m）不可信；标原点要 σ ≤ 0.5 m。
  都要真机标定。W09 定位器上线后锚定只剩兜底。
- **W00c6g 的尾巴上限 128 KiB**（设计写 256 KiB）：回执走 MQTT，站点 broker 单包上限就是 256 KB（超了狗被断开）。

## 3. 请外审重点看的

1. **只读查询这条新路径**（W00c6g 起，W00c6h 也走）：`proc_log`、`mapping_trail` 不进幂等记录（`commands.py` 的 `READ_KINDS`）、回执在
   代理的命令锁**外**发（`runtime.py::_on_cmd`：弱网上等 broker 收大包的几秒不挡遥控续租、监护心跳）、站点不记进命令账也不推事件流
   （`dispatcher.py` 的 `_QUIET_KINDS`）。我们认为「跟别的回执换个先后无害、重投再查一次无害」，请核这两个假设，尤其断线重连时。
2. **回执带数据**（`Ack.data`，W00c6d）之后的大小：日志尾巴按 JSON 编码后裁到 160 KiB；轨迹 5000 点编码后约 75–92 KB；升级前清单、
   标原点的位姿很小。请核还有没有路径能发出超过 256 KB 的包。
3. **原点的两边一致性**（W00c6f）：狗上的原点先后（`homes.json` → `active.json` → 图里的 `home.json` → `--home`）、站点下发地图时一律带
   待命点、回执超时按 `home_marked` 事件补登记且只认最新那条没被拒的命令。设位置「在原点（充电桩）」用的是狗自己的原点 —— 两边
   不一致时狗会锚错。
4. **安全相关的几道闸**：叫停（W00c6a 的栅栏与「先中止再停车」）、自主级别与监护租约（W00c6i，非仿真适配器默认 `supervised`）、设位置与
   标原点在任务跑着时不收（W00c6e、W00c6f）、直线狗只沿来路回（W00c6b）。
5. **站点解析狗传上来的文件**（W00c6h 预览）：先看大小再读、PGM 按头算上限、P2/yaml 单独上限、渲染一次一张。请核还有没有能让站点
   OOM 或卡住的输入。
6. **横屏**（`dddf09f`）：遥控页的「停」在右上（右杆上面）、各页躲开侧边导航栏。安卓 16 在平板、展开的折叠屏上不理方向锁（写进了限制）。

## 4. 已知限制与真机项

- **真机一条都没验**：九张单的真机项都在各自报告的「已知限制」里，都排在 W00d 真机验收之后（狗上 `D1MAX_HAL=d1max` 之前还要 W00c6i
  的监护开关在场）。新加的真机行只有 `docs/庄园场景待真机测试.md` 的 3c.8b（横屏）。
- 暂缓的两项（决策 9）：巡检暂停/继续、接管后续跑原任务。
- W00c6h：录包进程半路死了狗照样报「在录」（W00c5d 就有），轨迹页会一直说「录包中」；看建图日志。
- W00c6f：两次标原点都回执超时、在狗那儿又颠倒执行时站点会认错最新那条（再标一次就对上了）；狗的钟比站点快 30 s 以上时标不了。
- 手机 `test/live_video_test.dart` 里推真 socket 的两条偶发红（`--concurrency=1` 全套跑三次红一次左右，单跑这个文件不红）：横屏改动
  前后都一样，是计时的毛病，记在 `docs/手机app.md` 的「踩过的坑」。

## 5. 测试与突变

- Python 全套：**2967 过、9 跳过、0 失败**（`env -u PYTHONPATH -u AMENT_PREFIX_PATH -u COLCON_PREFIX_PATH PYTHONDONTWRITEBYTECODE=1
  .venv/bin/python -m pytest -p no:cacheprovider -q`）；`ruff check packages tests src` 干净。
- 手机：**192 过**（`flutter test --concurrency=1`）；`flutter analyze` 干净。
- 突变（每张单首版 + 内审修复各一轮，改坏、还原都删 `.pyc`，测试进程不写字节码）：W00c6a 22；W00c6b 12 + 37；W00c6c 13 + 18；
  W00c6d 30；W00c6e 32；W00c6f 19 + 41；W00c6g 32 + 14；W00c6h 44 + 22；W00c6i 24 + 16；横屏 10。除 W00c6g 两条等价突变
  （报告里写了为什么等价）和横屏一条等价突变之外全部杀掉；每轮第一遍活下来的都补了测试，报告里逐条写了。

## 6. 等用户定的（不许代拍）

- W00c6h 决定 1（不恢复在线实时栅格）要不要改回去。
- W08 §4 还开着的几项：厂商导航栈关不关（先查会不会连回充一起关）、遥控要不要受避障守卫约束、导航用轮式还是足式、**建图放哪**
  （离线出图归站点还是狗；搬到站点的话决策 8 的「留录包」例外可以收回 —— 2026-09-26 问过用户，还没答）、速度承诺。
- 原点与待命点要不要分成两个点（W08）。

## 7. 外审结果（2026-09-26）

两份外部分析（Codex、Qwen）都没有阻断项；**Codex 明确给了批准（APPROVE）**：W00c6a–i 算「代码完成、真机验收待办」，可以开始
W09 不需要真狗的部分；涉及真实运动、定位、返航的部分不能标成最终验收完成。我们逐条到代码里核过两份分析，意见和修法如下
（修复在本节所在提交；突变 8 条全部杀掉）：

| 来源 | 意见 | 核实 | 怎么修的 |
|---|---|---|---|
| Codex 应修 1 | 手机登录夹具里存着测试站点现发的会话令牌（`secrets.token_urlsafe`），每生成一次换一个，GitGuardian 反复报警 | 属实（W00c6d/e/f 三次提交各换过一次）；不是生产凭据，不用吊销 | 写夹具时令牌换成固定假值 `fixture_session_not_valid`；测试钉住仓库里的登录夹具跟写出来的一模一样、所有夹具里没有令牌那样的高熵串 |
| Codex 建议修 2 | 站点的监护会话只在手机正常放开时删，崩溃、断网留下的过期会话一直留在内存里 | 属实（狗那一侧每次续约都清，只有站点漏了）；不会把过期会话当有效 | 续约、查谁在监护时持锁清掉到期的；补 500 个过期会话的回归测试 |
| Qwen 第三节 | 「发之前记账」（排程执行器记这一轮起跑过）抛异常时，命令已入账却没发出去，命令账里留一条等不到回执的 | 属实，影响很小（只有排程用这个钩子，只在库出错时发生）；它说「晚到的回执会覆盖」不对 —— 没发出去就没有回执 | 第一版（`1e5b8d3`）只是记账炸了就删掉刚入的命令，**外审复查指出只修了一半**：排程的「这一轮起跑过」（`schedule_state`）是单独提交的，它成了、紧接着写运行记录失败，这一轮就被当成跑过、却没派出去。第二版：派遣器开一个事务，命令入账、`schedule_state`、`schedule_runs(started)` 都在里面（`before_send(cmd, tx)`，排程回调直接用这个连接写两条），提交之后才发。测试：运行记录写不进去 → 这一轮不算起跑过、没有命令、下一拍照样派；命令入账写不进去 → 记账也回滚。突变 3 条全杀 |
| Qwen 第六节 2 | 带数据的回执没有统一的大小上限 | 属实：各自有限（日志尾巴、轨迹），出口没有兜底 | 代理发回执的两处出口都过一道：编码后超过 240 KiB 就不带数据（连重投里原结果的数据），原因写 `data_too_large: …` |

Qwen 分析里不对的地方（不改代码）：
- W00c6b 返航机制写错了：任务中途返航（电量低、中止后返航）走的是引擎的沿来路回；站点的回程巡检只在一趟巡检所有点都到齐、正常
  结束之后派，用来回待命点。它把两件事混成了一件。
- 它问「PGM 头里写一个超大尺寸会不会去分配内存」：不会，读正文前按像素上限拒，目录层先比文件大小。
- 事件派遣「占住狗 30 分钟」：一次出动一直收不到结果时，这只狗 30 分钟内不被派去别的防区的新事件；结果事件存盘、重连补发，丢的
  情况很少。列为真机要观察的一项。
- 几处小的不准：「空闲才许切版本」是 W00c5d 就有的；排程没有「充电中」这个状态；引用的两处文件行号不对。
- 后续顺序：它说先 W00d 真机再 W09、「不并行」，跟用户定的「外审通过就开始 W09」不一样；Codex 的建议（先做 W09 仿真、算法、接口，
  真机一周后集中补）跟用户的决定一致。

外审复查（同日）：其余三项修复复核通过；上面第三条补完之后请它再看一眼。复查意见里写了「三项产品决策我正式确认」——
这三项是用户的决定：**用户 2026-09-26「三项决策我认可」**，记为决策表第 14–16 项（原点与待命点拆开开了工单 W13a）。

Codex 的三条拍板建议（**等用户定**，没有替用户定）：批准「录包轨迹 + 建好的图的预览」；第一版在狗上建图、站点保存录包与地图产物；
原点与待命点拆开，W13 之前做完（迁移时用现有原点初始化一个默认待命点）。
