# W00 系列 · 外审交接（W00c2a – W00d）

> 用户 2026-09-25：「把整个 W00 跑完再给外审，你直接先跑，都按你推荐的来」。下面七张单的设计决定都按推荐项取（每份设计稿的状态行都标了「按推荐」），每张都做了测试先行、突变检查和一轮只读子代理内部评审，内审阻断都已修。**这一批一起请外审。**

日期：2026-09-25 · 分支：`master` · 范围：`5cbc908`（W00c1 外审通过）之后到本文档所在提交（代码到 `00bdb31` 为止 23 个提交，205 个文件，+9.6k/−1.8k 行）

已经过外审的不在这一批：W00（契约 + adapter-sim）、W00b（robot-agent 骨架）、W00c1（站点骨架），三张都已 APPROVE。

## 0. 第一轮外审之后（请复审）

第一轮结论是先修再批准（FIX-THEN-APPROVE），两组阻断都已修，W00c2a、W00c2b、W00c3、W00c4 没有新阻断：
- **阻断 1**（W00d：速度目标越过停车或急停继续生效；C++ 只有源码字符串测试）：新增 `motion/vel_gate.hpp`（运动安全门与速度驱动）和 `motion/test_vel_gate.cpp`（假 SDK 的 C++ 行为测试，含外审描述的交错）。详见 `docs/W00d-完工报告.md` 的「外审」一节。
- **阻断 2**（W00c2c：同防区合并与事件幂等不是原子的）：占位改成一个事务一步做完；等回执的也算合并目标；首条失败就提升后续事件。详见 `docs/W00c2c-完工报告.md` 的「外审」一节。
- **W00d 的状态**：代码待复审；真机验收未做（§3b 共 7 条）。工单表已照此标注，不标最终验收。
- **修完之后的全量**：Python 4182 过、9 跳过、5 失败（仍是已知的 5 条环境失败）；`make test_vel_gate && ./test_vel_gate` 全部通过；`make patrol_agent` 编译通过；ruff 通过。这一轮没动手机端（上一轮 311 条全过）。
- **决策 7**：外审的意见（可以批准，并附批准条件和建议措辞）已原样记进工单表的待拍板第 7 条，**等用户定**。

## 1. 这一批有什么

| 单 | 做了什么 | 设计稿（`docs/superpowers/specs/`） | 报告 | 内审阻断（都已修） |
|---|---|---|---|---|
| W00c2a | 契约加 `patrol` 任务类型；几何、任务、排程、任务包格式搬进契约包；代理在引擎上跑整趟巡检；站点导入任务包、跑排程、派 `patrol` | `2026-09-25-W00c2-dispatcher-design.md` | `docs/W00c2a-完工报告.md` | 2 条 |
| W00c2b | 站点优先级表（待命 −10 < 排程 10–49 < 手动 60 < 事件 80），代理只让严格更高的抢占；多个待命点，任务做完才自动回 | `2026-09-25-W00c2b-preemption-standby-design.md` | `docs/W00c2b-完工报告.md` | 2 条（abort 后、失败后会自动开走） |
| W00c2c | 外部事件派遣：签名 HTTP 回调（每个事件源一把 HMAC 密钥，时间戳 5 分钟窗口，按 `event_id` 去重），防区映射到拦截点，选最近的狗，同防区 60 s 内的事件合并 | `2026-09-25-W00c2c-event-dispatch-design.md` | `docs/W00c2c-完工报告.md` | 1 条（同时到的事件派给同一台狗） |
| W00c3 | 三个固定角色 admin/guard/owner 与固定权限表；账号管理（最后一个 admin 不能停用或降级，改动时吊销会话）；只追加的审计表 | `2026-09-25-W00c3-accounts-roles-design.md` | `docs/W00c3-完工报告.md` | 2 条（一次改多项时部分提交；审计记不下改了谁） |
| W00c4 | 手机加「站点」模式（钉站点服务证书的 SHA-256、按角色显示按钮、SSE 实时刷新）；直连狗的几屏保留，标为过渡；删掉根包的引擎别名壳 | `2026-09-25-W00c4-phone-to-site-design.md` | `docs/W00c4-完工报告.md` | 无 |
| W00d | 真狗的 RobotHAL：旁路进程协议 v3 加 `vel`（带有效期的持续速度）；`D1MaxHal`；`d1max-agent --hal d1max`；真机探针与运动检查工具 | `2026-09-25-W00d-adapter-d1max-design.md` | `docs/W00d-完工报告.md` | 1 条（运动检查默认参数下被夹到最大比例、系数算错） |

## 2. 请外审特别看的地方

1. **W00d 的运动安全**（`motion/patrol_agent.cpp` 的 `DoVel`/`VelLoop`、`packages/adapter-d1max`）。**真机上一条都没跑**，C++ 只验了能编译。速度线程没有行为测试（要造一个假的 `SDKClient`，没做）。请重点看：有效期自停、`halt`/`estop` 插队、急停与姿态检查、拿锁之后的复查。
2. **W00c2c 的事件入口**（`packages/site-node/src/d1max_site/incidents.py`）：这是站点唯一不走登录的入口。请看验签、防重放、去重和合并。
3. **W00c3 的权限**：每条路由声明所需权限；停用账号后令牌立刻失效；SSE 也查角色。
4. **W00c4 的证书钉扎**（`mobile/lib/net/site_client.dart`）：只信任钉住的那张证书、不跟重定向、口令不落盘。
5. **契约包搬家**（W00c2a、W00c4）：旧名字不许再出现，三处导入的是同一个模块对象，有工作区测试守着。

## 3. 没做、留给以后的

- **待拍板第 7 条（等用户定）**：站点转发人的遥控，算不算决策 5 禁止的「服务器遥控」。没有代拍。在它定下来之前，遥控和视频仍然只能手机直连狗。
- **W00c5（已进工单表）**：手机直连退役（遥控与视频走站点）、老 `server.py` 退役、`schedule/alerts/baselines` 的去留。
- **W00d 的真机项**：`docs/庄园场景待真机测试.md` §3b 共 7 条，要用户在场，其中 4 条会让狗动。换算系数、死区、停止阈值、急停原始值、待命站姿，**都是待测值**。
- 各单记下的取舍见各自报告的「设计取舍」一节（如事件源的密钥轮换与限流、代理同级排队、按账号名锁定可被用来拒绝服务、转向死区）。

## 4. 怎么复现

```bash
# Python(约 12 分钟;已知 5 条环境失败:tests/app/test_static.py 1 条、tests/console/test_store.py 3 条、tests/test_packaging.py 1 条)
env -u PYTHONPATH -u AMENT_PREFIX_PATH -u COLCON_PREFIX_PATH .venv/bin/python -m pytest -p no:cacheprovider -q
.venv/bin/ruff check packages tests src
# 手机(Flutter 3.47.5 / Dart 3.13.4)
cd mobile && flutter test && flutter analyze
# C++ 旁路进程(只编译)
make -C motion patrol_agent
```

## 5. 最后一次全量结果（`00bdb31` 之后）

- Python：**4175 过、9 跳过、5 失败**。失败的正好是已知的 5 条环境失败（`tests/app/test_static.py` 1 条、`tests/console/test_store.py` 3 条、`tests/test_packaging.py` 1 条），没有新增。
- 手机：**311 条全过**，`flutter analyze` 无问题。已知有一条不稳定的老测试：`live_video_test.dart` 的「盖住的页面退掉之后画面自己回来」。
- ruff：通过。C++：`make patrol_agent` 编译通过。
