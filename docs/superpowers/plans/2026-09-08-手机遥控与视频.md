# 第 7 卷：手机遥控与视频 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让一个拿着手机、握着 L1 控制权的人，能看着实时画面把这只狗开走——并且在看不见画面的那一刻，怎么推摇杆都开不动。

**Architecture:** 三层。视频层从「一个观众一条 ffmpeg」改成「一路相机一条 ffmpeg、字节扇给 N 个观众」，顺手把「这一路现在在线吗」变成一个可以问的状态；遥控层拿这个状态做 §5.9 那道无豁免的硬闸，同时补上 `MIN_LAT`、两档节奏、以及「引擎挂起时持租约放行」这条新通路；手机层第一次有了操作界面——原生的双摇杆遥控屏、视频屏、控制权面板、盘况屏。跨语言的接口形状靠一份两边都读的夹具文件锁住。

**Tech Stack:** Python 3.10 / 标准库 `http.server`（`ThreadingHTTPServer`）/ ffmpeg 子进程 / pytest；Flutter 3.47.2 / Dart 3.13.2（仅 Android）。

**Spec:** `docs/superpowers/specs/2026-09-06-商业化交付架构设计.md`，本卷对应 §5.9、§5.10（其中的引擎与遥控那一半）、§7.7、§7.8、§10 待办 1。

---

## 开工前的四条定夺

这四条在计划落笔前就定了，实施者不要再问，也不要顺手改回去。

**定夺一：§5.10 里的 `src/d1max_console/` 值守屏和 `engine/alerts.py` 不在本卷。**
§5.10 那张「落到代码」表横跨两卷。`docs/分卷与进度.md:32` 把第 7 卷框在
§7.7、§7.8、§5.9、§10，把「值守与远程接管」整个留给第 8 卷。所以本卷只取
§5.10 的引擎那一半——`SUSPENDED` 状态、挂起快照、「持租约 + 已挂起 → 放行」。
**错了的代价：**第 8 卷开工时发现值守屏要的告警形状跟本卷的 `SUSPENDED` 快照
对不上，返工一个 dataclass。

**定夺二：§10 待办 2（MJPEG 换 H.264）不在本卷。**
理由三条，按分量排：（a）这台开发机上**根本没有 ffmpeg**，H.264 转码的正确性
在本地一行都验不了，而 MJPEG 那条路之所以能测，是因为测的是「切帧」不是「转码」
（`tests/app/test_video.py` 的模块开头原话：转码本身是 ffmpeg 的事，不归我们测）；
（b）Flutter 这头放 H.264 直播要选播放器，这个决定没人做过，而 MJPEG 在
`Image.network` 上是白送的；（c）机器狗自己身上那个网页版 app（第 1–6 卷做的）
的 `<img>` 直接吃 MJPEG，硬切等于把它打瞎。
**本卷要做的是让这次替换将来变便宜**：codec 只出现在 `CameraFeed.argv` 和一个
content-type 常量里，换 H.264 是改命令行加改 content-type，不是重写。
**产品后果要说清楚：**规格把 H.264 记成第二档（三个人同时看）的前置。扇出改完
之后，**热点上三个人同看 MJPEG 是够的**（一路 5fps / q6，AP 上是 Mbps 量级），
真正非 H.264 不可的是**远程回传那条上行**——那是第 9 卷。所以第二档不会因为这条
定夺往后推，往后推的是「远程看视频」。这一条记进 `docs/分卷与进度.md`。
**错了的代价：**第二档在真机上发现 AP 带宽不够，得插一卷做 H.264。

**定夺三：TLS 与证书钉扎不在本卷。**
`docs/第2卷待办.md` 第 48 条里那句「第 7 卷做证书钉扎」是**模型的临时判断，不是
人拍的板**，本计划把它挪到第 8 卷并在那份文档里改正来源标注。理由：（a）第 48 条
问的是 PIN 只有 10^6 种熵够不够，而第 6 卷已经落地的指数退避锁（5 次失败起锁，
最长 900 秒）已经把爆破速率压到不可行，证书钉扎防的是中间人，是另一件事；
（b）它要动 `deploy/install.sh`——五个交付到客户现场的文件之一，证书怎么生成、
怎么进手机，这条链路没在真机上走过一次就写不诚实；（c）流量真正离开机器狗自己
那个热点，是 §7.8 的「远程模式」和第 9 卷的回传，那时候传输层加密才开始挣钱。
**错了的代价：**第 7 卷交出去的手机 app 在热点上是明文 HTTP。热点本身有 WPA2，
攻击者要先进热点；进了热点之后，第 6 卷 §6.5 已经写明**token 本身就是明文飞的**，
钉不钉证书都一样。所以这条定夺不改变今天的暴露面。

**定夺四：本卷允许改写既有测试的断言，但一条都不许删。**
Task 3 会把「一个观众一条 ffmpeg」改成「一路一条」，`tests/app/test_video.py` 里
有若干条断言钉的正是旧语义。**删测试是禁止的（全局约束）；把断言改成规格要求的
新行为是正常的 TDD。**代价控制手段写在 Task 3 的 Step 里：改一条就在报告里列出
「旧期望 → 新期望 → 为什么规格要求它变」，复评照着一条条看。测试总数只许涨。
**错了的代价：**一条本来在保护某个真实行为的测试被顺手改软了，而复评没看出来。

---

## Global Constraints

以下每一条都隐含在每个 Task 的要求里。

### 门禁（Python）

> **⚠️ 全量测试必须前台跑：`timeout: 600000`，`run_in_background` 必须是 `false`。**
> 全量约 5.5 分钟。已经有六个 agent 因为把它扔到后台而丢掉了结果。

- 全量：`python -m pytest -q --basetemp=D:/pytest-tmp/<自己起个名>`
- **判据是退出码**，不是文字汇总——这台机器有时候不打那行汇总。
- 计数：`python -m pytest --collect-only -q 2>&1 | awk -F': ' '/^tests.*: [0-9]+$/{s+=$2} END{print s}'`
- **基线 2773，只许涨。禁止删除或跳过任何已有测试**，零 skip、零 xfail。
- 每次 pytest 都要带 `--basetemp=D:/pytest-tmp/<名字>`——C 盘会被写爆。
- `ruff check src tests` 退出码 0。`ruff format --check` **不是**门禁。
- 中文测试名在控制台会乱码，命令前面加 `PYTHONIOENCODING=utf-8 PYTHONUTF8=1`。

### 门禁（Dart）

- 每次都要显式加 PATH：`export PATH="/d/toolchain/flutter/bin:$PATH"`，在 `mobile/` 下跑。
- `flutter analyze` 期望输出 `No issues found!`。
- `flutter test`，**基线 56，只许涨**。
- **Dart 的标识符不认中文**（`docs/手机app.md:97`）。测试名（字符串）可以是中文，
  变量名、类名、方法名不行。注释和文档注释用中文。
- `flutter_secure_storage` 钉在 v9，不许升（v11 要 compileSdk 37）。

### 分层

`protocol/` ← `backends/` ← `engine/` ← `app/`。**`engine/` 永远不许 import `app/`。**
引擎不认识 HTTP，也不认识遥控——遥控靠 `engine.add_busy_check()` 反向登记。

### 房规

- 中文 docstring 和注释。`.py` 里的中文散文用**半角**逗号（顿号 `、` 可以，
  全角 `，` 不行）；`.md` 里用**全角**逗号。
- `src/` 里不许出现全角弯引号。不许出现繁体字。
- **`b"中文"` 是硬性 SyntaxError**，要写 `"中文".encode()`。
- 时间必须可注入。被测代码里不许有 `sleep`、`datetime.now()`、`time.time()`
  （§8.5 第 2 条）。唯一获准的例外是 `AppContext.clock`。
- **不许在真后台线程上用固定 `time.sleep(margin)` 等**。获准的写法是
  `tests/app/test_control_wiring.py` 里那个带截止时间的 `等到()` 助手。
- 涉及信任边界的判断，docstring 里要写清楚假想的攻击者是谁。
  **不许默认「局域网 = 可信」**（§6.5）。
- 不许用 `# noqa` 绕过 `BLE`。
- 拆验证（故意改坏代码确认测试会红）之后，用**先 `cp` 到暂存目录、再 `cp` 回来**
  恢复，**绝对不许 `git checkout --`**。

### 分支

全卷都在 `feat/交付主线` 上。**不开新分支，不合并到 master。**
不要在本卷结束时跑 `superpowers:finishing-a-development-branch`。

### 交付文件红线

`deploy/install.sh`、`deploy/d1max-patrol.service`、`docs/装机清单.md`、
`docs/任务包格式.md`、`docs/鉴权与控制权.md` 这**五个文件会送到客户现场**，
里头永远不许出现热点口令 `12345678`、SSID `XG2WIFI*`、SSH 口令、厂商私有协议
细节、厂商导航端口 `10010`，以及生成出来的设备 PIN 字面量。本卷不改这五个文件。

---

## 文件结构

| 文件 | 责任 | 本卷动作 |
|---|---|---|
| `src/d1max_patrol/app/teleop.py` | 一拍一拍的手动遥控 + 守死人开关 | 扩写：`MIN_LAT`、单轴吸附、两档节奏、视频硬闸、挂起放行、现场/远程两档 |
| `src/d1max_patrol/app/video.py` | RTSP → 浏览器放得出来的东西 | 重构：一路一条 ffmpeg，字节扇出，在线状态 |
| `src/d1max_patrol/engine/machine.py` | 任务状态机 | 加 `SUSPENDED` 与挂起快照 |
| `src/d1max_patrol/engine/lease.py` | L1 控制权与留痕 | 加一种留痕：`mode_switched` |
| `src/d1max_patrol/app/server.py` | HTTP 路由 | 接线：视频路由改扇出、`/api/video/health`、`/api/teleop/mode`、`/api/run/suspend` |
| `mobile/lib/net/patrol_client.dart` | 手机这头唯一的 HTTP 出口 | 新建 |
| `mobile/lib/net/wire.dart` | 上线形状的 Dart 侧解析 | 新建 |
| `mobile/lib/ui/teleop_page.dart` | 双摇杆遥控屏 | 新建 |
| `mobile/lib/ui/widget/joystick.dart` | 虚拟圆心摇杆 | 新建 |
| `mobile/lib/ui/widget/live_video.dart` | MJPEG 画面 + 掉线判定 | 新建 |
| `mobile/lib/ui/control_panel.dart` | 控制权面板与倒计时 | 新建 |
| `mobile/lib/ui/storage_page.dart` | 盘况屏（挂账 15 的硬入场条件） | 新建 |
| `mobile/test/fixtures/*.json` | 两边都读的接口形状夹具 | 新建，由 Python 侧生成并断言 |

---

### Task 1: 侧移有自己的死区，斜推吸附到单轴

**Files:**
- Modify: `src/d1max_patrol/app/teleop.py:41-42`（加 `MIN_LAT`）、`:93-123`（`pulse`）
- Test: `tests/app/test_teleop.py`（追加，现有 8 条一条不动）

**Interfaces:**
- Produces: `teleop.MIN_LAT: float`、`teleop.snap_to_axis(fwd: float, lat: float) -> tuple[float, float]`。
  Task 2 会在 `pulse()` 里接着用它们，Task 11 的 Dart 摇杆做的是同一件事的界面侧。

今天 `teleop.py:121` 把 `MIN_FWD` 借给侧移用。这不对：横着走（蟹步）是另一种
步态，死区不是同一个数，而**这个数一辈子没在真机上量过**。借用的坏处不是「不
准」，是「借了之后没人记得它是借的」——真去标定那天，改一个常量会把前进也改了。

单轴吸附这条是规格 §7.7 点名的，理由在规格里写得很硬：底下的控制量是**百分比
不是速度**，而且死区很大（清单 #37/#38：`0.11` 几乎不动，`0.3~0.5` 才真的走）。
所以「把非零值顶到死区之上」这个动作，**只有在输入是单轴的时候才成立**——两个
轴一起顶，`(0.5, 0.2)` 会被顶成 `(0.5, 0.3)`，方向整个歪掉，而人看到的是狗往
一个他没指的方向走。

**吸附放在后端，不是只放在界面。** 界面那份是为了手感（Task 11），后端这份是
为了正确性：后端的逐轴顶死区逻辑只在单轴输入下有意义，那就得由后端自己保证
这个前提，而不是指望每一个将来的客户端都记得先吸附。

- [ ] **Step 1: 写失败的测试**

追加到 `tests/app/test_teleop.py`（用文件里已有的 fixture 和风格；这个仓库是
`asyncio_mode = "auto"`，**不要**加 `@pytest.mark.asyncio`）：

```python
def test_斜着推只留大的那一轴():
    """规格 §7.7:左摇杆吸附到单轴。

    **这不是手感,是正确性。** 底下的控制量是百分比不是速度,死区又大
    (清单 #37/#38),所以 ``_clamp_above_deadband`` 是逐轴把非零值顶到死区
    之上的。两个轴一起顶,``(0.5, 0.2)`` 会变成 ``(0.5, 0.3)`` —— 人指的是
    「基本朝前、稍微偏一点」,狗走的是「四十度斜着」。
    """
    assert snap_to_axis(0.5, 0.2) == (0.5, 0.0)
    assert snap_to_axis(0.2, -0.5) == (0.0, -0.5)


def test_一样大的时候留前进():
    """**平局判给前进。**

    正推四十五度是「我想往前,手抖了」的概率,远大于「我想横着走,手也抖了」;
    而且前进是这台机器唯一一个在真机上量过死区的轴(清单 #37/#38),侧移那个
    数至今是借来的。平局倒向量过的那一头。
    """
    assert snap_to_axis(0.4, 0.4) == (0.4, 0.0)
    assert snap_to_axis(-0.4, 0.4) == (-0.4, 0.0)


def test_零还是零():
    """全零是「停」,吸附不许把它变成别的。"""
    assert snap_to_axis(0.0, 0.0) == (0.0, 0.0)
    assert snap_to_axis(0.0, 0.7) == (0.0, 0.7)


async def test_侧移用自己的死区不再借前进的(fake_device, engine):
    """``MIN_LAT`` 是独立常量。**今天两个数一样,这条测试照样测得到东西** ——
    它盯的是「用的是哪一个常量」,不是「值是多少」。真机标定那天改 ``MIN_LAT``,
    这条会跟着变;而在此之前,有人把 ``MIN_LAT`` 删掉改回借用,这条会红。
    """
    tel = Teleop(fake_device, engine)
    with mock.patch.object(teleop_mod, "MIN_LAT", 0.66):
        await tel.pulse(0.0, 0.05, 0.0)
    assert fake_device.walks[-1][2] == pytest.approx(0.66)
```

`snap_to_axis`、`teleop_mod`、`mock` 按文件顶部现有的 import 风格补上
（`from unittest import mock`、`from d1max_patrol.app import teleop as teleop_mod`）。

`fake_device.walks` 这个属性名要跟 `tests/app/conftest.py` 里现有的假件对齐——
**先读一眼那个假件**，如果它记的字段叫别的名字、或者记的是一个 dataclass 而不是
元组，用它的写法，**不要改假件**。同理 `engine` fixture 的名字也照抄现成的。

- [ ] **Step 2: 跑，确认全红**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_teleop.py -q --basetemp=D:/pytest-tmp/v7t1a`

Expected: 收集就失败（`snap_to_axis` 不存在）。

- [ ] **Step 3: 实现**

`teleop.py` 里把 `MIN_FWD`/`MIN_YAW` 那一段改成：

```python
#: 死区之上的最小控制量。见清单 #37/#38。
MIN_FWD = 0.30
MIN_YAW = 0.30
#: 侧移(蟹步)的死区。**今天的值是从 MIN_FWD 抄来的,还没在真机上量过** ——
#: 清单 #37/#38 量的是前进那一轴。横着走是另一种步态,没有任何理由认为它跟
#: 前进共用一个死区。独立成一个常量不是为了现在有区别,是为了标定那天改它
#: 不会顺手把前进也改了。真机清单 #67。
MIN_LAT = 0.30
```

在 `_clamp_above_deadband` 下面加：

```python
def snap_to_axis(fwd: float, lat: float) -> tuple[float, float]:
    """左摇杆吸附到单轴:留下大的那一轴,另一轴清零(§7.7)。

    **这是正确性,不是手感。** ``_clamp_above_deadband`` 是逐轴把非零值顶到
    死区之上的,而那个动作只有在输入是单轴时才有意义:两个轴一起顶,
    ``(0.5, 0.2)`` 会变成 ``(0.5, 0.3)`` —— 人指的是「基本朝前」,狗走的是
    「四十度斜着」。

    机器狗自带的摇杆本来就是这个行为(推四十五度它也只挑一个轴),所以吸附
    还顺手让页面和实体摇杆的手感对上了。

    **平局判给前进。** 正推四十五度是「想往前、手抖了」的概率远大于反过来;
    而且前进是唯一一个真机量过死区的轴(清单 #37/#38),侧移那个数是借来的
    (见 ``MIN_LAT``)。平局倒向量过的那一头。
    """
    if abs(lat) > abs(fwd):
        return 0.0, lat
    return fwd, 0.0
```

`pulse()` 里，在三条 `_check_range` 之后、`(fwd, lat, yaw) == _STOP` 判断
**之前**插一行。顺序是要紧的：吸附完仍然可能是全零（本来就全零），那时候还得
走 `stop()` 那条路；而吸附**不会**把非零变成全零（大的那一轴留着），所以插在
这里不会凭空造出一个「停」：

```python
        fwd, lat = snap_to_axis(fwd, lat)
```

`walk` 那三行里把侧移那一路改成 `MIN_LAT`：

```python
        await self._device.walk(
            seconds,
            _clamp_above_deadband(fwd, MIN_FWD),
            _clamp_above_deadband(lat, MIN_LAT),
            _clamp_above_deadband(yaw, MIN_YAW),
        )
```

`__all__` 里加 `"MIN_LAT"` 和 `"snap_to_axis"`（那个列表是排过序的，插到对的位置）。

- [ ] **Step 4: 跑绿**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_teleop.py -q --basetemp=D:/pytest-tmp/v7t1b`

- [ ] **Step 5: 拆一次，确认吸附那条真的在测东西**

先把 `teleop.py` 复制一份到暂存目录。把 `snap_to_axis` 的函数体换成
`return fwd, lat`（直通）。`test_斜着推只留大的那一轴` **必须红**。在报告里写下
红的是哪几条、报的什么。然后**用 `cp` 把副本拷回来**恢复，**不要 `git checkout --`**。

- [ ] **Step 6: 提交**

```bash
ruff check src/d1max_patrol/app/teleop.py tests/app/test_teleop.py
git add -A
git commit -m "feat(teleop): 侧移有自己的死区常量,斜推吸附到单轴(§7.7)"
```

---

### Task 2: 两档节奏——漫游与扫图

**Files:**
- Modify: `src/d1max_patrol/app/teleop.py`（加 `PulseProfile`、`ROAM`、`SCAN`、`PROFILES`，`pulse()` 收 profile）
- Modify: `src/d1max_patrol/app/server.py:1349-1364`（`_teleop` 认 `profile` 字段）
- Test: `tests/app/test_teleop.py`（追加）、既有的服务器遥控路由测试文件（追加）

**Interfaces:**
- Consumes: Task 1 的 `snap_to_axis`、`MIN_LAT`
- Produces: `teleop.PulseProfile(name: str, fwd_seconds: float, yaw_seconds: float)` 带
  `seconds_for(fwd, lat, yaw) -> float`；`teleop.ROAM`、`teleop.SCAN`、
  `teleop.PROFILES: Mapping[str, PulseProfile]`；
  `Teleop.pulse(fwd, lat, yaw, seconds: float | None = None, *, profile: PulseProfile = ROAM)`。
  HTTP 上是 `POST /api/teleop` 的可选字段 `"profile": "roam" | "scan"`。Task 11 的
  遥控屏用它切档，Task 8 的夹具会把这两个词写进去。

规格 §7.7 要两档：**自主操控（漫游）**一拍 0.4 秒、转向给满；**扫图**一拍更短、
转向压低。

**「转向压低」不能实现成「把转向的控制量调小」。** 这是本任务唯一容易做错的地方。
控制量是百分比，死区在 `0.3` 附近（清单 #37/#38），压到 0.3 以下的结果是**狗原地
不动，而且不报错**——页面上看起来像掉线了。`teleop.py` 的模块开头已经把这条规矩
写死了：想让它慢一点，正确做法是**把一拍缩短**。

所以两档的差别落在**两个时长**上，而且转向的时长要能跟平移的分开：扫图的时候人
要的是「一点一点地转」，那是短转向拍；平移倒不必那么碎。

- [ ] **Step 1: 写失败的测试**

追加到 `tests/app/test_teleop.py`：

```python
async def test_扫图档的转向是把一拍缩短_不是把量压低(fake_device, engine):
    """**这一条是这个任务的全部要点。**

    控制量是百分比不是速度,死区在 0.3 附近(清单 #37/#38)。把扫图档的
    「转向压低」实现成「乘一个小于 1 的系数」,结果是控制量掉进死区 ——
    狗原地不动,而且不报错,页面上看起来像掉线。规格 §7.7 要的「压低」在
    这台机器上只有一种正确实现:**把一拍缩短**。

    所以断言有两半,缺一不可:时长变短了 **而且** 控制量仍然在死区之上。
    """
    tel = Teleop(fake_device, engine)
    await tel.pulse(0.0, 0.0, 1.0, profile=SCAN)
    seconds, _fwd, _lat, yaw = fake_device.walks[-1]
    assert seconds == pytest.approx(SCAN.yaw_seconds)
    assert seconds < ROAM.yaw_seconds
    assert abs(yaw) >= MIN_YAW


async def test_扫图档的平移用平移那个时长(fake_device, engine):
    """转向和平移是**两个**时长。扫图时人要的是转向一点一点来,
    平移倒不必碎成一样。混成一个数就没法分别调。
    """
    tel = Teleop(fake_device, engine)
    await tel.pulse(1.0, 0.0, 0.0, profile=SCAN)
    assert fake_device.walks[-1][0] == pytest.approx(SCAN.fwd_seconds)


async def test_漫游档是默认的(fake_device, engine):
    """不传 profile 就是漫游 —— 网页版 app 那些老调用方一行不用改。"""
    tel = Teleop(fake_device, engine)
    await tel.pulse(1.0, 0.0, 0.0)
    assert fake_device.walks[-1][0] == pytest.approx(ROAM.fwd_seconds)


async def test_同时有平移和转向时按长的那个时长走(fake_device, engine):
    """一拍只有一个时长,得挑一个。

    **挑长的那个。** 挑短的话,人推着摇杆前进兼转向,前进会被截成扫图那么短
    的一小步 —— 明明推的是「往前走并且拐个弯」,走出来的是「原地拐了一下」。
    """
    tel = Teleop(fake_device, engine)
    await tel.pulse(1.0, 0.0, 1.0, profile=SCAN)
    assert fake_device.walks[-1][0] == pytest.approx(
        max(SCAN.fwd_seconds, SCAN.yaw_seconds))


async def test_显式给了秒数就听人的(fake_device, engine):
    """``seconds`` 仍然压过 profile —— 网页版 app 现在传的就是它。"""
    tel = Teleop(fake_device, engine)
    await tel.pulse(1.0, 0.0, 0.0, seconds=1.5, profile=SCAN)
    assert fake_device.walks[-1][0] == pytest.approx(1.5)


def test_两档都在名录里_而且名字就是上线的那个词():
    """``PROFILES`` 的键是 HTTP 上直接收的字符串,不许跟 ``name`` 不一致 ——
    不一致的话,路由认得的词和 ``to_wire`` 报出去的词会是两套。
    """
    assert set(PROFILES) == {"roam", "scan"}
    assert all(k == v.name for k, v in PROFILES.items())


def test_每一档的时长都在合法范围里():
    """一拍不许超过 ``MAX_PULSE_S`` —— 超了等于把守死人开关架空:
    心跳断了以后还要再走这么久。
    """
    for prof in PROFILES.values():
        assert 0.0 < prof.fwd_seconds <= MAX_PULSE_S
        assert 0.0 < prof.yaw_seconds <= MAX_PULSE_S
```

- [ ] **Step 2: 跑，确认全红**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_teleop.py -q --basetemp=D:/pytest-tmp/v7t2a`

- [ ] **Step 3: 实现**

`teleop.py`，在 `MAX_PULSE_S` 下面加：

```python
@dataclass(frozen=True, slots=True)
class PulseProfile:
    """一档节奏。**两档的差别只在时长上,不在控制量上。**

    控制量是百分比不是速度,死区在 0.3 附近(清单 #37/#38)。想让狗慢一点,
    唯一正确的做法是把一拍缩短 —— 把控制量压低的结果是原地不动,而且不报错
    (见模块开头)。所以这里没有「转向系数」这种字段,只有两个时长。

    **为什么转向和平移各有一个时长。** 扫图的时候人要的是转向一点一点来,
    平移倒不必碎成一样。合成一个数就没法分别调,而这两件事在现场是分开
    调的。
    """

    #: 上线时用的名字。``PROFILES`` 的键跟它必须一致。
    name: str
    #: 平移一拍多长。
    fwd_seconds: float
    #: 转向一拍多长。
    yaw_seconds: float

    def seconds_for(self, fwd: float, lat: float, yaw: float) -> float:
        """这一拍该走多久。

        既有平移又有转向时**取长的那个**。取短的话,人推着「往前走并且拐个
        弯」,前进会被截成转向那么短的一小步,走出来像「原地拐了一下」。
        """
        moving = fwd != 0.0 or lat != 0.0
        turning = yaw != 0.0
        if moving and turning:
            return max(self.fwd_seconds, self.yaw_seconds)
        return self.yaw_seconds if turning else self.fwd_seconds


#: 自主操控(漫游):走路用的那一档,一拍 0.4 秒,转向给满(§7.7)。
ROAM = PulseProfile("roam", DEFAULT_PULSE_S, DEFAULT_PULSE_S)

#: 扫图:建图的时候用,一拍更碎,转向尤其碎 —— 建图要的是慢慢挪、慢慢转,
#: 让激光有时间把同一片地方看够(§7.7)。**碎是靠缩时长实现的,不是靠压
#: 控制量**,理由见 ``PulseProfile``。
SCAN = PulseProfile("scan", 0.25, 0.12)

#: 上线时认的两个词。
PROFILES: Mapping[str, PulseProfile] = MappingProxyType(
    {ROAM.name: ROAM, SCAN.name: SCAN})
```

顶部 import 补 `from dataclasses import dataclass`、
`from types import MappingProxyType`，并把 `from collections.abc import Callable`
改成 `from collections.abc import Callable, Mapping`。

`pulse()` 的签名和文档：

```python
    async def pulse(self, fwd: float, lat: float, yaw: float,
                    seconds: float | None = None, *,
                    profile: PulseProfile = ROAM) -> None:
        """走一拍。

        控制量取 [-1, 1],会被顶到死区之上;想走慢就换一档(``profile``)。
        三个轴全零等于 :meth:`stop`。

        ``seconds`` 显式给了就听人的,不给就按这一档的节奏算 —— 网页版 app
        现在传的就是显式秒数,它一行都不用改。
        """
```

体内，三条 `_check_range` 之后、Task 1 那句 `snap_to_axis` 之后，把秒数定下来，
**并且把原来那句范围检查挪到这里**（原来它在 `_check_range` 后面，现在必须在
算完默认值之后，否则 `None` 会去跟数字比大小）：

```python
        fwd, lat = snap_to_axis(fwd, lat)
        if seconds is None:
            seconds = profile.seconds_for(fwd, lat, yaw)
        if not 0.0 < seconds <= MAX_PULSE_S:
            raise ValueError(f"一拍得在 (0, {MAX_PULSE_S}] 秒之间,给的是 {seconds}")
```

**注意 `seconds` 的默认值从 `DEFAULT_PULSE_S` 改成了 `None`。** 先查一遍还有谁
在调它：

```bash
grep -rn "\.pulse(" src tests
```

只传三个数的调用方行为不变（漫游档的 `fwd_seconds` 就是旧的 `DEFAULT_PULSE_S`）。
位置传 `seconds` 的调用方也不变。**如果发现有测试断言「不传 seconds 时 walk 收到
`DEFAULT_PULSE_S`」，它仍然是绿的**——这是有意的，两个值本来就相等。

`__all__` 加 `"PROFILES"`、`"PulseProfile"`、`"ROAM"`、`"SCAN"`（排序插入）。

- [ ] **Step 3b: 路由认这个词**

`server.py` 的 `_teleop`，把 `seconds` 和 `profile` 两段改成：

```python
        seconds = None if body.get("seconds") is None else _number(body, "seconds")
        name = body.get("profile", "roam")
        if not isinstance(name, str) or name not in PROFILES:
            raise HttpError(400, "不认识的节奏档",
                            f"只有 {'、'.join(PROFILES)},给的是 {name!r}")
        profile = PROFILES[name]
        teleop = self._ctx.teleop
        self._call(lambda: teleop.pulse(fwd, lat, yaw, seconds, profile=profile))
```

`server.py` 顶部原本 import 了 `DEFAULT_PULSE_S`，现在这一句不再用它。
**先 `grep -n "DEFAULT_PULSE_S" src/d1max_patrol/app/server.py` 看它还有没有别的
用处**：没有就把它从 import 里去掉（留着 ruff 的 `F401` 会红），有就留着。
import 里加上 `PROFILES`。

- [ ] **Step 4: 路由测试**

先 `grep -rln "api/teleop" tests` 找到既有的那个文件，**追加到它里面，不要新建**：

```python
def test_不认识的节奏档报400(client):
    """**不许静默退回默认档。** 客户端拼错了词却照走漫游档,人在扫图的时候
    会拿到走路的节奏 —— 而扫图那一档存在的理由,恰恰是走路的节奏建不出图。
    """
    resp = client.post("/api/teleop", {"fwd": 1.0, "lat": 0.0, "yaw": 0.0,
                                       "profile": "sacn"})
    assert resp.status == 400
    assert "sacn" in resp.text


def test_不传节奏档就是漫游(client, fake_device):
    client.post("/api/teleop", {"fwd": 1.0, "lat": 0.0, "yaw": 0.0})
    assert fake_device.walks[-1][0] == pytest.approx(ROAM.fwd_seconds)
```

`client`、`fake_device`、`resp.status`、`resp.text` 这些名字**照抄那个文件里
现成的 fixture 和写法**，不要新造；这个仓库的 HTTP 测试有自己的一套助手。

- [ ] **Step 5: 跑绿**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app -q --basetemp=D:/pytest-tmp/v7t2b`

- [ ] **Step 6: 拆一次，确认「压低」没被做成乘系数**

先把 `teleop.py` 复制一份到暂存目录。把 `SCAN` 改成
`PulseProfile("scan", DEFAULT_PULSE_S, DEFAULT_PULSE_S)`（时长跟漫游一样，等于
「档位没起作用」）。`test_扫图档的转向是把一拍缩短_不是把量压低` 和
`test_扫图档的平移用平移那个时长` **必须红**。报告里写下红的是哪几条。用 `cp` 恢复。

- [ ] **Step 7: 提交**

```bash
ruff check src/d1max_patrol/app/teleop.py src/d1max_patrol/app/server.py tests/app
git add -A
git commit -m "feat(teleop): 漫游与扫图两档节奏,差别只在时长上(§7.7)"
```

---

### Task 3: 一路相机一条 ffmpeg，字节扇给 N 个观众

**Files:**
- Rewrite: `src/d1max_patrol/app/video.py:115-193`（`MjpegSource` → `CameraFeed`），
  常量段 `:36-52` 加两个
- Modify: `tests/app/test_video.py`（既有 25 条：**一条都不许删**；钉住旧语义的
  若干条改断言，改一条报告里记一条）
- Test: `tests/app/test_video.py`（追加新语义的条目）

**Interfaces:**
- Produces: `video.CameraFeed(rtsp_url, *, ffmpeg="ffmpeg", fps=5.0,
  clock=time.monotonic, stale_s=STALE_S, start_timeout_s=START_TIMEOUT_S)`，
  属性 `url`、`argv`、`viewers`、`online`，方法 `stream() -> Iterator[bytes]`、
  `health() -> dict[str, Any]`、`close() -> None`；
  常量 `STALE_S`、`START_TIMEOUT_S`，`MAX_VIEWERS` 改值。
  Task 4 把 `online`/`health()` 接到 HTTP 上，Task 6 拿 `online` 做 §5.9 的硬闸。
- `RtspStill` 一行不改——它是「到点抓一张」，跟实时流是两件事（见它自己的
  docstring：让一条 ffmpeg 在两个点位之间空转几分钟才是错的）。

规格 §10 待办 1 的原话是「`video.py` 改成一相机一条 ffmpeg，把字节扇给 N 个
连接」。今天是**一个观众一条 ffmpeg**（`_stream()` 里 `Popen` 在生成器体内），
两个人看同一路就在解两遍同一路 1080p——Orin 上这是实打实的 CPU，而它买不到
任何东西：两个人看的是同一帧。

顺带产出的是**「这一路现在在线吗」变成一个可以问的东西**。今天问不出来：
`viewers` 是个人数，`RtspStill.healthy()` 是拍照预检时**主动抓一张**。有了常驻
的泵，「最后一帧是什么时候进来的」是现成的，而 §5.9 那道硬闸（Task 6）非它不可。

**在线的定义要两半：泵活着 + 最近 `STALE_S` 秒内真收到过帧。** 只看进程在不在
是不够的——RTSP 断了 ffmpeg 未必立刻死，它可能挂在那儿重连，而那段时间画面
是冻的。冻着的画面是 §5.9 要防的头号情况：人看着一张不动的图，以为前面没障碍。

**慢的观众丢帧，不排队。** 每个观众只看得到「当前最新那一帧」，跟不上的就跳过
中间的。排队会让内存跟着最慢的那个人涨，而实时画面上一帧旧数据没有任何价值。

- [ ] **Step 1: 先读，再改**

```bash
sed -n '1,60p' tests/app/conftest.py            # _fake() 和五个假 ffmpeg 长什么样
grep -n "def test_\|MAX_VIEWERS\|viewers\|Popen\|两个\|两条" tests/app/test_video.py
```

把 `tests/app/test_video.py` 里**每一条钉住「一个观众一条 ffmpeg」的断言**列出来
（多半是围着 `MAX_VIEWERS == 2`、`viewers` 计数、和「超了就拒绝」那几条）。
这份清单要进报告：一条一行，写清楚**旧期望 → 新期望 → 为什么规格要求它变**。
**删除任何一条测试都是违规**；改断言是正常的 TDD，但每一条都要能说出理由。

- [ ] **Step 2: 写失败的测试**

追加到 `tests/app/test_video.py`。这个文件的规矩写在它自己的模块开头：
**不需要真 ffmpeg，也不需要 RTSP**，假的 ffmpeg 是一个往 stdout 吐 JPEG 的脚本。
沿用它现有的 `fake_ffmpeg`、`fake_ffmpeg_dies`、`fake_ffmpeg_forever` 等 fixture，
**名字照抄，不要新造假件**（真需要一个新形态时，照 `_fake(ffdir, script, name)`
的写法加，并在报告里说明为什么现有五个都不够用）。

```python
def test_两个观众只起一条ffmpeg(fake_ffmpeg_forever):
    """规格 §10 待办 1。**这是这个任务存在的理由。**

    两个人看同一路,今天是解两遍同一路 1080p。Orin 上那是实打实的 CPU,
    而它买不到任何东西 —— 两个人看的是同一帧。

    断言盯的是**进程数**,不是「看起来能同时看」:后者在旧实现上也是绿的。
    """
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_forever,
                      start_timeout_s=5.0)
    a = feed.stream()
    b = feed.stream()
    try:
        assert next(a).startswith(b"\xff\xd8")
        assert next(b).startswith(b"\xff\xd8")
        assert feed.处理器数 == 1
    finally:
        a.close()
        b.close()
        feed.close()


def test_最后一个观众走了才收掉ffmpeg(fake_ffmpeg_forever):
    """**不是第一个走就收。** 收早了,还在看的那个人画面会黑。"""
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_forever,
                      start_timeout_s=5.0)
    a, b = feed.stream(), feed.stream()
    next(a)
    next(b)
    a.close()
    assert feed.处理器数 == 1
    b.close()
    assert feed.处理器数 == 0
    feed.close()


def test_没人看的时候不在线(fake_ffmpeg_forever):
    """**「在线」的意思是「这一刻真有画面流过来」,不是「配了地址」。**

    这一条看着像多余,其实是 §5.9 那道闸的地基:没人看画面的时候不许开狗,
    正是那一条要的行为 —— 人不看着就不许动,这是规格的本意,不是副作用。
    """
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_forever,
                      start_timeout_s=5.0)
    assert feed.online is False
    s = feed.stream()
    try:
        next(s)
        assert feed.online is True
    finally:
        s.close()
        feed.close()
    assert feed.online is False


def test_画面冻住就算不在线(fake_ffmpeg_forever):
    """**只看进程在不在是不够的。**

    RTSP 断了 ffmpeg 未必立刻死,它可能挂在那儿重连,而那段时间画面是冻的。
    冻着的画面是 §5.9 要防的头号情况:人看着一张不动的图,以为前面没障碍。

    时刻是注进来的 —— 不许 sleep(§8.5 第 2 条)。
    """
    now = [1000.0]
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_forever,
                      clock=lambda: now[0], stale_s=2.0, start_timeout_s=5.0)
    s = feed.stream()
    try:
        next(s)
        assert feed.online is True
        now[0] += 2.5           # 把钟往前拨,一帧都没再进来
        assert feed.online is False
    finally:
        s.close()
        feed.close()


def test_起不来的时候第一帧就报错_而不是干等(fake_ffmpeg_silent):
    """路由靠「先取一帧」把失败变成一个说得清楚的 503(见 ``server._video``)。
    头一旦发出去就只能是 200,那时再出错,页面上是一个不动的破图标。

    **共享泵之后这条必须显式做。** 旧实现里「一帧都没出」是靠 ffmpeg 自己
    退出发现的;共享泵是常驻的,ffmpeg 活着但一直不吐帧的话,``next()`` 会
    永远卡住 —— 所以第一帧有自己的截止时间。
    """
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_silent,
                      start_timeout_s=0.3)
    s = feed.stream()
    with pytest.raises(VideoError):
        next(s)
    s.close()
    feed.close()


def test_慢的观众丢帧不排队(fake_ffmpeg_forever):
    """跟不上的观众只看得到**当前最新那一帧**,中间的跳过。

    排队会让内存跟着最慢的那个人涨,而实时画面上一帧旧数据没有任何价值。
    """
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_forever,
                      start_timeout_s=5.0)
    s = feed.stream()
    try:
        next(s)
        feed.等到第几帧(feed.帧号 + 3, timeout_s=5.0)
        assert feed.积压 == 0
    finally:
        s.close()
        feed.close()


def test_超了上限明确拒绝_不排队(fake_ffmpeg_forever):
    """排队意味着人盯着一个转圈的图标,不知道是没连上还是在等。"""
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_forever,
                      start_timeout_s=5.0)
    open_ones = []
    try:
        for _ in range(MAX_VIEWERS):
            s = feed.stream()
            next(s)
            open_ones.append(s)
        extra = feed.stream()
        with pytest.raises(VideoError):
            next(extra)
        extra.close()
    finally:
        for s in open_ones:
            s.close()
        feed.close()


def test_上限至少够三个人各看两路():
    """§3.6 把非本机会话钉在 3 个。每人前后两路 = 6。

    上限的**理由**在扇出之后变了:一个观众的成本从「一条 ffmpeg」降成
    「一个套接字加一个指向最新帧的指针」,剩下的成本是上行带宽。所以这个数
    是从会话上限推出来的,不是从 CPU 推出来的。
    """
    assert MAX_VIEWERS >= 6


def test_健康报告里有人看得懂的原因(fake_ffmpeg_silent):
    """``health()`` 是给人看的:值守的人要能从这一句话知道该去查什么。"""
    feed = CameraFeed("rtsp://x/front", ffmpeg=fake_ffmpeg_silent,
                      start_timeout_s=0.3)
    s = feed.stream()
    with pytest.raises(VideoError):
        next(s)
    s.close()
    got = feed.health()
    assert got["online"] is False
    assert got["viewers"] == 0
    assert got["detail"]
    feed.close()
```

`feed.处理器数`、`feed.帧号`、`feed.积压`、`feed.等到第几帧()` 这四个是**测试用的
观察窗**，实现里要真的提供（不是 `_` 开头的私有名，测试要用它们）。中文属性名在
Python 里是合法标识符，这个仓库的中文测试函数名是既有风格；但 `lease.py` 里的
`每拍都变的键` 是先例，所以中文属性名也可以。**如果实现者觉得别扭，改成
`live_processes`/`frame_seq`/`backlog`/`wait_for_frame` 也行——但要在报告里说一声，
并且把上面这几条测试里的名字同步改掉。**

- [ ] **Step 3: 跑，确认全红**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_video.py -q --basetemp=D:/pytest-tmp/v7t3a`

- [ ] **Step 4: 实现**

`video.py` 常量段加：

```python
#: 多久没进新帧就算这一路不在线。画面是 5fps,一帧 200ms —— 2 秒等于连着
#: 丢了十帧。**这个数是 §5.9 那道闸的分母**:定大了,人对着一张冻住的图
#: 还能开狗;定小了,WiFi 抖一下摇杆就灰。
STALE_S = 2.0

#: 第一帧最多等多久。RTSP 握手加上等一个关键帧,现场那条 WiFi 上偶尔要好几秒
#: —— 跟 ``STILL_TIMEOUT_S`` 同一个理由,同一个数。
#:
#: **共享泵之后这个超时必须显式做。** 旧实现里「一帧都没出」是靠 ffmpeg 自己
#: 退出发现的;常驻的泵在 ffmpeg 活着但一直不吐帧时不会退,``next()`` 会永远
#: 卡住,而路由正卡在它上面等着决定发 200 还是 503。
START_TIMEOUT_S = 20.0
```

`MAX_VIEWERS` 改成 6，并把它的理由重写——**旧注释说的是「一个观众一条 ffmpeg，
攒出几十个进程」，扇出之后这条理由不成立了，留着就是一句假话**：

```python
#: 同一路流最多几个人同时看。
#:
#: **这个数的理由在扇出之后变了。** 旧的理由是「一个观众一条 ffmpeg,一晚上
#: 能攒出几十个进程」—— 现在一路只有一条泵,观众的成本降成一个套接字加一个
#: 指向最新帧的指针。剩下的成本是**上行带宽**:热点上每个观众都在实打实地
#: 收 JPEG。所以这个数从会话上限推:§3.6 把非本机会话钉在 3 个,每人前后
#: 两路,6。
#:
#: 超了就明确拒绝,不排队 —— 排队意味着人盯着一个转圈的图标,不知道是没连上
#: 还是在等。
MAX_VIEWERS = 6
```

模块开头那段「一个观众一条 ffmpeg」和「为什么有并发上限」也要跟着改写，它们现在
描述的是被推翻的架构。

`MjpegSource` 整个换成：

```python
class CameraFeed:
    """一路 RTSP 流。**一条 ffmpeg,字节扇给 N 个观众**(§10 待办 1)。

    第一个观众进来时起泵,最后一个走掉时收泵。泵是一条后台线程,把 ffmpeg
    吐出来的 JPEG 切好放在 ``_latest`` 上,谁来了就拿最新那一张。

    **慢的观众丢帧,不排队。** 每个观众只看得到当前最新那一帧,跟不上就跳过
    中间的。排队会让内存跟着最慢的那个人涨,而实时画面上一帧旧数据没有任何
    价值。

    **「在线」有两半:泵活着 + 最近 ``stale_s`` 秒内真收到过帧。** 只看进程
    在不在是不够的 —— RTSP 断了 ffmpeg 未必立刻死,它可能挂在那儿重连,而那
    段时间画面是冻的。冻着的画面是 §5.9 要防的头号情况:人看着一张不动的图,
    以为前面没障碍。

    线程安全:HTTP 是一请求一线程的(``ThreadingHTTPServer``),再加上泵自己
    那条线程,所有共享状态都在 ``_ready`` 这把条件变量底下。
    """

    def __init__(self, rtsp_url: str, *, ffmpeg: str = "ffmpeg",
                 fps: float = 5.0,
                 clock: Callable[[], float] = time.monotonic,
                 stale_s: float = STALE_S,
                 start_timeout_s: float = START_TIMEOUT_S) -> None:
        self._url = rtsp_url
        self._ffmpeg = ffmpeg
        self._fps = fps
        self._clock = clock
        self._stale = stale_s
        self._start_timeout = start_timeout_s
        #: 所有共享状态都在这把锁底下。观众等新帧也等在它上面。
        self._ready = threading.Condition()
        self._viewers = 0
        self._latest: bytes | None = None
        self._seq = 0
        self._last_at: float | None = None
        self._detail = ""
        self._proc: subprocess.Popen[bytes] | None = None
        self._pump: threading.Thread | None = None
        #: 泵换一代就作废上一代 —— 收泵和起泵之间可能有观众来去。
        self._gen = 0
```

属性：

```python
    @property
    def url(self) -> str:
        return self._url

    @property
    def viewers(self) -> int:
        with self._ready:
            return self._viewers

    @property
    def 处理器数(self) -> int:
        """现在有几条 ffmpeg 在跑。**测试用的观察窗** —— 扇出这件事只有从
        这个数上看得出来,「两个人都能看到画面」在旧实现上也是绿的。
        """
        with self._ready:
            return 1 if self._pump is not None and self._pump.is_alive() else 0

    @property
    def 帧号(self) -> int:
        """进来的第几帧。测试用来等下一帧,不用 sleep。"""
        with self._ready:
            return self._seq

    @property
    def 积压(self) -> int:
        """攒了几帧没发出去。**永远是 0** —— 这里只存最新那一张,
        这个属性存在的意义就是让「不排队」这条规矩有一个测得到的形状。
        """
        return 0

    @property
    def online(self) -> bool:
        """这一刻真有画面流过来吗。**§5.9 那道闸读的就是它。**"""
        with self._ready:
            return self._online_locked()

    @property
    def argv(self) -> tuple[str, ...]:
        """ffmpeg 的完整命令行。

        ``-nostdin`` 是必须的:不加的话 ffmpeg 会去抢终端的标准输入,把
        父进程的 Ctrl+C 一起吃掉。

        **换 H.264 时只动这里和 content-type**(§10 待办 2,第 8 卷)——
        codec 的选择只出现在这一处,别处不许再判一次。
        """
        return (self._ffmpeg, "-nostdin", "-loglevel", "error",
                "-rtsp_transport", "tcp", "-i", self._url,
                "-f", "image2pipe", "-vcodec", "mjpeg",
                "-q:v", "6", "-r", f"{self._fps:g}", "-")
```

在线判断（**只在持锁时调**）与健康报告：

```python
    def _online_locked(self) -> bool:
        if self._pump is None or not self._pump.is_alive():
            return False
        if self._last_at is None:
            return False
        return (self._clock() - self._last_at) <= self._stale

    def health(self) -> dict[str, Any]:
        """给人看的一句话。值守的人要能从它知道该去查什么。"""
        with self._ready:
            since = (None if self._last_at is None
                     else round(self._clock() - self._last_at, 2))
            return {"online": self._online_locked(),
                    "viewers": self._viewers,
                    "since_frame_s": since,
                    "detail": self._detail}
```

等帧的助手（测试和观众共用）：

```python
    def 等到第几帧(self, seq: int, *, timeout_s: float) -> bool:
        """等到帧号追上 ``seq``。**带截止时间的等,不是 sleep。**

        真后台线程上不许用固定 ``sleep(余量)`` 等 —— 余量给小了偶发红,
        给大了每跑一次都白等。条件变量一有新帧就醒。
        """
        deadline = self._clock() + timeout_s
        with self._ready:
            while self._seq < seq:
                left = deadline - self._clock()
                if left <= 0:
                    return False
                self._ready.wait(left)
            return True
```

进出场与泵：

```python
    def _join(self) -> None:
        with self._ready:
            if self._viewers >= MAX_VIEWERS:
                raise VideoError(
                    f"{self._url} 已经有 {self._viewers} 个人在看了,"
                    f"最多 {MAX_VIEWERS} 个 —— 关掉一个再开")
            self._viewers += 1
            if self._pump is None or not self._pump.is_alive():
                self._gen += 1
                self._latest = None
                self._last_at = None
                self._detail = ""
                self._pump = threading.Thread(
                    target=self._run, args=(self._gen,),
                    name=f"video-{self._url}", daemon=True)
                self._pump.start()

    def _leave(self) -> None:
        with self._ready:
            self._viewers -= 1
            if self._viewers > 0:
                return
            self._stop_locked()

    def _stop_locked(self) -> None:
        """收泵。**必须在持锁时调** —— ``_gen`` 一动,泵那边下一圈就自己退。"""
        self._gen += 1
        proc, self._proc = self._proc, None
        self._pump = None
        self._last_at = None
        self._ready.notify_all()
        _kill(proc)

    def close(self) -> None:
        """关服务时收干净。**不给狗发任何指令** —— 关服务不该让它动一下。"""
        with self._ready:
            self._viewers = 0
            self._stop_locked()

    def _run(self, gen: int) -> None:
        """泵:一条 ffmpeg,把切好的帧放到 ``_latest`` 上。**在自己的线程里。**"""
        errs = tempfile.TemporaryFile()
        proc: subprocess.Popen[bytes] | None = None
        try:
            try:
                proc = subprocess.Popen(      # 命令行是自己拼的,没有 shell
                    self.argv, stdin=subprocess.DEVNULL,
                    stdout=subprocess.PIPE, stderr=errs)
            except OSError as exc:
                self._fail(gen, f"起不了 ffmpeg({self._ffmpeg}): {exc} —— "
                                f"装一个(apt install ffmpeg),或者用 --ffmpeg 指到它")
                return
            with self._ready:
                if gen != self._gen:
                    _kill(proc)
                    return
                self._proc = proc
            assert proc.stdout is not None   # Popen 的类型标注给不出这个
            got = 0
            for frame in _frames(proc.stdout):
                with self._ready:
                    if gen != self._gen:
                        break
                    got += 1
                    self._latest = frame
                    self._seq += 1
                    self._last_at = self._clock()
                    self._detail = ""
                    self._ready.notify_all()
            if got == 0:
                # ffmpeg 起来了但一帧都没出:多半是拉不到流(地址错、推流
                # 没开、网线没插)。把它自己的报错原样带给人。
                self._fail(gen, f"拉不到 {self._url}: "
                                f"{_tail(errs) or 'ffmpeg 没说原因'}")
            else:
                self._fail(gen, f"{self._url} 的画面断了: "
                                f"{_tail(errs) or 'ffmpeg 没说原因'}")
        finally:
            _kill(proc)
            with contextlib.suppress(OSError):
                errs.close()
            with self._ready:
                self._ready.notify_all()

    def _fail(self, gen: int, detail: str) -> None:
        with self._ready:
            if gen != self._gen:
                return
            self._detail = detail
            self._last_at = None
            self._ready.notify_all()
```

观众：

```python
    def stream(self) -> Iterator[bytes]:
        """一次吐一个完整 JPEG。

        **第一次 ``next()`` 才真的进场** —— 生成器就是这样。上限和「起不起得
        来」两件事因此都落在第一帧上,调用方可以先取一帧,把失败变成一个说得
        清楚的 HTTP 状态码,再决定要不要开始往外写。

        调用方**必须** ``close()`` 它:人随时会关标签页,那时它正等在条件变量
        上,退场(可能连带收泵)的逻辑只在 ``finally`` 里。
        """
        return self._stream()

    def _stream(self) -> Iterator[bytes]:
        self._join()
        try:
            with self._ready:
                # **不能写 ``seen = 0``。** 帧号是全流共用、不回头的,而 ``_join``
                # 清掉 ``_latest`` 却**不**重置 ``_seq`` —— 泵收掉又重启之后,
                # ``seen = 0`` 会让下面那个 ``_seq <= seen`` 立刻不成立,于是这一路
                # 把一帧空字节当成图发出去,HTTP 500。现实触发路径就是"关了视频页
                # 再打开"。
                #
                # 手上那一张就是这个观众的第一帧(所以是 ``_seq - 1``,不是 ``_seq``,
                # 否则新观众要白等一帧);泵刚换代时 ``_latest`` 是空的,那就从下
                # 一帧开始等。
                seen = self._seq - 1 if self._latest is not None else self._seq
            deadline = self._clock() + self._start_timeout
            first = True
            while True:
                with self._ready:
                    # ``or self._latest is None`` 不能省:泵换代时 ``_seq``
                    # 可能已经超过 ``seen`` 而 ``_latest`` 还是空的。
                    while self._seq <= seen or self._latest is None:
                        if self._detail:
                            raise VideoError(self._detail)
                        if self._pump is None or not self._pump.is_alive():
                            raise VideoError(
                                f"{self._url} 的画面没了 —— "
                                f"{self._detail or '拉流的进程已经退了'}")
                        left = deadline - self._clock() if first else None
                        if first and left is not None and left <= 0:
                            raise VideoError(
                                f"等 {self._url} 的第一帧超过 "
                                f"{self._start_timeout:g}s —— "
                                f"多半是推流没开或者网断了")
                        self._ready.wait(left)
                    seen = self._seq
                    frame = self._latest
                assert frame is not None    # _seq 涨过就一定有帧
                first = False
                yield frame
        finally:
            self._leave()
```

顶部 import 补 `from typing import IO, Any`（`IO` 已经有了），
`from collections.abc import Callable, Iterator`（`Iterator` 已经有了）。
`__all__` 里 `"MjpegSource"` 换成 `"CameraFeed"`，加 `"STALE_S"`、`"START_TIMEOUT_S"`。

**注意 `_stream` 里那个 `first` 只对第一帧生效。** 第一帧之后就无限等——泵活着
但画面冻了的时候，MJPEG 连接保持着、画面定住，这是浏览器该有的行为；「冻了」
这件事由 `online` 报给 §5.9 的闸，不是由掐断连接来表达。

- [ ] **Step 5: 改既有测试的断言**

按 Step 1 那份清单逐条改。每改一条，报告里记一行。**典型的一条**：旧的
「第三个观众被拒」现在要拿 `MAX_VIEWERS` 算，而不是硬编 `2`；旧的
「两个观众两条 ffmpeg」要翻过来。

- [ ] **Step 6: 跑绿**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_video.py -q --basetemp=D:/pytest-tmp/v7t3b`

- [ ] **Step 7: 拆一次，确认「一条泵」真的在测东西**

先把 `video.py` 复制一份到暂存目录。把 `_join()` 里那句
`if self._pump is None or not self._pump.is_alive():` 改成 `if True:`（每个观众
起一条泵，退回旧架构）。`test_两个观众只起一条ffmpeg` **必须红**。
再把 `_online_locked` 里的 stale 那一句改成 `return True`（只看进程在不在），
`test_画面冻住就算不在线` **必须红**。报告里写下红的是哪几条。用 `cp` 恢复。

- [ ] **Step 8: 提交**

```bash
ruff check src/d1max_patrol/app/video.py tests/app/test_video.py
git add -A
git commit -m "refactor(video): 一路一条 ffmpeg 字节扇出,在线成为可问的状态(§10 待办 1)"
```

---

### Task 4: 「视频在不在线」接到 HTTP 上

**Files:**
- Modify: `src/d1max_patrol/app/server.py:81`（import）、`:576`（`AppContext.video` 类型）、
  `:1014` 附近（加一条路由）、`:1444-1469`（`_video`）、`:3158`（构造）、
  `_StateHub._build`（加一个 `video` 段）
- Test: 既有的服务器测试文件（追加）

**Interfaces:**
- Consumes: Task 3 的 `CameraFeed.online` / `health()`
- Produces: `AppContext.video: Mapping[str, CameraFeed]`；
  `GET /api/video/health` → `{"cameras": {"front": {...}, "back": {...}}}`；
  `/api/state` 的快照里多一段 `"video": {"front": bool, "back": bool}`。
  Task 6 的闸从 `ctx.video` 读，Task 8 把这两个形状写进夹具，Task 10 的
  Dart 视频屏读 `/api/video/health`。

**详细的健康数字绝对不许进 `/api/state` 那条 SSE。** 第 6 卷刚在这上面栽过一次
（P1 回归：租约的相对倒计时被展开进常连的 SSE，握着控制权时那条流从「静止时安静」
变成每半秒一整帧）。`since_frame_s` 每拍都在变，进去就是同一个错误。
所以分两处：**布尔进 SSE**（`online` 变一次才响一次），**数字进轮询接口**。

- [ ] **Step 1: 写失败的测试**

先 `grep -rln "api/state\|_StateHub" tests` 找到既有的那个文件，追加：

```python
def test_健康接口报每一路相机(client):
    resp = client.get("/api/video/health")
    assert resp.status == 200
    cams = resp.json()["cameras"]
    assert set(cams) == set(CAMERAS)
    assert set(cams["front"]) == {"online", "viewers", "since_frame_s", "detail"}


def test_没配地址的相机也要出现在健康里(client_without_camera):
    """**「没配地址」和「配了但拉不到」在界面上是两句不同的话。**

    漏掉没配的那一路,界面只能显示「没有这一路」,人分不出是装机时忘了填
    还是推流挂了 —— 这两件事的处理方式完全不同。
    """
    cams = client_without_camera.get("/api/video/health").json()["cameras"]
    assert cams["front"]["online"] is False
    assert "没配地址" in cams["front"]["detail"]


def test_状态快照里的视频段只有布尔(client):
    """**每拍都变的数不许进这条 SSE。**

    第 6 卷刚在这上面栽过一次:租约的相对倒计时被展开进常连的 SSE,握着
    控制权的时候那条流从「静止时安静」变成每半秒一整帧 —— 在热点上是实打
    实的带宽,还会把真正的状态变化淹掉。``since_frame_s`` 每拍都在变。
    """
    seg = client.get("/api/state/once").json()["video"]
    assert set(seg) == set(CAMERAS)
    assert all(isinstance(v, bool) for v in seg.values())
```

`client`、`client_without_camera`、`/api/state/once` 这几个名字**照抄既有测试文件
里现成的写法**——那个文件已经有一套取快照的办法（第 6 卷的
`test_快照里没有每拍都变的量` 就在用），用它的，不要新造。**找不到对应的
fixture 时，照它的样子加一个，并在报告里说明。**

- [ ] **Step 2: 跑，确认全红**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app -q -k video --basetemp=D:/pytest-tmp/v7t4a`

- [ ] **Step 3: 实现**

`server.py`：

1. import：`from d1max_patrol.app.video import CAMERAS, CameraFeed, RtspStill, VideoError`
2. `AppContext.video` 的类型改成 `Mapping[str, CameraFeed]`，注释保持原意。
3. `:3158` 的构造改成 `CameraFeed(...)`，参数照旧。
4. 路由表里 `self.route("GET", "/api/video/<name>", self._video)` 之后加：
   `self.route("GET", "/api/video/health", self._video_health)`
   **注意顺序**：这个路由表如果是按登记顺序匹配的，`/api/video/<name>` 会先吃掉
   `/api/video/health`。**先读一眼 `route()` 和匹配逻辑**；如果确实是顺序匹配，
   把 `health` 那条登记在前面，并在报告里说明。
5. 新路由：

```python
    def _video_health(self, _req: Request) -> Response:
        """每一路相机现在在不在线。**这一屏是轮询的,数字放这儿。**

        每拍都变的量(``since_frame_s``)只出现在这条轮询接口上,不进
        ``/api/state`` 那条常连的 SSE —— 第 6 卷在这上面栽过一次,理由见
        ``engine/lease.py`` 的 ``每拍都变的键``。
        """
        cams: dict[str, Any] = {}
        for name in CAMERAS:
            feed = self._ctx.video.get(name)
            if feed is None:
                cams[name] = {"online": False, "viewers": 0,
                              "since_frame_s": None,
                              "detail": f"{name} 相机没配地址 —— "
                                        f"起 app 时用 --camera-host 指到推流的那台机器"}
            else:
                cams[name] = feed.health()
        return json_response({"cameras": cams})
```

6. `_StateHub._build`（`server.py:775` 附近那个 `self._build()`）里加一段：

```python
            "video": {name: (feed.online if (feed := ctx.video.get(name))
                             else False) for name in CAMERAS},
```

**只放布尔。** 写这一段的时候把理由抄一句在旁边，指向 `每拍都变的键`。
（海象表达式在这个仓库里不常见；如果和现有风格不合，写成一个小助手函数
`_video_wire(ctx) -> dict[str, bool]`，那样也更好测。）

- [ ] **Step 4: 跑绿 + 全量**

> **⚠️ 全量必须前台跑：`timeout: 600000`，`run_in_background: false`。**

```
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest -q --basetemp=D:/pytest-tmp/v7t4b
```
退出码 0 才算过。

- [ ] **Step 5: 拆一次**

先复制一份 `server.py`。把 `_StateHub._build` 里那段 `video` 改成直接塞
`feed.health()`（把每拍都变的数放进 SSE）。
`test_状态快照里的视频段只有布尔` **必须红**。报告里记一笔。用 `cp` 恢复。

- [ ] **Step 6: 提交**

```bash
ruff check src/d1max_patrol/app/server.py tests/app
git add -A
git commit -m "feat(app): 视频在线接到 HTTP —— 布尔进 SSE,数字进轮询接口"
```

---

### Task 5: `SUSPENDED` —— 引擎主动让开腿，并且记下让开时停在哪儿

**Files:**
- Modify: `src/d1max_patrol/engine/machine.py`（`RunState` :87、`RunSnapshot` :123、
  `_Live` :183、`_publish` :369、`_handle_command` :751、`_pause_until_resumed` :758 之后）
- Test: `tests/engine/test_machine_suspend.py`（新建）

**Interfaces:**
- Produces: `RunState.SUSPENDED`；`SuspendPoint(waypoint_index, waypoint_name,
  pose, reason, at_ms)` 带 `to_wire()`；`RunSnapshot.suspended_at: SuspendPoint | None`
  与上线字段 `"suspended_at"`；`MissionEngine.suspend(reason: str) -> None`（协程）；
  `MissionEngine.yielding -> bool`。
  Task 6 的 `TeleopBusy` 读 `yielding`，Task 8 把 `suspended_at` 的形状写进夹具。

规格 §5.10 的表里，`machine.py` 那一行写着「**本节唯一的硬改动**」：加 `SUSPENDED`
状态和挂起点位/位姿快照。今天仓库里 `SUSPENDED` 一个字都没有。

**`SUSPENDED` 不是 `PAUSED` 的同义词，两者的区别是「谁在动这条狗」。**
`PAUSED` 是人按了暂停，狗停着，**没有人碰它**，`_busy_checks` 全是空的。
`SUSPENDED` 是引擎**主动让开腿**，因为接下来这段路人要亲自开——挡在门口的箱子
挪不开、点位飘到了墙里、要人走过去看一眼。这段时间里 `_busy_checks` 恰恰是**满的**
（遥控在动），而这正是 §5.10 那条「持租约 + 引擎已挂起 → 放行」要放行的东西。

两个状态因此不能合并：合了之后「暂停时不许遥控」和「挂起时必须能遥控」这两条
规矩会落在同一个状态上，而它们是相反的。

**继续之前必须确认没人还握着摇杆。** 人在手机上点「继续」的时候，左手很可能
还压在摇杆上——那一瞬间引擎和人同时在给腿下指令。这不是理论问题：`add_busy_check`
这个机制存在的全部理由就是它。所以 `resume` 在 `SUSPENDED` 里要**先查再放**，
查不过就明确说出理由继续等着，而不是照单继续。

- [ ] **Step 1: 写失败的测试**

`tests/engine/test_machine_suspend.py`。**先读 `tests/engine/` 里既有的机器测试**
（`grep -rln "MissionEngine(" tests/engine`），照抄它建引擎、喂事件、等状态的那套
夹具，不要新造一套。

```python
"""引擎让开腿:``SUSPENDED``(§5.10)。

**跟 ``PAUSED`` 的区别是「谁在动这条狗」。** ``PAUSED`` 是人按了暂停,狗停着,
没有人碰它。``SUSPENDED`` 是引擎主动让开,因为接下来这段路人要亲自开 ——
挡在门口的箱子挪不开、点位飘到了墙里。这段时间 ``_busy_checks`` 恰恰是满的,
而那正是 §5.10 要放行的东西。

时刻全是注进来的,一个 ``sleep`` 都没有(§8.5 第 2 条)。
"""
```

```python
async def test_挂起进得去_并且状态是SUSPENDED(跑起来的引擎):
    eng = 跑起来的引擎
    await eng.suspend("门口有箱子,人去挪一下")
    await eng.wait_state(RunState.SUSPENDED)
    assert eng.state is RunState.SUSPENDED


async def test_挂起时引擎让位(跑起来的引擎):
    """``yielding`` 是给 ``TeleopBusy`` 读的那一个布尔(Task 6)。

    做成引擎自己的属性,而不是让遥控去比 ``state is RunState.SUSPENDED``:
    「哪些状态算让位」是引擎的词汇,外壳不该重新讲一遍(全局约束:业务层
    不许知道外壳,反过来外壳也不该复刻业务层的状态表)。
    """
    eng = 跑起来的引擎
    assert eng.yielding is False
    await eng.suspend("人来开")
    await eng.wait_state(RunState.SUSPENDED)
    assert eng.yielding is True


async def test_暂停不算让位(跑起来的引擎):
    """**这一条是这两个状态不能合并的证明。**

    ``PAUSED`` 的时候不许遥控,``SUSPENDED`` 的时候必须能遥控 —— 相反的两条
    规矩落在同一个状态上,就一定有一条是错的。
    """
    eng = 跑起来的引擎
    await eng.pause()
    await eng.wait_state(RunState.PAUSED)
    assert eng.yielding is False


async def test_挂起时记下停在哪个点(跑起来的引擎):
    eng = 跑起来的引擎
    await eng.suspend("门口有箱子")
    await eng.wait_state(RunState.SUSPENDED)
    点 = eng.snapshot.suspended_at
    assert 点 is not None
    assert 点.waypoint_name == eng.snapshot.waypoint_name
    assert 点.reason == "门口有箱子"


async def test_挂起时记下位姿(跑起来的引擎_喂过位姿):
    """**位姿是「人接管前狗在哪儿」的唯一记录。**

    人开着走一段之后,原来那个点位序号还在,但狗已经不在那儿了。要判断
    「接管结束后该从哪儿接着跑」,靠的是这一份快照,不是当前位姿。
    """
    eng = 跑起来的引擎_喂过位姿
    await eng.suspend("人来开")
    await eng.wait_state(RunState.SUSPENDED)
    assert eng.snapshot.suspended_at.pose is not None


async def test_没收到过位姿也照样挂得起来(跑起来的引擎):
    """位姿是**可有可无**的。旁路进程没连上、这一档不报位姿 —— 都不该
    让「人要接管」这件事卡住。挂不起来的后果是人挪不动狗,那比少一份
    位姿记录严重得多。
    """
    eng = 跑起来的引擎
    await eng.suspend("人来开")
    await eng.wait_state(RunState.SUSPENDED)
    assert eng.snapshot.suspended_at.pose is None


async def test_还有人握着摇杆就不许继续(跑起来的引擎):
    """**这条是这个任务里唯一真正危险的一条。**

    人点「继续」的时候左手很可能还压在摇杆上 —— 那一瞬间引擎和人同时在
    给腿下指令。``add_busy_check`` 存在的全部理由就是它。
    """
    eng = 跑起来的引擎
    握着 = ["遥控在动,先松手再继续"]
    eng.add_busy_check(lambda: 握着[0])
    await eng.suspend("人来开")
    await eng.wait_state(RunState.SUSPENDED)
    await eng.resume()
    await 等一拍(eng)                      # 见下面的助手
    assert eng.state is RunState.SUSPENDED
    assert "松手" in eng.snapshot.reason
    握着[0] = ""
    await eng.resume()
    await eng.wait_state(RunState.RUNNING)


async def test_继续之后挂起快照清掉(跑起来的引擎, nav):
    """留着就等于在界面上永远挂着一条「有人接管过」—— 那条信息第二天
    还在的时候,值守的人分不出是现在正被接管还是上礼拜被接管过。
    """
    eng = 跑起来的引擎
    await eng.suspend("人来开")
    await eng.wait_state(RunState.SUSPENDED)
    await eng.resume()
    # 不能等 wait_state(RunState.RUNNING)。``wait_state`` 判的是「出现过」
    # (``state not in self._seen``),而 ``EventEmitter.subscription()`` 是
    # **同步** contextmanager —— 目标状态已在 ``_seen`` 时,``wait_state``
    # 协程体里一个 ``await`` 表达式都不执行,不产生悬挂点;``resume()`` 只是
    # ``Queue.put``,未满时走 ``put_nowait`` 也不 await。两句连起来全程没让
    # 出控制权,引擎根本没机会取走 resume。夹具在 ``suspend()`` 之前已真实
    # 经过一次 RUNNING,所以 RUNNING 必在 ``_seen`` 里 —— **确定性红,不是
    # 偶发**。改等一个只会在 resume 真正生效之后才发生的事实:引擎接着派发
    # 下一个点位。
    await until(lambda: len(nav.goto_calls) == 2)
    assert eng.snapshot.suspended_at is None


async def test_挂起时也能中止(跑起来的引擎):
    """人接管到一半发现这趟没法跑了,得能直接收。"""
    eng = 跑起来的引擎
    await eng.suspend("人来开")
    await eng.wait_state(RunState.SUSPENDED)
    await eng.abort("现场不具备条件")
    assert await eng.wait_done(timeout_s=5.0) is RunState.ABORTED


async def test_上线的形状(跑起来的引擎):
    eng = 跑起来的引擎
    await eng.suspend("门口有箱子")
    await eng.wait_state(RunState.SUSPENDED)
    线 = eng.snapshot.to_wire()
    assert 线["state"] == "SUSPENDED"
    assert set(线["suspended_at"]) == {
        "waypoint_index", "waypoint_name", "pose", "reason", "at_ms"}


async def test_没挂起时上线是null(跑起来的引擎):
    assert 跑起来的引擎.snapshot.to_wire()["suspended_at"] is None
```

`等一拍(eng)` 是「让引擎的队列把已入队的命令处理掉」——**不许用 `sleep`**。
既有测试里已经有等状态的办法（`wait_state`），但这里要等的是「一个**不会**发生的
状态变化」。写成一个带截止时间的小助手，放在这个测试文件顶部：

```python
async def 等一拍(eng, *, timeout_s: float = 2.0) -> None:
    """等到引擎把队列里的东西吃完。**有截止时间,不是 sleep。**

    这里要证的是「点了继续但状态没变」,而「没变」只能靠等一小会儿再看 ——
    所以等的是**队列空**这个可观察的事实,不是一个拍脑袋的余量。
    """
    deadline = asyncio.get_running_loop().time() + timeout_s
    while not eng._queue.empty():
        if asyncio.get_running_loop().time() > deadline:
            raise AssertionError("引擎没把队列吃完")
        await asyncio.sleep(0)
```

**它碰了一个私有属性，这是明知故犯的。** 换成固定 `sleep(余量)` 才是真错：
余量给小了偶发红，给大了每跑一次都白等。如果既有测试里已经有更干净的办法
（比如一个「处理完了」的事件），**用那个，并在报告里说明**。

- [ ] **Step 2: 跑，确认全红**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/engine/test_machine_suspend.py -q --basetemp=D:/pytest-tmp/v7t5a`
Expected: 收集就失败（`RunState.SUSPENDED` 不存在）。

- [ ] **Step 3: 实现**

`RunState`（`machine.py:87`）里 `PAUSED` 之后加：

```python
    #: 引擎**主动让开腿**:接下来这段路人要亲自开。
    #:
    #: 跟 ``PAUSED`` 的区别是「谁在动这条狗」。``PAUSED`` 是人按了暂停,狗停着,
    #: 没有人碰它,``_busy_checks`` 全是空的。``SUSPENDED`` 期间 ``_busy_checks``
    #: 恰恰是满的 —— 遥控正在动 —— 而那正是 §5.10 要放行的东西。
    #:
    #: **不许跟 PAUSED 合并**:「暂停时不许遥控」和「挂起时必须能遥控」是
    #: 相反的两条规矩,落在同一个状态上就一定有一条是错的。
    SUSPENDED = "SUSPENDED"
```

`RunSnapshot` 之前加：

```python
@dataclass(frozen=True, slots=True)
class SuspendPoint:
    """让开腿的那一刻,狗停在哪儿(§5.10)。

    **位姿是「人接管前狗在哪儿」的唯一记录。** 人开着走一段之后,点位序号
    还在,但狗已经不在那儿了 —— 要判断接管结束后该从哪儿接着跑,靠的是这
    一份快照,不是当前位姿。

    ``pose`` 可以是 ``None``:旁路进程没连上、这一档不报位姿都会这样。
    **少一份位姿不该让「人要接管」卡住** —— 挂不起来的后果是人挪不动狗。
    """

    waypoint_index: int
    waypoint_name: str
    pose: Pose | None
    reason: str
    at_ms: int

    def to_wire(self) -> dict[str, Any]:
        return {"waypoint_index": self.waypoint_index,
                "waypoint_name": self.waypoint_name,
                "pose": self.pose.to_wire() if self.pose else None,
                "reason": self.reason, "at_ms": self.at_ms}
```

`RunSnapshot` 加一个字段（**放在 `reason` 之后**，它俩都有默认值）和一行上线：

```python
    suspended_at: SuspendPoint | None = None
```
```python
                "suspended_at": (self.suspended_at.to_wire()
                                 if self.suspended_at else None),
```

`_Live` 加两个字段：

```python
    #: 最近一次收到的位姿。挂起时抄一份进 ``SuspendPoint``。
    last_pose: Pose | None = None
    #: 现在挂着的那一份。``_publish`` 每次都从这儿取 —— 快照是重建出来的,
    #: 不从这儿取的话,挂起期间任何一次 publish 都会把它抹掉。
    suspended: SuspendPoint | None = None
```

`_publish` 的两处 `RunSnapshot(...)` 里带上它。**`live is None` 那一支传 `None`**
（没有 live 就没有挂起点），有 live 的那一支传 `suspended_at=live.suspended`。

`_handle` 里，在 `BatteryEvent` 那一段之后、`rule(...)` 之前记位姿：

```python
        if isinstance(item, DevicePoseEvent):
            live.last_pose = item.pose
            # 不 return:位姿照样交给规则表 —— 这里只是顺手留一份底,
            # 不是接管这个事件的处置。
```

`running` 属性旁边加：

```python
    @property
    def yielding(self) -> bool:
        """引擎现在让着腿吗(§5.10)。

        **做成引擎自己的属性,不让外壳去比状态**:「哪些状态算让位」是引擎的
        词汇,外壳复刻一遍状态表就等于把这条规矩存了两份,而两份迟早不一样。
        """
        return self._state is RunState.SUSPENDED
```

`pause`/`resume` 旁边加：

```python
    async def suspend(self, reason: str) -> None:
        """让开腿:接下来这段路人亲自开(§5.10)。

        **不是暂停。** 暂停是狗停着没人碰;挂起期间人正在用遥控开它。
        """
        await self._queue.put(_Command("suspend", reason))
```

`_Command` 的注释 `# pause / resume / abort` 改成 `# pause / suspend / resume / abort`。

`_handle_command` 加一支：

```python
        if cmd.kind == "suspend":
            await self._suspend_until_resumed(cmd.reason)
```

`_pause_until_resumed` 后面加：

```python
    async def _suspend_until_resumed(self, reason: str) -> None:
        """挂起:停下来让开腿,人接管完再继续,回来时重发当前点。

        跟 ``_pause_until_resumed`` 长得很像,但**不能合并**:这一支要在
        继续之前查 ``_busy_checks``,而暂停那一支不查也不该查(暂停期间本来
        就没人碰狗)。合了之后两条相反的规矩会落在同一段代码上。

        回来重发当前点,是因为人可能已经把狗开到别处去了 —— 接着往下走等于
        从一个没人知道的位置出发。
        """
        live = self._live
        assert live is not None
        wps = live.mission.waypoints
        idx = min(live.index, len(wps) - 1)
        live.suspended = SuspendPoint(
            waypoint_index=live.index, waypoint_name=wps[idx].name,
            pose=live.last_pose, reason=reason,
            at_ms=int(time.time() * 1000))
        await self._transition(RunState.SUSPENDED, reason)
        await self._stop_nav_quietly()
        while True:
            item = await self._next(None)
            if isinstance(item, _Command):
                if item.kind == "abort":
                    live.suspended = None
                    raise _AbortRun(item.reason or "人工中止")
                if item.kind == "resume":
                    blocked = self._busy_reason()
                    if not blocked:
                        break
                    # **人点「继续」的时候左手很可能还压在摇杆上。** 那一瞬间
                    # 引擎和人同时在给腿下指令 —— add_busy_check 存在的全部
                    # 理由就是它。说清楚为什么没继续,然后接着等。
                    self._note("resume_refused", reason=blocked)
                    self._publish(f"还不能继续: {blocked}")
                    continue
                continue
            if isinstance(item, BatteryEvent):
                live.battery_pct = item.percent
            elif isinstance(item, DevicePoseEvent):
                # 人开着的时候位姿一直在变。留着最新的 —— 但**不覆盖**
                # ``live.suspended.pose``:那一份记的是「人接管前在哪儿」,
                # 被现在的位置盖掉就什么都不剩了。
                live.last_pose = item.pose
            self._note("suspended_event", event=type(item).__name__)
        live.suspended = None
        await self._transition(RunState.RUNNING, "人工接管结束")
        raise _RetryWaypoint

    def _busy_reason(self) -> str:
        """现在有别人在动这条狗吗。返回理由,空串表示没有。"""
        for check in self._busy_checks:
            reason = check()
            if reason:
                return reason
        return ""
```

`start()` 里那段循环也改成用 `_busy_reason()`——同一条判断存两份，迟早不一样：

```python
        blocked = self._busy_reason()
        if blocked:
            raise EngineBusy(blocked)
```

import 补 `DevicePoseEvent`（从 `backends.base`）和 `Pose`（从
`protocol.nav_types`）；`__all__` 加 `"SuspendPoint"`。

- [ ] **Step 4: 跑绿**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/engine -q --basetemp=D:/pytest-tmp/v7t5b`

**注意 `FINAL_STATES` 不动**——`SUSPENDED` 不是终态。顺手 `grep -rn "RunState\." src tests`
看一眼有没有别处在穷举状态表（比如页面上的状态名映射、`static/app.js`）；有的话
补上 `SUSPENDED`，漏了的话界面上会显示一个空白状态。

- [ ] **Step 5: 拆一次，确认「还握着摇杆就不许继续」真的在测东西**

先把 `machine.py` 复制到暂存目录。把 `_suspend_until_resumed` 里那段
`blocked = self._busy_reason()` 到 `continue` 全删掉，只留 `break`。
`test_还有人握着摇杆就不许继续` **必须红**。
再把 `_suspend_until_resumed` 里的 `RunState.SUSPENDED` 改成 `RunState.PAUSED`，
`test_暂停不算让位` 那一组里应该有条红的。报告里写下红的是哪几条。用 `cp` 恢复。

- [ ] **Step 6: 提交**

```bash
ruff check src/d1max_patrol/engine/machine.py tests/engine/test_machine_suspend.py
git add -A
git commit -m "feat(engine): SUSPENDED —— 引擎让开腿,并记下让开时停在哪儿(§5.10)"
```

---

### Task 6: 视频不在线禁止移动 + 挂起时放行

**Files:**
- Modify: `src/d1max_patrol/app/teleop.py`（`Teleop.__init__` :73、`pulse()` :93、
  `_watchdog()` :168）
- Modify: `src/d1max_patrol/app/server.py`（`_make_teleop` :3080、启动顺序 :3145-3165）
- Modify: `tests/app/conftest.py:180`、`tests/app/test_end_to_end.py:235`、
  `tests/app/test_teleop.py:138`（三个构造点各补一个参数）
- Test: `tests/app/test_teleop.py`（追加）

**Interfaces:**
- Consumes: Task 3 的 `CameraFeed.online`、Task 5 的 `MissionEngine.yielding`
- Produces: `Teleop(device, engine, *, video_gate: Callable[[], str], ...)`
  ——**必填的关键字参数**，返回拦住的理由，空串表示可以走。

§5.9 的原话：

> 这条是**安全约束，不是体验约束**，因此不设「确认后继续」的绕过口子

今天 `teleop.py` 里没有任何视频判断——`grep -n "video" src/d1max_patrol/app/teleop.py`
是空的。**这是安全空白，不是体验空白。**

**`video_gate` 是必填的，没有默认值。** 给个默认就意味着有一天会有人建一个没有
闸的 `Teleop` 而不自知，而那一天没有任何测试会红。只有四个构造点，全改一遍。
三个测试里的构造点显式传 `video_gate=lambda: ""`，那一行本身就是在说
「这条测试不测闸」。

**闸放在 `_STOP` 早返之后。** 三轴全零走的是 `stop()`——**停车永远不许被闸挡住**。
一个「因为看不见所以不许停」的实现，会在视频掉线的那一刻把狗锁在最后一个动作上。
这是这个任务里唯一能造成实际伤害的错法。

- [ ] **Step 1: 写失败的测试**

追加到 `tests/app/test_teleop.py`。**照抄它现有的 `fake_device` / `fake_clock` /
`engine` 夹具**，只把构造改成带闸的：

```python
def 带闸(fake_device, engine, fake_clock, 理由: list[str]) -> Teleop:
    return Teleop(fake_device, engine, clock=fake_clock, watch_period_s=TICK,
                  video_gate=lambda: 理由[0])


async def test_视频不在线就不许动(fake_device, engine, fake_clock):
    """§5.9。**安全约束,不是体验约束。**

    没有画面还让人开着走,是在盲开:对人,盲开的狗会撞到人;对狗,它自己也
    会被撞坏、被开下台阶。
    """
    理由 = ["前相机没画面"]
    t = 带闸(fake_device, engine, fake_clock, 理由)
    with pytest.raises(TeleopBusy) as e:
        await t.pulse(0.5, 0.0, 0.0)
    assert "画面" in str(e.value)
    assert fake_device.walks == []


async def test_没有确认后继续这条路(fake_device, engine, fake_clock):
    """**规格明写不设绕过口子。**

    这一条钉的是「``pulse`` 上不存在任何放行参数」—— 加一个 ``force=True``
    之类的旁路,这条测试就该红。要挪狗,就必须能看得见;网差到没有画面,
    那就走过去挪。
    """
    import inspect
    sig = inspect.signature(Teleop.pulse)
    assert not {"force", "confirm", "override", "bypass"} & set(sig.parameters)


async def test_停车永远不许被闸挡住(fake_device, engine, fake_clock):
    """**这是这个任务里唯一能造成实际伤害的错法。**

    一个「因为看不见所以不许停」的实现,会在视频掉线的那一刻把狗锁在最后
    一个动作上 —— 掉线恰恰是最需要它停下来的时候。
    """
    理由 = ["前相机没画面"]
    t = 带闸(fake_device, engine, fake_clock, 理由)
    await t.pulse(0.0, 0.0, 0.0)          # 三轴全零 = 停
    await t.stop()
    assert fake_device.walks[-1] == (0.0, 0.0, 0.0, 0.0)


async def test_视频在线就照常走(fake_device, engine, fake_clock):
    t = 带闸(fake_device, engine, fake_clock, [""])
    await t.pulse(0.5, 0.0, 0.0)
    assert fake_device.walks


async def test_开着走的时候视频掉了_看门狗停狗(fake_device, engine, fake_clock):
    """**掉线时没有人在调 ``pulse``。**

    人的手还压在摇杆上,页面还在发心跳,``pulse`` 每一拍都进得来 —— 但真正
    危险的那一刻是「画面刚黑掉、人还没反应过来」的那半秒。守死人那条协程
    是唯一一直在看的东西,所以这一条判断得放在它里面。
    """
    理由 = [""]
    t = 带闸(fake_device, engine, fake_clock, 理由)
    await t.pulse(0.5, 0.0, 0.0)
    理由[0] = "前相机没画面"
    await 等到(lambda: not t.active)       # 用这个文件里既有的那个助手
    assert t.active is False
    assert fake_device.walks[-1] == (0.0, 0.0, 0.0, 0.0)


async def test_引擎在跑就不许遥控(fake_device, 跑着的engine, fake_clock):
    t = 带闸(fake_device, 跑着的engine, fake_clock, [""])
    with pytest.raises(TeleopBusy):
        await t.pulse(0.5, 0.0, 0.0)


async def test_引擎让开腿的时候可以遥控(fake_device, 挂起的engine, fake_clock):
    """§5.10:持租约 + 引擎已挂起 → 放行。

    **租约那一半不在这儿。** ``app/control.py`` 的 ``CONTROLLED`` 已经把
    ``POST /api/teleop`` 整条路由钉在租约后面了 —— 在这儿再查一遍就是把
    同一条规矩存两份,而两份迟早不一样。这儿只管「引擎让不让位」。
    """
    t = 带闸(fake_device, 挂起的engine, fake_clock, [""])
    await t.pulse(0.5, 0.0, 0.0)
    assert fake_device.walks


async def test_挂起了也一样要有画面(fake_device, 挂起的engine, fake_clock):
    """**让位是让引擎的位,不是让 §5.9 的位。** 挂起恰恰是人要亲自开的时候
    —— 那正是最需要看得见的时候。
    """
    t = 带闸(fake_device, 挂起的engine, fake_clock, ["前相机没画面"])
    with pytest.raises(TeleopBusy):
        await t.pulse(0.5, 0.0, 0.0)


async def test_急停按着仍然不许动(fake_device, engine, fake_clock):
    """闸多了一道,原来那两道不许弱。"""
    fake_device.estop = True
    t = 带闸(fake_device, engine, fake_clock, [""])
    with pytest.raises(TeleopBusy):
        await t.pulse(0.5, 0.0, 0.0)
```

`跑着的engine` / `挂起的engine` 两个夹具：**优先在这个文件里找现成的**
（`engine` 夹具多半是个假引擎）。如果它是个假的，加两个属性就够了；如果它是
真的 `MissionEngine`，用 Task 5 的 `suspend()` 把它推到 `SUSPENDED`。
两种走法都行，**在报告里说明走的是哪一种**。

`等到(...)` 是既有的那个带截止时间的助手（`tests/app/test_control_wiring.py`
里那个）。**不许在真后台线程/协程上用固定 `sleep(余量)` 等**：余量给小了偶发红，
给大了每跑一次都白等。这个文件里若还没有，从那边搬一份过来。

- [ ] **Step 2: 跑，确认全红**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_teleop.py -q --basetemp=D:/pytest-tmp/v7t6a`

- [ ] **Step 3: 实现**

`Teleop.__init__` 加一个**必填的关键字参数**：

```python
    def __init__(self, device: DeviceBackend, engine: MissionEngine,
                 *, video_gate: Callable[[], str],
                 clock: Callable[[], float] = time.monotonic,
                 watch_period_s: float = WATCH_PERIOD_S) -> None:
```
```python
        #: 「现在能看得见吗」。返回拦住的理由,空串表示可以走(§5.9)。
        #:
        #: **必填,没有默认值。** 给个默认就意味着有一天会有人建一个没有闸的
        #: ``Teleop`` 而不自知,而那一天没有任何测试会红 —— 这是安全约束,
        #: 不是体验约束。
        #:
        #: 做成回调而不是让遥控认识 ``CameraFeed``:跟 ``add_busy_check`` 同
        #: 一个理由,这一层不该长出对相机实现的依赖。
        self._video_gate = video_gate
```

`pulse()` 里，**`_STOP` 早返之后**、`engine.running` 之前插：

```python
        # **闸放在停车早返的后面。** 三轴全零走的是 stop() —— 停车永远不许被
        # 闸挡住:一个「因为看不见所以不许停」的实现,会在视频掉线的那一刻把狗
        # 锁在最后一个动作上,而掉线恰恰是最需要它停下来的时候。
        blind = self._video_gate()
        if blind:
            # §5.9 明写**不设「确认后继续」的绕过口子**:要挪狗,就必须能看得见。
            # 网差到没有画面,那就走过去挪。
            raise TeleopBusy(f"看不见就不许动:{blind}")
```

`engine.running` 那一条改成：

```python
        if self._engine.running and not self._engine.yielding:
            raise TeleopBusy("任务在跑,先停任务再遥控 —— 两边一起动是在抢腿")
```

并把 `TeleopBusy` 的 docstring 补一句：「引擎让开腿(``SUSPENDED``)时**不算**
任务在跑 —— 那正是人要亲自开的时候(§5.10)。租约那一半不在这儿，
``app/control.py`` 的 ``CONTROLLED`` 已经把整条路由钉在租约后面了。」

`_watchdog()` 的循环里，心跳判断之前加：

```python
            blind = self._video_gate()
            if blind:
                # 掉线时没有人在调 pulse:人的手还压在摇杆上,页面还在发心跳。
                # 真正危险的是「画面刚黑掉、人还没反应过来」那半秒,而这条协程
                # 是唯一一直在看的东西。
                self._active = False
                with contextlib.suppress(Exception):
                    await self._device.walk(0.0, 0.0, 0.0, 0.0)
                return
```

`server.py`：把 `video = {...}` 那一块**整段挪到 `teleop = ...` 之前**
（今天它在后面，:3158），然后：

```python
async def _make_teleop(device: DeviceBackend, engine: MissionEngine,
                       video: Mapping[str, CameraFeed]) -> Teleop:
    return Teleop(device, engine, video_gate=_video_gate(video))


def _video_gate(video: Mapping[str, CameraFeed]) -> Callable[[], str]:
    """§5.9 那道闸。**任何一路在线就算看得见。**

    要求两路都在线,后相机掉一路就把整条狗锁死 —— 而人开着走的时候看的是
    前面。要求「前相机在线」又太死:装机时可以只接一路,或者两路的名字换了。
    所以判据是「至少有一路有画面」,拦的是**一张画面都没有**那种情况,那才是
    §5.9 说的盲开。
    """
    def gate() -> str:
        live = [name for name, feed in video.items() if feed.online]
        if live:
            return ""
        return ("一路画面都没有 —— 打开视频再开狗;"
                "网差到没有画面,那就走过去挪")
    return gate
```

三个测试构造点各补 `video_gate=lambda: ""`。

- [ ] **Step 4: 跑绿 + 全量**

> **⚠️ 全量必须前台跑：`timeout: 600000`，`run_in_background: false`。**

```
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest -q --basetemp=D:/pytest-tmp/v7t6b
```
退出码 0 才算过。

- [ ] **Step 5: 拆两次**

先复制一份 `teleop.py`。
1. 把 `pulse()` 里那段 `blind` 判断挪到 `_STOP` 早返**之前**。
   `test_停车永远不许被闸挡住` **必须红**。
2. 恢复后，把 `_watchdog()` 里那段 `blind` 判断删掉。
   `test_开着走的时候视频掉了_看门狗停狗` **必须红**。

报告里写下每次红的是哪几条。用 `cp` 恢复。

- [ ] **Step 6: 提交**

```bash
ruff check src/d1max_patrol/app/teleop.py src/d1max_patrol/app/server.py tests/app
git add -A
git commit -m "feat(app): 视频不在线禁止移动(§5.9),引擎让位时放行(§5.10)"
```

---

### Task 7: 现场档与远程档 —— 切过去要确认，确认自动留痕

**Files:**
- Modify: `src/d1max_patrol/app/teleop.py`（加模式）
- Modify: `src/d1max_patrol/engine/lease.py:47`（`AUDIT_KINDS` 加一个）、
  `LeaseBook` 加 `note()`
- Modify: `src/d1max_patrol/app/control.py`（`CONTROLLED` 加一条、`ControlDesk` 加转发）
- Modify: `src/d1max_patrol/app/server.py`（路由 + 处理器）
- Test: `tests/app/test_teleop.py`、`tests/engine/test_lease.py`、服务器测试文件

**Interfaces:**
- Consumes: Task 6 的 `Teleop`
- Produces: `teleop.MODES = ("onsite", "remote")`；
  `Teleop.mode(holder_ref: str) -> str`、`Teleop.set_mode(name, holder_ref) -> None`；
  `LeaseBook.note(*, at_ms, kind, who, detail)`；`AUDIT_KINDS` 多一个
  `"mode_switched"`；`POST /api/teleop/mode`。
  Task 8 把 `mode_switched` 那条留痕写进夹具，Task 11 的遥控屏切档。

§7.8 的原话：

> **每次都弹同一个框，第三次就被条件反射点掉了**，那种提示在事后什么也兜不住。
> …… **留痕才是真兜底。** 一个被点掉的弹窗证明不了任何事；一条带署名和时间、
> 跟着 §4 归档走、删不掉的事件记录可以。

所以这个任务的产出**不是一个弹窗**，是一条审计记录。弹窗是 Task 11 的事，
而且弹窗就算被点掉，留痕也已经落下了。

**「档」跟 Task 2 的「节奏」是两件事，别混。** 节奏（`roam`/`scan`）管一拍多长，
是手感；档（`onsite`/`remote`）管人在不在现场，是留痕。两者正交：远程档也能用
扫图节奏。放在同一个文件里，但不共用任何状态。

**换人就掉回现场档。** 档是「这个人现在在哪儿」，不是狗的属性。甲切了远程然后
掉线，乙接过控制权时必须从现场档起步——否则乙会在一个自己从没确认过的档上开狗，
而事后的留痕上写着甲的名字。

**§5.9 不受本节影响。** 远程档一样要有画面（Task 6 的
`test_挂起了也一样要有画面` 是同一条道理，这里再钉一条）。

- [ ] **Step 1: 写失败的测试**

`tests/engine/test_lease.py` 追加：

```python
def test_留痕的种类是白名单(book):
    """``AUDIT_KINDS`` 不许只是个摆设。

    写一条没登记过的种类进去,值守屏和手机上的图标映射就会漏一个 —— 而漏的
    表现是界面上一条空白记录,不是报错。**在写入口拦住,比在读的地方兜底好**:
    读的地方有好几处,写只有这一处。
    """
    with pytest.raises(ValueError):
        book.note(at_ms=1, kind="随便编的", who=Holder("ab12"), detail="")


def test_模式切换是一种留痕():
    assert "mode_switched" in AUDIT_KINDS


def test_note写进同一个环(book):
    book.note(at_ms=1, kind="mode_switched", who=Holder("ab12", "张三"),
              detail="切到远程遥控")
    最后 = book.audit[-1]
    assert 最后.kind == "mode_switched"
    assert 最后.operator == "张三"
    assert 最后.ref == "ab12"
```

`book` / `Holder` 用这个文件里既有的夹具和 import。

`tests/app/test_teleop.py` 追加：

```python
def test_默认是现场档(fake_device, engine, fake_clock):
    t = 带闸(fake_device, engine, fake_clock, [""])
    assert t.mode("ab12") == "onsite"


def test_切到远程(fake_device, engine, fake_clock):
    t = 带闸(fake_device, engine, fake_clock, [""])
    t.set_mode("remote", "ab12")
    assert t.mode("ab12") == "remote"


def test_换个人就掉回现场档(fake_device, engine, fake_clock):
    """**档是「这个人现在在哪儿」,不是狗的属性。**

    甲切了远程然后掉线,乙接过控制权时必须从现场档起步 —— 否则乙会在一个
    自己从没确认过的档上开狗,而事后的留痕上写着甲的名字。
    """
    t = 带闸(fake_device, engine, fake_clock, [""])
    t.set_mode("remote", "ab12")
    assert t.mode("cd34") == "onsite"


def test_不认识的档直接拒(fake_device, engine, fake_clock):
    t = 带闸(fake_device, engine, fake_clock, [""])
    with pytest.raises(ValueError):
        t.set_mode("半远程", "ab12")


def test_远程档不放宽视频那道闸(fake_device, engine, fake_clock):
    """§7.8 明写:**§5.9 不受本节影响,仍是无豁免的硬规则。**"""
    t = 带闸(fake_device, engine, fake_clock, ["前相机没画面"])
    t.set_mode("remote", "ab12")
    with pytest.raises(TeleopBusy):
        await t.pulse(0.5, 0.0, 0.0)
```

（最后一条是 `async def`——这个文件是 `asyncio_mode = "auto"`，**不加
`@pytest.mark.asyncio`**。）

服务器测试文件追加：

```python
def test_切远程要确认(client_with_lease):
    """**没确认就切,是 409 不是 400。**

    400 的意思是「你发的东西不对」,而这里发的东西完全正确 —— 缺的是一次
    确认。分错了,手机上只能显示一句「请求格式错误」,人不知道该点什么。
    """
    resp = client_with_lease.post("/api/teleop/mode", {"mode": "remote"})
    assert resp.status == 409
    assert "确认" in resp.json()["detail"]


def test_确认之后切得过去(client_with_lease):
    resp = client_with_lease.post("/api/teleop/mode",
                                  {"mode": "remote", "confirmed": True})
    assert resp.status == 200
    assert resp.json()["mode"] == "remote"


def test_这次确认自动写进留痕(client_with_lease):
    """§7.8:**留痕才是真兜底。** 一个被点掉的弹窗证明不了任何事。

    盯的是「切档这个动作本身产生了记录」,不是「界面弹过框」—— 后者在事后
    什么都证明不了,而这一条是删不掉的。
    """
    client_with_lease.post("/api/teleop/mode",
                           {"mode": "remote", "confirmed": True})
    留痕 = client_with_lease.get("/api/control/audit").json()["audit"]
    最后 = 留痕[-1]
    assert 最后["kind"] == "mode_switched"
    assert "远程" in 最后["detail"]
    assert 最后["ref"]              # 谁切的,有指纹


def test_切回现场不用确认(client_with_lease):
    """往安全的方向走不该设卡。多设一道,人就会懒得切回来。"""
    client_with_lease.post("/api/teleop/mode",
                           {"mode": "remote", "confirmed": True})
    resp = client_with_lease.post("/api/teleop/mode", {"mode": "onsite"})
    assert resp.status == 200


def test_没控制权不许切档(client_no_lease):
    """切档要留名,而没控制权的人没有名字可留。"""
    resp = client_no_lease.post("/api/teleop/mode",
                                {"mode": "remote", "confirmed": True})
    assert resp.status in (401, 403, 409)


def test_切档这条路要租约():
    from d1max_patrol.app.control import needs_lease
    assert needs_lease("POST", "/api/teleop/mode")
```

`client_with_lease` / `client_no_lease` **照抄服务器测试文件里既有的名字**
（第 6 卷做租约时一定造过）；名字不一样就用那边的，别新造。

- [ ] **Step 2: 跑，确认全红**

Run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/engine/test_lease.py tests/app/test_teleop.py -q --basetemp=D:/pytest-tmp/v7t7a`

- [ ] **Step 3: 实现**

`lease.py`：`AUDIT_KINDS` 里加一行（**连同注释**，这个集合的每一项都该说清楚
它是什么事）：

```python
AUDIT_KINDS: frozenset[str] = frozenset({
    "acquired", "released", "expired", "dropped",
    "takeover_asked", "taken_over", "forced",
    # 切到远程遥控(§7.8)。**这条是本节唯一的产出** —— 规格说得很直白:
    # 「每次都弹同一个框,第三次就被条件反射点掉了」,弹窗事后什么也兜不住,
    # 一条带署名和时间、删不掉的记录可以。
    "mode_switched",
})
```

`LeaseBook` 加一个公开的写入口（放在 `audit_since` 附近）：

```python
    def note(self, *, at_ms: int, kind: str, who: Holder,
             detail: str = "") -> None:
        """往审计环里补一条**不是租约本身**的事(§7.8 的切档就是一例)。

        种类必须是 ``AUDIT_KINDS`` 里登记过的:写一条没登记的进去,值守屏和
        手机上的图标映射就会漏一个,而漏的表现是界面上一条空白记录,不是报错。
        **在写入口拦住比在读的地方兜底好** —— 读的地方有好几处,写只有这一处。
        """
        if kind not in AUDIT_KINDS:
            raise ValueError(
                f"没登记过的留痕种类 {kind!r};"
                f"要加就先加进 AUDIT_KINDS,顺带说清楚它是什么事")
        with self._lock:
            self._log(at_ms, kind, who, detail)
```

（锁的用法照 `audit` / `audit_since` 那两个属性抄——`_log` 本身不加锁，
它现在的调用方都在锁里。**确认一下**：`grep -n "_log(" src/d1max_patrol/engine/lease.py`，
如果现有调用方不在锁里，`note` 里也别加，保持一致。）

`teleop.py`：

```python
#: 遥控的两档(§7.8)。**跟 Task 2 的节奏(``PROFILES``)是两件事,别混。**
#: 节奏管一拍多长,是手感;档管人在不在现场,是留痕。两者正交 —— 远程档
#: 也能用扫图节奏。
MODES: tuple[str, ...] = ("onsite", "remote")

#: 切到远程时要人确认的那句话。**放在狗这头,不放在手机那头**:
#: 措辞改一次要跟着到每一个客户端上,而狗只有一个。
REMOTE_CONFIRM = ("远程遥控:你看不见狗周围的实际情况,只能看见画面里的那一块。"
                  "确认切过去 —— 这次确认会记在案。")
```

`Teleop` 里：

```python
        #: (档名, 是谁切的)。**档是「这个人现在在哪儿」,不是狗的属性** ——
        #: 甲切了远程然后掉线,乙接过控制权时必须从现场档起步,否则乙会在一个
        #: 自己从没确认过的档上开狗,而事后的留痕上写着甲的名字。
        self._mode: tuple[str, str] = ("onsite", "")
```
```python
    def mode(self, holder_ref: str) -> str:
        """这个人现在在哪一档。**不是他切的那一档就算现场档。**"""
        name, who = self._mode
        return name if who == holder_ref else "onsite"

    def set_mode(self, name: str, holder_ref: str) -> None:
        if name not in MODES:
            raise ValueError(f"只有 {'、'.join(MODES)} 两档,给的是 {name!r}")
        self._mode = (name, holder_ref)
```

`control.py` 的 `CONTROLLED` 加一条（**切档要留名，而没控制权的人没有名字可留**）：

```python
    ("POST", re.compile(r"^/api/teleop/mode$")),
```

`server.py` 路由表（`/api/teleop/heartbeat` 那一行之后）：

```python
        self.route("POST", "/api/teleop/mode", self._teleop_mode)
```

处理器：

```python
    def _teleop_mode(self, req: Request) -> Response:
        """切现场档 / 远程档(§7.8)。**同步的** —— 它不碰腿。

        规格不硬禁远程遥控,要的是留痕:「每次都弹同一个框,第三次就被条件反射
        点掉了」。所以这里真正的产出是**审计环里那一条**,不是返回值。
        """
        body = req.json()
        if not isinstance(body, dict):
            raise HttpError(400, "请求体得是一个对象", type(body).__name__)
        name = body.get("mode")
        if name not in MODES:
            raise HttpError(400, "不认识的档",
                            f"只有 {'、'.join(MODES)},给的是 {name!r}")
        who = self._control.book.state(now_ms=self._ctx.clock()).holder
        assert who is not None      # 这条路由在 CONTROLLED 里,进不来就没租约
        if name == "remote" and self._ctx.teleop.mode(who.ref) != "remote":
            # **没确认就切是 409 不是 400。** 发的东西完全正确,缺的是一次
            # 确认 —— 分错了,手机上只能显示一句「请求格式错误」,人不知道
            # 该点什么。
            if body.get("confirmed") is not True:
                raise HttpError(409, "切远程遥控要先确认一次", REMOTE_CONFIRM)
            self._control.book.note(
                at_ms=self._ctx.clock(), kind="mode_switched", who=who,
                detail="切到远程遥控")
        # 切回现场不用确认:往安全的方向走不该设卡,多设一道人就懒得切回来。
        self._ctx.teleop.set_mode(name, who.ref)
        return json_response({"mode": name, "confirm": REMOTE_CONFIRM})
```

**`self._control.book.state(...)` 这个取当前持有者的写法要照实际 API 调整**：
先 `grep -n "def state\|def snapshot\|book\." src/d1max_patrol/app/control.py`
看 `ControlDesk` 现在怎么把持有者暴露出来。**如果 `book` 不是公开属性，就在
`ControlDesk` 上加一个转发方法**（`note(kind, detail, *, now_ms)` 自己取当前
持有者），别在路由里伸手掏私有属性。哪种走法都行，在报告里说明。

- [ ] **Step 4: 跑绿 + 全量**

> **⚠️ 全量必须前台跑：`timeout: 600000`，`run_in_background: false`。**

```
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest -q --basetemp=D:/pytest-tmp/v7t7b
```

- [ ] **Step 5: 拆一次**

先复制 `teleop.py`。把 `mode()` 改成 `return self._mode[0]`（不看是谁）。
`test_换个人就掉回现场档` **必须红**。
再复制 `lease.py`，把 `note()` 里的 `if kind not in AUDIT_KINDS` 那两行删掉，
`test_留痕的种类是白名单` **必须红**。报告里记下红的是哪几条。用 `cp` 恢复。

- [ ] **Step 6: 提交**

```bash
ruff check src/d1max_patrol tests
git add -A
git commit -m "feat(app): 现场/远程两档,切远程的确认自动写进留痕(§7.8)"
```

---

### Task 8: 上线形状的契约夹具（挂账 11）

**Files:**
- Create: `tests/app/test_wire_fixtures.py`
- Create: `mobile/test/fixtures/*.json`（六份，由上面这条测试生成；Task 9 会再加第七份 `proof_vectors.json`）
- Create: `mobile/test/wire_fixtures_test.dart`

**Interfaces:**
- Consumes: Task 3-7 的全部上线形状
- Produces: `mobile/test/fixtures/` 下六份 JSON —— Task 9-13 的 Dart 测试都读它们。

`docs/第2卷待办.md` 第 11 条要的是：**同一份形状，两头都在读**。今天狗这头的
`to_wire()` 改一个字段名，Dart 那头要等到真机上点开那一屏才会发现——那时候
狗在客户现场。

做法：**Python 这头从真的路由处理器里取输出**，写进
`mobile/test/fixtures/`；Dart 那头解析**同一批签进仓库的文件**。狗这头改了形状
而没重生成，Python 的这条测试先红；重生成了而 Dart 没跟上，Dart 那条测试红。
两种漏法都堵住了。

**从路由取，不从 `to_wire()` 取。** `to_wire()` 只是其中一层，路由外面还包着一层
（`{"cameras": ...}`、`{"audit": [...], "max": 200}`）。Dart 解析的是整个响应体，
夹具就得是整个响应体，否则夹在中间那一层永远没人测。

**每次都变的值要按类型换成定值，不是抹掉。** 时间戳、token 指纹每跑一次都不一样，
直接比会天天红。但**换成 `null` 或者字符串占位就等于把类型信息也删了**——Dart 那头
正是靠这份文件知道 `at_ms` 是个整数。所以换的是值，不是类型：
`at_ms → 1757000000000`，`ref → "ab12cd34"`。

- [ ] **Step 1: 写生成兼断言的那条测试**

`tests/app/test_wire_fixtures.py`：

```python
"""上线形状的契约夹具(挂账 11)。

**同一份形状,两头都在读。** 狗这头的 ``to_wire()`` 改一个字段名,Dart 那头
要等到真机上点开那一屏才会发现 —— 那时候狗在客户现场。

形状变了要重生成:

    D1MAX_UPDATE_FIXTURES=1 python -m pytest tests/app/test_wire_fixtures.py \\
        --basetemp=D:/pytest-tmp/fx

**重生成之后一定要跑一遍 Dart 测试**(``cd mobile && flutter test``):
Python 这头重生成就不红了,而 Dart 那头的解析可能刚刚被这次改动打断。
"""

from __future__ import annotations

import json
import os
from pathlib import Path
from typing import Any

import pytest

夹具目录 = Path(__file__).resolve().parents[2] / "mobile" / "test" / "fixtures"

#: 每跑一次都不一样的键 -> 换上去的定值。
#:
#: **换的是值,不是类型。** 换成 null 或者一个字符串占位就等于把类型信息也
#: 删了,而 Dart 那头正是靠这份文件知道 ``at_ms`` 是个整数。
易变的键: dict[str, Any] = {
    "at_ms": 1_757_000_000_000,
    "started_ms": 1_757_000_000_000,
    "expires_ms": 1_757_000_030_000,
    "grace_ends_ms": 1_757_000_015_000,
    "expires_in_ms": 30_000,
    "grace_in_ms": 15_000,
    "since_frame_s": 0.12,
    "elapsed_s": 12.5,
    "ref": "ab12cd34",
    "seq": 1,
    "free_mb": 12_345,
    "used_mb": 45_678,
    "total_mb": 58_023,
}


def 定住(x: Any) -> Any:
    if isinstance(x, dict):
        return {k: (易变的键[k] if k in 易变的键 else 定住(v))
                for k, v in x.items()}
    if isinstance(x, list):
        return [定住(v) for v in x]
    return x


def 对(name: str, payload: Any) -> None:
    """把 ``payload`` 跟签进仓库的那份比。形状变了就报清楚怎么重生成。"""
    夹具目录.mkdir(parents=True, exist_ok=True)
    路 = 夹具目录 / f"{name}.json"
    正 = json.dumps(定住(payload), ensure_ascii=False,
                    indent=2, sort_keys=True) + "\n"
    if os.environ.get("D1MAX_UPDATE_FIXTURES") == "1":
        路.write_text(正, encoding="utf-8")
        return
    assert 路.exists(), (
        f"{路} 还没生成 —— D1MAX_UPDATE_FIXTURES=1 跑一遍这个文件")
    旧 = 路.read_text(encoding="utf-8")
    assert 旧 == 正, (
        f"{name} 的上线形状变了。\n"
        f"改对了就重生成:D1MAX_UPDATE_FIXTURES=1 python -m pytest "
        f"tests/app/test_wire_fixtures.py\n"
        f"**重生成之后一定要跑一遍 cd mobile && flutter test** —— "
        f"Python 这头重生成就不红了,Dart 那头的解析可能刚被打断。")
```

然后六条，一屏一条。**每一条都走真的路由**，`client` 用服务器测试文件里既有的
那个夹具（`grep -rn "def client" tests/app/conftest.py`）：

```python
def test_夹具_租约(client_with_lease):
    """**走 ``GET /api/control``,不是 ``/api/state`` 里的 ``control`` 段。**

    那两个不是同一个形状:``/api/state`` 那一份是所有人共用的广播快照,
    按定义没有 ``mine``,也没有 ``remote_sessions``/``max_sessions``/``notice``
    (理由见 ``_control_wire`` 自己的 docstring)。而手机的控制权面板
    (Task 12)读的正是那四个键 —— 拿广播那一份当夹具,面板的测试就会
    对着一份缺了一半的报文写,缺的那一半永远没人钉。
    """
    对("lease_state", client_with_lease.get("/api/control").json())


def test_夹具_运行快照(client_挂起的):
    """**取的是挂起中的那一份** —— ``suspended_at`` 非空的形状只有这时候有,
    而那正是 Dart 那头最容易解析错的一块(Task 5)。
    """
    对("run_snapshot", client_挂起的.get("/api/state/once").json()["run"])


def test_夹具_视频健康(client):
    对("video_health", client.get("/api/video/health").json())


def test_夹具_控制权留痕(client_切过远程的):
    """**取的是切过远程之后的那一份**:``mode_switched`` 那条留痕的形状
    (Task 7)要在 Dart 那头解析得出来,否则值守的人翻记录时看到一条空白。
    """
    对("control_audit", client_切过远程的.get("/api/control/audit").json())


def test_夹具_盘况(client):
    对("storage", client.get("/api/storage").json())


def test_夹具_身份(client):
    对("identity", client.get("/api/identity").json())
```

`/api/state/once` 和那三个特制 client 的名字**照抄既有测试文件里的写法**；
`"control"` / `"run"` 这两个键名也一样——**先 `grep` 确认快照里这两段叫什么，
别按这里写的名字硬来**。取到的键名跟这里不一样时以仓库为准，并在报告里说明。

- [ ] **Step 2: 生成，再跑一遍确认稳定**

```
D1MAX_UPDATE_FIXTURES=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_wire_fixtures.py -q --basetemp=D:/pytest-tmp/v7t8a
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_wire_fixtures.py -q --basetemp=D:/pytest-tmp/v7t8b
```

第二遍**必须绿**。红了说明还有每跑一次就变的值没定住——**把它加进 `易变的键`，
别去改断言**。加了之后在报告里列出来：加了哪个键、为什么它每次都变。

`cat mobile/test/fixtures/*.json` 看一眼：**里面不许有真 token、真 PIN、
真热点密码**。`ref` 已经是 token 的指纹不是 token 本身（`lease.py:78` 写着），
但生成出来的东西要交付到手机仓库里，眼睛过一遍是零成本的。

- [ ] **Step 3: Dart 这头解析同一批文件**

`mobile/test/wire_fixtures_test.dart`：

```dart
/// 上线形状的契约夹具(挂账 11)。
///
/// **这些 JSON 不是手写的**,是 `tests/app/test_wire_fixtures.py` 从真的
/// 路由输出里生成的。狗那头改了形状,这里就该红 —— 而不是等到真机上点开
/// 那一屏才发现,那时候狗在客户现场。
///
/// 所以这个文件里**不许出现手写的 JSON 字面量**:那等于在这头又抄了一份
/// 形状,抄的那份跟狗那头不一样的那天,两边都是绿的。
import 'dart:convert';
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';

Map<String, dynamic> 读(String name) => jsonDecode(
        File('test/fixtures/$name.json').readAsStringSync())
    as Map<String, dynamic>;

void main() {
  test('租约:倒计时是相对量', () {
    final m = 读('lease_state');
    expect(m['expires_in_ms'], isA<int>());
    expect(m['ttl_ms'], isA<int>());
  });

  test('运行快照:挂起点位解析得出来', () {
    final m = 读('run_snapshot');
    expect(m['state'], 'SUSPENDED');
    final s = m['suspended_at'] as Map<String, dynamic>;
    expect(s['waypoint_name'], isA<String>());
    expect(s['at_ms'], isA<int>());
  });

  test('视频健康:每一路都有 online', () {
    final cams = 读('video_health')['cameras'] as Map<String, dynamic>;
    expect(cams.keys, containsAll(<String>['front', 'back']));
    for (final v in cams.values) {
      expect((v as Map<String, dynamic>)['online'], isA<bool>());
    }
  });

  test('留痕:切远程那条认得出来', () {
    final audit = 读('control_audit')['audit'] as List<dynamic>;
    expect(audit.map((e) => (e as Map<String, dynamic>)['kind']),
        contains('mode_switched'));
  });

  test('盘况和身份都解析得出来', () {
    expect(读('storage'), isNotEmpty);
    expect(读('identity'), isNotEmpty);
  });
}
```

**六份夹具每一份都要有人读。** 一份没人读的夹具是纯负担：它会因为形状变化而红，
却证明不了任何解析是对的。

`mobile/pubspec.yaml` 里确认 `flutter_test` 已经在 `dev_dependencies`（它一定在），
夹具目录**不需要**登记进 `assets`——这是测试直接读文件，不走 asset bundle。

- [ ] **Step 4: 两头都跑绿**

```
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_wire_fixtures.py -q --basetemp=D:/pytest-tmp/v7t8c
```
```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter analyze && flutter test
```
`flutter analyze` 要出 `No issues found!`；Dart 用例数**只许涨**（基线 56）。

- [ ] **Step 5: 拆一次，确认这道闸真的会响**

把 `src/d1max_patrol/app/video.py` 里 `health()` 返回的 `"online"` 改名成
`"is_online"`（先 `cp` 备份）。
`test_夹具_视频健康` **必须红**，而且报错里要写着怎么重生成。
然后 `D1MAX_UPDATE_FIXTURES=1` 重生成，`flutter test` 里
`视频健康:每一路都有 online` **必须红**——**这一步是这个任务的全部意义**：
它证明「狗那头改形状会打到手机那头」。两边都验完，用 `cp` 恢复 `video.py`
并重生成夹具，确认两头都回绿。报告里写下两次红的分别是哪条。

- [ ] **Step 6: 提交**

```bash
ruff check tests/app/test_wire_fixtures.py
git add -A
git commit -m "test: 上线形状的契约夹具,Python 生成 Dart 解析(挂账 11)"
```

---

### Task 9: Flutter 的 HTTP 客户端 —— PIN 从不上网

**Files:**
- Create: `mobile/lib/net/patrol_client.dart`、`mobile/lib/net/wire.dart`
- Create: `mobile/test/patrol_client_test.dart`、`mobile/test/proof_test.dart`
- Create: `mobile/test/support/fake_dog.dart`（假狗，Task 11-13 共用）
- Modify: `mobile/pubspec.yaml`（加 `crypto`）
- Modify: `tests/app/test_wire_fixtures.py`（多生成一份证明向量）

**Interfaces:**
- Consumes: Task 8 的夹具机制
- Produces: `PatrolClient(baseUrl, {HttpClient? io})` 带
  `unlock(pin, operator)`、`get(path)`、`post(path, body)`、`token`、`close()`；
  `proofFor(pin, nonce) -> String`；
  `wire.dart` 里 `LeaseView`、`VideoHealth`、`RunView` 三个只读视图。
  Task 10-13 的每一屏都走这个客户端。

狗那头的鉴权是**质询-应答**（`app/auth.py:593` 的 `proof_for`）：
`GET /api/auth/challenge` 拿一个一次性 nonce，
`POST /api/auth` 交 `HMAC-SHA256(PIN, nonce)` 的十六进制。
`auth.py` 开头写着这条路的意义：**PIN 从不上网**。热点上报文人人可解，
明文 PIN 那条老路在狗的热点上只换得到只读凭证。

**所以手机这头只许走质询-应答那条路。** 图省事发 `{"pin": ...}` 也能换到 token，
但换到的是只读的，遥控会全部 403——而且报错会指向权限，不指向「你把 PIN 明文发
出去了」。有一条测试专门盯着请求体里没有 `pin` 这个键。

**跨语言的 HMAC 必须有向量证。** 两头各自实现同一个算法、各自绿、合起来对不上，
是这一类代码最典型的翻车法。Task 8 的夹具机制现成，多生成一份 `(pin, nonce, proof)`
三元组即可。

- [ ] **Step 1: Python 这头多生成一份证明向量**

`tests/app/test_wire_fixtures.py` 追加：

```python
def test_夹具_证明向量():
    """质询-应答的跨语言向量(``auth.proof_for``)。

    **两头各自实现同一个算法、各自绿、合起来对不上**,是这一类代码最典型的
    翻车法。它翻车的表现是「手机上输对 PIN 也进不去」,而两边的测试都是绿的。

    PIN 是编的,不是任何一台真机上的:这份文件要签进手机仓库。
    """
    from d1max_patrol.app.auth import PROOF_ALG, proof_for
    样本 = [("000000", "00" * 16), ("123456", "a1b2c3d4" * 4),
            ("864209", "ff" * 16)]
    对("proof_vectors", {
        "alg": PROOF_ALG,
        "vectors": [{"pin": p, "nonce": n, "proof": proof_for(p, n)}
                    for p, n in 样本],
    })
```

**这三个 PIN 是编的**，别用任何一台真机上的——这份文件要签进手机仓库。

生成：
```
D1MAX_UPDATE_FIXTURES=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest tests/app/test_wire_fixtures.py -q --basetemp=D:/pytest-tmp/v7t9a
```

- [ ] **Step 2: 加依赖**

```bash
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter pub add crypto
```
（网是通的，已经验过。真拉不下来时**停下来报告**，不要自己手写一份 SHA-256——
一个手写的哈希是这一卷里最不该有的东西。）

- [ ] **Step 3: 写失败的测试**

`mobile/test/proof_test.dart`：

```dart
/// 质询-应答的跨语言向量。
///
/// **两头各自实现同一个算法、各自绿、合起来对不上**,是这一类代码最典型的
/// 翻车法。它翻车的表现是「手机上输对 PIN 也进不去」,而两边的测试都是绿的。
///
/// 向量由 `tests/app/test_wire_fixtures.py` 从 `auth.proof_for` 生成。
import 'dart:convert';
import 'dart:io';

import 'package:d1max_patrol/net/patrol_client.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('证明跟狗那头算出来的一样', () {
    final fx = jsonDecode(
        File('test/fixtures/proof_vectors.json').readAsStringSync())
        as Map<String, dynamic>;
    expect(fx['alg'], 'HMAC-SHA256');
    for (final v in fx['vectors'] as List<dynamic>) {
      final m = v as Map<String, dynamic>;
      expect(proofFor(m['pin'] as String, m['nonce'] as String), m['proof'],
          reason: 'PIN ${m['pin']} / nonce ${m['nonce']}');
    }
  });
}
```

`mobile/test/support/fake_dog.dart` —— **假狗放在这里，不放在测试文件里**：Task 11、12、13 的测试都要 `import '../support/fake_dog.dart';` 用同一只。复制三份的话，三份会各自跟狗分家。

`mobile/test/patrol_client_test.dart`：**起一个真的本地 `HttpServer`**，不是 mock。
`dart:io` 的 `HttpServer.bind(InternetAddress.loopbackIPv4, 0)` 在 `flutter test`
里跑得起来，而 mock 掉 `HttpClient` 就等于把「请求真的长什么样」这件事也 mock 掉了
——而这个任务要证的恰恰是那个。

```dart
/// 狗的 HTTP 客户端。
///
/// **测试起的是真的 `HttpServer`,不是 mock。** 这个任务要证的就是「请求真的
/// 长什么样」—— PIN 在不在请求体里、token 在不在请求头里 —— 把 HttpClient
/// mock 掉,恰好把要证的那件事一起 mock 掉了。
import 'dart:convert';
import 'dart:io';

import 'package:d1max_patrol/net/patrol_client.dart';
import 'package:flutter_test/flutter_test.dart';

/// 一台假狗。记下每个请求,好让测试事后翻。
class 假狗 {
  late HttpServer _s;
  final List<({String method, String path, String? auth, dynamic body})> 收到 =
      <({String method, String path, String? auth, dynamic body})>[];
  final Map<String, dynamic> 回复 = <String, dynamic>{};
  int 状态 = 200;

  String get baseUrl => 'http://127.0.0.1:${_s.port}';

  Future<void> 起() async {
    _s = await HttpServer.bind(InternetAddress.loopbackIPv4, 0);
    _s.listen((HttpRequest req) async {
      final raw = await utf8.decoder.bind(req).join();
      收到.add((
        method: req.method,
        path: req.uri.path,
        auth: req.headers.value('authorization'),
        body: raw.isEmpty ? null : jsonDecode(raw),
      ));
      req.response.statusCode = 状态;
      req.response.headers.contentType = ContentType.json;
      req.response.write(jsonEncode(回复[req.uri.path] ?? <String, dynamic>{}));
      await req.response.close();
    });
  }

  Future<void> 收() => _s.close(force: true);
}

void main() {
  late 假狗 狗;
  late PatrolClient c;

  setUp(() async {
    狗 = 假狗();
    await 狗.起();
    狗.回复['/api/auth/challenge'] = <String, dynamic>{
      'nonce': 'a1b2c3d4a1b2c3d4a1b2c3d4a1b2c3d4',
      'ttl_s': 60,
      'alg': 'HMAC-SHA256',
    };
    狗.回复['/api/auth'] = <String, dynamic>{
      'token': 'tok-123',
      'operator': '张三',
      'operator_verified': false,
      'readonly': false,
    };
    c = PatrolClient(狗.baseUrl);
  });

  tearDown(() async {
    c.close();
    await 狗.收();
  });

  test('PIN 从不上网', () async {
    await c.unlock('864209', operator: '张三');
    final 换 = 狗.收到.firstWhere((r) => r.path == '/api/auth');
    final body = 换.body as Map<String, dynamic>;
    expect(body.containsKey('pin'), isFalse,
        reason: '明文 PIN 在狗的热点上人人可解;而且那条老路只换得到只读凭证');
    expect(body['proof'], isA<String>());
    expect(body['nonce'], 'a1b2c3d4a1b2c3d4a1b2c3d4a1b2c3d4');
  });

  test('拿到 token 之后每个请求都带着它', () async {
    await c.unlock('864209', operator: '张三');
    await c.get('/api/state/once');
    final 后 = 狗.收到.last;
    expect(后.auth, 'Bearer tok-123');
  });

  test('换 token 之前不带请求头', () async {
    await c.unlock('864209', operator: '张三');
    final 质询 = 狗.收到.first;
    expect(质询.path, '/api/auth/challenge');
    expect(质询.auth, isNull);
  });

  test('operator_verified 是 false 要能读出来', () async {
    final s = await c.unlock('864209', operator: '张三');
    expect(s.operatorVerified, isFalse,
        reason: '§6.3:狗记下名字但不核实。界面上不许说成「已登录:张三」');
  });

  test('狗回错就抛出说得清楚的异常', () async {
    狗.状态 = 401;
    狗.回复['/api/auth'] = <String, dynamic>{
      'error': 'PIN 不对',
      'detail': '还可以试 3 次',
    };
    expect(() => c.unlock('000000', operator: ''),
        throwsA(isA<PatrolError>()
            .having((e) => e.status, 'status', 401)
            .having((e) => e.detail, 'detail', contains('3 次'))));
  });

  test('连不上也抛 PatrolError,不是裸的 SocketException', () async {
    final 死的 = PatrolClient('http://127.0.0.1:1');
    expect(() => 死的.get('/api/state/once'), throwsA(isA<PatrolError>()));
    死的.close();
  });
}
```

最后一条不是凑数：**界面上要显示「连不上狗」而不是一段英文栈**。让每一处调用点
各自去认 `SocketException` / `HandshakeException` / `TimeoutException`，早晚漏一处，
漏的那一处在手机上就是一个红屏。

- [ ] **Step 4: 跑，确认全红**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter test test/proof_test.dart test/patrol_client_test.dart
```

- [ ] **Step 5: 实现**

`mobile/lib/net/patrol_client.dart`：

```dart
/// 跟狗说话。**PIN 从不上网。**
///
/// 狗那头是质询-应答（`src/d1max_patrol/app/auth.py`）：先取一个一次性
/// nonce，再交 `HMAC-SHA256(PIN, nonce)` 的十六进制。`auth.py` 开头写得很
/// 直白：热点上报文人人可解。
///
/// **不许走 `{"pin": ...}` 那条老路。** 它也换得到 token，但在狗的热点上换到
/// 的是**只读**凭证 —— 遥控会全部 403，而报错指向权限，不指向「你把 PIN 明文
/// 发出去了」。
///
/// 用 `dart:io` 的 `HttpClient` 而不是 `package:http`：这个 app 只做 Android，
/// 少一个依赖少一次升级。
library;

import 'dart:async';
import 'dart:convert';
import 'dart:io';

import 'package:crypto/crypto.dart';

/// 这一轮质询的证明：`HMAC-SHA256(PIN, nonce)` 的十六进制。
///
/// 跟 `auth.proof_for` 逐字对应。**跨语言的向量在
/// `test/fixtures/proof_vectors.json` 里**，那份文件是狗那头生成的。
String proofFor(String pin, String nonce) =>
    Hmac(sha256, utf8.encode(pin)).convert(utf8.encode(nonce)).toString();

/// 跟狗说话时出的任何岔子。
///
/// **连不上、超时、狗回了 4xx —— 全归一到这一个类型。** 让每一处调用点各自
/// 去认 `SocketException` / `TimeoutException`，早晚漏一处，而漏的那一处在
/// 手机上是一个红屏。
class PatrolError implements Exception {
  /// HTTP 状态码。压根没连上是 0。
  final int status;
  final String error;
  final String detail;

  const PatrolError(this.status, this.error, [this.detail = '']);

  @override
  String toString() => detail.isEmpty ? error : '$error：$detail';
}

/// 换到的那次会话。
class Session {
  final String token;
  final String operator;

  /// **狗记下名字但不核实**（§6.3）。界面上不许说成「已登录：张三」。
  final bool operatorVerified;
  final bool readonly;

  const Session({
    required this.token,
    required this.operator,
    required this.operatorVerified,
    required this.readonly,
  });
}

class PatrolClient {
  final String baseUrl;
  final HttpClient _io;
  String? _token;

  PatrolClient(this.baseUrl, {HttpClient? io}) : _io = io ?? HttpClient() {
    // 狗在热点上，慢一点是常态，但不该等到人以为死机。
    _io.connectionTimeout = const Duration(seconds: 5);
  }

  String? get token => _token;

  void close() => _io.close(force: true);

  /// PIN 换 token。**PIN 不出这个函数。**
  Future<Session> unlock(String pin, {required String operator}) async {
    final ch = await _send('GET', '/api/auth/challenge', null, auth: false);
    final nonce = ch['nonce'] as String;
    final got = await _send('POST', '/api/auth', <String, dynamic>{
      'nonce': nonce,
      'proof': proofFor(pin, nonce),
      'operator': operator,
    }, auth: false);
    _token = got['token'] as String;
    return Session(
      token: _token!,
      operator: (got['operator'] as String?) ?? '',
      operatorVerified: got['operator_verified'] == true,
      readonly: got['readonly'] == true,
    );
  }

  Future<Map<String, dynamic>> get(String path) => _send('GET', path, null);

  Future<Map<String, dynamic>> post(String path, [Object? body]) =>
      _send('POST', path, body);

  Future<Map<String, dynamic>> _send(String method, String path, Object? body,
      {bool auth = true}) async {
    try {
      final req = await _io.openUrl(method, Uri.parse('$baseUrl$path'));
      // token 走请求头，不走 cookie 也不走查询串。狗那头的理由写在
      // `auth.py` 开头：cookie 是浏览器自动带的，等于替人开狗。
      if (auth && _token != null) {
        req.headers.set(HttpHeaders.authorizationHeader, 'Bearer $_token');
      }
      if (body != null) {
        req.headers.contentType = ContentType.json;
        req.write(jsonEncode(body));
      }
      final resp = await req.close().timeout(const Duration(seconds: 10));
      final raw = await utf8.decoder.bind(resp).join();
      final parsed = raw.isEmpty ? <String, dynamic>{} : jsonDecode(raw);
      final m = parsed is Map<String, dynamic> ? parsed : <String, dynamic>{};
      if (resp.statusCode >= 400) {
        throw PatrolError(resp.statusCode,
            (m['error'] as String?) ?? '狗回了 ${resp.statusCode}',
            (m['detail'] as String?) ?? '');
      }
      return m;
    } on PatrolError {
      rethrow;
    } on TimeoutException {
      throw const PatrolError(0, '狗没回话', '等了 10 秒。看看还连着它的热点吗');
    } catch (e) {
      throw PatrolError(0, '连不上狗', '$e');
    }
  }
}
```

`mobile/lib/net/wire.dart`：**只读视图，一个字段一句话**。不要写成能改的模型——
这几样东西的真理源在狗身上，手机改了也不算数。

```dart
/// 狗发过来的那几样东西，在手机这头的样子。
///
/// **全是只读的。** 真理源在狗身上，手机改了也不算数；能改的模型只会诱使人
/// 在界面上先改再发，而那两步之间的窗口里，屏幕上显示的是一件没发生的事。
///
/// 形状由 `test/fixtures/*.json` 钉着，那些文件是狗那头生成的（挂账 11）。
library;

class LeaseView {
  /// 还剩多久到期（毫秒）。
  ///
  /// **一律读这个相对量，不许拿绝对时刻去减手机自己的钟**：狗上没有 NTP，
  /// 两头的钟能差出几分钟（`engine/lease.py` 的 `LeaseState` 写着）。
  final int expiresInMs;
  final int? graceInMs;
  final String? holderRef;
  final String holderOperator;

  const LeaseView({
    required this.expiresInMs,
    this.graceInMs,
    this.holderRef,
    this.holderOperator = '',
  });

  factory LeaseView.fromJson(Map<String, dynamic> m) {
    final h = m['holder'] as Map<String, dynamic>?;
    return LeaseView(
      expiresInMs: (m['expires_in_ms'] as num?)?.toInt() ?? 0,
      graceInMs: (m['grace_in_ms'] as num?)?.toInt(),
      holderRef: h?['ref'] as String?,
      holderOperator: (h?['operator'] as String?) ?? '',
    );
  }

  bool get held => holderRef != null;
}

class VideoHealth {
  /// 相机名 -> 这一刻有没有画面。
  final Map<String, bool> online;

  const VideoHealth(this.online);

  factory VideoHealth.fromJson(Map<String, dynamic> m) {
    final cams = (m['cameras'] as Map<String, dynamic>?) ?? <String, dynamic>{};
    return VideoHealth(<String, bool>{
      for (final e in cams.entries)
        e.key: (e.value as Map<String, dynamic>)['online'] == true,
    });
  }

  /// §5.9 那道闸在手机这头的样子：**一路都没有就不许开狗**。
  ///
  /// 判据跟狗那头的 `_video_gate` 一致 —— 两头不一致的话，摇杆是亮的而每一拍
  /// 都被拒，人只会以为 app 坏了。
  bool get anyLive => online.values.any((v) => v);
}

class RunView {
  final String state;
  final String waypointName;
  final int waypointIndex;
  final int total;

  /// 引擎让开腿时停在哪儿（§5.10）。没挂起就是 null。
  final String? suspendedAtWaypoint;

  const RunView({
    required this.state,
    required this.waypointName,
    required this.waypointIndex,
    required this.total,
    this.suspendedAtWaypoint,
  });

  factory RunView.fromJson(Map<String, dynamic> m) {
    final s = m['suspended_at'] as Map<String, dynamic>?;
    return RunView(
      state: (m['state'] as String?) ?? 'IDLE',
      waypointName: (m['waypoint_name'] as String?) ?? '',
      waypointIndex: (m['waypoint_index'] as num?)?.toInt() ?? 0,
      total: (m['total'] as num?)?.toInt() ?? 0,
      suspendedAtWaypoint: s?['waypoint_name'] as String?,
    );
  }

  /// 引擎让着腿吗。**遥控在这个状态下是放行的**（§5.10）。
  bool get suspended => state == 'SUSPENDED';
}
```

`mobile/test/wire_fixtures_test.dart`（Task 8 建的那个）再加三条，**用夹具喂
这三个视图**——夹具的意义就在这儿：

```dart
  test('三个视图都吃得下狗发的那份', () {
    expect(LeaseView.fromJson(读('lease_state')).expiresInMs, isPositive);
    expect(VideoHealth.fromJson(读('video_health')).online, isNotEmpty);
    expect(RunView.fromJson(读('run_snapshot')).suspended, isTrue);
  });
```

- [ ] **Step 6: 跑绿**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter analyze && flutter test
```
`flutter analyze` 要出 `No issues found!`。用例数只许涨。

- [ ] **Step 7: 拆一次**

把 `unlock` 里的 `'proof': proofFor(pin, nonce)` 换成 `'pin': pin`（先备份）。
`PIN 从不上网` **必须红**。用 `cp` 恢复。

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "feat(mobile): 狗的 HTTP 客户端,PIN 走质询-应答从不上网"
```

---

### Task 10: Flutter 视频屏与在线判定

**Files:**
- Create: `mobile/lib/ui/widget/live_video.dart`
- Create: `mobile/test/live_video_test.dart`

**Interfaces:**
- Consumes: Task 9 的 `PatrolClient`、`VideoHealth`
- Produces: `LiveVideo({required String baseUrl, required String camera,
  required Stream<VideoHealth> health, String token = ''})`；
  `VideoHealthPoller({required PatrolClient client, Duration period})` 带
  `Stream<VideoHealth> stream`、`stop()`。
  Task 11 的遥控屏拿 `VideoHealth.anyLive` 决定摇杆灰不灰。

**`LiveVideo` 收 `baseUrl` + `token`，不收 `PatrolClient`。** 它要的是一条
URL 字符串（`Image.network` 自己去发请求），不是一个能 POST 的客户端；
把整个客户端递进一个只画画面的 widget，等于给它开了改狗的权限。
`token` 默认空串是为了让 widget 测试不必造一个会话。

**`VideoHealthPoller` 那个 `client` 声明成 `PatrolClient`。** Dart 的每个类
都自带一个隐式接口，测试里的假客户端 `implements PatrolClient` 即可，
不需要为它单独抽一个抽象类。

MJPEG 在 Flutter 里是白送的：`Image.network` 吃 `multipart/x-mixed-replace`。
**这是定夺二里「先不换 H.264」的一半理由**——换了就得自己找一个 H.264 播放器，
而那个决定从来没人做过。

**在线判定走 `/api/video/health` 轮询，不看图片有没有加载出来。**
`Image.network` 的加载状态回调只在**第一帧**上响；MJPEG 是一条不结束的响应，
第一帧到了之后它就再也不说话了——画面冻住的时候它跟一切正常长得一模一样。
而「冻住的画面」正是 §5.9 要防的头号情况。

- [ ] **Step 1: 写失败的测试**

`mobile/test/live_video_test.dart`：

```dart
/// 视频屏。
///
/// **在线判定走 `/api/video/health` 轮询，不看图片加载状态。**
/// `Image.network` 的加载回调只在第一帧上响；MJPEG 是一条不结束的响应，
/// 第一帧到了之后它再也不说话了 —— 画面冻住的时候它跟一切正常长得一模一样，
/// 而冻住的画面正是 §5.9 要防的头号情况。
```

```dart
  testWidgets('在线时画出画面', (t) async {
    // 用一个假的 health 流喂进去，不起真的 HTTP。
    final ctl = StreamController<VideoHealth>();
    await t.pumpWidget(包一层(LiveVideo(
        baseUrl: 'http://x', camera: 'front', health: ctl.stream)));
    ctl.add(const VideoHealth(<String, bool>{'front': true}));
    await t.pump();
    expect(find.byType(Image), findsOneWidget);
  });

  testWidgets('不在线时说人话，不是转圈', (t) async {
    final ctl = StreamController<VideoHealth>();
    await t.pumpWidget(包一层(LiveVideo(
        baseUrl: 'http://x', camera: 'front', health: ctl.stream)));
    ctl.add(const VideoHealth(<String, bool>{'front': false}));
    await t.pump();
    expect(find.textContaining('没有画面'), findsOneWidget);
    expect(find.byType(CircularProgressIndicator), findsNothing,
        reason: '一直转圈的圈跟「连不上」长得一样，人分不出该等还是该去看看');
  });

  testWidgets('掉线再回来要重新拉流', (t) async {
    /// **这一条是这个 widget 唯一的技术难点。**
    ///
    /// MJPEG 那条连接断了之后，`Image.network` 不会自己重连 —— 它认为自己
    /// 已经成功了。所以恢复的时候必须换一个 key，逼 Flutter 重建这个元素。
    final ctl = StreamController<VideoHealth>();
    await t.pumpWidget(包一层(LiveVideo(
        baseUrl: 'http://x', camera: 'front', health: ctl.stream)));
    ctl.add(const VideoHealth(<String, bool>{'front': true}));
    await t.pump();
    final k1 = (t.widget<Image>(find.byType(Image))).key;
    ctl.add(const VideoHealth(<String, bool>{'front': false}));
    await t.pump();
    ctl.add(const VideoHealth(<String, bool>{'front': true}));
    await t.pump();
    final k2 = (t.widget<Image>(find.byType(Image))).key;
    expect(k2, isNot(k1));
  });

  test('轮询器按周期问，停了就不再问', () async {
    final 假 = 数着次数的client();
    final p = VideoHealthPoller(client: 假, period: const Duration(milliseconds: 10));
    await p.stream.first;
    p.stop();
    final 停时 = 假.次数;
    await Future<void>.delayed(const Duration(milliseconds: 50));
    expect(假.次数, 停时,
        reason: '人切走了还在问，就是在白耗电和白占热点带宽');
  });
```

`包一层(...)` 是把 widget 塞进 `MaterialApp`/`Directionality` 的小助手；
`数着次数的client()` 是一个只记调用次数的假客户端。**先看
`mobile/test/widget_test.dart` 里既有的写法**，有现成的就用现成的。

`Image.network` 在 `flutter test` 里默认会去真的发请求并失败——这是既知的坑。
**用 `TestWidgetsFlutterBinding` 的 HTTP 覆盖**（`HttpOverrides.runZoned` 配一个
返回一张 1×1 透明 PNG 的假 `HttpClient`），或者给 `LiveVideo` 开一个
`imageBuilder` 注入口只在测试里用。**两种都行，在报告里说明走的哪一种**——
但**不许**把断言弱化成「不抛异常就算过」。

- [ ] **Step 2: 跑，确认全红**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter test test/live_video_test.dart
```

- [ ] **Step 3: 实现**

`mobile/lib/ui/widget/live_video.dart` 的要点，逐条：

1. `StatefulWidget`，`StreamSubscription<VideoHealth>` 订阅传进来的流。
2. 在线时画 `Image.network('$baseUrl/api/video/$camera?token=$token',
   key: ValueKey('$camera-$_代'), fit: BoxFit.cover, gaplessPlayback: true)`。
   **`token` 走查询串是狗那头明确开的口子**（`auth.py` 开头那段：
   `<img src=...>` 没有设请求头的地方，所以只认 GET 且只认列出的只读路径）。
   在注释里写清楚这一点，否则下一个人会以为这是个疏漏，顺手"修"成请求头，
   然后画面全黑。
3. `_代` 是个整数，**每次从「不在线」翻回「在线」就 +1**。这是这个 widget 唯一
   的技术难点：MJPEG 那条连接断了之后 `Image.network` 不会自己重连——它认为
   自己已经成功了。换 key 才能逼 Flutter 重建。
4. 不在线时画一块深色底 + 一句话：`'$camera 没有画面'` 加一行小字
   `'开不了狗。看看还连着热点吗，或者走过去挪'`。**不许画转圈**：一直转圈的圈
   跟「连不上」长得一样，人分不出该等还是该去看看。
5. `dispose()` 里取消订阅。

`VideoHealthPoller`：

```dart
/// 定期问「现在有画面吗」。
///
/// **走 `/api/video/health` 轮询，不看图片加载状态**（理由见文件头）。
///
/// 2 秒一次：狗那头 `STALE_S` 也是 2 秒，问得比它还密没有更多信息，
/// 问得比它稀就会在「画面已经冻了」和「摇杆变灰」之间留一段空窗。
class VideoHealthPoller {
  static const Duration defaultPeriod = Duration(seconds: 2);
  ...
  void stop();               // 取消 Timer 并 close 掉 controller
}
```

轮询出错（`PatrolError`）时**发一个全 false 的 `VideoHealth`**，不是把流关掉：
问不到狗等于看不见，而看不见就该变灰——把流关掉的话摇杆会停在最后一个状态上，
那个状态很可能是「亮着」。

- [ ] **Step 4: 跑绿**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter analyze && flutter test
```

- [ ] **Step 5: 拆一次**

把 `_代` 的自增删掉（key 恒定）。`掉线再回来要重新拉流` **必须红**。
再把轮询出错那一支改成 `_ctl.close()`，`轮询器...` 那条或新加一条要红——
**如果没有测试盯着这个错法，补一条**，然后在报告里说明补的是哪条。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(mobile): 视频屏,在线判定走 health 轮询而不是图片加载状态"
```

---

### Task 11: Flutter 双摇杆遥控屏

**Files:**
- Create: `mobile/lib/ui/widget/joystick.dart`、`mobile/lib/ui/teleop_page.dart`
- Create: `mobile/test/joystick_test.dart`、`mobile/test/teleop_page_test.dart`

**Interfaces:**
- Consumes: Task 9 的 `PatrolClient`、Task 10 的 `LiveVideo` / `VideoHealthPoller`
- Produces: `Joystick(onChanged:, axis:, enabled:)`；`TeleopPage(client:, robot:)`。
  Task 12 的控制权面板嵌在这一屏上。

§7.7 定下的布局（`docs/分卷与进度.md:32` 与 §7.10 的 `mobile/` 那一行）：

> 遥控屏：视频铺满 + 两个虚拟摇杆（左单轴平移 / 右转向）、持续发拍与松手主动停、
> 视频掉线即变灰

四条，一条一条来，每条都有它自己的理由：

**虚拟圆心。** 手指按下的那一点就是圆心，不是屏幕上画死的一个圈。戴着手套、
在阳光下、眼睛盯着狗不盯着屏幕——这三种情况下人按不准一个画死的圈。

**左杆只出平移，而且吸附到单轴。** 真机的遥控器推 45° 也只走一个轴（§7.9 待测项
「摇杆推 45° 是不是只走单轴」）。吸附在 Task 1 已经落在**后端**了——手机这头
再吸一次不是重复，是让**屏幕上画的那根杆**跟狗实际要走的方向一致；不吸的话，
人斜推着，杆画在斜的位置，狗直着走。

**松手必须主动发一次全零停。** 守死人（0.6 秒）是**兜底**，不是停车方式。
靠它停意味着松手之后狗还会往前走大半秒——那半秒足够撞上东西。

**视频掉线摇杆直接变灰失效，不是弹一个可关掉的框。** §5.9 是无豁免的硬规则。
弹框能被点掉，灰掉的杆点不动。

- [ ] **Step 1: 写失败的测试**

`mobile/test/joystick_test.dart`：

```dart
  testWidgets('按下的那一点就是圆心', (t) async {
    /// **不是屏幕上画死的一个圈。** 戴着手套、在阳光下、眼睛盯着狗不盯着
    /// 屏幕 —— 这三种情况下人按不准一个画死的圈。
    Offset? 出;
    await t.pumpWidget(包一层(Joystick(
        axis: JoystickAxis.translate, onChanged: (v) => 出 = v)));
    final 角落 = t.getTopLeft(find.byType(Joystick)) + const Offset(20, 20);
    final g = await t.startGesture(角落);
    await t.pump();
    expect(出, Offset.zero, reason: '刚按下还没动，输出必须是零');
    await g.moveBy(const Offset(0, -40));
    await t.pump();
    expect(出!.dy, lessThan(0));
    await g.up();
  });

  testWidgets('平移杆斜推只出一个轴', (t) async {
    Offset? 出;
    await t.pumpWidget(包一层(Joystick(
        axis: JoystickAxis.translate, onChanged: (v) => 出 = v)));
    final g = await t.startGesture(t.getCenter(find.byType(Joystick)));
    await g.moveBy(const Offset(30, -50));   // 斜的，前多一点
    await t.pump();
    expect(出!.dx, 0.0, reason: '§7.7：真机推 45° 也只走一个轴');
    expect(出!.dy, isNot(0.0));
    await g.up();
  });

  testWidgets('转向杆只出横轴', (t) async {
    Offset? 出;
    await t.pumpWidget(包一层(Joystick(
        axis: JoystickAxis.turn, onChanged: (v) => 出 = v)));
    final g = await t.startGesture(t.getCenter(find.byType(Joystick)));
    await g.moveBy(const Offset(40, -40));
    await t.pump();
    expect(出!.dy, 0.0, reason: '右杆只转向。推上去要转弯是学不会的');
    await g.up();
  });

  testWidgets('松手回零', (t) async {
    final 记 = <Offset>[];
    await t.pumpWidget(包一层(Joystick(
        axis: JoystickAxis.translate, onChanged: 记.add)));
    final g = await t.startGesture(t.getCenter(find.byType(Joystick)));
    await g.moveBy(const Offset(0, -50));
    await t.pump();
    await g.up();
    await t.pump();
    expect(记.last, Offset.zero);
  });

  testWidgets('变灰之后推不动', (t) async {
    final 记 = <Offset>[];
    await t.pumpWidget(包一层(Joystick(
        axis: JoystickAxis.translate, enabled: false, onChanged: 记.add)));
    final g = await t.startGesture(t.getCenter(find.byType(Joystick)));
    await g.moveBy(const Offset(0, -50));
    await t.pump();
    expect(记, isEmpty, reason: '§5.9 是无豁免的硬规则，不是一个能点掉的框');
    await g.up();
  });

  testWidgets('推到底也不超过 1', (t) async {
    Offset? 出;
    await t.pumpWidget(包一层(Joystick(
        axis: JoystickAxis.translate, onChanged: (v) => 出 = v)));
    final g = await t.startGesture(t.getCenter(find.byType(Joystick)));
    await g.moveBy(const Offset(0, -5000));
    await t.pump();
    expect(出!.dy.abs(), lessThanOrEqualTo(1.0),
        reason: '狗那头 [-1,1] 之外直接 400，越界的一拍等于白发');
    await g.up();
  });
```

`mobile/test/teleop_page_test.dart`。**先写一个 `挂上(t)` 助手**，下面每条测试
都从它开始，所以代码块里的 `...` 一律指这三行：

```dart
/// 起一只假狗、造一条 health 流、把遥控屏挂上去。每条测试都从这儿开始。
Future<(假狗, StreamController<VideoHealth>)> 挂上(WidgetTester t,
    {bool online = true}) async {
  final 狗 = 假狗();                       // test/support/fake_dog.dart
  await 狗.起();
  final 健康 = StreamController<VideoHealth>.broadcast();
  final c = PatrolClient(狗.baseUrl);
  await c.unlock('864209', operator: '张三');
  await t.pumpWidget(MaterialApp(home: TeleopPage(client: c, robot: 一台狗())));
  健康.add(VideoHealth(<String, bool>{'front': online}));
  await t.pump();
  return (狗, 健康);
}
```

（`假狗记账()` 就是上面这个 `假狗`；`一台狗()` 照 `mobile/test/robot_test.dart`
里既有的造法来，别自己发明字段。）

`mobile/test/teleop_page_test.dart`：

```dart
  testWidgets('松手主动发一次全零停', (t) async {
    /// **守死人（0.6 秒）是兜底，不是停车方式。**
    /// 靠它停意味着松手之后狗还会往前走大半秒 —— 那半秒足够撞上东西。
    final 狗 = 假狗记账();
    ...
    await g.up();
    await t.pump();
    final 最后 = 狗.发过的.where((r) => r.path == '/api/teleop').last;
    expect(最后.body, containsPair('fwd', 0.0));
    expect(最后.body, containsPair('lat', 0.0));
    expect(最后.body, containsPair('yaw', 0.0));
  });

  testWidgets('视频掉线摇杆变灰', (t) async {
    ...
    健康.add(const VideoHealth(<String, bool>{'front': false, 'back': false}));
    await t.pump();
    expect(t.widget<Joystick>(find.byType(Joystick).first).enabled, isFalse);
  });

  testWidgets('视频掉线不弹可关掉的框', (t) async {
    /// §5.9 明写**不设「确认后继续」的绕过口子**。弹框能被点掉，灰掉的杆
    /// 点不动。这一条盯的是「屏幕上没有一个能让人继续开狗的按钮」。
    ...
    expect(find.byType(AlertDialog), findsNothing);
    expect(find.textContaining('继续'), findsNothing);
  });

  testWidgets('推着的时候在持续发拍', (t) async {
    /// 一拍最长 2 秒（狗那头 `MAX_PULSE_S`），所以推着不放必须续。
    /// 不续的话狗走完一拍就停，人感觉像卡顿，会本能地把杆推得更狠。
    ...
    await t.pump(const Duration(milliseconds: 700));
    expect(狗.发过的.where((r) => r.path == '/api/teleop').length,
        greaterThan(1));
  });

  testWidgets('两个杆同时推,合到一拍里发出去', (t) async {
    /// §7.7 明写这是允许的：左杆前推 + 右杆右推 = 向右画弧。
    /// **两个杆各发各的拍是错的** —— 后发的那一拍会把先发的那一拍覆盖掉，
    /// 于是人推着两个杆，狗只执行其中一个，而且是哪个手先动就听哪个。
    final 狗 = 假狗记账();
    ...
    final 左 = await t.startGesture(左杆中心);
    await 左.moveBy(const Offset(0, -50));
    final 右 = await t.startGesture(右杆中心);
    await 右.moveBy(const Offset(50, 0));
    await t.pump(const Duration(milliseconds: 400));
    final 拍 = 狗.发过的.where((r) => r.path == '/api/teleop').last;
    expect((拍.body as Map)['fwd'], greaterThan(0.0));
    expect((拍.body as Map)['yaw'], isNot(0.0));
    await 左.up();
    await 右.up();
  });

  testWidgets('心跳一直在发', (t) async {
    ...
    expect(狗.发过的.any((r) => r.path == '/api/teleop/heartbeat'), isTrue);
  });

  testWidgets('切远程要先确认，而且确认过一次就不再问', (t) async {
    /// §7.8：**每次都弹同一个框，第三次就被条件反射点掉了。**
    /// 所以确认只问一次；真正兜底的是狗那头那条留痕（Task 7）。
    ...
  });

  testWidgets('现场档只有一行轻提示', (t) async {
    /// §7.8：现场模式是**轻提示** —— 「请与机器狗保持在同一视线范围内」。
    /// 不是弹框：现场档是默认档，每次进来都弹框的话，它就是那个第三次被
    /// 条件反射点掉的框。
    expect(find.textContaining('视线'), findsOneWidget);
    expect(find.byType(AlertDialog), findsNothing);
  });
```

- [ ] **Step 2: 跑，确认全红**

- [ ] **Step 3: 实现**

`joystick.dart` 的要点：

```dart
/// 虚拟圆心摇杆。**按下的那一点就是圆心。**
///
/// 不画死一个圈：戴着手套、在阳光下、眼睛盯着狗不盯着屏幕 —— 这三种情况下
/// 人按不准一个画死的圈。§7.7。
enum JoystickAxis {
  /// 左杆：平移。**吸附到单轴** —— 真机遥控器推 45° 也只走一个轴。
  ///
  /// 后端（`app/teleop.py` 的 `snap_to_axis`）已经吸了一次。这里再吸不是
  /// 重复：那一次保证**狗**只走一个轴，这一次保证**屏幕上画的那根杆**跟狗
  /// 实际要走的方向一致。不吸的话人斜推着、杆画在斜的位置、狗直着走。
  translate,

  /// 右杆：只转向。推上去要转弯是学不会的。
  turn,
}
```

- 半径常量 `_radius = 72.0`（逻辑像素）。输出 = `(手指 - 圆心) / _radius`，
  **各轴独立夹到 [-1, 1]**，然后按 `axis` 吸附/清轴。
- **y 轴要翻号**：屏幕往上是 `dy` 变小，而「前进」是正的。这一行错了的表现是
  推上去往后退，**在真机上是撞人**。在代码里写一句注释钉住它，并让
  `按下的那一点就是圆心` 那条测试的 `lessThan(0)` / `greaterThan(0)` 方向对得上
  ——**实现时以「推上去 = fwd 为正」为准，测试里的符号跟着这条走**。
- `enabled == false`：`GestureDetector` 直接不装（或 `IgnorePointer`），
  并且画成半透明灰。**不是 `onChanged` 里 return**——那样杆还会跟着手指动，
  屏幕上看着像在开，其实一拍都没发出去。
- 松手：`onPanEnd` 里 `onChanged(Offset.zero)`。

`teleop_page.dart` 的要点：

- `Stack`：底层 `LiveVideo` 铺满（`Positioned.fill`），上面左下、右下两个
  `Joystick`，顶部一条状态带（Task 12 的控制权面板挂这儿）。
- 一个 `Timer.periodic(const Duration(milliseconds: 300))`：
  - 两个杆都在零位 → **不发**（发零拍等于反复叫狗停，白占热点带宽）；
  - 有非零 → `POST /api/teleop {fwd, lat, yaw, profile}`。
    **`seconds` 不传**，让狗那头按 `profile` 取默认（Task 2）。
- 独立的心跳 `Timer.periodic(const Duration(milliseconds: 200))` →
  `POST /api/teleop/heartbeat`。**跟发拍分开**：手停在零位的时候心跳不能停，
  停了狗那头会判掉线——虽然那时候它本来也不在走，但一恢复推杆就要重新起一拍，
  中间那一下人感觉是卡了。
- 松手（杆回零）**立即**发一次全零，不等下一个周期。
- `VideoHealthPoller` 的流喂给 `LiveVideo`，同时 `anyLive` 决定两个杆的 `enabled`。
- 顶部现场档时挂一行轻提示：`请与机器狗保持在同一视线范围内`。
  切远程走一个 `showDialog` 确认，确认后 `POST /api/teleop/mode
  {"mode":"remote","confirmed":true}`，**只问一次**。
- 发拍收到 `PatrolError` 时：**不弹框**，在顶部状态带上显示 `e.toString()`，
  并且**连续三次失败就把杆变灰**。狗不回话跟看不见画面是一回事。

- [ ] **Step 4: 跑绿**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter analyze && flutter test
```

- [ ] **Step 5: 拆三次**

先备份两个文件。
1. 把松手那次立即的全零发送删掉（只留周期发拍）。
   `松手主动发一次全零停` **必须红**。
2. 把 `enabled == false` 的实现从「不装手势」改成「`onChanged` 里 return」。
   `变灰之后推不动` 应该还是绿的——**如果是绿的，说明那条测试不够**，
   补一条盯「杆的视觉位置没跟着手指走」的，然后再拆一次确认它红。
   （这一条是故意留的陷阱：报告里必须写清楚补了什么。）
3. 把 y 轴的翻号去掉。`按下的那一点就是圆心` **必须红**。

用 `cp` 恢复。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(mobile): 双摇杆遥控屏,松手主动停,视频掉线即变灰(§7.7/§5.9)"
```

---

### Task 12: 控制权面板与倒计时

**Files:**
- Create: `mobile/lib/ui/control_panel.dart`
- Create: `mobile/test/control_panel_test.dart`
- Modify: `mobile/lib/ui/teleop_page.dart`（把面板挂到顶部状态带）

**Interfaces:**
- Consumes: Task 9 的 `PatrolClient`、`LeaseView`；Task 11 的 `TeleopPage`
- Produces: `ControlPanel(client:, onHeld:)`，一个挂在遥控屏顶部的窄条。
  `onHeld(bool)` 让遥控屏知道自己手上有没有控制权。

狗那头（`app/control.py` 的 `CONTROLLED`）**所有会动腿的接口都要控制权**。
手机上不做这一屏的话，人打开遥控屏推杆，得到的是一串 409，屏幕上什么解释也没有。

四条接口：`GET /api/control`（谁在拿）、`POST /api/control/acquire`（拿）、
`POST /api/control/heartbeat`（续）、`POST /api/control/takeover`（抢）、
`POST /api/control/takeover/approve`（放）。报文里有
`holder / mine / expires_in_ms / grace_in_ms / remote_sessions / max_sessions / notice`。

**倒计时一律读 `expires_in_ms`，不许拿 `expires_ms` 去减手机自己的钟。**
狗上没有 NTP（`engine/lease.py` 写着），两头的钟差几分钟是常态。差出来的表现
是倒计时显示负数、或者显示还剩二十分钟而下一秒就掉——两种都会让人以为 app 坏了。
`expires_ms` 那个绝对时刻在报文里，但它是给日志用的，不是给这块屏用的。

**倒计时在本地跑，不是每秒问一次狗。** 收到一次 `expires_in_ms` 之后，用一个
本地 `Ticker`/`Timer` 每秒减 1000 往下走；每 3 秒问一次狗校准。每秒问一次狗
就是每秒一个 HTTP 往返，在热点上跟视频抢带宽——而视频掉一拍的代价比倒计时差
一秒大得多。

- [ ] **Step 1: 写失败的测试**

`mobile/test/control_panel_test.dart`：

```dart
/// 控制权面板。
///
/// **倒计时读 `expires_in_ms`，不减手机的钟。** 狗上没有 NTP，两头的钟差
/// 几分钟是常态；减出来的结果要么是负数，要么是"还剩二十分钟"下一秒就掉，
/// 两种都会让人以为 app 坏了。
```

```dart
  testWidgets('没人拿的时候给一个「取得控制权」', (t) async {
    狗.回复['/api/control'] = 空的控制权();
    await 挂上(t);
    expect(find.text('取得控制权'), findsOneWidget);
  });

  testWidgets('自己拿着就走倒计时,而且是本地在走', (t) async {
    狗.回复['/api/control'] = 我拿着(expiresInMs: 30000);
    await 挂上(t);
    await t.pump();
    expect(find.textContaining('30'), findsOneWidget);
    final 问过几次 = 狗.收到.length;
    await t.pump(const Duration(seconds: 1));
    expect(find.textContaining('29'), findsOneWidget);
    expect(狗.收到.length, 问过几次,
        reason: '每秒问一次狗就是每秒一个往返,在热点上跟视频抢带宽');
  });

  testWidgets('别人拿着就给「请求接手」,并且写出是谁', (t) async {
    狗.回复['/api/control'] = 别人拿着(operator: '李四');
    await 挂上(t);
    expect(find.text('请求接手'), findsOneWidget);
    expect(find.textContaining('李四'), findsOneWidget);
  });

  testWidgets('名字后面必须带上「未核实」', (t) async {
    /// §6.3 / `auth.OPERATOR_NOTICE`：**狗记下名字但不核实。**
    /// 光写「李四」等于替一个没验过的名字背书;真出了事,现场只会指着这块
    /// 屏说"上面写着是李四"。
    狗.回复['/api/control'] = 别人拿着(operator: '李四');
    await 挂上(t);
    expect(find.textContaining('未核实'), findsOneWidget);
  });

  testWidgets('抢的时候把宽限期倒计时显示出来', (t) async {
    狗.回复['/api/control'] = 我在抢(graceInMs: 15000);
    await 挂上(t);
    await t.pump();
    expect(find.textContaining('15'), findsOneWidget);
    expect(find.textContaining('等对方'), findsOneWidget);
  });

  testWidgets('倒计时归零显示「已过期」,不显示负数', (t) async {
    狗.回复['/api/control'] = 我拿着(expiresInMs: 1500);
    await 挂上(t);
    await t.pump(const Duration(seconds: 3));
    expect(find.textContaining('-'), findsNothing);
    expect(find.textContaining('已过期'), findsOneWidget);
  });

  testWidgets('拿着的时候在续期', (t) async {
    狗.回复['/api/control'] = 我拿着(expiresInMs: 30000);
    await 挂上(t);
    await t.pump(const Duration(seconds: 6));
    expect(狗.收到.where((r) => r.path == '/api/control/heartbeat').length,
        greaterThan(0));
  });

  testWidgets('席位满了要说人话', (t) async {
    /// §3.6：3 个会话封顶。第 4 个人连上来看到的必须是"满了",
    /// 不是一个点了没反应的按钮。
    狗.回复['/api/control'] = 空的控制权(remoteSessions: 3, maxSessions: 3);
    await 挂上(t);
    expect(find.textContaining('席位已满'), findsOneWidget);
  });

  testWidgets('丢了控制权要通知外面', (t) async {
    /// 遥控屏靠这个信号把杆变灰 —— 没有控制权还能推，推出来的每一拍都是
    /// 409，而屏幕上什么解释也没有。
    final 记 = <bool>[];
    狗.回复['/api/control'] = 我拿着(expiresInMs: 30000);
    await 挂上(t, onHeld: 记.add);
    狗.回复['/api/control'] = 别人拿着(operator: '李四');
    await t.pump(const Duration(seconds: 4));   // 等一次校准
    expect(记.last, isFalse);
  });
```

`空的控制权()` / `我拿着()` / `别人拿着()` / `我在抢()` 是本文件里的小助手，
**照着 `mobile/test/fixtures/lease_state.json`（Task 8 生成的）改字段**，不要
手搓一份 JSON 字面量——手搓的那份会跟狗慢慢分家。

- [ ] **Step 2: 跑，确认全红**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter test test/control_panel_test.dart
```

- [ ] **Step 3: 实现**

`mobile/lib/ui/control_panel.dart` 的要点：

```dart
/// 控制权面板。挂在遥控屏顶上的一条窄带。
///
/// 狗那头会动腿的接口全都要控制权（`app/control.py` 的 `CONTROLLED`）。
/// 没有这块屏，人推杆得到的是一串 409，而屏幕上什么解释也没有。
///
/// **倒计时读 `expires_in_ms`，绝不拿 `expires_ms` 去减手机的钟。** 狗上没有
/// NTP（`engine/lease.py`），两头的钟差几分钟是常态。
///
/// **倒计时在本地跑，3 秒跟狗校准一次。** 每秒问一次狗就是每秒一个 HTTP 往返，
/// 在热点上跟视频抢带宽 —— 视频掉一拍的代价比倒计时差一秒大得多。
```

- 两个 `Timer`：`_tick`（1 秒，本地减）和 `_sync`（3 秒，`GET /api/control`
  校准 + 自己拿着时顺手 `POST /api/control/heartbeat`）。
  心跳周期必须**小于** `LEASE_HEARTBEAT_MS = 10_000`；3 秒有富余，掉一两次
  也还追得回来。
- 本地减到 0 就**钳在 0**，显示"已过期"。负数不许上屏。
- 三种态各一个按钮：
  - 没人拿 → `取得控制权` → `POST /api/control/acquire`
  - 自己拿着 → `交还` → `POST /api/control/release`，旁边一行 `还剩 N 秒`
  - 别人拿着 → `请求接手` → `POST /api/control/takeover`，旁边写
    `现在是 李四（未核实）`
  - 抢的宽限期里 → `等对方回应，还剩 N 秒`（按钮禁用）
- 别人在抢**我的**控制权时（`challenger != null && mine`）→ 一个 `同意移交`
  → `POST /api/control/takeover/approve`。这一条不是可选的：不给这个按钮，
  唯一的移交方式就是干等 15 秒。
- 名字后面**永远跟着「未核实」**。这不是措辞讲究：`auth.OPERATOR_NOTICE`
  明说狗只记不核，屏幕上写死一个名字等于替它背书。
- `remoteSessions >= maxSessions && !mine && holder == null` → 按钮禁用 +
  `席位已满（3/3）`。
- 每次 `mine` 变化就调 `onHeld(mine)`。
- `dispose()` 里两个 timer 都取消。

`teleop_page.dart` 改动：顶部 `Positioned(top: 0, left: 0, right: 0,
child: ControlPanel(client: client, onHeld: (h) => setState(() => _held = h)))`，
并且摇杆的 `enabled` 从 `anyLive` 变成 `anyLive && _held`。

**注意两条闸的先后：** 没有控制权和没有画面都变灰，但**提示语要分开**——
"没有控制权，先在上面取得"和"没有画面，开不了狗"是两件完全不同的事，
合成一句"不能操作"的话，人会朝错误的方向去排查。

- [ ] **Step 4: 跑绿**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter analyze && flutter test
```

- [ ] **Step 5: 拆两次**

先备份。
1. 把本地倒计时改成 `DateTime.now()` 减 `expires_ms`。
   `自己拿着就走倒计时,而且是本地在走` **必须红**（假狗给的 `expires_ms`
   是个跟手机钟对不上的数）。**如果那条测试没红**，说明助手里的
   `expires_ms` 造得太老实了——把它造成一个明显偏移的值，再拆一次。
2. 把名字后面的「未核实」删掉。`名字后面必须带上「未核实」` **必须红**。

用 `cp` 恢复。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(mobile): 控制权面板,倒计时读 expires_in_ms 不减手机的钟"
```

---

### Task 13: 盘况屏（挂账 15）

**Files:**
- Create: `mobile/lib/ui/storage_page.dart`
- Create: `mobile/test/storage_page_test.dart`
- Modify: `mobile/lib/ui/roster_page.dart`（进得去这一屏）

**Interfaces:**
- Consumes: Task 9 的 `PatrolClient`
- Produces: `StoragePage(client:, robot:)`；`StorageView.fromJson`（放在
  `net/wire.dart` 里，跟另外三个视图作伴）。

**挂账 15 把这一屏写成了第 7 卷的硬入场条件**：`GET /api/storage` 从第 3 卷起
就在狗上跑着，至今**没有任何一个客户端**。一个没人读的接口，坏了没人知道——
而它偏偏是那个"东西马上要被永久删掉"的唯一告知点（§7.6 纪律 2）。

`/api/storage` 的报文（`server.py:1831` 那个 handler）：

```
used_bytes / total_bytes / used_ratio / runs_total / runs_bytes
baselines_bytes / exports_bytes
forecast: {generated_at, used_ratio, bytes_at_risk, runs: [...], detail}
notice_written: bool
notice_detail: str
backup: {level, detail}
```

**三样东西必须上屏，一样都不能省：**

1. `notice_written == false` 时那句 `notice_detail`。**这一条是这块屏存在的
   理由。** 它为假的意思是：盘满了或者被挂成只读，"再过几天开始删除"那句话
   **说了不算**——钟没开始走。不显示的话，人看到的是一块一切正常的屏，而删除
   永远不会发生，归档就此无声地堆到盘炸。
2. `backup.detail`。备份盘落后 / 满了 / 没配。
3. `forecast.detail` 和 `bytes_at_risk`——"再过几天，这些要被删"。

**不要在这块屏上做「清盘」。** `POST /api/storage/sweep` 那条路的另一头是
`shutil.rmtree`（狗那头自己的 docstring 说它是全仓最危险的一行），它的安全
前提是"人先看名单，再点第二下"。在手机上、戴着手套、站在现场，那第二下点得
太容易了。这一卷只读不删。

- [ ] **Step 1: 写失败的测试**

`mobile/test/storage_page_test.dart`：

```dart
/// 盘况屏。**只读，不清盘。**
///
/// `POST /api/storage/sweep` 那条路的另一头是 `shutil.rmtree`，它的安全前提
/// 是"人先看名单，再点第二下"。在手机上、戴着手套、站在现场，那第二下点得
/// 太容易了。
```

```dart
  testWidgets('盘用了多少要看得见', (t) async {
    狗.回复['/api/storage'] = 一份盘况(usedRatio: 0.72);
    await 挂上(t);
    await t.pumpAndSettle();
    expect(find.textContaining('72'), findsOneWidget);
  });

  testWidgets('预告没落盘要顶到脸上', (t) async {
    /// **这一条是这块屏存在的理由。** `notice_written == false` 的意思是
    /// 盘满了或者被挂成只读 —— "再过几天开始删除"那句话说了不算，钟没开始
    /// 走。不显示的话，人看到的是一块一切正常的屏，而归档会无声地堆到盘炸。
    狗.回复['/api/storage'] = 一份盘况(
        noticeWritten: false, noticeDetail: '预告没能记到盘上(只读文件系统)');
    await 挂上(t);
    await t.pumpAndSettle();
    expect(find.textContaining('预告没能记到盘上'), findsOneWidget);
  });

  testWidgets('备份盘那句话原样上屏', (t) async {
    狗.回复['/api/storage'] = 一份盘况(
        backupLevel: 'warn', backupDetail: '镜像盘落后 12 趟');
    await 挂上(t);
    await t.pumpAndSettle();
    expect(find.textContaining('落后 12 趟'), findsOneWidget);
  });

  testWidgets('要过期的那几趟列得出来', (t) async {
    await 挂上(t);
    await t.pumpAndSettle();
    expect(find.textContaining('2026-08-30 巡检A'), findsOneWidget);
  });

  testWidgets('这块屏上没有删除按钮', (t) async {
    await 挂上(t);
    await t.pumpAndSettle();
    expect(find.textContaining('清盘'), findsNothing);
    expect(find.textContaining('删除'), findsNothing,
        reason: '"再过几天开始删除"那句预告除外 —— 见下一条');
  });
```

上面最后一条跟第三条的文案会打架：预告本身就带"删除"两个字。
**按钮和文案要分得开**：断言写成 `find.widgetWithText(ElevatedButton, '清盘')`
和 `find.byType(ElevatedButton)` 的数量，别去 `findsNothing` 一个词。
实现时**以"这块屏上一个会改变狗上状态的按钮都没有"为准**，测试照这个写：

```dart
  testWidgets('这块屏上一个会改狗的按钮都没有', (t) async {
    await 挂上(t);
    await t.pumpAndSettle();
    for (final b in t.widgetList<ButtonStyleButton>(
        find.byType(ButtonStyleButton))) {
      expect(b.onPressed, isNull,
          reason: '这一卷只读不删。清盘那条路的另一头是 rmtree');
    }
  });
```

（刷新按钮走 `IconButton`，不在 `ButtonStyleButton` 之列；真要加了别的按钮，
这条会红，那时再决定它该不该在。）

```dart
  testWidgets('狗不回话时说人话', (t) async {
    狗.状态 = 500;
    await 挂上(t);
    await t.pumpAndSettle();
    expect(find.textContaining('读不到盘况'), findsOneWidget);
  });
```

- [ ] **Step 2: 跑，确认全红**

- [ ] **Step 3: 实现**

`net/wire.dart` 里加：

```dart
/// 盘况（`GET /api/storage`）。
class StorageView {
  final int usedBytes;
  final int totalBytes;
  final double usedRatio;
  final int runsTotal;
  final int runsBytes;
  final int baselinesBytes;
  final int exportsBytes;

  /// 预告到底记到盘上了没有。
  ///
  /// **为假的时候必须上屏。** 它的意思是盘满了或者被挂成只读，"再过几天
  /// 开始删除"那句话说了不算 —— 删除的钟根本没开始走。
  final bool noticeWritten;
  final String noticeDetail;

  final String backupLevel;
  final String backupDetail;
  final String forecastDetail;
  final int bytesAtRisk;

  /// 有到期日的那几趟：`(mission, startedAt, daysLeft)`。
  final List<({String mission, String startedAt, int retentionDays})> runs;
  ...
}
```

`storage_page.dart` 的要点：

- 一次 `GET /api/storage`，`FutureBuilder`。**不轮询**：盘况是分钟级变化的
  东西，轮询它只是在热点上白占带宽。给一个 `IconButton(Icons.refresh)`。
- 顶上一条容量进度条 + `已用 72%（36.0 GB / 50.0 GB）`。
  字节换 GB 的助手写在这个文件里，别散到各处。
- **`noticeWritten == false` 时，一块红底卡片顶在最上面**，正文是
  `noticeDetail` 原样（狗那头已经把话写全了，手机不要重新组织——重新组织的
  版本迟早跟狗分家）。
- `backupLevel` 映射颜色：`ok` 中性、`warn` 黄、`push` 红。`backupDetail` 原样。
- 中间列 `runs`：`{startedAt} {mission}`，右边 `保留 {retentionDays} 天`。
- 底下一行 `forecastDetail` + `其中 {bytesAtRisk} 即将到期`。
- 出错时整屏一句 `读不到盘况：{e}`，带刷新按钮。
- **不放任何会改狗的按钮。**

`roster_page.dart`：每台狗那一行加一个进盘况屏的入口。**照着这个文件已有的
导航写法来**——先读它，别自己发明一套。

- [ ] **Step 4: 跑绿**

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter analyze && flutter test
```

- [ ] **Step 5: 拆一次**

把 `noticeWritten == false` 那块红卡片删掉。`预告没落盘要顶到脸上`
**必须红**。用 `cp` 恢复。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(mobile): 盘况屏,只读不清盘(挂账 15)"
```

---

### Task 14: 收尾——把说错的话改对，把要上真机的事记下来

**Files:**
- Modify: `mobile/lib/main.dart`（陈旧的文件头）
- Modify: `docs/第2卷待办.md`（挂账 11、挂账 15、**挂账 48**）
- Modify: `docs/手机app.md`
- Modify: `docs/真机待验证清单.md`
- Modify: `docs/分卷与进度.md`

**Interfaces:**
- Consumes: 前 13 个任务的全部产出
- Produces: 无代码接口。这是文档任务。

这一卷动过的几处文档，现在说的是不再成立的话。**陈旧的文档比没有文档更贵**：
没有文档的时候人会去读代码，陈旧的文档会让人按着它去改，改出来的东西跟事实
对不上，而对不上的地方要到很久以后才暴露。

- [ ] **Step 1: `main.dart` 的文件头**

现在那段话说"操作界面这一层还没定，也许手机浏览器直接开狗上的网页就够了"。
§7.7 早就定了：**原生界面，不套 WebView**。理由写进去：

> 视频要铺满、摇杆要跟手、松手要立刻停 —— 这三件事在 WebView 里都得靠一层
> JS 桥，而那层桥的延迟正好加在"松手到停"这条最要命的路径上。

改成一段说清"这个 app 是什么"的话：一台手机管多台狗（`model/registry.dart`）、
PIN 存在系统钥匙串里（`store/secure_pin_vault.dart`）、跟狗说话走
`net/patrol_client.dart`、遥控在 `ui/teleop_page.dart`。

- [ ] **Step 2: 挂账 11 —— 一句说错了的话**

`docs/第2卷待办.md` 第 11 条现在写着 `GET /api/identity` "只存在于注释里"。
**它是真的**：`src/d1max_patrol/app/server.py:1003` 注册了这条路由,
`_identity` 的实现在 `:1303`。把那句话改掉,并把这一条标成**本卷完成**——
Task 8 的契约夹具正是它要的东西。

- [ ] **Step 3: 挂账 15 —— 结掉**

第 15 条把"盘况屏没有客户端"写成了第 7 卷的硬入场条件。Task 13 做完了，
标完成，指向 `mobile/lib/ui/storage_page.dart`。

- [ ] **Step 4: 挂账 48 —— 一条署错了名的决定**

**这一条要改的不是技术，是署名。**

第 48 条（证书固定 / PIN 位数）现在挂着一行
`【已拍板，2026-09-08，人定的】`。**那个署名是错的：没有人拍过这个板。**
它是一次工具调用的返回值被当成了人的答复写进去的。

把那行改成：

```
【本卷未定，2026-09-08 记】证书固定挪到第 8 卷。PIN 维持 6 位。
**这两条都还没有人拍过板** —— 之前那行"人定的"是记错了,已订正。
第 8 卷动这一块的时候要当面问一次。
```

**为什么要专门改一行署名。** 一条挂着"人定的"的决定，后面所有人都不会再问；
它会被当成前提，被引用，被在它上面继续盖东西。等到某一天发现没人定过，
上面已经盖了三层。这一行的代价现在是一次编辑，半年后是三层返工。

同时把证书固定从第 7 卷的范围里去掉——**这一卷全程在狗自己的热点上跑，
HTTP 明文，没有证书可固定**（`auth.py` 开头那段讲的就是这个前提）。
第 8 卷要走公网远程接管，那时候才有 TLS，才谈得上固定谁。

- [ ] **Step 5: `docs/手机app.md`**

把这一卷做出来的东西写进去：
- 鉴权走质询-应答，**PIN 从不上网**，跨语言向量在
  `mobile/test/fixtures/proof_vectors.json`（狗那头生成）
- 遥控屏：视频铺满 + 双虚拟圆心摇杆（左平移吸单轴 / 右只转向）、
  持续发拍、**松手主动发一次全零停**、**视频掉线或没有控制权即变灰**
- 现场档 / 远程档，切远程要确认一次，**只问一次**
- 控制权面板：倒计时读 `expires_in_ms`，本地走，3 秒校准
- 盘况屏：只读
- **两条闸的提示语是分开的**：没有控制权 / 没有画面

再补一段"还没做的"：H.264（第 9 卷）、告警与值守（第 8 卷）、
任务下发与地图（还没排卷）。**写清楚哪些没做，比写清楚做了什么更有用**——
下一个人第一件事就是找这份清单。

- [ ] **Step 6: `docs/真机待验证清单.md`**

追加四条，每条写清楚**怎么量、什么算过**：

1. **`MIN_LAT`（侧移死区，暂定 0.30）。**
   量法：把 `lat` 从 0.05 起每次加 0.05 发一拍，记下狗第一次真的挪了的那个值。
   低于死区的一拍等于人推着杆而狗不动——人会以为杆坏了，然后推到底。
   （对应挂账 67）

2. **摇杆推 45° 是不是只走单轴。**
   量法：拿真机自带遥控器推 45°，看它走了哪个轴。
   这一条定的是 `snap_to_axis` 该不该留：真机也走合成方向的话，
   吸附反而是我们自己加的一个限制。

3. **视频端到端延迟。**
   量法：手机拍一个跑秒表的屏幕，把手机举到狗的相机前，同时拍到秒表和
   app 里的画面，两个读数相减。
   **§7.9 原话：这条不量出来，遥控不能算定了。** 超过 300ms 的话，
   MJPEG 这条路要重新论证（第 9 卷的 H.264 那一条会因此提前）。

4. **松手到停的时间。**
   量法：全速前进中松手，看狗还走了多远。
   算过的标准：**明显短于**靠守死人（0.6 秒）停下来的距离。
   量不出差别的话，说明 Task 11 那次主动全零停没有真的发出去——
   而那正是它存在的全部理由。

5. **视频掉线时摇杆确实变灰。**
   量法：遥控中把相机的网线拔了 / 把 RTSP 源停掉，看杆多久变灰。
   `STALE_S = 2.0` 加一次轮询（2 秒），最坏 4 秒。**超过 5 秒就要改小。**

- [ ] **Step 7: `docs/分卷与进度.md`**

把第 7 卷标完成，列出这一卷交付的东西（后端 7 处 + 手机 5 屏），
并把**开工前四条定夺的产品后果**写进去，一条一句：

- **定夺一**：§5.10 拆在第 7、8 两卷。第 7 卷做的是引擎让开腿（`SUSPENDED`）
  和遥控的放行；**自动恢复巡检**那一半在第 8 卷。
- **定夺二**：MJPEG 留到第 9 卷再换 H.264。
  **但第二档（远程值守）不因此推迟**——第 8 卷照原计划走，
  它用的是同一条 MJPEG。真机延迟量出来超标的话，这一条要重新拍。
- **定夺三**：证书固定挪到第 8 卷（这一卷没有 TLS 可固定）。
  挂账 48 那行错误署名已订正。
- **定夺四**：改写过的既有断言清单（那张 旧期望 → 新期望 → 为什么 的表）
  连同这一卷一起进 `docs/分卷与进度.md`,**不许只留在计划文件里**——
  计划文件读完就没人再打开了。

再更新两个基线数（**用真实跑出来的数，不要照抄这里**）：
Python 用例数（开工前 2773）、Dart 用例数（开工前 56）。

- [ ] **Step 8: 全量跑一次**

```
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m pytest -q --basetemp=D:/pytest-tmp/v7final
```
**前台跑，`timeout: 600000`，不许 `run_in_background`。** 整套约 5.5 分钟。
**以退出码为准**（这台机器有时不打那行汇总）。

```
export PATH="/d/toolchain/flutter/bin:$PATH"
cd mobile && flutter analyze && flutter test
```
`flutter analyze` 要出 `No issues found!`。

```
python -m ruff check src tests
```

两边用例数都只许涨。**降了就是删了测试**，回去找回来。

- [ ] **Step 9: 提交**

```bash
git add -A
git commit -m "docs: 第 7 卷收尾,订正挂账 11/48 的陈述,补真机待验证 5 条"
```

---
