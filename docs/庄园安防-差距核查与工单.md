# 庄园安防：差距核查与工单

**基准**：`master` @ `59a7620`，2026-09-22。对照《私人庄园智能机器狗巡检与安防解决方案 V1.0》。
完整报告（需求覆盖、平台事实、三方对照、口径订正）：<https://claude.ai/artifact/TqwdFrtn2q4ZiUAbpha9Ds>
这份文件只留**工单与决策**，是"一个一个做"的那张单子。做完一条回来勾一条。

**一句话结论**：现有代码是单机巡检产品（有人在场、人工换电、狗热点上用）；方案卖的是无人值守的
户外入侵响应。R1–R10 今天可直接交付 0 条，部分具备 6 条，基本为零 4 条（R1、R4、R7、R9）。
缺的三样地基：看地图绕障且够快的导航、没人盯着也会出发和回充的自主性、真正存在的中央服务器。

**真机验证状态**：2026-09-22 起，W01 的装机/迁移路径已在 Orin 上验过（狗未移动）；其余"已有"仍指代码存在且仿真通过。

---

## 阶段 0 · 止血（不需要真机）

| # | 工单 | 要点 | 规模 | 依赖 | 状态 |
|---|---|---|---|---|---|
| W01 | 持久数据目录 | `--runs-root` 默认相对路径 `runs`、落在版本槽里，`release.prune()` 直接 `rmtree` 老槽、不看 `.uploaded`；升两次级丢一批证据，且切槽那刻老槽的待传队列就停传。改到绝对持久路径（如 `/var/lib/d1max`），`install.sh` 建目录，老机器一次性迁移，卸载保留名单与 `tests/test_deploy_coexist.py` 同步。**已做（2026-09-22）**：`D1MAX_DATA_ROOT` + `engine/datadir` + `release migrate-data`（原子拷贝、只增不覆盖、可重跑）+ `prune` 对五样数据（runs/missions/队列/基线/导出）的保险 + 装机 stop→migrate→start + 卸载对账 + footprint 勘察 `/var/lib`。真机项 **✅ 2026-09-22 已验**（首次装机、造槽内数据后迁移、幂等重跑、`/api/missions` 读数据根；见 W01 报告末尾）。后续账：prune 因数据拒删时只有日志、没上报到值守屏 | M | — | ☑ 2026-09-22 |
| W02 | 上传变更检测 | `UploadQueue.offer()` 只比字节数（`size <= old.size` 跳过）；每次判读/复核都会删 `report.md` 重生成。`QueueItem` 记 mtime 或内容哈希，非追加型文件变了就偏移归零。**已做（2026-09-22）**：`QueueItem.mtime_ns`（wire 兼容，老条目为 0）；`offer()` 三分支——长了接着传 / 没长但 mtime 变了从 0 重传并清 `done` / 都没变不动；老条目第一次见到 mtime：改写型文件（`REWRITTEN_IN_PLACE`：report.md/html、findings/review.json）且已 `done` 的从 0 重传一次（老 `done` 不可信，外部审核阻断项，`2d18615`），照片/追加流只记不重开；`scan()` 传 `st_mtime_ns`。服务器哈希核对仍是第二道网。**W02 让改写型文件重新入队，也就扩大了 `.uploaded` 永不撤销那个既有竞态的暴露面——W03 必须处理**。真机项：判读一次报告后看服务器副本是否更新 | S | — | ☑ 2026-09-22 |
| W03 | 上传白名单与 `.uploaded` 语义 | `findings.json`、`review.json`、`report.html` 进 `_EXACT`；`.uploaded` 必须覆盖这几份才算传完，否则保留策略会删掉人工复核的唯一副本。**已做（2026-09-22，`68a5bd8`）**：三份进 `_EXACT`（findings/review 二级，report.html 三级）；`.uploaded` 不再永不撤销——`_该打可删了吗` 见队列里该趟有未 done 的就 `unmark_uploaded`，全传完再打回；清盘路由拿方案前用活队列把 `uploaded` 遮掉（`_mask_uploaded_by_queue`），堵住 scan 重开到撤标之间那一拍的窗。真机项：判读后 `findings.json` 出现在服务器；判读期间 `/api/storage/sweep` 方案里不含该趟 | S | W02 | ☑ 2026-09-22 |
| W04 | 返航兜底 | `_go_home` 只调 `return_home()`，`LocalNavBackend` 没实现 → 低电时 `ABORTED`、狗原地停。实现为沿已走航点回溯、分段 `goto`（直线会穿墙；单段 120 s 超时约 50 m）。**已做（2026-09-23，`86aeb28`）**：引擎层兜底——`_go_home` 捕获后端的 `NavRequestError`，改为 `_retrace_home()`：`waypoints[:live.index]` 倒序各 `goto` 一段，最后到原点，每段沿用 StandBy 等待 + 终态等待（`RETURN_TIMEOUT_S`），返航途中接管/继续仍走原循环并按当时 `live.index` 重算。厂商后端的 `return_home` 路径不变。真机项：自建导航下低电返航真的沿来路走回原点 | M | — | ☑ 2026-09-23 |
| W05 | 限速生效 | `set_speed` 只写 `_speed`，运动循环不读。作为上限生效；低于死区的值拒绝而非回显；单位统一（基类 m/s，此处百分比）。**已做（2026-09-23，`9fcc447`）**：`_fwd_cap/_yaw_cap` 进 `_advance/_turn` 的饱和上限；`set_speed` 收 m/s、rad/s，按 `fwd_speed_mps/yaw_speed_rps` 换算，超上限夹住并回报真实值，低于死区 `NavRequestError`，侧移拒绝；`get_speed` 回 m/s。真机项：`d1max speed` 设 0.42 m/s 后看每拍 fwd ≤ 0.35 | S | — | ☑ 2026-09-23 |
| W06 | 排程执行器 | `server.py::_schedule` 自己写着"到点自动出发这件事，整个仓库里没有人做"。补轮询循环 + `last_started_ms` 回查，两条必须一起补。**已做（2026-09-23，`7356c0a`）**：`_StateHub._schedule_runner` 每 30 s 一拍：读当前任务包排程 → `decide`（`last_started` = 内存记录 ∨ `runs/<任务>/<时刻>` 目录名回查）→ `pick` → 引擎在跑或有人握着控制权就不起 → 任务从包里的 `missions/<id>.json` 取（退到手写 yaml）→ 与手动起跑同一套起飞检查 → `engine.start`。`/api/schedule` 报真实 `last_started` 与 `executor` 块。协程崩了走独立的 `schedule_died` P1；到点没轮到记 `last_displaced`；钟偏超阈值不起。真机项：排程到点狗真的出发；重启服务不重复起 | M | — | ☑ 2026-09-23 |
| W01b | OTA 升级路径携带并安装单元文件 | 评审发现：`deploy/` 不进发布包，OTA 升上来的机器单元不更新、`D1MAX_DATA_ROOT` 没设，数据继续落槽（W01 只加了启动警告）。要让 `release activate` 能更新 `/etc/systemd/system/d1max-patrol.service`（需 root，服务以 robot 跑，得走 sudoers 或 systemd 单元 drop-in） **已做（2026-09-23）**：`release pack` 带 `deploy/`；root 助手 `/usr/local/sbin/d1max-privileged` + `/etc/sudoers.d/d1max` 白名单（`install.sh` 5/7 装，`visudo -cf` 先验）；`activate`（HTTP/CLI）先探重启权限、再经助手装新版单元（内容白名单校验）、再切链；回滚三条路装回旧版单元；重启走助手且失败可见。报告 `docs/W01b-完工报告.md` | M | W01 | ☑ 2026-09-23 |
| W01c | 厂商导航未知报文刷日志 | **真机发现（2026-09-22）**：服务起来后 `VendorNavBackend` 对厂商导航 WebSocket 推来的 `app_sub_topic` 报文每条打一行"忽略无法解析的报文"，**约 40 行/秒、9 千行/4 分钟**，会把 journal 和 eMMC 写满、把真正的错误冲掉。改为：未知类型按类型只警告一次（或 debug 级 + 计数进 stats）。顺带：`d1max-patrol.service` 里 `StartLimitIntervalSec` 写在 `[Service]` 段被 systemd 忽略，应挪到 `[Unit]`；`/api/selfcheck` 无旁路进程时 10 s 超时并打 Traceback，应降为一句人话 **已做（2026-09-23）**：`UnknownFrameType` + `_note_unparseable` 按类只警告一次、每 10 万条补一行 INFO、计数 `unparseable_frames` 跨重连累计；`StartLimitIntervalSec=0` 挪进 `[Unit]`；自检对后端已知错只记一行人话、`/api/selfcheck` 预算 `POSTCHECK_TIMEOUT_S=90 s`。报告 `docs/W01c-完工报告.md` | S | — | ☑ 2026-09-23 |
| W07 | 文档订正 | `分卷与进度.md`：卷 9 上传侧代码已存在，不是"未开工"；分支口径已改为 master；跑测试要剥掉 ROS 的 `PYTHONPATH`（否则 `launch_testing` 缺 `lark`）。**已做（2026-09-23）**：`分卷与进度.md` 顶部加「口径更新」段（分支→master、方向→安防平台、卷 9 状态、测试命令与 5 条已知环境失败），卷 9 行改状态；`README.md`、`真机现场执行单.md`、`真机联调手册.md` 的测试命令与分支说法同步 | S | — | ☑ 2026-09-23 |

## 阶段 0.5 · 骨架（阶段 0 之后、阶段 1 之前；规格第七节）

| # | 工单 | 要点 | 规模 | 依赖 | 状态 |
|---|---|---|---|---|---|
| W00 | 最小契约 + adapter-sim | 规格第六节范围：设备认证与注册、能力/状态两份报文、`goto`、`abort`、命令幂等与过期、`control_epoch`、事件序号、断线与重连对账、资源表与断线策略表的数据结构。**不一次冻结全部能力**。验收：站点派 `goto` → 代理校验 → sim 执行 → 回执/进度 → 中途断线 → 重复投递 → 重连对账 → `abort` → 停止确认，端到端过 **已做（2026-09-24）**：`packages/{contract,adapter-sim,robot-agent}` 三包；报文数据类为真理源 + 黄金夹具；`Transport` 接口 + `MemoryBroker`（验收在它上面）+ `PahoTransport`（冒烟可选）；`RobotHAL` 全表定名、`SimRobot` 实现 W00 子集；代理核（校验链、幂等落盘、事件簿、资源仲裁/抢占、断线策略、reconcile、`goto`）；§6 验收一条端到端过。报告 `docs/W00-完工报告.md` | L | 阶段 0（W01–W05、W07） | ☑ 2026-09-24 |
| W00b | robot-agent 骨架 | `engine/` 搬入 `packages/robot-agent`，接 cmd/event 主题，用 adapter-sim 跑通 W00 验收流程；`server.py` 先包成可替换适配层不删 **已做（2026-09-24）**：`engine/` 整棵搬进 robot-agent，根包留 `sys.modules` 别名壳；`HalNavBackend/HalDeviceBackend` 两个桥，`goto` 跑在 `MissionEngine` 上、W00 四条验收原样通过；新入口 `d1max-agent`（可选 `--legacy-http` 同进程托管老 `AppServer` 共用引擎）；`release pack` 打 `packages/`、`install.sh` 装三个包、`d1max-agent.service` 装而不 enable。报告 `docs/W00b-完工报告.md` | L | W00 | ☑ 2026-09-24 |
| W00c1 | 站点骨架 | `packages/site-node`；Mosquitto + mTLS + 按 `robot_id` 的 ACL；站点自建 CA（openssl）签发狗证书、吊销；最小派遣（`goto`/`abort` 经 `DispatchClient`，落 SQLite）；最小站点 API（登录、机器人列表与状态、派单、事件流）。设计 `docs/superpowers/specs/2026-09-24-W00c-site-node-design.md`。**已做（2026-09-25）**：新包 `packages/site-node`（只依赖契约包）；openssl 自建 CA、Mosquitto mTLS + ACL（与 `TopicAcl` 同表生成、client_id 绑证书名）、SQLite 注册表/账号/命令/事件、派遣器（新鲜度按站点的钟、先落库再发、`issued_by` 来自令牌）、站点 API（HTTPS off-loopback、SSE）、`d1max-site` 命令行、`deploy/site/`；端到端验收用真 Mosquitto + 真站点进程 + 真 agent 进程。报告 `docs/W00c1-完工报告.md` | L | W00b | ☑ 2026-09-25 |
| W00c2a | 巡检任务 + 站点排程 | 契约加 `patrol` 任务类型（资源表、断线策略表、黄金夹具），任务与排程的解析搬进契约包；代理 `PatrolTask` 在引擎上跑整趟；站点导入任务包、跑排程执行器（吸收 W06）、派 `patrol`。设计 `docs/superpowers/specs/2026-09-25-W00c2-dispatcher-design.md`。**已做（2026-09-25）**：几何/任务/排程/任务包格式与指纹搬进契约（原位置转手同一对象）；`patrol` 两张表与夹具；代理 `PatrolTask`；站点导入任务包、排程执行器（发前记账、按狗逐条派、导入前的轮次不跑）、`/api/robots/<id>/patrol`、`/api/schedule`。报告 `docs/W00c2a-完工报告.md` | L | W00c1 | ☑ 2026-09-25 |
| W00c2b | 抢占与待命点 | 站点侧抢占规则、多个待命点（W14） | M | W00c2a | ☑ 2026-09-25 |
| W00c2c | 外部事件派遣 | 签名 HTTP 回调入口 → 选狗 → 去拦截点；拦截点进站点地图数据（W16） | M | W00c2b | ☑ 2026-09-25 |
| W00c3 | 账号与角色 | 业主/保安/管理员；已认证用户绑定命令、租约、审计；遥控 手机→站点→代理（§3.5） | L | W00c1 | ☑ 2026-09-25 |
| W00c4 | 手机改连站点 | `patrol_client.dart` 换 base URL 与鉴权，四屏逐屏迁；老 `server.py` 退役路径与路由测试改写；`schedule/alerts/baselines` 搬 site-node、删别名壳（W00c2 设计决定一挪过来） | L | W00c2c W00c3 | ☑ 2026-09-25 |
| W00c5a | 告警与值守搬到站点 | 告警簿、告警来源、值守汇总搬 site-node（总设计 §5）；站点从 MQTT 的状态、事件、遥测里生成告警；手机值守屏改连站点 | L | W00c4 | ☑ 2026-09-25；**代码外审通过**（W00c5 系列第二轮 APPROVE，`7f17df1`），真机验收未做 |
| W00c5b | 视频经站点 | 狗把相机流推给站点、站点扇出给手机与大屏（总设计 §5「视频扇出与录像→站点」）；狗上不留录像 | L | W00c5a | ☑ 2026-09-25（真机项 3c 未跑）；**代码外审通过**（W00c5 系列第二轮 APPROVE，`7f17df1`），真机验收未做 |
| W00c5c | 遥控经站点 | 按决策 7 的定稿与全部条件：手机→站点→代理的独立低延迟通道、控制租约、代理侧遥控（走 `vel`），没画面不许动 | L | W00c5b | ☑ 2026-09-25（真机项 3c.8–3c.16 未跑；报告 `docs/W00c5c-完工报告.md`）；**代码外审通过**（W00c5 系列第二轮 APPROVE，`7f17df1`），真机验收未做 |
| W00c5d | 维护功能与数据搬到站点 | 盘况/存储、建图与地图、任务运行记录与证据、导出备份、发布安装都走站点；按决策 8 数据不落狗，断网暂存发件箱 | L | W00c5c | ☑ 2026-09-25（真机项 3c.17–3c.26 未跑；报告 `docs/W00c5d-完工报告.md`）；**代码外审通过**（W00c5 系列第二轮 APPROVE，`7f17df1`），真机验收未做 |
| W00c5e | 老服务退役 | 删手机直连模式与狗上 `server.py`（71 条路由）、改写路由测试；`d1max-agent` 不再 `--legacy-http` | L | W00c5d | ☑ 2026-09-26（真机项 3c.27 未跑；报告 `docs/W00c5e-完工报告.md`）；W00c5 系列外审第一轮先修再批准，两条阻断已修（点名切版本绕过老槽保护、遥控连接未建立时切后台遗留租约），见 `docs/W00c5-系列-外审交接.md` §2；第二轮 APPROVE（`7f17df1`）：**代码外审通过，真机验收未做** |
| W00d | adapter-d1max 真机验 HAL | 控制权、速度单位与死区、运动模式、停止语义；**尽早，不等站点与手机全改完**。**W08 追加**：`deadband_wz`、按运动模式分开的上限与死区；按 #38 重标 `mps_per_unit`（粗测每单位约 1.1 m/s，默认 0.4 偏在危险一侧）；核里程开机是否归零 | M | W00b 真机 | ☑ 2026-09-25 代码外审通过（第三轮 APPROVE，`364b0c9`）；**真机验收未做**（`docs/庄园场景待真机测试.md` §3b，先确认真狗空闲时两路急停都报 Recover） |
| W00c6a | 写盘失败不卡死、叫停停得住（核查 B，P0） | 巡检途中归档写不进去（盘满、eMMC 只读重挂）：引擎任务带着异常死掉、快照停在 RUNNING、代理任务永不结束；此时叫停只 `hal.stop()` 一次，导航桥还在 ACTIVE、下一拍又发速度（仿真实测 5 秒走了 4 m）。**真机运动之前必须修** | M | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6a-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6b | 真狗返航沿来路（核查 A，P0） | `HalNavBackend.return_home()` 登记了原点就直线 `goto(原点)`，引擎的 W04 沿来路回（`_retrace_home`）根本不走；返航超时 300 s 是单段预算，直线一段就是全程。**W00d 真适配器验收之前必须修**。**按 W08 决定 11**：过渡期直线桥不认 `return_home` → 走沿来路回；规划器上线后按决定 6（规划回家，失败原地停 + 告警） | M | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6b-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6c | 排程没派出去要报警（核查 C，P1） | `ambiguous`/`no_robot`/`skip`/`alarm`/`skew` 只记 `schedule_runs` 一行，连 `on_missed: alarm` 也不推给人；只有整拍抛异常才报 `schedule_died`。正式无人值守之前做完 | S | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6c-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6d | 升级前检查（退役能力恢复，P0） | W00c5e 退役的老服务 `selfcheck` 七项检查；现在代理只查空闲、电量 ≥30%、盘 | M | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6d-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6e | 重定位 / 初始位姿（退役能力恢复，P0） | 老服务的 `/api/pose/initial`、`reset` 没有替代；**真适配器正式启用之前做完**。**按 W08 决定 4**：过渡实现 = 代理侧里程锚定（`T_map_odom`），接口跟定位器一致 | M | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6e-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6f | 当前位置标原点（退役能力恢复，P1） | 老服务 `PUT /api/maps/<id>/home`；先做成站点管理员能力 | S | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6f-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6g | 建图进程日志（退役能力恢复，P1） | 老服务 `/api/procs/<name>/log`；现在只能 SSH 上狗看 | S | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6g-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6h | 建图时看实时地图（退役能力恢复，P2） | 老服务的建图桥 + 栅格；现在重建完才在目录里看到图 | M | W00c5 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6h-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |
| W00c6i | 过渡期自主移动技术闸（W08 决定 11，P0） | W11 避障真机验收之前：d1max 适配器下不报 `patrol` 能力；`goto` 只在手机持有的「监护租约」有效时才接，一断就停（机制同遥控租约）；站点只向报了「可自主」能力的狗派排程和事件任务。**在 `D1MAX_HAL` 切到 `d1max` 之前做完** | M | W08 | ☑ 2026-09-26 代码完成、内审修复（报告 `docs/W00c6i-完工报告.md`）；**代码外审通过**（2026-09-26 Codex 批准；两份外审的四条非阻断意见已修，见 `docs/W00c6-系列-外审交接.md` §7），真机未验 |

## 阶段 1 · 单机自主（需要真机）

验收：无人干预完成"出发—巡逻—绕障—回充—再出发"。

| # | 工单 | 要点 | 规模 | 依赖 | 状态 |
|---|---|---|---|---|---|
| W08 | 导航架构定案 | 见决策 1，产出决策记录 | S | 拍板 | ☑ 2026-09-26 决策记录 `docs/W08-导航架构决策记录.md`（内审一轮已修；§4 八项待用户确认） |
| W09 | 定位 | 室内：里程计+重锚先跑通，留 AMCL 接口；户外：RTK（要装天线+账号+4G）与雷达融合。**按 W08**：定位器在 ROS 侧、经本机桥给代理（σ、来源、跳变）；地图位姿只来自定位器；先做桥与骨架（不要狗），真定位器要狗要天线；对时；RTK 用自建基站（UM982，决策 13），改正数据经站点主机、庄园 WiFi 给狗，狗上写「改正 → LK1220D 串口」的桥。**用户 2026-09-26：不并行，等 W00c6 整批做完、外审通过再开始** | L | W08、W00c6 外审通过 | ☐ 拆成 W09a–e（用户 2026-09-26「按推荐开工」），一张做完再做下一张 |
| W09a | 本机定位桥与定位质量 | 代理里抽出「定位来源」接口（里程锚定 / 定位器）；本机桥协议（Unix 套接字、对端账号认人、版本握手、序号、心跳）；定位质量（按收到时刻判新鲜、σ 过线、跳变、跟里程交叉校验、两次定位之间里程推算有上限）；重定位、换先验的请求；假定位器与仿真定位器。设计 `docs/superpowers/specs/2026-09-26-W09a-loc-bridge-design.md` | M | W00c6 外审 | ☑ 代码完成、内审问题已修（`docs/W09a-完工报告.md`）；外审随 W09 系列、真定位器在 W09b |
| W09b | 定位器节点与录包回放 | ROS 2 侧节点：雷达、IMU、里程 → 先验地图上点云匹配（推荐 MOLA 只定位模式）+ 跳变检测 + 跟里程交叉校验 → 经桥给代理；6 个室内录包离线回放出指标 | L | W09a | ☐ |
| W09c | 三维先验进地图版本 | 几何版本里带点云先验；换图时经桥换先验。按决策 15、17：第一版在狗上**边走边建**（MOLA 建图模式在线跑，走完即出图），站点保存录包与地图产物 | M | W09b | ☐ |
| W09d | 对时 | 站点主机给狗对时（Orin 钟不准、没有 NTP）；之后 GNSS 授时 | S | W09a | ☐ |
| W09e | RTK | 基站改正数据经站点转给狗；狗上「改正 → LK1220D 串口」的桥；RTK 进融合；地图与经纬度配准。软件先用假串口、假改正源；真测要天线与基站 | L | W09b、W09d | ☐ |
| W10 | 全局规划 + 禁行区 | 占据栅格上规划；区域模型进任务包格式；虚拟墙真正被遵守。厂商导航 API 同样没有禁行区。**按 W08**：规划在代理里；区域挂在几何版本上、有修订号、只收紧可热更；水体、落差等发布前人工确认画成禁行区；规划栅格生成要管矮障碍 | L | W08 W09 | ☐ |
| W11 | 局部避障 | 落地 `2026-09-05-避障设计.md`；先解雷达朝向挂账 #64。**按 W08**：感知节点（凸障碍 + 负障碍）+ 代理守卫（机身轮廓扫过区）+ 被挡计时归后端 + 旁路进程净空许可（第二层）；不做侧移 | L | W10 | ☐ |
| W12 | 速度 | SDK `SetSpeed(1/2/3)` = 1/2/3 m/s，代码从没调过，停在低档，有效约 0.42 m/s。接入 sidecar；直线段连续速度、末段保留脉冲；近人降速。**按 W08**：「末段保留脉冲」作废（已是连续速度）；限速区；第一版按 ≤0.6 m/s 设计 | M | W05 W11 | ☐ |
| W13a | 原点与待命点拆开 | 决策 16：原点（安全返航、回充语义，一张图通常一个）与待命点（运营调度，可以多个、可调）分开维护；迁移时用现有原点初始化一个默认待命点。W13 之前做完 | M | W00c6f | ☐ |
| W13 | 自动回充 | 仅 SDK 0.2.0 有 `StartRechargeTask`/`StartUnDockTask`；仅"无图回充"（正对桩约 1.5 m、看全二维码）；回充从不上报 SUCCESS，要用 `BatteryData` 确认。先修 0.2.0 状态回调解析错误（`真机待验证清单.md`） | L | W09 充电桩 | ☐ |
| W14 | 任务抢占与待命点 | 高优先级任务打断巡逻（今天是 `EngineBusy`）；多待命点；随机路线、夜间加密 | M | W06 | ☐ |

## 阶段 2 · 安防闭环

验收：模拟入侵 → 保安手机响 → 能看、能回放。

| # | 工单 | 要点 | 规模 | 依赖 | 状态 |
|---|---|---|---|---|---|
| W15 | 中央服务器补完 | `d1max_console` 只有 `ConsoleStore`；补接收入口、鉴权、TLS 与证书钉扎、界面；`http_sink.py` 引用的 `d1max_console.intake` 与 `tests/console/test_no_motion.py` 都不存在 | L | W01–W03 | ☐ |
| W16 | 事件派遣入口 | 外部事件 → 选狗 → 去拦截点。需先改 §5.5 约束（决策 5） | M | W14 W15 拍板 | ☐ |
| W17 | 告警真送达 | 安防事件类型（今天全是狗的健康告警），带截图/位姿/地图；`server.py` 把 `due_escalations` 的结果丢了；手机前台服务+本地通知+声音；WAN 通道 | L | W15 | ☐ |
| W18 | 连续录像 | 今天只在有人看时起流、`recorder.py` 录的是导航协议帧。常开录制、事件前后缓冲、片段上传、回放索引；顺带 H.264 | L | W15 | ☐ |
| W19 | CCTV 接入 | ONVIF/RTSP + 人形/区域入侵检测 → W16 | L | W16 客户信息 | ☐ |
| W20 | 角色与模式 | 业主/保安账号；布防/在家/访客；操作人实名（挂账 65/73） | M | W15 | ☐ |

## 阶段 3 · 驱离与自保（需上装选型）

| # | 工单 | 要点 | 规模 | 依赖 | 状态 |
|---|---|---|---|---|---|
| W21 | 上装 HAL | RS485（`/dev/ttyCH9344USB5`）驱动喇叭/警灯/警笛/聚光灯；`SetPeriphPower` 管供电；音频优先级仲裁。SDK 无任何音频接口 | L | 硬件选型 | ☐ |
| W22 | 分级驱离状态机 L0–L4 | 可升级、可撤回、保安可跳级；多语种话术 | M | W21 W17 | ☐ |
| W23 | 拦截点 | 方案原文是"预设位置"：第一版"区域→拦截点"查表 + 可达性校验，不做实时几何解算 | S | W10 W16 | ☐ |
| W24 | 人员检测与测距 | 相机检测 + RS-Airy 测距（狗上无深度相机）；目标丢失、多人 | L | — | ☐ |
| W25 | 保持距离与退让 | 3–5 m 距离环；后退避障；背后空间不足侧向脱困；**无路可退时的安全行为要先定义** | L | W11 W24 | ☐ |
| W26 | 异常受力检测 | IMU 冲击 + 关节力矩残差（SDK 有关节 effort，**无足端力、无跌倒/被抱起状态**）；翻倒后卸力 | L | W13 的 SDK 修复 | ☐ |
| W27 | 对讲 | 见决策 4 | M–L | W21 拍板 | ☐ |

## 阶段 4 · 多机与全天候（方案 B/C 才需要）

| # | 工单 | 要点 | 规模 | 依赖 | 状态 |
|---|---|---|---|---|---|
| W28 | 多机调度与轮换交接 | 今天只有名册。按可用性/电量/定位质量选狗；B 就位后 A 才去充电 | L | W13 W16 | ☐ |
| W29 | 全天候策略 | 雷暴停巡、雨天降速、轮足打滑与定位退化的降级、雷达雨点滤波 | L | W09 W11 | ☐ |
| W30 | 白名单、PDPA、发布 | 人脸特征与授权留痕；断网加密缓存；release 签名（今天是 debug）；iOS | L | W19 W20 | ☐ |

---

## 待人拍板（不许代拍）

| # | 决策 | 建议 | 状态 |
|---|---|---|---|
| 1 | 导航用厂商还是自建 | 自建（定位+规划+避障），运动走 SDK 连续速度。厂商导航无禁行区、避障黑盒，做不了保持距离/退让/拦截 | ☑ 2026-09-26 用户「先定决策1，按你推荐的自建，做W08」 |
| 2 | 现在开始阶段 0 | 是，从 W01 起 | ☑ 2026-09-22 用户「我们来一个一个做」（阶段 0 已于 2026-09-23 做完；表上一直没勾，2026-09-26 补记） |
| 3 | 方案书 11 处口径订正 | 发第二版前改；4.1 速度、远程锁定、8.2 自相矛盾最要紧 | ☐ |
| 4 | App 内对讲还是实体对讲机 | 第一版用实体对讲机 | ☐ |
| 5 | §5.5"服务器不许有任何移动能力" | 改为"可下发任务级指令，不可遥控" | ☑ 2026-09-24 用户「按推荐」（随 W00c 设计一并确认） |
| 6 | 是否支持 iPhone | 先问客户 | ☐ |
| 7 | 站点转发人的遥控（总设计 §3.5「手机 → 站点 → 代理」）算不算决策 5 禁止的「服务器遥控」 | 不算：站点只转发人在手机上的实时杆量，自己不产生任何移动；手机断开站点不替它续命（§3.5 原文）。确认后另开遥控转发工单（W00c3 设计决定二）。**外审 2026-09-25 的意见**：不算，可以批准，但要写明是人工遥操作数据通道，建议措辞「允许站点转发经过认证、持有控制租约的人类实时操作输入；禁止站点自主生成、重放或在操作者断线后延续运动指令」；批准条件：站点只转发（不生成、不补帧、不录制重放、不在断线后延续）、任一段断开狗靠 TTL 自停、每帧带单调序号与产生时间和很短的 TTL、同一时间只有一个控制租约、任务与人工接管先停车再移交、急停与 halt 不走普通遥控队列、审计记操作者与租约起止和接管原因、不暴露底层 SDK 命令。**定稿措辞**：「允许站点转发经过认证、持有控制租约的人类实时操作输入；禁止站点自主生成、重放或在操作者断线后延续运动指令。」外审的批准条件全部采纳，另加三条：① 规矩由狗那头的代理执行（序号递增、产生时间新鲜、有效期约 300 ms、租约代次对得上，不合就丢），站点出错最坏只会让狗停下；② 人工遥控优先于所有自动任务（事件、排程、回待命点都不许抢，人放手或断线停稳后才交回），急停与停车仍最高且不走遥控通道；③ 遥控限速先按自动导航上限的一半，真机量过延迟再放开。手机逐帧签名、狗验签暂不做 | ☑ 2026-09-25 用户「那就批准」 |
| 8 | 业务数据放哪 | 用户 2026-09-25 定：**所有信息都在服务器（站点）上，不能在狗上**；只有网络断联时临时存到临时硬盘/狗上，硬件还在确认。落实：站点是唯一权威；狗上只许有运行必需的工作副本（当前地图、正在跑的任务包、幂等记录，可随时从站点重下）与断网暂存发件箱（恢复后上传、站点确认即删，位置可配置、有上限） 。**例外**（外审第二轮批准、用户 2026-09-26 采纳）：录包上传站点后，允许狗端保留有期限、有容量上限的临时工作副本，用于本机重建；成功重建、满 7 天或磁盘压力触发时删除。站点副本是权威副本，正在录制、写入或重建的文件不得删除 | ☑ 2026-09-25 用户定；例外 ☑ 2026-09-26 |
| 9 | W00c5e 退役掉的能力补不补、按什么顺序 | P0 升级前七项检查、重定位（真适配器正式启用之前做完）；P1 当前位置标原点（先做成站点管理员能力）、建图日志；P2 建图实时地图；暂缓巡检暂停/继续、接管后续跑原任务（「结束旧任务并重新派单」更容易保证状态和安全语义，除非现场证明重派成本不可接受）。工单 W00c6d–h | ☑ 2026-09-26 用户「全部采纳」（外审第二轮建议） |
| 10 | 核查 W01–W07 外审分析时新发现的 A、B、C 开不开单 | 全开：B（写盘失败卡死、叫停停不住）P0、A（真狗返航绕过 W04）P0、C（排程没派出去不报警）P1。工单 W00c6a–c | ☑ 2026-09-26 用户「全部采纳」（外审第二轮建议） |
| 11 | W08 过渡期技术闸开不开单 | 开，P0（W00c6i） | ☑ 2026-09-26 用户「开，排在 W00c6 里一起做」 |
| 12 | 修订解耦总设计 §2.2（W08 决定 9） | 雷达、IMU 不走 HAL 拉取原语，改由适配器包提供 ROS 感知节点与机型描述、经本机桥给代理；`Health.loc_quality` 改义为「里程新鲜」 | ☑ 2026-09-26 用户「同意」 |
| 13 | RTK 改正数据与硬件（W08 §4 第 6 项） | 用户定：**自建基站**，采购 UM982 模块 + 天线；通信 WiFi（2.4/5 GHz）优先、4G/5G 备用；改正数据经站点主机发给狗，不依赖 MyRTKnet 与外网 | ☑ 2026-09-26 用户定 |
| 14 | W00c6h 决定 1：建图时看地图要不要恢复在线实时栅格 | 不恢复；用「录包轨迹 + 建好的图的预览」。W09/W11 以后要展示的是规划路径、局部障碍、代价地图，不重新绑定退役的旧实时地图链路 | ☑ 2026-09-26 用户「三项决策我认可」（外审 Codex 建议） |
| 15 | 建图放哪（W08 §4 第 7 项） | 第一版在狗上建图，站点保存录包与地图产物（断网也能建图、最接近已验证的链路）；建图程序保持可移植，真机测出录包体积、Orin 负载与建图耗时后再定要不要加站点重建 | ☑ 2026-09-26 用户「三项决策我认可」（外审 Codex 建议） |
| 16 | 原点与待命点拆不拆 | 拆：原点是安全返航与回充语义（一张图通常一个），待命点是运营调度语义（可以多个、可调）；W13 之前做完，迁移时现有原点初始化一个默认待命点。工单 W13a | ☑ 2026-09-26 用户「三项决策我认可」（外审 Codex 建议） |
| 17 | 建图是走完再建，还是边走边建 | **边走边建**：建图时狗上的 MOLA 在建图模式下在线跑，狗走完图就出来（开发机实测建图比走得快：走 7 分 54 秒的一层楼 6 分 15 秒建完，Orin 待真机量）；录包照录、照传站点（决策 15、决策 8 例外），用于站点重建与复查。Orin 跟不上实时的退路：先降建图参数（体素、关键帧间隔），再不行在站点主机上重建。验收指标：走完到图可用不超过 5 分钟（真机定）。落在 W09c | ☑ 2026-09-27 用户「建图是边走边建 这个提议非常好」 |
