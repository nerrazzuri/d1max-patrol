# 第 9 卷待办（第 1–8 卷全分支终审留下的账）

2026-09-11 深夜，在上真机的前一晚，对 `feat/交付主线` 整条分支跑了四片并行终审
（交付面 / engine / app / mobile），范围是
`git merge-base master feat/交付主线` = `162596b3` 到 HEAD。

**四片全部 ❌，合计 25 条必修、约 40 条建议。**

## 这份文档的判准变过一次，看的时候要知道

**第一版判准是「明天会不会咬人」**，据此把 25 条分成三档，绝大多数推给第 9 卷。
那一版建立在「终审之后不再动代码」这条当晚的安排上。

**判准已经被推翻。** 项目主人的原话是：「我还说了第一卷到第八卷都要跑通，这么多
必修能跑通吗？」以及「都在上真机以前发现必修的问题那必须先修完」。**新判准是
「第 1–8 卷都要跑通」，不是「会不会咬人」。** 25 条随即全部派工返修，分五条赛道
并行（交付面 / engine / app / mobile / 任务包格式），全部落在 `feat/交付主线` 上。

下面第 1–10 节是**第一版的原文，保留不删**——它记着每一条的推导和现场后果，
第 9 卷真动手的时候要看的是那些推导，不是结论。**但每一节开头的「第 9 卷再修」
这类话已经不作数了**，以本节的处置表为准。

### 25 条必修的落地处置

| 片 | 条目 | 处置 |
| --- | --- | --- |
| 交付面 1 | `install.sh` 屏幕提示里的裸 `curl` 会 401 | 已修 `7886c9f` |
| 交付面 2 | venv 门只看解释器在不在 | 已修 `29a5201` |
| 交付面 3 | 离线 wheel；`--upgrade pip` 在门外 | 已修 `29a5201` |
| 交付面 4 | 清单没写中途失败要重跑 | 已修 `29a5201` |
| 交付面 5 | 两份文档对租约到期说法打架 | 已修 `29a5201` |
| 交付面 6 | 4 处过期的「第 8 卷落地」承诺 | 已修 `b443927` |
| 交付面 7 | `鉴权与控制权.md` 交叉引用指错节 | 已修 `e81b67a` |
| 交付面 8 | `值守与告警.md` 口吻与未定义术语 | 已修 `e81b67a` |
| 交付面 9 | 两份文档零内容护栏 | 已修 `b443927` |
| 交付面 10 | 厂商协议红线；PIN 位数耦合 | 已修 `b443927` + `e81b67a` |
| 交付面 11 | 六份文件累积起来的可利用性 | 已修 `e81b67a` + `95d73c1`，**这条的切分方式项目主人尚未表态，见下** |
| engine 1 | `SuspendPoint.at_ms` 走墙钟 | 引擎侧已修 `1aaf5b6`；app 侧那半边另派赛道收 |
| engine 2 | `_SUSPEND_UNSAFE_STATES` 先按暂停就能绕过去 | 已修 `a3cf0ff` |
| engine 3 | `grab_control` 最后一圈假阴性 | 已修 `a3cf0ff` |
| engine 3 附带 | engine 侧 `# noqa: BLE001` | 已清零 `1aaf5b6`（base 上是 6 处，不是先前记的 5 处） |
| app 1 | `/api/release/rollback` 没有前置闸 | 已修 `556dd89` |
| app 2 | `/api/mapping/rebuild` 会抹掉原点 | 已修 `556dd89` |
| app 3 | `/api/estop` 闲置之后 401 | 已修 `850abb9` |
| app 4 | `app/` 里的墙钟读数 | 10 处改成可注入 `556dd89`；4 处归档口径批准保留为 `_归档钟()`；第 15 处见挂账 133 |
| app 5 | `Guard._issue()` 名额检查不是原子的 | 已修 `556dd89` |
| mobile 1 | 全片没有 `WidgetsBindingObserver` | 已修 `e75131d` |
| mobile 2 | `dispose` 不发停止指令 | 已修 `e75131d` |
| mobile 3 | 左摇杆吸附单轴 | **驳回**——规格 §7.7 明文规定并给了死区理由 |
| mobile 4 | 没有在途闸 | 已修 `e75131d` |
| mobile 5 | 手机显示人手打的原名 | 已修 `5266a22` |
| mobile 6 | 续租挂在标定那条路径上 | 已修 `e75131d` |

**交付面必修 11 是唯一一条还欠项目主人一个批复的。** 做法是把六份交付件里那些
「合起来能凑出攻击路径」的段落整段搬进 `docs/内部安全说明.md`（**不随机器交付**），
交付面只留限制说明和现场处置。终审复核当时的原话是「要不要这么改是项目主人的判断，
不是我的」。**这条切分是协调者拍的，不是产品主批的。** 撤回的成本被刻意压到最低：
搬走的段落是原样剪贴，`docs/内部安全说明.md` §五列清了整条链子以及哪两环有意留在
交付面——要撤就是粘回去，不是重写。

---
## 1. 手机 app 全片没有一处生命周期处理（mobile 必修 1，最要紧）

**现状**：`mobile/lib/ui` 全片搜不到 `WidgetsBindingObserver`。app 不知道自己
被切到了后台——锁屏、接电话、切走，发拍定时器照常跑，拿着**最后那个非零杆量**
一直发下去，**狗可能不停**；同时租约也照常续着，别人接不了管。

**今晚怎么守的**：执行单 §2.1 红线一条（握着杆的时候不许锁屏、不许接电话、
不许切出 app），加新增实测项 **10.3a**，在 5 米空场、最慢步态、手按硬急停的
条件下，分别量「推着杆锁屏」和「推着杆退出遥控屏」两条路各几秒停。

**第 9 卷怎么修**：监听 `AppLifecycleState.paused` → 立即同步发一次零杆量 →
停发拍 → 主动弃租。**先读 10.3a 量到的秒数**：如果现场量出来就是 ~1 秒
（狗侧 `HEARTBEAT_TIMEOUT_S = 0.6` 的看门狗兜住了），那这条的紧迫性从
「可能不停」降到「多走 1 秒」，修法不变但优先级可以让位。

**不修的代价**：现场全靠人记住一条纪律。人一累就忘。

## 2. 退出遥控屏不发停止指令（mobile 必修 2）

`dispose` 只停了定时器，**最后那个非零杆量留在狗那边**，靠狗侧 0.6 秒看门狗兜。
上界约 1 秒，不是零。已并进 10.3a 的第二次按压一起量。

**第 9 卷**：`dispose` 之前同步发一次零杆量，别指望看门狗。

## 3. 续租挂在「一次成功的 GET /api/control」上（mobile 必修 6）

校准不通就一发心跳都不发。租约掉了之后 **app 这头还认为自己握着，杆照样亮**，
而屏上会报那四句里的某一句（任务在跑 / 急停按着 / 控制权在别人手里 / 没有画面），
**一句都不是真原因**——真原因是操作员自己的租约过期了。

执行单新增 **10.3b** 专门记这个症状（掉租之后杆还亮不亮、屏上报的是哪一句）。

**第 9 卷**：续租与校准解耦——续租只需要租约还在，不需要校准通过。

## 4. 软急停需要一个没过期的 token（app 必修 3）

**这是四份报告里现场最要命的一条。** `/api/estop` 不在免 token 的两条路径里；
热点上换来的凭证闲置 30 分钟作废；而值守屏**不轮询、SSE 长连也不续期**。
**手机放在值守屏上干等半小时，再按软急停，很可能返回 401。**

**今晚怎么守的**：执行单 §2.1 红线一条（别让唯一那台手机停在值守屏上干等），
并把软急停**明确降级**——「第一手段永远是机身硬急停」。新增 **9.9a** 是全天挂着
的一条：停在值守屏上记下时间，≥35 分钟后回来第一件事就按软急停，按不动就是命中。

**第 9 卷要产品主先定一件事**：`/api/estop` 要不要进 `OPEN_PATHS`。放进去等于
**同一个热点上任何人都能按停这条狗**——这是安全权衡，不是技术选择。两条备选：
（a）进 `OPEN_PATHS`；（b）不进，但值守屏加一条后台续期心跳。

## 5. 回滚与重建图都没有「任务在跑就拒绝」的闸（app 必修 1/2）

- `/api/release/rollback` 直接 `systemctl restart`。服务重启那几秒到几十秒里，
  **软急停打不通**。
- `/api/mapping/rebuild` 第一件事就是把这张图的原点删掉。**删完之后谁都起不了飞，
  而且不留痕、查不出是谁删的。**

**今晚怎么守的**：执行单 §2.2 一条纪律——任务在跑的时候这两条一律不发，
升级、回滚、重建图等这一趟跑完再做。

**第 9 卷**：两条都加「有活跃任务或有人握着控制权则 409」；`rebuild` 另加操作留痕
（谁、什么时候、删的哪张图）。

## 6. `grab_control` 最后一圈的假阴性（engine 必修 3）

即使 `acquire_control()` 真的成功了，也要等**下一圈** `has_control()` 才承认，
而最后一圈没有下一圈——于是报「试了 3 次也没拿到 SDK 会话」，
**一次成功的抢占被当成失败，触发自动回滚。**

**今晚怎么守的**：执行单 4.2 加判读注——看到这句理由先别急着判失败，
原样抄下理由，然后手动确认一次会话到底在不在我们手里。

**第 9 卷**：`acquire_control()` 返回成功后立即再查一次 `has_control()`，
不要等下一圈。

## 7. engine 侧的 `# noqa: BLE001`（engine 必修，实现者自陈）

不是复核漏掉的，是登记在案的技术债。ruff 的 `BLE` 规则本项目**不允许 noqa**。

**已在 `1aaf5b6` 清零，不必等第 9 卷。** base 上实际是 **6 处**（`machine.py` 1、
`preflight.py` 1、`selfcheck.py` 4），不是这里原先写的 5 处 —— 那个数是协调者
记岔的，以 `git grep -n noqa 95d73c1 -- src/d1max_patrol/engine/` 数出来的为准。
收法不是收窄到具体异常类型：这六处的裸捕获都是**故意**的（一项炸了只算这一项
没过，收窄反而是错的），走的是「兜住 + `exc_info` 记日志」——ruff 认这种写法，
而且补上了一个真缺口：原来这六处的 traceback 整段丢掉，事后没人查得出炸在哪一行。

## 8. 其余必修，按报告文件重读

- **交付面**：必修 5、6、9（连同挂账 111「六份交付件缺『每份都真被泄密用例读过』
  的绊线」、挂账 112「交付件里写死的常量值没有任何东西绑着」）。
- **交付面必修 11：六份交付件的累积泄密面。** 这是产品主的判断，不是技术裁定。
  明天不把这六份文件交给客户，所以当晚不挡。
- **engine**：必修 1、2。
- **app**：必修 4、5。
- **mobile**：必修 4、5。

## 9. `install.sh` 的 venv 门只看解释器在不在（交付面必修 2 剩下的窄口子）

`if [[ ! -x "$槽/venv/bin/python" ]]` 是**按槽（按版本）**判的，同版本重跑复用
venv 是**故意的幂等**，不是 bug——所以必修 2 整体驳回。但有一个真口子：
第一次跑到一半断了（网断、Ctrl+C、pip 挂了）再重跑，它会**静默跳过整个依赖安装**，
留一个能起但缺包的 venv。

**今晚怎么守的**：执行单 4.0 加「重跑前必读」——中途失败要重跑，
先 `sudo rm -rf <槽>/venv` 再跑。

**第 9 卷**：装完依赖之后落一个 `.deps-ok` 哨兵文件，把门从「解释器在不在」
改成「依赖装完没有」。

## 10. 四份终审报告的约 40 条建议

**不逐条抄。** 第 9 卷开工时按同样的四片切法**重跑一次终审**，用新的眼睛看，
比抄一份过期的建议列表可靠。生成 diff 的命令：

    git diff 162596b3..HEAD -- <各片路径> > final-N-<片名>.diff

四片切法：交付面（`deploy/` + 六份交付件）、engine（`src/d1max_patrol/engine/`）、
app（`src/d1max_patrol/app/`）、mobile（`mobile/`）。

---

## 11. 返修那一轮自己带出来的账（挂账 127–138）

25 条必修返修的时候，五条赛道各自核出一批「真的，但不是这一轮该动的」。
**全部记在这儿，一条不漏。** 每条后面那句是「为什么不在出发前修」。

| 号 | 事 | 为什么推到第 9 卷 |
| --- | --- | --- |
| 127 | SSE 刷闲置那一半救不了「狗停着不动」的那种闲置 | 要改的是闲置的定义，不是一处调用 |
| 128 | `TokenStore.count` 按 `_seen` 算，不是按当下 | 会动名额口径，执行单 9.5/9.6/9.7 正要量它 |
| 129 | 首装时 `_boot_postcheck` 提前 return 的窄路径 | 只在首装那一次出现，装机清单已覆盖 |
| 130 | `divergence()` 至今没有任何调用方 | 接它要先定「怎么呈现分歧」，是设计不是修 |
| 131 | `任务包格式.md` 里还有 8 处 `§3.x` 设计 spec 章节号 | 交付件的节号重排，出发前动风险大于收益 |
| 132 | 执行单 §2 红线项与阶段二条目撞号（都叫 2.2） | **出发前绝不重编号** —— 人明天照着念的就是这份 |
| 133 | `app/video.py:531` 是第 15 处墙钟读数（`RtspStill.grab` 的 `captured_at_ms`） | 构造点在 `server.py:4093`，没有注入口，要改构造链 |
| 134 | `app/watch.py::_镜像` 把 `ctx.clock()` 喂给 `datetime.fromtimestamp` 再进 `scan_runs` | **生产行为与现在完全一致**（`ctx.clock` 缺省就是 `_wall_ms`），只是测试保真度问题 |
| 135 | `mapping.rebuild_started` 只在事件流上，没进 `engine/alerts.py::LEVEL_OF` | 要先定它该是哪一级 |
| 136 | `CONTROLLED` 是「默认放行」的白名单，加一条破坏性路由就得有人记得往表里加 | 翻成黑名单是重构级决定 |
| 137 | `PatrolClient._pin` 活到客户端被丢掉为止，`close()` 不清它 | 清了退遥控屏那一拍全零停机帧就没有 401 兜底 —— 跟 `logout` 一起议 |
| 138 | PIN 真被改了时每条周期请求变成 3 次往返，没有退避 | 有在途闸压着，不会打炸狗 |

**另外两条不是挂账，是还欠项目主人一个批复的判断：**

1. **交付面必修 11 的切分方式**（见本文档开头）。
2. **`/api/estop` 最终该不该进 `OPEN_PATHS`。** 这一轮走的是中间解：
   把它放进 `IDLE_EXEMPT_PATHS`，闲置死掉的 token 在这一条路上仍然认。
   「软急停到底要不要鉴权」是产品判断，不是复核能拍的。

---

## 附：这份文档第一版末尾那段话，已经作废

第一版末尾记着「SDD 常规流程是终审有发现就派一轮修复、再派一次定向复评，
这一次没走」，理由是当晚「不再动代码」的安排。

**那条安排已经被推翻，修复轮真的跑了**——五条赛道并行，每条的 diff 由协调者
逐条核过，核出来的问题当场再派（赛道 1b→我的 `95d73c1`、赛道 3b→赛道 2c）。
留这段在这儿是为了让读的人知道：**这份文档的前半截和后半截是两套判准下写的。**
