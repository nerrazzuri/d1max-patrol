# W00c6h 建图时看得见 · 完工报告（供审核）

> 来源：W00c5e 退役的老服务的「建图桥 + 栅格」（建图时页面上看得到正在长的那张图）；决策 9，P2。设计决定按推荐取。
> **跟老能力不一样**（设计稿决定 1）：不恢复在线实时栅格，换成「录包轨迹 + 建好的图的预览」。外审、用户可以否。

日期：2026-09-26 · 规模 M · 设计：`docs/superpowers/specs/2026-09-26-W00c6h-mapping-view-design.md`

## 问题

老服务建图是在线跑 2D slam_toolbox，建图桥一帧帧推「正在长的那张图」。W00c5d 改成「录包 → 在狗上离线重建 → 传到站点登记」之后：
录包时手机上什么都看不到（不知道哪儿走过了）；重建完了，手机上也只有地图目录里的一行字，看不到图长什么样。

## 为什么不恢复在线栅格（决定 1，外审、用户可以否）

要在录包的同时在线跑 2D 建图：W08 决定 4 已定「2D slam_toolbox 那一路不再往下走」（户外靠点云匹配 + RTK，离线出图归站点工具）；在
Orin 上跟录包抢算力；过渡期的里程本来就漂（9 m 漂 2 m）。替代的两样覆盖「建图时看得见」的两个实际用处：**哪儿走过了**、**建出来的
图对不对**。以后 W09 用 MOLA 建图，预览只认 `.pgm + .yaml`，MOLA 出的栅格照样能看（它出点云的话要先转栅格，归 W09）。

## 交付

| 层 | 改了什么 | 文件 |
|---|---|---|
| 代理 | 录包时的轨迹 `MappingTrail`：**开录成功当场清空、换一个 `epoch`**（一趟一个号），录着的每拍喂一帧里程（读不到、不新鲜、NaN 的不喂）；以录包起点为原点、起点朝向为 +x；每挪 0.3 m 或转 15° 记一点；一拍挪 1 m 以上或转 1 rad 以上当里程跳了，轨迹在跳的地方接上（记 `jumps`）；最多 5000 点，满了不再记、报 `full`。新命令 `mapping_trail {since?}`（只读、不进幂等记录、回执在命令锁外发）→ 回执 `{points: [[x, y]], since, total, full, epoch, jumps, recording, starting}`（坐标到厘米；`starting`：收下了开录、录包还没起来）。有录包能力才报 | `mapping_trail.py`（新）、`runtime.py`、`commands.py` |
| 站点 | `GET /api/robots/<id>/mapping/trail?since=`（管理员）→ 命令，回执的数据原样给；这条回执不推事件流。**图的预览**：纯 Python 解 PGM（P5/P2）、编灰度 PNG（站点主机没有 PIL），颜色照 `map_server` 三值，大图按整数倍缩到最大边 1024（一格里有占据就算占据），**先看文件大小再读**（P5 不许比头里说的大、P2 最多 8 MiB、yaml 最多 64 KB、最多 2500 万像素），渲染全站一次一张、同一版一起点只渲一次、缓存 8 张；`GET /api/maps/<id>/<version>/preview`（查看权限）给 `{width, height, m_per_px, left_x, top_y, source, warning?, standby}`（这张图这一版上每台狗登记的待命点），`GET …/preview.png` 给图；没有这张图、没有栅格 404，文件坏了 500 说是哪个文件 | `map_preview.py`（新）、`maps.py`、`api.py`、`dispatcher.py` |
| 手机 | **录包轨迹页**：每 2 秒问一次新增的点，画出来（绿点起点、红点现在，按比例缩进画布、y 朝上），写走过的范围；录包还在起（`starting`）接着问、说「正在起」；狗说停了不再问、点刷新再看；换了一趟（`epoch` 变了，或点数变少）从头取；切到后台、退出页面就不问；点满了、里程跳过都说清楚。单狗页给管理员「录包轨迹」，「开始录包」收下了也自己打开。**图的预览页**：地图页每一行「看图」（谁都能点）→ 图（能缩放）+ 右边一栏待命点、比例、用的是哪份 yaml、原点带转角的警告，待命点画在图上。两页都按横屏排 | `ui/site_mapping.dart`（新）、`ui/site_maps.dart`、`ui/site_page.dart`、`net/site_client.dart` |

## 与设计稿不同的地方

设计稿已跟着改（标了「实现时定的」）：

1. 点满 5000 不再记、报 `full`（设计没说满了怎么办；抽稀会让手机手上的 `since` 对不上）；回执多带 `recording`、`total`，手机据此停止
   轮询、发现重新开录。
2. 预览多一个 `…/preview` 接口给坐标换算与待命点（设计只写了 PNG；手机要把待命点画上去，得知道米和像素怎么换）。「按分辨率标尺」
   由手机按 `m_per_px` 写一行比例，不画在 PNG 上。
3. 轨迹查询同 W00c6g 的日志：只读、不进幂等记录、回执不推事件流（每 2 s 一条）；W00c6g 内审之后也不记进命令账、回执在命令锁外发。

## 已知限制

- 轨迹是狗自己的里程（录包起点为原点），不是地图坐标：新地方还没有图，也没锚；走远了会漂（W08 粗测 9 m 漂 2 m）。只用来看「哪儿走过了」。
- 预览不认 `origin` 的转角（建图工具出的都是 0）；只认栅格（点云图 404「没有栅格」）；PGM 最大值 > 255（16 位）不认。
- 预览里的待命点只画位置，不画朝向。
- 轨迹只说明「狗走过哪儿」，不说明「激光扫到了哪儿」（玻璃、超出量程的地方要等重建完、看预览才知道）。
- 录包进程半路死了，狗上 `mapper.recording` 照样是真（W00c5d 就有的问题），轨迹页会一直说「录包中」、照样记点；要看进程死没死，
  看建图日志（W00c6g）。
- 同一版里有几份 yaml 时取文件名排第一的那份（狗自己建的图只有一份），预览上写着用的是哪份。
- 真机没验：真狗录包时看轨迹、重建完看预览（3c.23 之后）。

## 测试与突变

- **测试**（先红后绿）：
  - 代理（`test_mapping_trail.py`）：按距离、转角抽稀，以起点为原点（起点朝北，往北走是 +x），`since`；上限与 `full`、清空；真代理
    + 仿真狗：录包时记、`since` 只回新增、停了不再记（轨迹留着）、再录清空；`since` 不像话拒；里程不新鲜不记；没有录包能力不报；
    不进幂等记录。
  - 站点（`test_site_map_preview.py`、`test_site_mapping_trail.py`）：三种格子的颜色与坐标换算；反色、文本 PGM、注释；3000×1200 的图
    缩到 1024 内、一像素宽的墙还在、比例对；坏的 PGM（彩色、短了、太大）、yaml 没分辨率或为负；接口：保安能看、待命点只给这一版的、
    PNG 能解、不登录 401；没有这张图 404、没有栅格 404、坏的 500 说是哪个文件（JSON 和 PNG 两个接口都是）；同一版只渲染一次。轨迹：
    管理员、`since` 进命令（默认 0）、保安 403、`since` 不像话 400、老代理 409、狗拒了 409；真代理端到端取得到轨迹、事件流里不推。
  - 手机（`site_mapping_view_test.dart`）：每 2 秒只要新增的、画出来、停了不再问、刷新再看；重新开录从头取；退出页面不再取；单狗页
    管理员有入口、保安没有、开始录包收下了就打开轨迹页；地图页「看图」→ 图 + 待命点（坐标换算钉到像素）+ 能缩放；没有栅格说清楚；
    画布把点缩进去、y 朝上。`site_client_test.dart`：请求形状、预览图是字节。`landscape_test.dart`：两页横屏不溢出。
- **突变 44 条（Python 33、手机 11），全部杀掉**。第一轮活 3 条：缩图时只取每格第一行（测试里只有竖墙，每一行都有它）→ 补一道不在格子第一行的横墙；「太大」「彩色」两道检查被后面的「短了」兜住（照样抛错，但不是这一道抛的）→ 断言错误说的是哪一条。

## 内部评审

只读评审（固定在 `0e8ab67` 的工作树里跑全套、做实验与突变）：**先修再批准**，没有阻断，应修 3 条、小问题 12 条。W00c6g 评审里的
几条（读回执锁内发、查询记账等）评审时已知、正在修（`2baffba`），这一单跟着受益。

| # | 问题 | 怎么修的 | 钉住的测试 |
|---|---|---|---|
| 应修 1 | 轨迹没有「哪一趟」：收下开录就回执、录包要十几秒才起来，自动打开的轨迹页第一次问到「没在录」+ 上一趟的轨迹，就不问了（原来的测试第一条假回复就是「在录」，假绿）；换一趟只靠点数判断，新一趟的点数不少于手机手上的时，新的会接在旧的后面拼成一条错轨迹；两拍之间又停又开，边沿看不到，轨迹不清 | 代理：开录成功**当场**清空、换 `epoch`（不靠每拍看边沿）；回执带 `epoch` 与 `starting`（录包那个后台槽正在「开」）。手机：`epoch` 变了从头取；`starting` 时接着问 | 开录还没起来报 `starting`、起来之后换号从头记、在停不算在起；手机：先「正在起」后「在录」、换号从头画 |
| 应修 2 | 预览渲染没有资源上限：先把整份文件读进来再看头（单文件上限 8 GiB），一台被攻破的狗传上来几个 GB 的 .pgm，谁点一下「看图」站点就 OOM；P2 解析吃内存（1600 万像素峰值约 1 GB）；同一版几个人一起点渲几遍 | 先 `stat`：PGM 按头算最多多大（P5 = 头 + 宽×高 + 16）、超了不读；P2 最多 8 MiB；yaml 最多 64 KB；最多 2500 万像素（250 m 见方，庄园约 106 m）；渲染全站一次一张，等锁的人拿缓存；最大值不是 255 的换算改查表 | 稀疏大文件不整份读就拒；比头里说的大拒；P2 太大拒；yaml 太大拒；四个人一起点只渲一次；最大值 100 的图颜色对 |
| 应修 3 | 左右镜像没钉住：轨迹 y 取反、画布 x 取反，测试都绿 | 补测试 | 起点朝北往西走 → (0, +2)；画布 x 朝右 |
| 小 1 | NaN、无穷的里程每拍记一点，很快塞满；回执里是 JSON 的 NaN，手机解不开、显示 0 点、悄悄不问了 | 不是有限数的不记 | 见左 |
| 小 2 | 里程半路跳变、归零：画出一条长直线，后面整段错位 | 一拍挪 1 m 以上或转 1 rad 以上当跳了，换起点让轨迹接上，记 `jumps`，手机说「里程跳过 N 次」 | 跳到 (100, 50) 之后接着往前 0.5 m，轨迹是 0、0.5、1.0 |
| 小 3 | 录包进程半路死了照样显示「录包中」 | 没改（W00c5d 就有的问题），写进已知限制；看建图日志 | — |
| 小 4 | 点满之后红点停在第 5000 点，文案说「现在的位置」不对 | 满了说「红点是记下的最后一点，不是狗现在的位置」 | 见左 |
| 小 5 | 画布只按点数判断要不要重画，重取之后点数碰巧一样不重画 | 每次都重画 | — |
| 小 6 | 同一版几份 yaml 取排第一的，导入的目录里可能是没编辑的那份 | 预览上写用的是哪份 yaml；写进已知限制 | 见左 |
| 小 7 | `origin` 带转角悄悄不管，待命点画错位置 | 预览带一句 `warning`，手机显示 | 见左 |
| 小 8 | P5 头用 `\r\n` 时正文错一个字节 | 没改（不合 PGM 规范，建图工具不这么写） | — |
| 小 9 | 切到后台照样每 2 秒问 | 听生命周期：后台不问，回来马上问一次 | 见左 |
| 小 10 | 两个新页面没有左右安全区 | W00c6g 那一轮已经给整个 app 包了一层（`2baffba`） | 横屏测试 |
| 小 11 | 测试注释「太大：先看头，不去读」只对解析那一层成立 | 现在目录那一层也先看大小；注释改了 | — |
| 小 12 | 坏图不缓存，每次重读 | 没改：有了大小上限，重读的代价有界 | — |

**修复之后的突变 22 条（Python 16、手机 6），全部杀掉**。第一轮活 1 条：「在停也算在起」—— 补一段在停的时候问，`starting` 要是假。
