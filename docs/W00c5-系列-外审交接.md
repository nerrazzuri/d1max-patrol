# W00c5 系列 · 外审交接（W00c5a – W00c5e）

> 用户 2026-09-25：「从W00c5a到W00c5e直接做完，W00c5d都在服务器上，所有信息都不能在狗上，除非网络断联，
> 若断联，临时存储到临时硬盘/狗子上，硬件还在确认中」。五张单的设计决定都按推荐项取（每份设计稿的状态
> 行都标了「按推荐」），每张都做了测试先行、突变检查和只读子代理内部评审，内审阻断都已修，再一起交外审。

日期：2026-09-26 · 分支：`master` · 范围：`5d175f9`（W00 系列第三轮外审 APPROVE）之后到本文档所在提交
（367 个文件，+20 548 / −56 176 行；删的大头是 W00c5e 退役的老服务与它的测试）

**两条约束贯穿这一批**：

- **决策 7**（用户批准、外审正式批准）：站点可以转发经过认证、持有控制租约的人类实时操作输入；站点不许
  自主生成、重放或在操作者断线后延续运动指令。附加三条：规矩由狗上的代理执行、人工遥控优先于一切自动任务、
  遥控限速是导航最高速的一半。逐条对照见 `docs/W00c5c-完工报告.md`「决策 7 对照」。
- **决策 8**（用户）：信息都在站点上，狗上只许有运行必需的工作副本与有上限、位置可配置的断网暂存发件箱。
  逐项对照见 `docs/W00c5d-完工报告.md`「决策 8 对照：狗上还留什么」。

**当前状态（2026-09-26）**：**代码外审通过**（第二轮 APPROVE，`7f17df1`，见 §2）；**真机验收未完成** ——
真机项 §3c（3c.1–3c.27）一条都没跑，W00d 真机验收（§3b）也还没做，都要狗、站点主机和人在场。

## 1. 这一批有什么

| 单 | 做了什么 | 设计稿（`docs/superpowers/specs/2026-09-25-…`） | 报告 | 内审 |
|---|---|---|---|---|
| W00c5a | 告警与值守搬到站点：站点从 MQTT 的状态、事件、遥测里生成告警，告警簿与值守汇总在站点，手机值守屏连站点 | `W00c5a-alerts-to-site-design.md` | `docs/W00c5a-完工报告.md` | 先修再批准，阻断 1 |
| W00c5b | 视频经站点：狗按需 SRT 加密推流，站点 MJPEG 扇出给手机，狗上不留录像 | `W00c5b-video-via-site-design.md` | `docs/W00c5b-完工报告.md` | 先修再批准，阻断 2 |
| W00c5c | 遥控经站点（决策 7）：手机 WebSocket → 站点遥控台 → QoS 0 专用主题 → 代理判帧的最高优先级遥控任务；租约、接管、没画面不许动、限速一半、halt 不走遥控通道 | `W00c5c-teleop-via-site-design.md` | `docs/W00c5c-完工报告.md` | 先修再批准，阻断 2 |
| W00c5d | 维护与数据搬到站点（决策 8）：狗专用 mTLS 接收口、发件箱确认即删、盘况进遥测；证据、判读、复核、导出、备份在站点；地图经站点（站点是地图的唯一权威）；发布经站点（下载、核对、落槽、建 venv、切、退，启动参数随版本走） | `W00c5d-maintenance-to-site-design.md` | `docs/W00c5d-完工报告.md` | 三部分各一轮：阻断 1、1、2 |
| W00c5e | 老服务退役：删狗上 `server.py`（71 条路由）与只有它用的代码、特权助手、手机直连模式；狗上只剩代理；叫停之后站点保持停着 | `W00c5e-legacy-retirement-design.md` | `docs/W00c5e-完工报告.md` | 先修再批准，阻断 1 |

每份报告里有：交付、与设计稿不同的地方、已知限制、测试与突变、内部评审的每一条及修法。

## 2. 外审经过

**第一轮（`565c069`）：先修再批准（FIX-THEN-APPROVE），两条阻断，修复在 `7f17df1`。** 其余重点机制
（叫停保持、停车失败上报、站点遥控租约、证书身份绑定、证据与地图上传、本地录包清理）外审没有发现新的阻断。

- **点名切版本绕得过「不退回老服务那一代」**。原来这道判据只在「上一版」、在途退回、开机守卫里有；站点管理员
  点名切一个装好了的老槽（有 `.deps-ok`、没有代理启动脚本），`ready()` 照样过，切过去、重启，狗就没有服务了。
  - 修法：判据挪进所有换链的共同入口 `engine/release.py::activate()`（站点命令、命令行 `release activate`、
    装机脚本 7/7 都走它）；`rollback`、开机守卫、「上一版」用同一个 `can_switch(frm, to)`。
  - 判据：目标的启动脚本得是**带执行位的普通文件（不是链接）**；目标没有启动脚本的，只有当前也没有（老服务
    那一代之间）才换。
  - 运行时在收下命令之前就拒，回执 `no_agent_start`，不进「收下之后什么都不接」那一段。
  - **配套（外审没提，是执行位这条要求带出来的）**：现场按装机清单用 U 盘带包，FAT/exFAT 不存执行位
    （Ubuntu 自动挂载带 `showexec`），启动脚本到狗上是 0644。以前是切过去之后代理起不来（203/EXEC）；
    现在会被上面那道拒，照清单装机就一定切不过去。所以：
    - **落槽时补上执行位**。执行位不进 `tree_sha256`，不动指纹；只补普通文件（`O_NOFOLLOW` 打开、`fstat`、
      `fchmod`，查和改是同一个文件，链接不跟）。已经落过的槽再落一遍也补：重跑装机脚本、站点对这一版
      再点一次「装」，都能救回以前落下的 0644 槽。
    - **装机脚本在动盘之前先查**包里有没有启动脚本：没有、或者是链接，停在 1/7 之前（以前要到 7/7 才炸，
      那时候 5/7 已经把老服务清掉了）。
  - **修复的内部评审（先修再批准，无阻断，应修 2 条、小问题 8 条）之后又改了**：
    - 开机守卫「链丢了就修」（REPAIRED）以前按名字取最后一个槽 —— 同一天打的包按哈希排序，老服务那一代
      可能排在最后，修过去下次开机说 OK、代理却起不来。→ 修到**起得来的**最新一版，一版都没有才按名字取。
    - 不知道在跑哪一版（没有链、链指着已经删了的槽）时以前按「老服务那一代」放行 → 盘上有代理那一代的
      槽就按代理那一代算。
    - 「起得来」要三个执行位都在（跟落槽补的一样；以前有一个就算，0o010 也过）。
    - **老服务那一代的包，站点登记时就拒、狗上装的时候（解开之后、落槽之前）也拒**：以前照收，下载、
      落槽、建 venv 都做完，切的时候才拒，还占一个槽位、可能被守卫修链挑中。
    - 路径常量 `AGENT_START` 挪进契约 `d1max_contract.releases`，狗和站点用同一个。
  - 测试：
    - 引擎：点名切回老槽、没有执行位、是链接、两代之内照旧能切、守卫不退到启动脚本坏了的上一版、
      落槽补执行位、补执行位不跟链接；
    - 命令行 `release activate`；
    - `ReleaseOps`：新版在跑点名切老槽、执行位经下载解包落槽保住、槽里被改掉的拒；
    - 运行时命令（用真的 `ReleaseOps`）：回执拒收、链不动、不重启；
    - 装机脚本假根真跑：没有启动脚本、是链接，两种都在动盘之前停。
- **遥控连接还在路上时切后台，后来才回来的连接留着租约**。`_open()` 等到连接回来，没看本机是不是已经收手，
  照常存下、订阅：手机上写着遥控结束了，站点上的租约却一直占着，排程、事件派遣、别人接管都派不了。
  - 修法：连接回来时本机已经收手（`_ended`）或页面没了，就对这条连接发零速、放租、关掉，不存、不订阅；
    `_onMsg` 在本机收手之后一概不理（晚到的 `granted` 不再点起定时器、不把屏上翻回「你在遥控」）。
  - 按「停」有同一个交错（连接还没回来就按停），一并覆盖。
  - 测试：连接还没回来就切后台、连接还没回来就按停、连上了还没授权就按停、连接还没回来就退出这一页
    （内部评审补的：这一条以前没测试钉住），四条组件测试。
  - **内部评审顺带发现、一并修了**（不是这一批引入的）：`WsTeleopLink.close()` 要等关握手完才把
    `_closed` 置上，而 dart:io 调过 `close()` 之后再 `add` 会抛 `StateError`。关握手期间（站点不回话最长
    几秒）页面销毁，`dispose` 里第一句 `send` 就炸，后面的放租、关连接都跳过。→ `close()` 一开始就标记
    「在关了」，这期间 `send`/`release` 不发。测试用真 WebSocket 复现了 `StateError`。
- **突变 33 条**：31 条杀掉；2 条等价：
  - 第一版「补执行位时跟着链接」：Linux 上链接的 `lstat` 权限恒为 0777，条件本来就不成立（改成
    `O_NOFOLLOW` 之后，同一类突变「打开时跟链接」能杀掉了）；
  - 「`deploy` 是个文件时只接 `FileNotFoundError`」：`NotADirectoryError` 落到下面的 `OSError` 分支，
    照样不拦落槽，只多一行警告。
- **没做的一条小问题**：手机版本页显示的是原始回执码（「A 没接：no_agent_start」）。已有的
  `not_installed` 等回执码也是这么显示的，站点原样转发、契约里没有回执码枚举；要做回执码到中文的对照表
  另开一张单。
- **外审在它的环境里没能跑完全量**（输出 8 个失败标记之后执行通道停住，拿不到名单）。我们这边不知道是哪 8 条。
  已知这批测试里有要监听本机回环端口的（仿真旁路、站点、broker）和要系统装的 mosquitto 的；W00 那一轮外审的
  沙箱就不许监听本地端口，可能是同一个原因。**请把失败名单贴回来**。本机全量结果见 §6。
- **外审的产品建议，待用户拍板（这一轮没有替用户决定）**：
  - 决策 8 的例外（录包传到站点之后，狗上留有界的工作副本，供本机重建）：外审建议批准，定性为「受限缓存，
    不算永久双存」。
  - 退役能力补不补、按什么顺序：外审建议 P0 升级前七项检查、重定位；P1 当前位置标原点、建图日志（可以先做成
    站点管理员工具）；P2 建图时的实时地图；暂缓巡检暂停/继续、接管后续跑同一趟。

**第二轮（`7f17df1`）：APPROVE，没有阻断。W00c5 代码收卷；真机验收未完成，这个标记要一直留着。**
- 外审核过：
  - 唯一的换链入口 `activate()` 在写在途标记、换链之前查 `can_switch()`；目标启动脚本要普通文件、不是
    链接、三个执行位都在；不知道在跑哪一版时往保守判；退回、守卫、「上一版」用同一判据；运行时收命令时就回
    `no_agent_start`。
  - 补执行位用 `O_NOFOLLOW` + `fstat` + `fchmod` 对同一个文件描述符，没有「查了再按路径改」的窗口。
  - 站点登记、狗上安装、装机脚本都提前拒没有启动脚本的老包。
  - 遥控：连接回来时 `!mounted || _ended` 就零速、放租、关；收手之后晚到的 `granted` 不再点起定时器；五种
    交错（建连前切后台、建连前按停、连上未授权按停、建连前退出页面、关连接期间再发帧或放租）都有测试。
- 外审独立跑过：发布、守卫、命令行、安装相关 85 条通过；`ruff` 通过；`git diff --check 565c069..HEAD` 通过。
  它的沙箱里涉及本机监听、进程通信的站点与运行时测试仍会停住，所以 Python 全量和 Flutter 没有独立复跑、
  也没拿到具体失败断言，以本仓库的结果（§6）为准。
- 非阻断备注：手机版本页直接显示 `no_agent_start` 这类回执码，跟其他回执一致，不影响安全与任务状态；
  可以单开易用性工单。
- **外审第二轮的「拍板」意见（是外审给的；是否作为用户决定、记进「待人拍板」表，等用户确认）**：
  1. 决策 8 的例外：**批准**。建议正式记为「录包上传站点后，允许狗端保留有期限、有容量上限的临时工作副本，
     用于本机重建；成功重建、满 7 天或磁盘压力触发时删除。站点副本是权威副本，正在录制、写入或重建的文件
     不得删除」。
  2. 退役能力恢复顺序：**批准**上面那份建议（P0 升级前七项检查、重定位；P1 当前位置标原点、建图日志；P2 建图
     实时地图；暂缓暂停/继续、接管后续跑原任务）。原点设置先做成站点管理员能力；重定位要在真适配器正式启用
     之前做完。
  3. 我们核查另一份外审对 W01–W07 的分析时新发现的三条（A、B、C）**全部开单**：
     - B（P0）：巡检途中写盘失败，引擎卡死、叫停停不住 —— 控制与停车可靠性，真机运动之前必须修；
     - A（P0）：真狗返航走直线、绕过 W04 的沿来路回 —— W00d 真适配器验收之前必须修；
     - C（P1）：排程没派出去不报警 —— 静默漏巡，正式无人值守之前要做完。

## 3. 请外审特别看的地方

1. **遥控（决策 7）的执行点都在代理上**（W00c5c）：代次、序号、在途积压、帧有效期（单调钟）、限速、
   没推流不动、优先级都由代理判；站点只转发、不补帧、断线就零速加放租。手机切后台结束遥控（W00c5e）。
2. **叫停之后保持停着**（W00c5e 新加的行为）：站点记下「被谁叫停了」，派单、事件派遣、回待命点、遥控都不派，
   管理员、保安点「恢复」才派；业主能停不能恢复。停车本身失败时狗回 `stop_failed`、站点回 502。
   请看这个口径对不对（以前狗上的软急停是按下要人解除）。
3. **狗专用口与发件箱**（W00c5d）：mTLS + 证书指纹对登记表，狗 A 写不了狗 B；站点永远不收的回 422、
   狗隔离不删；确认、安定、不是正在写三条都满足才删一趟。
4. **发布经站点**（W00c5d 第三部分 + W00c5e）：代理单元只指着这一版带的启动脚本，**切版本不碰单元、
   代理运行时不需要 root**；连上站点才坐实；开机守卫数够次数退回上一版，**上一版是老服务那一代
   （没有代理启动脚本）就不退**，留在新版并告警。
5. **决策 8 的一个口子，请明确批或不批**：录包（建图的原料）传到站点之后，狗上还留一阵——重建过、
   或者录完满 7 天、或者盘紧了才删，因为重建在狗上跑（要 ROS）。不批的话改成「传完即删，重建时从站点
   下回来」（另开工单）。见 `docs/W00c5d-完工报告.md`「与设计稿不同」第 1 条。
6. **退役删掉的能力**（W00c5e）：大部分在站点上有替代，**有几样没有**，列在
   `docs/W00c5e-完工报告.md`「丢了的能力」—— 巡检中暂停/继续、让开腿（接管后接着跑同一趟）、建图时的实时
   栅格、手机上在当前位置标原点、初始位姿/重定位、建图进程日志、升级前七项检查。这些**要不要补由用户定**，
   这一批没有替用户决定。

## 4. 没做、留给以后的

- **真机项**：§3c 共 27 条（画面、遥控延迟与丢帧、断网、站点主机、备份盘、判读密钥、建图录包、发布装切退、
  狗上重跑新装机脚本），§3b W00d 真机验收 7 条。
- **真狗没有「载入一张图」**：d1max 适配器不报 `map_activate`，站点不给下发按钮；要另开工单。
- **手机新 APK 没编**：这台开发机没有 Android SDK；`dist/app-release.apk` 是老的直连版，不能用。
- **站点侧的配额与保留**：没有按狗的配额；录包、地图目录、备份只增不删。
- **导出按狗的钟**、**录包的「满 7 天」按狗的钟**：钟的事跟 3c.7、3c.16 一起解决。
- 其余各张报告「已知限制」里的条目。

## 5. 怎么复现

```bash
# Python(整仓;以前那 5 条「已知环境失败」随老测试一起删了,现在应当全过)
# 仿真旁路、站点、broker 的测试要能监听本机回环端口;真 broker 的测试要装 mosquitto。
env -u PYTHONPATH -u AMENT_PREFIX_PATH -u COLCON_PREFIX_PATH .venv/bin/python -m pytest -p no:cacheprovider -q
.venv/bin/ruff check packages tests src
# 手机(Flutter 3.47.5 / Dart 3.13.4)
cd mobile && flutter test && flutter analyze
```

这一批没有动 `motion/`（C++ 运动安全门与旁路进程），W00 那一轮的 C++ 测试结果照旧。

## 6. 最后一次全量结果

第一轮外审修复之后（本节所在提交）：

- Python 整仓 **2646 条通过、9 条跳过、0 失败**（跑了 9 分 40 秒）。跳过的 9 条：7 条是导航契约测试
  按后端能力走的分支（这个后端不建图、不管地图增删改、支持重定位之类），2 条是 paho 真 broker 冒烟
  （没设 `D1MAX_MQTT_TEST_URL`；站点那几条真 broker 测试用的是系统装的 mosquitto，照跑）。
- 手机 **129 条全过**，`flutter analyze` 无问题。已知有一条不稳定的老测试：`live_video_test.dart` 的
  「盖住的页面退掉之后画面自己回来」（只在整份文件一起跑时偶尔红，单跑必过）。
- `ruff` 通过；三份部署脚本 `bash -n` 通过，`install.sh` 在假根里真跑七种情形通过（原来五种，加上
  「包里没有启动脚本」「启动脚本是链接」）。

交外审时（`e726fe2`）：Python 2633 条全过，手机 124 条全过。
