# W01b OTA 升级携带并安装单元文件 · 完工报告（供审核）

日期：2026-09-23 · 分支：`master` · 规模 M · 工单：`docs/庄园安防-差距核查与工单.md` W01b（依赖 W01）
设计：`docs/superpowers/specs/2026-09-23-W01b-ota-unit-and-privileges-design.md` · 计划：`docs/superpowers/plans/2026-09-23-W01b-ota-unit-and-privileges.md`

## 问题（为什么做）

1. `release pack` 不打 `deploy/`，OTA（`POST /api/release/install` + `activate`）升上来的机器，`/etc/systemd/system/d1max-patrol.service` 永远是装机那天的版本：没有 `D1MAX_DATA_ROOT`（W01）、`StartLimitIntervalSec` 在错的段（W01c）。W01 只在启动时打一句警告。
2. 服务以 `User=robot` 跑，写 `/etc/systemd/system/`、`daemon-reload` 都要 root。
3. 同一个进程要发 `systemctl restart`/`reboot`。`真机待验证清单` 第 6 条一直悬着：默认 polkit 下 `robot` 多半发不出去，而 `_spawn_restart` 只 `Popen` 不看结果——界面显示「已重启」，机器一动没动。
4. OTA 路径上没人做 W01 的槽内数据迁移（只有 `install.sh` 7/7 做 stop → migrate → start）。

## 方案（设计文档里比较了四种，采用 A）

root 拥有的小助手 `/usr/local/sbin/d1max-privileged` + `/etc/sudoers.d/d1max` 一行白名单（`robot ALL=(root) NOPASSWD: /usr/local/sbin/d1max-privileged`）。助手子命令：`check`、`install-unit <版本名>`、`restart`（用 `systemd-run` 起临时单元跑 `restart-now`，脱离服务自己的 cgroup）、`restart-now`（stop → 以 robot 跑 `release migrate-data` → start，与 `install.sh` 7/7 同一条路，只由 `restart` 经 `systemd-run` 调）、`reboot`。由 `install.sh` 装（基础件），**不随 OTA 更新自己**（它是提权面；改它重跑 `install.sh`）。

否决的：polkit 规则只解决重启不解决写单元；`ExecStartPre=+` 在自己启动过程中替换单元并 `daemon-reload`，systemd 行为不可靠；服务改 root 跑不可接受。

## 改了什么

| 文件 | 改法 | 守着它的测试 |
|---|---|---|
| `src/d1max_patrol/engine/release.py` | `_PACK_INCLUDE` 加 `deploy` | `tests/engine/test_release_pack.py`：包里有 `deploy/d1max-patrol.service`、`d1max-privileged`、`sudoers-d1max` |
| `deploy/d1max-privileged`（新，bash） | 见上。**单元内容白名单校验**：**按段校验**：`[Unit]` 只许 `Description After Wants StartLimitIntervalSec`，`[Service]` 只许 `Type User ExecStart ExecStartPre Restart RestartSec Environment EnvironmentFile WorkingDirectory`，`[Install]` 只许 `WantedBy`，段头之前不许有键，放错段的键一律拒；以 `\` 结尾的续行一律拒；`User=robot` 必须出现在 `[Service]` 段且只能是它；`ExecStart*` 不许 `+ ! : @` 前缀、去 `-` 后必须在 `/opt/d1max/` 下；`EnvironmentFile` 只许 `-/etc/d1max/env`；`Environment` 一行只许一个 `D1MAX_*=值`（值里不许空白/引号）或 `PYTHONUNBUFFERED=1`；`WorkingDirectory` 在 `/opt/d1max/` 下；版本名过正则；源不许是符号链接、≤ 64 KiB。**只打开源一次：`cp -P`（不跟链）拷进 root 拥有的目标目录（`mktemp` 在 `/etc/systemd/system` 下），之后 `-L`、大小、校验、`cmp`、`mv` 全对那份拷贝做**——源在 robot 可写的槽里，任何按路径的重开都是 TOCTOU 窗口。拒绝时只打印行号和键，不回显内容。非 root 运行必须显式 `--root/--dest/--no-systemctl`（测试模式），root 下这三个参数一律拒绝 | `tests/test_deploy_privileged.py` 42 条，跑真 bash：合法单元 installed→unchanged；6 种坏版本名、源不在、源是链接、16 种能变 root 的写法（`User=root`、`Group=`、`ExecStartPre=+…`、`ExecStart=/bin/sh`、`EnvironmentFile=/etc/passwd`、`Environment=LD_PRELOAD=…`、`ExecStartPost`、`AmbientCapabilities`、`[Socket]`…）全部拒且不落盘、拒时有一句原因；少 `User=`、太大也拒；6 种绕过校验的写法（`User` 放 `[Unit]`、续行吞 `User`/段头、`Environment` 多赋值、键放错段）拒；拒绝不回显内容；结构性断言校验的是 root 目录里的拷贝、`restart` 走 `systemd-run`；坏子命令退 64 |
| `deploy/sudoers-d1max`（新） | 一行 | 内容精确匹配；本机有 `visudo` 就 `visudo -cf` |
| `deploy/install.sh` | 5/7：单元、助手、sudoers 三样优先取包内 `deploy/`（老包退回脚本所在目录）；`install -m 0755 -o root -g root … /usr/local/sbin/d1max-privileged`；`visudo -cf` **先于** `install -m 0440 … /etc/sudoers.d/d1max`；声明块两行 `@写盘` | `tests/test_deploy_files.py` 三条；`tests/test_deploy_coexist.py` 对账（写盘 ↔ 删除、代码里出现过的路径都声明过） |
| `deploy/uninstall.sh` | 声明块两行 `@删除`；`rm_sys` 范围锁加两处精确路径；3/5 步先删 sudoers 再删助手（反过来白名单会有一瞬指着不存在的路径） | `tests/test_deploy_files.py::test_卸载脚本删助手和sudoers`；`test_deploy_coexist.py::test_范围只盖我们自己的东西`（对这两处精确路径放行，要求不带通配、名字含 d1max） |
| `deploy/footprint.sh` | 勘察范围加 `/usr/local/sbin:1`、`/etc/sudoers.d:1` | `test_足迹勘察覆盖助手和sudoers目录` |
| `src/d1max_patrol/engine/privileged.py`（新） | `Privileged(helper, runner)`：`present()`、`available()`（`sudo -n … check`，5 s）、`install_unit(name)`（回 `installed`/`unchanged`/`skipped: …`，失败抛 `PrivilegedError` 带助手原话）、`restart_argv(plan)`（助手在 → `sudo -n helper restart|reboot`，不在 → `plan.argv` 原样）、`ensure_can_restart()`（助手在而 sudo 不通才抛） | `tests/engine/test_privileged.py` 15 条，注入 runner |
| `src/d1max_patrol/app/server.py` | `AppContext.privileged`，`__post_init__` 把它绑进默认的 `restart`；`_spawn_restart(plan, privileged=None)` 先 `ensure_can_restart()` 再 `Popen(restart_argv)`；`_release_activate`：precheck → **`ensure_can_restart()` → `install_unit(name)` → `activate()` → `restart`**，前两步失败 409「新版单元文件没装上,没有切换」且链一动不动；响应加 `unit`；`_release_rollback` 与开机自检回滚：`rollback()` 后 `_install_unit_after_rollback(back)`（失败只 WARNING，不拦回滚）；`ctx.restart` 抛 `PrivilegedError` → 500 说实话「版本切了,但重启命令发不出去」（开机回滚路径单独记一句） | `tests/app/test_api_release.py` W01b 段 8 条；`tests/app/test_postcheck_boot.py::test_开机自检没过回滚时也把上一版的单元装回去` |
| `src/d1max_patrol/cli.py` | `_helper_for(layout)`：助手在**且版本根是 `/opt/d1max`** 才走它（`--root 别处` 会装错树的单元）；`release activate` → 先 `install_unit`（失败退 2 且不切）；`rollback` → 之后装回旧版单元，失败只打印 | `tests/test_cli_release.py` W01b 段 6 条 |
| `docs/装机清单.md` §三 | 验收加第六条：`sudo -u robot sudo -n /usr/local/sbin/d1max-privileged check` 退 0；`systemctl cat` 含 `D1MAX_DATA_ROOT` | — |
| `docs/真机待验证清单.md` 第 6 条 | 按新口径改写：验 `check`、OTA 一次后 `systemctl cat` 是新版、服务真重启了、`/var/lib/d1max` 有搬过来的数据 | — |
| 工单表 | W01b 打勾 | — |

## 设计取舍（审核请重点看）

1. **为什么先装单元再切链。** 单元只引用 `current` 和几条稳定路径，新单元配老代码是安全的；反过来（链切了、单元没装上、或切了却重启不了）机器就挂在「在途」里等人——正是本工单要消灭的状态。所以 `ensure_can_restart` 和 `install_unit` 都放在 `activate()` 之前，失败 409 且一动没动。
2. **回滚不被单元拦。** 三条回滚路（人工、开机自检、CLI）都装回旧版单元，但失败只记一笔——回到能跑的代码比单元一致更要紧。
3. **助手是提权面，护栏挡的是「借单元变成 root」。** robot 已经能以 robot 跑任意代码，所以护栏不是防 robot 跑代码，而是防 `User=`/`ExecStartPre=+`/`EnvironmentFile`/`LD_PRELOAD`/能力集这些把 robot 的代码升成 root 的写法。不在白名单里的键一律拒。真机上 `robot` 本来就在 sudo 组（厂商出厂配置），这条护栏在今天的机器上是纵深防御；在客户按我们清单交付的机器上它是唯一一道。
4. **TOCTOU。** 第一稿校验源再拷源，中间 robot 能换文件或换成链；评审又指出 `cp` 会跟链。现在 `cp -P` 只打开源一次，之后一切只看 root 目录里的拷贝，源上的存在性检查只为报错清楚，不是护栏。
5. **助手不随 OTA 更新。** 让一个 sudo 白名单脚本从 robot 可写的目录更新自己 = 把 root 送给 robot。它必须小而稳，改它重跑 `install.sh`。设计里写明。
6. **开发机 / W01b 之前装的机器退回现状。** 助手不在 → 单元不装（回 `skipped:` 并说怎么补）、重启走裸 `systemctl`。只有「助手在、sudo 不通」才是喊出来的错。
7. **数据迁移放在助手 `restart-now` 里而不是 `ExecStartPre`。** `Restart=always` 每次重试都会跑 `ExecStartPre`；迁移只该在 stop 之后、没人写槽时做一次，这跟 `install.sh` 7/7 是同一条路。整机 `reboot` 那条路上不迁移——开机后槽内数据仍在原名、prune 不删，下一次服务重启（任何一次 OTA）会搬。
8. **boot-guard 不动。** 开机前守卫回滚时没有 app 进程、单元已被 systemd 读入；新单元向后兼容，留到下次 `activate` 对齐。设计里写明。
9. **`restart` 必须脱离服务自己的 cgroup。** 服务 `Popen` 起的 sudo/bash 都在 `d1max-patrol.service` 的 cgroup 里，`systemctl stop` 按 `KillMode=control-group` 会把助手一起杀掉——第一稿的 stop → migrate → start 会死在第一步，OTA 之后服务停着不起（显式 stop 不触发 `Restart=always`）。现在 `restart` 只做 `systemd-run --collect -- helper restart-now`，真正的三步在 systemd 自己的 cgroup 树里跑。`reboot` 是入队的 job，不受影响。
10. **sudoers 不限参数**（`NOPASSWD: /usr/local/sbin/d1max-privileged` 允许任意参数）。参数安全全在脚本里：子命令只有四个，版本名过正则，测试模式参数在 root 下一律拒。

## 测试与突变检查

- 命令：`env -u PYTHONPATH -u AMENT_PREFIX_PATH -u COLCON_PREFIX_PATH .venv/bin/python -m pytest -p no:cacheprovider`
- 新增/改动测试：`test_release_pack` 2、`test_deploy_privileged` 42（新文件）、`test_deploy_files` 5、`test_deploy_coexist` 1 改、`test_privileged` 15（新文件）、`test_api_release` 8、`test_postcheck_boot` 1、`test_cli_release` 6、`tests/app/conftest.py` 默认注入「没有助手」。
- ruff：通过。`bash -n` 四个脚本通过。
- 全量与突变检查：见文末。

## 真机待验证（狗开机时做，都不动机器）

1. 重跑 `sudo bash deploy/install.sh <本版包>`（W01b 的助手和 sudoers 只有 `install.sh` 会装）；看 5/7 那一步没报错。
2. `sudo -u robot sudo -n /usr/local/sbin/d1max-privileged check; echo $?` → `0`。
3. `sudo -u robot sudo -n /usr/local/sbin/d1max-privileged install-unit <当前版本名>` → `unchanged`。
4. 用两个包演一次 OTA：`POST /api/release/install` 新包 → `POST /api/release/activate`；响应里 `unit` 是 `installed`；`systemctl cat d1max-patrol` 是新版包里那份；`systemctl show d1max-patrol -p ActiveEnterTimestamp` 变了（真重启了）；journal 里有「单元文件: installed」；`/var/lib/d1max` 出现搬过来的数据（如果槽里还有）。
5. `POST /api/release/rollback` → 响应 200，journal 里「退回 …,单元文件: installed|unchanged」。
6. 把 `/etc/sudoers.d/d1max` 临时改名再 `POST /api/release/activate`：应 409「重启命令发不出去」，链未动；改回后恢复。

## 没做的

- 没做 polkit；没动 boot-guard；没让助手更新自己。
- 没把 `sudoers` 收到「只许这几个参数」——脚本自己校验参数，见取舍 9。
- 没有真机验证（狗未开机）。

## 审核者核对

```bash
git log --oneline b90040f..HEAD
bash -n deploy/d1max-privileged && visudo -cf deploy/sudoers-d1max
env -u PYTHONPATH -u AMENT_PREFIX_PATH -u COLCON_PREFIX_PATH .venv/bin/python -m pytest -p no:cacheprovider -q \
  tests/test_deploy_privileged.py tests/engine/test_privileged.py tests/app/test_api_release.py \
  tests/test_cli_release.py tests/test_deploy_files.py tests/test_deploy_coexist.py tests/engine/test_release_pack.py
grep -n 'ensure_can_restart\|install_unit\|_install_unit_after_rollback' src/d1max_patrol/app/server.py src/d1max_patrol/cli.py
```

## 内部评审（子代理，只读，安全视角，2026-09-24）

第一轮 19 条，**4 条阻断**，全部用真脚本复现过，全部已修（提交 `39d07b9`）：

| # | 评审发现 | 修法 | 钉住它的测试 |
|---|---|---|---|
| 1 | `validate_unit` 不分段：`User=robot` 写在 `[Unit]` 段能过校验，而 systemd 对放错段的键只记一句 ignoring → 服务以 root 起（**无需竞态**） | 按段校验：三个段各自的键白名单，`User=` 只在 `[Service]` 段算数，放错段的键一律拒 | `test_绕过校验的写法一律拒绝[User在Unit段]`、`[ExecStart在Unit段]` |
| 2 | 按物理行校验，反斜杠续行能把 `User=robot` 并进上一行的值、把 `[Service]` 段头吞掉 | 以 `\` 结尾的行直接拒（真单元里没有续行） | `[续行吞掉User]`、`[续行吞掉段头]` |
| 3 | TOCTOU：`-L`/`-f`/`stat`/校验/`cmp`/`install` 各按路径重开源文件 6 次，源目录归 robot；校验完到装之间 `mv` 换文件，或换成指向 `/etc/shadow` 的链让 `install` 跟着拷成世界可读 | **只打开源一次**：`cp -P`（不跟链）拷进 `/etc/systemd/system` 下 `mktemp` 出来的 root 私有文件，之后 `-L` 检查、大小、校验、`cmp`、`mv` 全对那份拷贝做 | `test_校验的是root目录里的那份拷贝而不是源`（结构性）、`test_源是符号链接也拒绝` |
| 4 | `restart` 在服务自己的 cgroup 里 `systemctl stop`：`KillMode=control-group` 连助手一起杀，`migrate`/`start` 永远跑不到；显式 stop 不触发 `Restart=always` → OTA 后服务停着不起 | `restart` 只做一件事：`systemd-run --collect --unit=d1max-restart-<时间>-<pid> -- /usr/local/sbin/d1max-privileged restart-now`，真正的 stop → migrate → start 在那个临时单元里做（systemd 自己的 cgroup 树） | `test_restart不在自己的cgroup里停自己`、`test_测试模式下restart只打印计划` |

建议项也全部落实：拒绝时只打印行号和键、**不回显内容**（源可被换成 root 才可读文件的链，stderr 会透传到 HTTP 409）→ `test_拒绝时不回显那一行的内容`；`Environment` 正则锚到行尾且不许空白/引号（`D1MAX_A=1 LD_PRELOAD=/x.so` 原来能过）→ `[Environment多赋值]`；`_spawn_restart` 不再自己 `new Privileged()`，`AppContext.__post_init__` 把 `ctx.privileged` 绑进默认 `restart`（少发一次 sudo，测试注入的 runner 管得到重启路径）→ `test_没注入restart时默认绑的是ctx里的privileged`；开机自检回滚时 `ctx.restart` 抛 `PrivilegedError` 单独记一句「重启命令发不出去」而不是被通用 except 记成「落盘炸了」；CLI 只在版本根 == `/opt/d1max` 时走助手（`--root 别处` 会装错树的单元）→ `test_版本根不是opt_d1max时不走助手`；`tests/app/conftest.py` 的 `make_ctx` 默认注入「没有助手」的 `Privileged`（不然在装了助手的狗上跑测试会真的发 `sudo -n`）。

评审确认无问题的：参数处理（`-*` 一律 usage、root 下拒绝测试参数，sudoers 允许任意参数这一点脚本自己扛住）；`restart-now` 里 `runuser -u robot` 跑 robot 的代码不构成提权；sudo 默认 `env_reset`+`secure_path`、脚本不读环境变量；`install.sh` `visudo -cf` 在落盘之前、`$PKG` 定义在 5/7 之前；`uninstall.sh` 先删 sudoers 再删助手、`rm_sys` 两条精确路径；activate 顺序与 409 语义；全仓 `src/` 无别处裸调 `systemctl`。

**第二轮复评**（同样只读，拿真脚本 + 真 systemd 试）：上一轮 4 阻断 + 6 建议全部确认已修；另试了尾空格/注释段头、前导空白键、第二条 `User=`、重复段、`-+`/`--`/`!!` 前缀、引号、`;`、BOM、西里尔同形字、CR-only、70 KB 长行、坏版本名等 20 余种写法均被拒。**新发现 1 条阻断**，已修（提交见 `git log`）：

| # | 评审发现 | 修法 | 钉住它的测试 |
|---|---|---|---|
| 11 | **NUL 字节绕过**：源里写 `Us\0er=robot`，bash 的 `read` 静默丢掉 NUL，校验器看到 `User=robot`；systemd 把 NUL 当行分隔，看到的是 "Us" 与 "er=robot" 两行垃圾——装进去的单元没有 `User=`，以 root 起（在真 systemd 上 `systemd-analyze verify` 复现） | 拷贝之后先 `LC_ALL=C grep -a -P` 查 NUL 与其他控制字符（只放行 `\t \n \r`；UTF-8 中文注释不受影响），有就拒 | `test_NUL字节藏起来的键也拒绝`、`test_其他控制字符也拒绝[×4]`、`test_合法的中文注释和CRLF照样放行` |
| 12 | `Wants=`/`After=` 值不校验：`Wants=debug-shell.service` 会拉起挂在 tty9 的无认证 root shell（需要物理接键盘，不算远程提权，但是「借单元拉起 root 进程」的口子） | 依赖值只许 `network-online.target` / `network.target` | `test_借依赖拉起root进程或用点点绕出opt_d1max一律拒绝[Wants…/After…]` |
| 13 | 源是 FIFO（robot 能 `mkfifo`）时 root 的 `cp` 永远阻塞在 open 上，Python 侧 30 s 超时只杀得掉 `sudo`，root 的 bash+cp 留成孤儿 | `timeout 5 cp -P` | `test_源是FIFO时不会挂死` |
| 14 | `ExecStart=/opt/d1max/../../bin/sh`、`WorkingDirectory=/opt/d1max/../..` 放行（仍是 robot 身份，不提权，但白名单意图被架空） | `Exec*`/`WorkingDirectory` 值里不许有 `..` | 同上参数化的另外三条 |

复评还确认：`mktemp` 落在 root-only 目录、`rm -f` 后 `cp -P` 到不存在的路径无人能竞争；`.XXXXXX` 后缀不是合法单元名，systemd 不会加载；`mv -f` 是 rename 不跟链；EXIT trap 不改退出码；sudoers 无 `SETENV`，脚本里未加路径的命令走 `secure_path`；`__post_init__` 绑定、开机回滚单独记错、`_helper_for`、`make_ctx` 默认注入均如实落地。剩余一条冗余（一次 activate 发两次 `sudo check`：路由一次、`_spawn_restart` 一次）保留——第二次是最后一道保险，代价一个 `exit 0` 的进程。

## 全量结果与突变检查

**全量**（最终提交，同一条命令）：**5 failed**，恰好是 `test_static` 1、`console/test_store` 3、`test_packaging` 1 这 5 条已知环境性失败，无名单外失败——满足「失败集合 ⊆ 已知 5 条」。第一轮修完（`39d07b9`）也跑过一遍全量，结果相同。

**突变检查**（每条都实际看到红再还原）：

| # | 撤掉的修法 | 变红的测试 |
|---|---|---|
| M1 | `_PACK_INCLUDE` 去掉 `deploy` | `test_包里只有白名单那四样加一份自述`、`test_单元文件和特权助手随包走` |
| M2 | 助手允许 `User=`/`ExecStart=` 出现在 `[Unit]` 段 | `test_绕过校验的写法一律拒绝[User在Unit段]`、`[ExecStart在Unit段]` |
| M3 | 助手放行反斜杠续行 | `[续行吞掉User]`、`[续行吞掉段头]` |
| M4 | 助手校验源而不是拷贝 | `test_校验的是root目录里的那份拷贝而不是源` |
| M5 | `Environment` 正则只锚前缀 | `[Environment多赋值]` |
| M6 | 路由 activate 跳过 `install_unit` | `test_切版本先装单元再切链再重启`、`test_单元装不上就不切链`、`test_没有特权助手的开发机照旧切` |
| M7 | 路由 activate 不先 `ensure_can_restart` | `test_切版本先装单元再切链再重启`、`test_重启命令发不出去就不切` |
| M8 | 回滚不装回旧版单元 | `test_回滚把上一版的单元装回去`、`test_回滚时单元装不回去也照样退回去`、`test_开机自检没过回滚时也把上一版的单元装回去` |
| M9 | CLI activate 跳过助手 | `test_activate在有助手时先装单元` 等 4 条 |
| M10 | `install.sh` 先落 sudoers 再 `visudo -cf` | `test_sudoers先visudo校验再落盘` |

第二轮的四处修法各自的测试在修之前都看过红（NUL、控制字符 ×4、依赖/点点 ×6、FIFO 超时）。
