# 庄园场景待真机测试

汇总 2026-09-22 起各工单报告里「真机待验证」那几节，按上机一次能顺手做完的顺序排。**每条都标了动不动机器**；用户口径是「测试不能有移动机器的」，所以带 🐕 的几条要等用户在场、场地允许时再做。做完把结果填进最后一列，或者回填到对应的 `docs/W0x-完工报告.md`。

老的逐条假设核对表仍在 `docs/真机待验证清单.md`（SDK、运动参数、各卷），本文只收工单阶段的账，两份不重复。

## 0. 每次上机前

| 项 | 怎么做 | 备注 |
|---|---|---|
| 连狗 | 笔记本连热点 `XG2WIFI-C40011`（口令见现场记录），NM 配置里已有 `192.168.168.0/24 via 192.168.234.1` 的静态路由；`ssh robot@192.168.168.100` | Orin 无外网、时钟错（读 2026-03）；`journalctl` 要 `sudo` |
| 带包 | 笔记本上 `.venv/bin/python -m d1max_patrol.cli release pack . <outdir>`，scp 到 `/home/robot/`；W01b 之后包里自带 `deploy/`，不用再单独 scp `deploy/` | 离线轮子在 `/home/robot/wheels`，`D1MAX_PIP_ARGS='--no-index --find-links=/home/robot/wheels'` |
| **先重跑一遍 `install.sh`** | `sudo D1MAX_PIP_ARGS=… bash <包>/deploy/install.sh <包>` | W01b 的特权助手、`d1max-restart-now`、sudoers 只有 `install.sh` 会装；W01c 挪段的单元也要靠它落盘。不重跑，下面 W01b/W01c 的条目全部验不了 |
| 测试判据 | 全量 `pytest` 失败集合 ⊆ 5 条已知环境性失败（`test_static` 1、`console/test_store` 3、`test_packaging` 1） | 命令要剥 ROS 环境：`env -u PYTHONPATH -u AMENT_PREFIX_PATH -u COLCON_PREFIX_PATH` |

## 1. 不动机器就能做的

| # | 来源 | 步骤 | 期望 | 结果 |
|---|---|---|---|---|
| 1.1 | W01b | `sudo -u robot sudo -n /usr/local/sbin/d1max-privileged check; echo $?` | `0` | |
| 1.2 | W01b | `sudo -u robot sudo -n /usr/local/sbin/d1max-privileged install-unit <当前版本名>` | 打印 `unchanged` | |
| 1.3 | W01b | `sudo -u robot sudo -n /usr/local/sbin/d1max-restart-now; echo $?` | **被拒**（sudoers 里没有它；提示要口令或 `a password is required`），服务没被停 | |
| 1.4 | W01b | `ls -l /usr/local/sbin/d1max-privileged /usr/local/sbin/d1max-restart-now /etc/sudoers.d/d1max` | `0755 root`、`0700 root`、`0440 root` | |
| 1.5 | W01c | `systemctl show d1max-patrol -p StartLimitIntervalUSec` | `0` 或 `infinity`（键已在 `[Unit]` 段） | |
| 1.6 | W01c | `sudo journalctl -u d1max-patrol -f` 看一分钟 | 只见 1 行「忽略无法解析的报文 … app_sub_topic … 同类以后只计数」，之后没有同类行 | |
| 1.7 | W01c | 旁路进程不在时，按 `docs/装机清单.md` §三换 token 后 `curl -sS -H "Authorization: Bearer $TOKEN" http://127.0.0.1:8095/api/selfcheck` | 几秒内返回 JSON；`control` 项 `ok=false` 且 detail 含「还没连上旁路进程」；journal 里只有一行 WARNING「试了 3 次也没拿到 SDK 会话」，**没有 traceback** | |
| 1.8 | W01b | 用两个包演一次 OTA：`POST /api/release/install` 新包 → `POST /api/release/activate` | 响应 200 且 `unit` 为 `installed`；`systemctl cat d1max-patrol` 是新包里那份（含 `D1MAX_DATA_ROOT`）；`systemctl show d1max-patrol -p ActiveEnterTimestamp` 变了（真重启了）；`journalctl -u 'd1max-restart-*'` 能看到临时单元跑过 stop → migrate → start；journal 里有「单元文件: installed」 | |
| 1.9 | W01b / W01 | 上一条之后 `ls /var/lib/d1max /opt/d1max/releases/*/` | 槽里若还有数据，已搬到 `/var/lib/d1max`，槽里只剩 `*.migrated` | |
| 1.10 | W01b | `POST /api/release/rollback` | 200；journal 有「退回 …,单元文件: installed｜unchanged」；`systemctl cat` 回到旧版那份 | |
| 1.11 | W01b | `sudo mv /etc/sudoers.d/d1max /root/d1max.sudoers.bak`，再 `POST /api/release/activate` | **409**「重启命令发不出去」，`current` 链没动；`mv` 回去后恢复 | |
| 1.12 | W01b | `sudo mv /usr/local/sbin/d1max-restart-now /root/`，再 `activate` | **500**「版本切了,但重启命令发不出去」（systemd-run 排不进去），链已切、服务没重启；手工 `sudo systemctl restart d1max-patrol`，`mv` 回去 | |
| 1.13 | W01 | 老机器升级后 `ls /var/lib/d1max/runs`；`journalctl` 无「还有巡检数据」警告 | 历史齐全；`/opt/d1max/releases/*/` 只见 `*.migrated` | 2026-09-22 首次装机路径 ✅ 已验（见 W01 报告）；OTA 路径待验 |
| 1.14 | W06 | `/api/schedule` 看 `executor` 字段 | 有 `alive`/`last_tick` 一类心跳；服务重启后不重复起同一趟（靠 `runs/` 回查） | 起跑那一步见 2.4 |
| 1.15 | W05 | `d1max speed --x 0` | 被拒并提示用 stop | |
| 1.16 | 附带 | `ssh firefly@192.168.234.1`（口令 firefly，从未试过）；查厂商固件版本 | 能登；记下版本号 | 用户 2026-09-22 提出 |
| 1.17 | W00b | 重跑 `install.sh` 后 `ls -l /opt/d1max/current/venv/bin/d1max-agent`；`systemctl is-enabled d1max-agent`；`ls /etc/systemd/system/d1max-agent.service` | 入口存在；`disabled`（W00b 装而不 enable）；单元在 | 不起 agent、不动机器 |
| 1.18 | W00b | 离线装（`D1MAX_PIP_ARGS='--no-index --find-links=<U盘>/wheels-aarch64-py310'`）重跑 `install.sh`；装完 `/opt/d1max/current/venv/bin/python -c "import paho.mqtt, d1max_agent"` 与 `/opt/d1max/bin/python -c "import d1max_patrol.engine.machine"` | 2/7、4/7 不停；两条 import 都不报错（`paho-mqtt` 轮子已进目录，`sha256sum -c` 七行 OK） | 不起 agent、不动机器 |

## 2. 要动机器的（🐕 用户在场、场地允许才做）

| # | 来源 | 步骤 | 期望 | 结果 |
|---|---|---|---|---|
| 2.1 🐕 | W04 | `--nav local` 下走到第二个点后触发低电（或把返航电量线调高） | 狗先回第一个点再回原点，终态 `DONE`；journal 有「后端不支持返航…引擎沿来路回原点」 | |
| 2.2 🐕 | W04 | 返航途中接管、把狗挪开、再继续 | 从当时位置先去「上一个到过的点」 | |
| 2.3 🐕 | W05 | `--nav local` 下 `d1max speed --x 0.42`，再走一个点 | 旁路进程日志里每拍 `fwd ≤ 0.35`（上限在 m/s，按死区/上限夹） | |
| 2.4 🐕 | W06 | 任务包排程设到「两分钟后」 | 狗自己出发；journal 有「排程 … 到点 … 起跑任务」；`/api/schedule` 的 `executor.last_started` 有值 | |
| 2.5 🐕 | W06 | 手机握着控制权时到点 | `executor.last_skip` 提示控制权；还回去后下一拍起 | |
| 2.6 🐕 | 真机清单 #5 | 没上装的机器 `systemctl restart d1max-patrol` 后 SDK 会话是否抢得回来 | `/api/selfcheck` 的 `control` 项 ok | 以后 OTA 每次都走这条路，要先验 |

## 3. 要服务器 / 判读 / 外网的（狗不必在场，但要有 `D1MAX_CONSOLE_URL`）

| # | 来源 | 步骤 | 期望 | 结果 |
|---|---|---|---|---|
| 3.1 | W02 | 判读一趟（`POST /api/runs/<id>/judge`） | 服务器上 `report.md` 随之更新；journal 里该 key 第二轮 `sent`/`done` | |
| 3.2 | W03 | 判读后看服务器落盘目录 | 出现 `findings.json`；人工复核后出现 `review.json` | |
| 3.3 | W03 | 判读进行中调 `POST /api/storage/sweep`（不带 apply） | 方案的 `delete` 里不含该趟 | |
| 3.4 | RTK 调研 | 装天线（BT560/SMA）后看 `/rtk_pvh` | `svs_num` > 0、`pos_type` 变化；接 RTCM（RTKdata 试用或 MyRTKnet）后 `pos_type` 到 FIX（50） | 需买天线 + 4G/CPE；细节见 `docs/RTK-室外定位-调研.md` |

## 3a. 站点主机（W00c1 起；狗不必在场）

站点主机是一台独立的 Ubuntu 22.04 小主机（W00c 设计决定五）。这几项不动狗。

| # | 来源 | 做什么 | 期望 | 备注 |
|---|---|---|---|---|
| 3a.1 | W00c1 | `sudo bash deploy/site/install-site.sh --site-id <id> --hostname <主机名> --hostname <局域网 IP>` | 5/5 走完；`systemctl is-active d1max-mosquitto d1max-site` 两个 `active`；`mosquitto.service` 已停用 | 主机要能上网（apt、pypi） |
| 3a.2 | W00c1 | 同一条命令再跑一遍 | 4/5 打印「已 init 过，跳过」；`/var/lib/d1max-site/ca/ca.crt` 的指纹不变 | 重建 CA 等于所有狗失联 |
| 3a.3 | W00c1 | `ls -l /var/lib/d1max-site/ca/ca.key /var/lib/d1max-site/ca/server/server.key` | 属主 `d1max-site`，权限 `-rw-------` | |
| 3a.4 | W00c1 | `add-admin` 后从另一台机器 `curl -k https://<站点>:8443/api/robots` | 401；登录后 200、列表为空 | 手机信任站点 CA 归 W00c4 |
| 3a.5 | W00c1 | `enroll <robot_id>`，把证书包拷到狗：`ca.crt robot.crt robot.key` → `/etc/d1max/tls/`（`robot.key` 0600、属主 `robot`），`registration.json` → `/etc/d1max/` | 文件齐、权限对；**先不** `enable d1max-agent` | 狗连站点要等狗上网方案（总设计 §7 风险 3） |

## 3b. W00 系列的真机项（W00d）

W00 本身跑在 sim 上。W00d 交付了真狗适配器 `D1MaxHal`（`packages/adapter-d1max`）与两件工具，**开发机上只对仿真旁路测过，下面这些一条都没在真机上跑过**。顺序照表：先只读，再动。

| # | 来源 | 做什么 | 期望 | 备注 |
|---|---|---|---|---|
| 3b.1 | W00d | 狗上 `cd motion && make test_vel_gate && ./test_vel_gate`（运动安全门的行为测试，不碰 SDK）再 `make patrol_agent`，用新二进制替换旁路进程，重启 RK3588 让它抢开机窗口 | 旁路进程起来、握住控制权（hello 里 `held=true`、`proto=3`） | 版本严格相等：v3 的 Python 连不上 v2 的旁路进程，反过来也一样。**旁路二进制与这一版发布包要一起换**，不然老 `d1max-patrol.service` 会连不上 |
| 3b.2 | W00d | `.venv/bin/python tools/w00d_probe.py`（**不动机器**，不发任何命令） | `ok: true`；`proto` 3；`held` true；`odom_hz` 约 50；记下狗站着待命时的 `sdk_motion`、`estop_raw` 两路原始值、站着不动时的 `odom_speed_max` | 日志在 `runs/field-logs/w00d-probe-*.json`。若待命站姿不是 `General`，适配器会报 STANDING 而不是 READY，任务全被拒；`estop_raw` 硬急停一路若报 `Unknown`（不是 `Recover`），适配器和旁路进程的运动安全门都按急停算、一概不走；`odom_speed_max` 若大于 0.02，代理要配 `--stopped-eps`（比它大一截）。三样都把实测值告诉我 |
| 3b.3 | W00d 🐕 | 狗站起、周围 2 m 净空、有人守硬急停：`.venv/bin/python tools/w00d_motion_check.py --i-am-present` | 五步走完：原地慢转方向对（`direction_ok`）、前进 2 s 的实测速度、`stop` 到停稳的秒数、`ttl` 500 ms 到期自停、比例 0.1 不动。命令按 SDK 比例值给（默认 0.3，上限 0.35），不经过任何未实测的系数 | 日志末尾有建议的 `--mps-per-unit`、`--radps-per-unit`、`--invert-yaw`。**方向反了先别跑导航**：转向环会越转越偏 |
| 3b.4 | W00d 🐕 | 换一个比例再跑一遍：`w00d_motion_check.py --i-am-present --fwd-fraction 0.2 --turn-fraction 0.2` | 两次的建议值相差不大（±15%）；差得多说明比例到速度不是线性的，要告诉我 | 换算系数定下来，写进 `/etc/d1max/env` 给代理用 |
| 3b.5 | W00d 🐕 | 停掉 `d1max-patrol.service`，前台跑 `d1max-agent --transport memory:// --hal d1max --mps-per-unit … --radps-per-unit … --registration … --store-dir … --runs-root … --map … --home 0,0,0`，另开终端用 `DispatchClient` 或站点派一条 1 m 的 goto | 狗走到约 1 m 处停下；任务 DONE；`Ctrl+C` 后旁路进程**还握着控制权**（再跑 3b.2 看 `held`） | 里程是运控自己的（`OnMcData`），不是地图定位；这一步只验速度环。**同一时刻只能有一个 Python 连旁路进程开车** |
| 3b.6 | W00d 🐕 | 3b.5 走的过程中按软急停（手机或 `estop`）、再试一次拔掉网线/杀代理进程 | 急停：立刻停、之后的速度被拒（`estop`）；代理没了：最多 `ttl`（300 ms）加减速时间内停下 | 杀代理后狗必须自己停 —— 这是 `ttl` 的全部意义 |
| 3b.7 | W00d | 真狗的故障帧 `level` 分级 | 现在按 `level>=2` 算致命（假设） | 见到真故障帧时记下 level/code/文字 |

W00b 留给 W00d 的几条（桥在 sim 上的假设，真机要换成实测或厂商原语）：

- 导航状态机的两段时间：`goto` 之后 Initializing 多久进 Active（sim 取 0.3 s）、Succeed/Failed/Cancelled 驻留多久回 StandBy（sim 取 0.5 s）。goto 任务要等回到 StandBy 才进终态、才放资源，驻留越长抢占越慢。
- **定位丢失**：HAL 没有重定位原语，桥的 `reset_localization` 是空操作，引擎停下来等定位质量回来（上限 30 s）再重发当前点，超时整趟中止。真机上要决定是否映到厂商的重定位，以及 `loc_quality` 由什么量得出。
- `HAL.connect()` 必须幂等（代理与两个桥各调一次）；adapter-d1max 不许因此重连 SDK。
- 电量事件按整数百分点跨格发，厂商电量的刷新粒度要看一眼。

## 4. 已在真机上验过的（留档）

- 2026-09-22：W01 首次装机 7/7、槽内数据迁移、幂等重跑、`/api/missions`、`/api/storage`、厂商 `navigo` 共存 —— 见 `docs/W01-完工报告.md` 末节。
- 2026-09-22：RTK 只读侦察（`/rtk_pvh` 有话题无数据、LK1220D 在 `/dev/ttyTHS3`、改正源只走六分 SDK）—— 见 `docs/RTK-室外定位-调研.md`。
