# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# C++ build
bridge/build/
build/

# Runtime artifacts
runs/
*.log

# 厂商原始资料落位于 refs/。
# 除 urdf 外全部入库 —— 出差那台笔记本 clone 就有全套资料，不用手工拷贝。
# urdf 单独排除：252 MB，其中 126 MB 是 meshes、另有一份完全重复的 install 拷贝，
# 现场用不到，真要用就跑 scripts/extract_refs.sh 从原始压缩包解出来。
refs/urdf/

# 覆盖率产物
.coverage

# 运动控制编译产物（源码在 motion/*.cpp，二进制可重编）
motion/patrol_agent
motion/m0
motion/m1
motion/stand2
motion/walk1
motion/walkonly
motion/liedown
motion/stand01
motion/hold01
motion/hold02

# SDD 的工作区：账本、brief、报告、review 包 —— 全是过程产物
.superpowers/
