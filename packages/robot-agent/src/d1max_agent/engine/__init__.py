"""任务引擎 —— 巡检任务的定义、归档、起飞检查、降级规则与状态机。

**这一层不认识 HTTP。** 它是个纯库对象,能脱离 app 单独用、单独测。
app 只是它的一个调用者;W00b 起它住在 robot-agent 包里(总设计 §5),W00c4 删了根包里
那层别名壳,引擎只有 ``d1max_agent.engine`` 一个名字。有一条测试
(tests/engine/test_engine_layering.py)会扫本包的源码来守「不认识 HTTP」这条约束。

``mission`` 的真身在契约包(W00c2a,本包里是转手)。告警、基线、排程、备份、导出按总设计
都在站点上(W00c5a–e);狗上那几份随老 HTTP 面退役删了。
"""
